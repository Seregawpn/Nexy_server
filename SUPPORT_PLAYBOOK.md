# 🛠️ Support Playbook: Nexy Permissions

Руководство для службы поддержки по решению проблем с разрешениями Nexy.

---

## 📊 Типовые сценарии

### Сценарий 1: "Приложение не реагирует на пробел"

**Симптомы:**
- Пользователь нажимает пробел, но ничего не происходит
- Иконка в menu bar не меняется
- Нет записи звука

**Диагностика:**
```bash
./health_check.sh
```

**Причина:** Отсутствуют разрешения TCC (Microphone, Input Monitoring, Accessibility)

**Решение:**

1. Попросите пользователя запустить:
   ```bash
   open /Applications/Nexy.app
   ```

2. Инструкция пользователю:
   - Нажмите и держите **пробел** 2-3 секунды
   - Должны появиться системные диалоги с запросом разрешений
   - Подтвердите **все** диалоги

3. Если диалоги не появились:
   - Откройте **System Settings** → **Privacy & Security**
   - Включите Nexy в разделах:
     - ♿ **Accessibility**
     - 🎤 **Microphone**
     - ⌨️ **Input Monitoring**
     - 📸 **Screen Recording** (опционально)

4. Перезапустите приложение

**Проверка:** `./health_check.sh` должен показать ✅

---

### Сценарий 2: "Галочки стоят, но не работает"

**Симптомы:**
- В System Settings разрешения включены (галочки стоят)
- Но приложение всё равно не работает
- CoreAudio error 35 в логах

**Диагностика:**
```bash
./check_bundle_mismatch.sh
```

**Причина:** Несоответствие между System Settings UI и реальной базой TCC.db

**Возможные подпричины:**
1. TCC daemon не перечитал базу (кэш)
2. Разрешения выданы для другого Bundle ID
3. Разрешения выданы для приложения из другого пути (не `/Applications/Nexy.app`)

**Решение:**

1. Автоматический способ:
   ```bash
   ./fix_tcc_cache.sh
   ```
   Следуйте интерактивным подсказкам

2. Ручной способ:
   ```bash
   # Остановите приложение
   pkill -9 Nexy
   
   # Перезапустите TCC daemon
   sudo killall -9 tccd
   sleep 2
   
   # Сбросьте разрешения
   sudo tccutil reset Microphone com.nexy.assistant
   sudo tccutil reset ListenEvent com.nexy.assistant
   sudo tccutil reset Accessibility com.nexy.assistant
   
   # Запустите приложение
   open /Applications/Nexy.app
   ```

3. Попросите пользователя нажать пробел и подтвердить диалоги

**Проверка:** `./check_permissions.sh` должен показать 4/4 разрешения

---

### Сценарий 3: "Микрофон активируется, но ничего не распознаётся"

**Симптомы:**
- Иконка в menu bar меняется при нажатии пробела
- Но текст не распознаётся
- Нет отправки на сервер

**Диагностика:**
```bash
log stream --predicate 'processImagePath contains "Nexy"' --level debug
```

**Причина:** Проблема с аудио устройством или сетью

**Что искать в логах:**
- ✅ Хорошо: `Audio stream started`, `chunks=X` (X > 0), `Распознано: [текст]`
- ❌ Плохо: `HALC_ProxyIOContext error 35`, `chunks=0`, `Нет аудио данных`

**Решение:**

1. Проверьте аудио устройства:
   - **Settings** → **Sound**
   - Убедитесь, что микрофон работает
   - Попробуйте встроенный микрофон (отключите AirPods)

2. Если используются AirPods:
   - Убедитесь, что вход и выход — одно устройство
   - Или временно переключитесь на встроенные устройства

3. Проверьте сеть:
   - Приложение должно иметь доступ к интернету
   - Проверьте файрвол

**Проверка:** В логах должно появиться `gRPC sending request`

---

### Сценарий 4: "После обновления перестало работать"

**Симптомы:**
- Раньше работало
- После обновления/переустановки не работает
- Разрешения есть, но не применяются

**Диагностика:**
```bash
./check_bundle_mismatch.sh
```

**Причина:** Изменение Bundle ID или подписи приложения

**Решение:**

1. Проверьте Bundle ID:
   ```bash
   defaults read /Applications/Nexy.app/Contents/Info.plist CFBundleIdentifier
   ```
   Должен быть: `com.nexy.assistant`

2. Полный сброс и переустановка:
   ```bash
   # Остановите приложение
   pkill -9 Nexy
   
   # Удалите старые разрешения
   sudo tccutil reset All com.nexy.assistant
   
   # Переустановите PKG
   sudo installer -pkg Nexy.pkg -target /
   
   # Запустите приложение
   open /Applications/Nexy.app
   ```

3. Выдайте разрешения заново

**Проверка:** `./health_check.sh` должен показать ✅

---

## 🔍 Диагностические инструменты

| Инструмент | Назначение | Время |
|------------|------------|-------|
| `./health_check.sh` | Быстрая проверка всех компонентов | 30 сек |
| `./check_permissions.sh` | Статус разрешений TCC | 10 сек |
| `./check_bundle_mismatch.sh` | Поиск несоответствий Bundle ID | 1 мин |
| `./diagnose_permissions.sh` | Полная диагностика | 1 мин |
| `./fix_tcc_cache.sh` | Автоматическое исправление | 2 мин |
| `./watch_logs.sh` | Мониторинг логов в реальном времени | постоянно |

---

## 📋 Чеклист для пользователя

Отправьте пользователю этот чеклист:

### Шаг 1: Проверка установки
- [ ] Приложение установлено в `/Applications/Nexy.app`
- [ ] Версия PKG: [указать версию]

### Шаг 2: Проверка разрешений
- [ ] Откройте **System Settings** → **Privacy & Security**
- [ ] Включите Nexy в:
  - [ ] Accessibility
  - [ ] Microphone
  - [ ] Input Monitoring
  - [ ] Screen Recording (опционально)

### Шаг 3: Тестирование
- [ ] Запустите приложение: `open /Applications/Nexy.app`
- [ ] Дождитесь появления иконки в menu bar
- [ ] Нажмите и держите **пробел** 2-3 секунды
- [ ] Скажите что-нибудь в микрофон
- [ ] Отпустите пробел

### Шаг 4: Проверка результата
- [ ] Иконка изменилась при нажатии пробела?
- [ ] Появилась запись звука?
- [ ] Текст распознался?

**Если что-то не работает**, отправьте нам:
1. Скриншот System Settings → Privacy & Security → Microphone
2. Вывод команды: `./health_check.sh`
3. Версию macOS

---

## 🚨 Эскалация в разработку

Передайте разработчикам, если:

1. **Все разрешения выданы**, но приложение не работает
2. **Логи показывают неизвестные ошибки** (не error 35)
3. **Проблема воспроизводится** на нескольких машинах
4. **Health check проходит**, но функциональность нарушена

### Что предоставить разработчикам:

```bash
# Соберите диагностическую информацию
./diagnose_permissions.sh > diagnostic_report.txt

# Соберите логи
tar -czf nexy_logs.tar.gz ~/Library/Application\ Support/Nexy/logs/

# Соберите системные логи
log show --predicate 'processImagePath contains "Nexy"' --last 1h > system_logs.txt
```

Отправьте файлы:
- `diagnostic_report.txt`
- `nexy_logs.tar.gz`
- `system_logs.txt`
- Описание проблемы
- Шаги для воспроизведения

---

## 📚 Дополнительные ресурсы

- **Полная документация:** `QUICK_PERMISSIONS_CHECK.md`
- **Архитектура разрешений:** раздел "Кто хранит разрешения" в документации
- **FAQ:** [ссылка на внутренний wiki]

---

## 🔄 История изменений

| Дата | Версия | Изменения |
|------|--------|-----------|
| 2025-10-11 | 1.0 | Первая версия playbook |

---

**Важно:** Все разрешения хранит macOS (TCC.db), а не наше приложение. Мы только запрашиваем их через официальные API.

