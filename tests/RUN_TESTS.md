# Инструкции по запуску тестов

## gRPC Smoke Test (PR-3)

### Запуск smoke-теста

```bash
# Production сервер
python scripts/grpc_smoke.py 20.151.51.172 443

# Локальный сервер
python scripts/grpc_smoke.py localhost 50051
```

### Что проверяет smoke-тест

1. **Подключение к серверу**
   - Установление gRPC соединения
   - Проверка доступности сервера

2. **RPC метод InterruptSession**
   - Базовый вызов метода
   - Валидация ответа

3. **RPC метод StreamAudio**
   - Базовый streaming вызов
   - Валидация первых ответов

### Ожидаемый результат

```
✅ Подключение к 20.151.51.172:443 установлено
🧪 Тестирование InterruptSession...
✅ InterruptSession успешен: success=True
🧪 Тестирование StreamAudio...
   → Получен ответ: text_chunk
   → Получен ответ: audio_chunk
✅ StreamAudio успешен: получено 3 ответов
✅ Все smoke-тесты прошли успешно
```

---

## Health/Status Check (PR-3)

### Запуск проверки health/status

```bash
# Production сервер
python scripts/check_grpc_health.py 20.151.51.172 443

# Локальный сервер
python scripts/check_grpc_health.py localhost 8080
```

### Что проверяет health check

1. **/health endpoint**
   - HTTP 200 OK
   - Наличие версий в ответе

2. **/status endpoint**
   - HTTP 200 OK
   - Валидный JSON ответ

3. **Порт gRPC (50051)**
   - Доступность порта
   - Проверка через nc или socket

4. **Согласованность версий**
   - Версии в health совпадают
   - Версии - строки

5. **/updates/health endpoint**
   - HTTP 200 OK (если доступен)

### Ожидаемый результат

```
✅ Health endpoint доступен: HTTP 200
✅ Health содержит версии
✅ Status endpoint доступен: HTTP 200
✅ Status возвращает JSON
⚠️ Порт 50051 недоступен (может быть нормально, если используется Nginx reverse proxy)
✅ Версии согласованы: 1.0.1
✅ Updates health endpoint доступен: HTTP 200
✅ Все проверки пройдены успешно!
```

---

## Unit тесты (PR-2.1)

### Запуск unit тестов

```bash
# Все тесты
python3 -m pytest server/tests/ -v

# Конкретный файл
python3 -m pytest server/tests/test_pr2_1_coordinator.py -v

# Конкретный тест
python3 -m pytest server/tests/test_pr2_1_coordinator.py::TestModuleCoordinatorIntegration::test_coordinator_registration -v
```

---

## Smoke-тест единого вызова памяти

### Запуск smoke-теста памяти

```bash
# Локальный запуск
python3 -m pytest server/tests/test_memory_single_call_smoke.py -v

# С подробным выводом
python3 -m pytest server/tests/test_memory_single_call_smoke.py -v -s
```

### Что проверяет smoke-тест памяти

1. **Единый вызов в StreamingWorkflowIntegration**
   - Память запрашивается ровно один раз в `StreamingWorkflowIntegration`
   - Вызов происходит в правильном месте (line 145)
   - Правильный `hardware_id` передаётся в вызов

2. **Отсутствие дублирования в GrpcServiceIntegration**
   - В `GrpcServiceIntegration` нет прямого вызова памяти
   - Память запрашивается только через `StreamingWorkflowIntegration`

3. **Полный flow без дублирования**
   - При полном flow через `GrpcServiceIntegration` память запрашивается только один раз
   - Нет дублирующих вызовов `get_memory_context_parallel`

### Ожидаемый результат

```
============================= test session starts ==============================
collected 3 items

server/tests/test_memory_single_call_smoke.py::TestMemorySingleCallSmoke::test_memory_called_once_in_streaming_workflow PASSED
server/tests/test_memory_single_call_smoke.py::TestMemorySingleCallSmoke::test_memory_not_called_in_grpc_service PASSED
server/tests/test_memory_single_call_smoke.py::TestMemorySingleCallSmoke::test_no_duplicate_memory_calls_in_full_flow PASSED

============================== 3 passed in 0.12s ===============================
```

### Критическая проверка

Тест проверяет, что `get_memory_context_parallel` вызывается **ровно 1 раз** при обработке запроса. Если тест падает с сообщением о множественных вызовах, это означает наличие дублирования в коде.

---

### Что проверяют unit тесты

1. **ModuleCoordinator**
   - Регистрация модулей
   - Получение модулей по capability
   - Статус координатора
   - Очистка модулей

2. **GrpcServiceManager**
   - Инициализация с координатором
   - Использование координатора когда фича включена
   - Статус менеджера
   - Очистка менеджера

3. **Адаптеры**
   - Инициализация адаптеров
   - Обработка запросов
   - Статус адаптеров

4. **Фича-флаги**
   - Конфигурация фича-флагов
   - Конфигурация kill-switches
   - Методы проверки фича-флагов

5. **Прямые импорты**
   - Отсутствие прямых импортов модулей в grpc_service_manager.py

---

## Интеграционные тесты

### Проверка ingress (PR-0)

```bash
# Production сервер
./scripts/check_ingress.sh 20.151.51.172 443

# Локальный сервер
./scripts/check_ingress.sh localhost 8080
```

### Проверка конфигурации

```bash
# Валидация конфигурации
python -m pytest tests/test_config_validation.py -v

# Проверка drift конфигурации
python -m pytest tests/test_config_drift.py -v
```

---

## CI/CD интеграция

### GitHub Actions (если используется)

```yaml
# Пример шага для smoke-теста
- name: gRPC Smoke Test
  run: |
    python scripts/grpc_smoke.py 20.151.51.172 443

# Пример шага для health check
- name: Health/Status Check
  run: |
    python scripts/check_grpc_health.py 20.151.51.172 443

# Пример шага для проверки порта
- name: Port Check
  run: |
    nc -zv 20.151.51.172 50051 || echo "Port check skipped (Nginx reverse proxy)"
```

---

## Troubleshooting

### Проблемы с импортами

Если возникают ошибки импорта:
```bash
# Убедитесь, что вы в корне проекта
cd /path/to/server

# Установите зависимости
pip install -r requirements.txt

# Проверьте путь к модулям
export PYTHONPATH="${PYTHONPATH}:$(pwd)"
```

### Проблемы с protobuf

Если возникают ошибки с protobuf:
```bash
# Регенерируйте protobuf файлы
cd modules/grpc_service
python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. streaming.proto
```

### Проблемы с подключением

Если не удается подключиться к серверу:
- Проверьте, что сервер запущен
- Проверьте firewall/security groups
- Для HTTPS (443) убедитесь, что SSL настроен правильно
- Для локального тестирования используйте insecure channel (порт 50051)
