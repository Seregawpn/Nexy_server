# 🎯 ПРОБЛЕМА НАЙДЕНА: Cursor.app не имеет разрешения на микрофон!

## Что мы выяснили:

Вы запускаете Python из **Cursor** (fork VSCode), а не из обычного VSCode или Terminal.

```
Parent Process: /Users/sergiyzasorin/.cursor/extensions/anthropic.claude-code-2.0.20-darwin-arm64/...
```

**Cursor.app** - это отдельное приложение, и оно требует **своё собственное разрешение** на доступ к микрофону!

---

## 🔧 РЕШЕНИЕ (займёт 2 минуты):

### Шаг 1: Откройте System Preferences

```bash
# Можно открыть через терминал:
open "x-apple.systempreferences:com.apple.preference.security?Privacy_Microphone"
```

Или вручную:
1. System Preferences (Системные настройки)
2. Security & Privacy (Безопасность и конфиденциальность)
3. Вкладка Privacy (Конфиденциальность)
4. Выберите Microphone (Микрофон) в списке слева

### Шаг 2: Найдите Cursor в списке

В списке приложений найдите:
- **Cursor** или
- **Cursor.app**

### Шаг 3а: Если Cursor ЕСТЬ в списке

✅ Убедитесь, что **галочка стоит** напротив Cursor

![image](https://user-images.githubusercontent.com/placeholder/cursor-permission.png)

### Шаг 3б: Если Cursor НЕТ в списке

1. Нажмите **замок** внизу окна (введите пароль администратора)
2. Нажмите кнопку **"+"** под списком
3. Перейдите в:
   ```
   /Applications/Cursor.app
   ```
4. Выберите Cursor.app и нажмите Open
5. **Поставьте галочку** напротив Cursor

### Шаг 4: ОБЯЗАТЕЛЬНО перезапустите Cursor!

**ВАЖНО:** Разрешения применяются только после перезапуска приложения!

1. **Полностью закройте Cursor:**
   - Cmd+Q (или Cursor → Quit Cursor)
   - НЕ просто закрыть окно - нужно выйти из приложения!

2. **Подождите 2-3 секунды**

3. **Откройте Cursor заново**

4. **Откройте проект Nexy**

### Шаг 5: Проверьте, что работает

Запустите тест:

```bash
python3 tests/verify_permissions.py
```

Вы должны увидеть:
```
✅ AIRPODS: РАБОТАЕТ
✅ MACBOOK: РАБОТАЕТ (если разрешение дано)
```

---

## 📋 Быстрая команда для проверки:

После выполнения всех шагов, запустите:

```bash
python3 -c "
import pyaudio, numpy as np
p = pyaudio.PyAudio()
info = p.get_default_input_device_info()
print(f'Устройство: {info[\"name\"]}')
stream = p.open(format=pyaudio.paInt16, channels=1, rate=int(info['defaultSampleRate']),
                input=True, input_device_index=info['index'], frames_per_buffer=1024)
data = stream.read(1024, exception_on_overflow=False)
audio = np.frombuffer(data, dtype=np.int16)
rms = np.sqrt(np.mean(audio.astype(np.float32)**2))
print(f'RMS: {rms:.6f}')
print('✅ РАБОТАЕТ!' if rms > 0.1 else '❌ Не работает - нужен перезапуск Cursor')
stream.close()
p.terminate()
"
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ:

### 1. Cursor ≠ VSCode

Cursor - это **отдельное приложение**, fork от VSCode.

Даже если у VSCode есть разрешение на микрофон, **Cursor нужно отдельное разрешение!**

### 2. Обязательный перезапуск

macOS применяет разрешения **только после перезапуска** приложения.

Просто добавить разрешение **недостаточно** - нужно Cmd+Q → Открыть заново.

### 3. Все процессы Cursor

Убедитесь, что ВСЕ процессы Cursor закрыты:

```bash
# Проверка:
ps aux | grep -i cursor | grep -v grep

# Если есть процессы - убейте их:
killall Cursor
```

### 4. Кеш разрешений

Иногда macOS кеширует разрешения. Если не помогает:

```bash
# Перезапуск TCC daemon (требует root):
sudo killall tccd

# Или перезагрузите Mac
```

---

## 🔍 Почему раньше AirPods работали?

Вы писали, что AirPods работали раньше, но сейчас не работают.

**Возможные причины:**

1. **Разрешение было временным** - macOS могла дать временное разрешение на один сеанс
2. **Запускали из другого приложения** - возможно, запускали через Terminal раньше
3. **CoreAudio завис** - аудио-драйвер мог зависнуть

Попробуйте перезапустить CoreAudio:

```bash
sudo killall coreaudiod
# Подождите 3 секунды
python3 tests/verify_permissions.py
```

---

## 📱 Альтернативные решения:

### Вариант 1: Запускать из Terminal (временно)

Terminal уже может иметь разрешение:

```bash
# В Terminal:
cd /Users/sergiyzasorin/Development/Nexy/client
python3 main.py
```

### Вариант 2: Создать .app bundle

Создать правильное macOS приложение с Info.plist:

```xml
<key>NSMicrophoneUsageDescription</key>
<string>Nexy нужен доступ к микрофону для распознавания голосовых команд</string>
```

### Вариант 3: Использовать только AirPods

AirPods (Bluetooth устройства) часто работают сразу, без разрешений.

Но сейчас даже они не работают из-за отсутствия разрешения для Cursor.

---

## ✅ Чеклист:

- [ ] Открыл System Preferences → Security & Privacy → Microphone
- [ ] Нашёл Cursor в списке (или добавил через "+")
- [ ] Поставил галочку напротив Cursor
- [ ] Полностью закрыл Cursor (Cmd+Q)
- [ ] Подождал 2-3 секунды
- [ ] Открыл Cursor заново
- [ ] Запустил `python3 tests/verify_permissions.py`
- [ ] Увидел ✅ РАБОТАЕТ!

---

## 🆘 Если всё равно не работает:

1. **Перезапустите CoreAudio:**
   ```bash
   sudo killall coreaudiod
   ```

2. **Перезапустите TCC daemon:**
   ```bash
   sudo killall tccd
   ```

3. **Перезагрузите Mac** (самый надёжный способ)

4. **Используйте Terminal вместо Cursor** (временно):
   - Terminal обычно уже имеет разрешения
   - Или легче добавить

5. **Запустите GUI приложение:**
   ```bash
   python3 tests/mic_permission_app.py
   ```

---

## 📞 Проверка после исправления:

```bash
# Быстрый тест:
python3 tests/verify_permissions.py

# Полный тест:
python3 tests/test_complete_recognition_chain.py

# Запуск основного приложения:
python3 main.py
```

**Ожидаемый результат:**

```
✅ AIRPODS: РАБОТАЕТ
✅ MACBOOK: РАБОТАЕТ (если дали разрешение)

RMS > 0 (не ноль!)
```

---

**Удачи! После добавления разрешения для Cursor всё заработает! 🎤✨**

---

**P.S.:** Если вопросы или проблемы - запускайте:

```bash
bash tests/check_tcc_status.sh
```

Этот скрипт покажет текущее состояние и даст рекомендации.
