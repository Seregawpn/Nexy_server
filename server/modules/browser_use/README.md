# Browser Use Module

**Feature ID:** F-2025-015-browser-use

AI-управляемая автоматизация браузера для выполнения веб-задач.

## Возможности

- 🌐 Выполнение веб-задач через AI-агента (поиск, клики, заполнение форм)
- 🔄 Персистентные сессии — браузер остаётся открытым между задачами
- 📦 Автоматическая установка браузера при первом запуске
- 🔊 Голосовое уведомление при скачивании браузера (macOS)

## Конфигурация

В `config.env`:

```bash
BROWSER_USE_ENABLED=true        # Включить модуль
BROWSER_USE_KEEP_OPEN=true      # Сохранять браузер открытым
BROWSER_USE_TIMEOUT=120         # Таймаут задачи (сек)
BROWSER_USE_MAX_STEPS=50        # Макс. шагов на задачу
BROWSER_USE_MODEL=gemini-2.5-flash  # LLM модель
```

## Команды

| Команда | Описание |
|---------|----------|
| `browser_use` | Выполнить веб-задачу |
| `close_browser` | Закрыть браузер |

## Архитектура

```
browser_use/
├── module.py          # Основной модуль
├── adapter.py         # Адаптер для UniversalModuleInterface
├── constants.py       # Константы (FEATURE_ID)
└── README.md          # Этот файл
```

## Зависимости

```bash
pip install browser-use playwright
playwright install chromium
```

Модуль автоматически устанавливает Chromium при первом запуске сервера.
