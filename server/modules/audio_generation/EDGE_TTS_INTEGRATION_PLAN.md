# План интеграции Edge TTS в существующую архитектуру

## 📋 Цель
Интегрировать Edge TTS как дополнительный провайдер с поддержкой fallback механизма, **не заменяя** существующий Azure TTS провайдер.

## 🏗️ Текущая архитектура

### Компоненты
1. **AudioProcessor** (`core/audio_processor.py`)
   - Координатор модуля генерации аудио
   - Сейчас использует только AzureTTSProvider
   - Методы: `generate_speech()`, `generate_speech_streaming()`

2. **AzureTTSProvider** (`providers/azure_tts_provider.py`)
   - Реализует `UniversalProviderInterface`
   - Основной провайдер (priority=1)

3. **AudioGenerationConfig** (`config.py`)
   - Конфигурация только для Azure TTS
   - Валидация требует Azure ключи

4. **AudioGenerationAdapter** (`adapter.py`)
   - Адаптер для использования через ModuleCoordinator
   - Использует AudioProcessor

### Точки интеграции
- `AudioProcessor._create_provider()` - создание провайдера
- `AudioProcessor.generate_speech()` - генерация речи
- `AudioGenerationConfig` - конфигурация

## 🎯 Варианты решения

### Вариант 1: Множественные провайдеры с приоритетами (РЕКОМЕНДУЕТСЯ)
**Сложность:** Средний  
**Время:** 4-6 часов  
**Риски:** Низкий

#### Описание
- Добавить поддержку множественных провайдеров в AudioProcessor
- Edge TTS как fallback (priority=2)
- Azure TTS остается основным (priority=1)
- Автоматический fallback при ошибках Azure

#### Преимущества
- ✅ Полная обратная совместимость
- ✅ Azure TTS остается основным
- ✅ Автоматический fallback
- ✅ Легко добавить больше провайдеров

#### Недостатки
- ⚠️ Нужно модифицировать AudioProcessor
- ⚠️ Нужна логика выбора провайдера

#### Изменения
1. Создать `EdgeTTSProvider` (новый файл)
2. Модифицировать `AudioProcessor` для поддержки списка провайдеров
3. Добавить fallback логику
4. Обновить `AudioGenerationConfig` для Edge TTS настроек
5. Обновить валидацию (Azure ключи не обязательны, если есть Edge TTS)

---

### Вариант 2: Провайдер-менеджер (СРЕДНИЙ)
**Сложность:** Средний-Сложный  
**Время:** 6-8 часов  
**Риски:** Средний

#### Описание
- Создать отдельный `ProviderManager` класс
- Управление множественными провайдерами
- Health checks и автоматическое переключение

#### Преимущества
- ✅ Чистое разделение ответственности
- ✅ Легко расширять
- ✅ Продвинутые возможности (health checks, метрики)

#### Недостатки
- ⚠️ Больше изменений в архитектуре
- ⚠️ Больше кода для поддержки

---

### Вариант 3: Конфигурационный выбор провайдера (ПРОСТОЙ)
**Сложность:** Простой  
**Время:** 2-3 часа  
**Риски:** Низкий

#### Описание
- Добавить параметр `provider_type` в конфигурацию
- Выбор провайдера при инициализации
- Один провайдер за раз

#### Преимущества
- ✅ Минимальные изменения
- ✅ Простая реализация

#### Недостатки
- ⚠️ Нет автоматического fallback
- ⚠️ Нужно перезапускать для смены провайдера

---

## ✅ Рекомендуемое решение: Вариант 1

### Детальный план реализации

#### Шаг 1: Создание EdgeTTSProvider
**Файл:** `modules/audio_generation/providers/edge_tts_provider.py`

**Задачи:**
- Реализовать `UniversalProviderInterface`
- Поддержка streaming через `edge_tts.Communicate.stream()`
- Конвертация MP3 в PCM (если нужно)
- Настройки: voice, rate, volume, pitch
- Priority: 2 (fallback)

**Ключевые методы:**
```python
async def initialize() -> bool
async def process(text: str) -> AsyncGenerator[bytes, None]
async def cleanup() -> bool
```

**Особенности:**
- Edge TTS возвращает MP3, нужно конвертировать в PCM для совместимости
- Или оставить MP3 и обновить клиент
- Использовать `pydub` для конвертации если нужно

---

#### Шаг 2: Обновление AudioGenerationConfig
**Файл:** `modules/audio_generation/config.py`

**Добавить:**
- `edge_tts_enabled: bool` - включен ли Edge TTS
- `edge_tts_voice_name: str` - голос Edge TTS
- `edge_tts_rate: str` - скорость (например, "+0%")
- `edge_tts_volume: str` - громкость
- `edge_tts_pitch: str` - высота тона

**Модифицировать:**
- `validate()` - Azure ключи не обязательны, если Edge TTS включен
- `get_edge_tts_config()` - новый метод для конфигурации Edge TTS

---

#### Шаг 3: Модификация AudioProcessor
**Файл:** `modules/audio_generation/core/audio_processor.py`

**Изменения:**
1. `self.provider` → `self.providers: List[UniversalProviderInterface]`
2. `_create_provider()` → `_create_providers()` - создает список провайдеров
3. `generate_speech()` - пробует провайдеры по приоритету
4. Добавить `_get_active_provider()` - выбор активного провайдера

**Логика fallback:**
```python
async def generate_speech(self, text: str):
    for provider in sorted(self.providers, key=lambda p: p.priority):
        try:
            async for chunk in provider.process(text):
                yield chunk
            return  # Успешно
        except Exception as e:
            logger.warning(f"Provider {provider.name} failed: {e}")
            continue  # Пробуем следующий
    raise Exception("All providers failed")
```

---

#### Шаг 4: Обновление конфигурации
**Файл:** `config/unified_config.py` (если нужно)

**Добавить настройки Edge TTS:**
- `edge_tts_enabled: bool = True`
- `edge_tts_voice_name: str = "en-US-AriaNeural"`
- `edge_tts_rate: str = "+0%"`
- `edge_tts_volume: str = "+0%"`
- `edge_tts_pitch: str = "+0Hz"`

---

#### Шаг 5: Тестирование
**Файлы:**
- `scripts/test_edge_tts_integration.py` - интеграционные тесты
- `scripts/test_provider_fallback.py` - тесты fallback механизма

**Тесты:**
1. Edge TTS работает самостоятельно
2. Azure TTS работает как основной
3. Fallback работает при ошибке Azure
4. Оба провайдера работают параллельно

---

## 📊 Сравнительная таблица вариантов

| Критерий | Вариант 1 | Вариант 2 | Вариант 3 |
|----------|-----------|-----------|-----------|
| Сложность | Средний | Средний-Сложный | Простой |
| Время | 4-6 часов | 6-8 часов | 2-3 часа |
| Fallback | ✅ Автоматический | ✅ Продвинутый | ❌ Нет |
| Обратная совместимость | ✅ Полная | ✅ Полная | ✅ Полная |
| Расширяемость | ✅ Хорошая | ✅ Отличная | ⚠️ Ограниченная |
| Риски | Низкий | Средний | Низкий |

---

## 🔍 Анализ рисков

### Риск 1: Несовместимость форматов аудио
**Описание:** Edge TTS возвращает MP3, Azure TTS возвращает PCM  
**Вероятность:** Средняя  
**Критичность:** Средняя  
**Решение:** 
- Конвертировать MP3 в PCM через pydub
- Или обновить клиент для поддержки MP3

### Риск 2: Разные интерфейсы провайдеров
**Описание:** Edge TTS и Azure TTS могут иметь разные параметры  
**Вероятность:** Низкая  
**Критичность:** Низкая  
**Решение:** UniversalProviderInterface уже унифицирует интерфейс

### Риск 3: Производительность fallback
**Описание:** Задержка при переключении на fallback  
**Вероятность:** Низкая  
**Критичность:** Низкая  
**Решение:** Логирование для мониторинга, оптимизация при необходимости

### Риск 4: Конфликты зависимостей
**Описание:** edge-tts может конфликтовать с другими библиотеками  
**Вероятность:** Очень низкая  
**Критичность:** Низкая  
**Решение:** edge-tts уже в requirements.txt, протестировано

---

## 📝 Детальный план реализации (Вариант 1)

### Фаза 1: Подготовка (30 минут)
- [ ] Изучить формат аудио Edge TTS (MP3)
- [ ] Определить необходимость конвертации
- [ ] Подготовить структуру файлов

### Фаза 2: EdgeTTSProvider (2 часа)
- [ ] Создать `edge_tts_provider.py`
- [ ] Реализовать `UniversalProviderInterface`
- [ ] Добавить streaming поддержку
- [ ] Реализовать конвертацию MP3 → PCM (если нужно)
- [ ] Добавить настройки voice/rate/volume/pitch
- [ ] Unit тесты

### Фаза 3: Конфигурация (1 час)
- [ ] Обновить `AudioGenerationConfig`
- [ ] Добавить Edge TTS настройки
- [ ] Обновить валидацию
- [ ] Обновить unified_config (если нужно)

### Фаза 4: AudioProcessor (1.5 часа)
- [ ] Модифицировать для множественных провайдеров
- [ ] Реализовать fallback логику
- [ ] Обновить методы генерации
- [ ] Обновить статус/метрики

### Фаза 5: Тестирование (1 час)
- [ ] Интеграционные тесты
- [ ] Тесты fallback
- [ ] E2E тесты
- [ ] Проверка обратной совместимости

### Фаза 6: Документация (30 минут)
- [ ] Обновить README
- [ ] Добавить примеры конфигурации
- [ ] Обновить архитектурную документацию

---

## 🎯 Критерии успеха

1. ✅ Azure TTS остается основным провайдером
2. ✅ Edge TTS работает как fallback
3. ✅ Обратная совместимость сохранена
4. ✅ Все существующие тесты проходят
5. ✅ Новые тесты для Edge TTS проходят
6. ✅ Fallback работает автоматически
7. ✅ Нет регрессий в производительности

---

## 📚 Следующие шаги

1. **Согласование плана** - обсудить с командой
2. **Начало реализации** - начать с Фазы 1
3. **Итеративная разработка** - по фазам с тестированием
4. **Code review** - перед merge
5. **Документация** - обновить после завершения

---

## 🔗 Связанные файлы

### Новые файлы
- `modules/audio_generation/providers/edge_tts_provider.py`
- `scripts/test_edge_tts_integration.py`
- `scripts/test_provider_fallback.py`

### Модифицируемые файлы
- `modules/audio_generation/core/audio_processor.py`
- `modules/audio_generation/config.py`
- `config/unified_config.py` (возможно)

### Неизменяемые файлы
- `modules/audio_generation/providers/azure_tts_provider.py` (только если нужно)
- `modules/audio_generation/adapter.py` (не требует изменений)

---

## 💡 Дополнительные соображения

### Формат аудио
**Проблема:** Edge TTS возвращает MP3, Azure TTS возвращает PCM  
**Варианты решения:**
1. Конвертировать MP3 → PCM через pydub (унификация)
2. Оставить MP3 и обновить клиент (меньше обработки)
3. Конфигурируемый формат (гибкость)

**Рекомендация:** Вариант 1 (конвертация) для полной совместимости

### Приоритеты провайдеров
- Azure TTS: priority=1 (основной)
- Edge TTS: priority=2 (fallback)
- В будущем можно добавить больше провайдеров

### Health checks
- Реализовать в EdgeTTSProvider
- Использовать для автоматического переключения
- Логировать переключения для мониторинга

---

## ✅ Готовность к реализации

**Статус:** ✅ Готово к реализации  
**Блокеры:** Нет  
**Зависимости:** edge-tts>=7.2.7 (уже установлен и протестирован)

---

*Документ создан: 2025-01-XX*  
*Последнее обновление: 2025-01-XX*

