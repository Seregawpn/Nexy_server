# Анализ проблемы зависания команд Azure CLI

**Дата создания:** 2 октября 2025  
**Статус:** ✅ Активно используется

---

## 📋 **Проблема**

Команды Azure CLI `az vm run-command invoke` зависают и не завершаются.

**Симптомы:**
- Команда выполняется очень долго (более 5-10 минут)
- Ошибка: `ERROR: (Conflict) Run command extension execution is in progress`
- Невозможно выполнить новые команды
- VM работает (Status: Running), но команды блокируются

---

## 🔍 **Возможные причины**

### **1. Долгие операции в скрипте**

**Проблема:**
- Команды с `sleep` увеличивают время выполнения
- `curl` без таймаута может зависнуть при проблемах с сетью
- Длинные команды превышают таймаут Azure CLI (~90 секунд)

**Пример проблемного скрипта:**
```bash
--scripts "
  echo '1'
  sleep 5        # ← Проблема: долгая операция
  echo '2'
  sleep 5        # ← Проблема: долгая операция
  curl http://example.com  # ← Проблема: без таймаута
  echo '3'
"
```

**Решение:**
- Разбивать длинные команды на короткие
- Использовать таймауты для сетевых операций
- Избегать `sleep` в скриптах

---

### **2. Проблема с таймаутом Azure CLI**

**Проблема:**
- Azure CLI имеет таймаут выполнения команд (обычно 90 секунд)
- Если команда выполняется дольше, она может зависнуть
- Azure CLI не может корректно обработать долгие команды

**Решение:**
- Использовать короткие команды (< 30 секунд)
- Разбивать длинные операции на несколько команд
- Использовать фоновые процессы для долгих операций

---

### **3. Проблема с состоянием расширения RunCommand**

**Проблема:**
- Расширение RunCommand на VM может "зависнуть" в состоянии "Running"
- Azure CLI считает, что команда все еще выполняется
- Новые команды блокируются до завершения предыдущей

**Решение:**
- Перезапустить VM для сброса состояния
- Подождать завершения команды (может занять 10-15 минут)
- Использовать SSH напрямую для обхода проблемы

---

### **4. Проблема с сетью или доступностью VM**

**Проблема:**
- Проблемы с сетью между Azure CLI и VM
- VM недоступна или перегружена
- Проблемы с расширением RunCommand на VM

**Решение:**
- Проверить доступность VM
- Проверить состояние расширения RunCommand
- Использовать SSH напрямую

---

### **5. Команда действительно выполняется очень долго**

**Проблема:**
- Команда может выполнять долгие операции (установка пакетов, компиляция, и т.д.)
- Команда может ждать завершения других процессов
- Команда может зависнуть на каком-то этапе

**Решение:**
- Добавить логирование для отслеживания прогресса
- Использовать таймауты для долгих операций
- Разбивать команды на этапы

---

### **6. Проблема с кешем состояния Azure CLI**

**Проблема:**
- Azure CLI может кешировать состояние выполнения команды
- Кеш может быть устаревшим или некорректным
- Azure CLI не обновляет состояние корректно

**Решение:**
- Очистить кеш Azure CLI
- Подождать завершения команды
- Перезапустить VM для сброса состояния

---

## 🔧 **Решения**

### **Решение 1: Использовать короткие команды**

**❌ Плохо:**
```bash
az vm run-command invoke \
  --resource-group Nexy \
  --name nexy-regular \
  --command-id RunShellScript \
  --scripts "
    echo '1'
    sleep 5
    echo '2'
    sleep 5
    echo '3'
  "
```

**✅ Хорошо:**
```bash
# Разбить на несколько коротких команд
az vm run-command invoke \
  --resource-group Nexy \
  --name nexy-regular \
  --command-id RunShellScript \
  --scripts "echo '1'"

az vm run-command invoke \
  --resource-group Nexy \
  --name nexy-regular \
  --command-id RunShellScript \
  --scripts "echo '2'"
```

---

### **Решение 2: Использовать таймауты**

**❌ Плохо:**
```bash
--scripts "curl http://example.com"
```

**✅ Хорошо:**
```bash
--scripts "timeout 10 curl http://example.com || echo 'Timeout'"
```

---

### **Решение 3: Перезапустить VM**

**Если команда зависла:**
```bash
# Перезапустить VM для сброса состояния
az vm restart --resource-group Nexy --name nexy-regular

# Подождать перезапуска (30-60 секунд)
sleep 60

# Выполнить команду снова
az vm run-command invoke \
  --resource-group Nexy \
  --name nexy-regular \
  --command-id RunShellScript \
  --scripts "sudo systemctl restart voice-assistant.service"
```

---

### **Решение 4: Использовать SSH напрямую**

**Если есть SSH доступ:**
```bash
ssh azureuser@20.151.51.172
# Затем выполнить команды напрямую
sudo systemctl restart voice-assistant.service
curl -s http://127.0.0.1:8080/health
```

---

### **Решение 5: Создать скрипт на сервере**

**Шаг 1: Загрузить скрипт на сервер (короткая команда)**
```bash
az vm run-command invoke \
  --resource-group Nexy \
  --name nexy-regular \
  --command-id RunShellScript \
  --scripts "cat > /home/azureuser/fix_502.sh << 'SCRIPT'
#!/bin/bash
# ... скрипт ...
SCRIPT
chmod +x /home/azureuser/fix_502.sh
"
```

**Шаг 2: Выполнить скрипт позже (короткая команда)**
```bash
az vm run-command invoke \
  --resource-group Nexy \
  --name nexy-regular \
  --command-id RunShellScript \
  --scripts "bash /home/azureuser/fix_502.sh"
```

---

## 🚨 **Критические моменты**

### **❌ НЕ ДЕЛАЙТЕ:**
- ❌ Не используйте длинные команды с множеством операций
- ❌ Не используйте `sleep` без необходимости
- ❌ Не запускайте интерактивные команды
- ❌ Не запускайте команды без таймаутов
- ❌ Не используйте `curl` без таймаута

### **✅ ОБЯЗАТЕЛЬНО:**
- ✅ Разбивайте длинные команды на короткие (< 30 секунд)
- ✅ Используйте таймауты для сетевых операций
- ✅ Используйте флаги `--yes` или `-y` для интерактивных команд
- ✅ Проверяйте статус выполнения перед следующей командой
- ✅ Используйте SSH напрямую для долгих операций

---

## 📊 **Рекомендации**

1. **Используйте короткие команды** - разбивайте длинные операции на несколько коротких команд
2. **Используйте таймауты** - добавляйте таймауты для сетевых операций
3. **Избегайте sleep** - не используйте `sleep` в скриптах для Azure CLI
4. **Используйте SSH** - для долгих операций используйте SSH напрямую
5. **Перезапускайте VM** - если команда зависла, перезапустите VM для сброса состояния

---

## 🔗 **Связанные документы**

- [TROUBLESHOOTING_502.md](./TROUBLESHOOTING_502.md) - Диагностика проблемы 502 Bad Gateway
- [AZURE_CLI_TIMEOUT_ISSUE.md](./AZURE_CLI_TIMEOUT_ISSUE.md) - Проблема с таймаутом Azure CLI

