# Шаблон PR-чеклиста для серверной части Nexy

**Назначение:** Единый чеклист для всех PR, затрагивающих серверную часть. Заполняется перед мерджем.

---

## 📋 Обязательные проверки

### **1. Код и архитектура**

- [ ] Изменения соответствуют принципам из `Docs/SERVER_DEVELOPMENT_RULES.md`
- [ ] Нет прямых межмодульных вызовов (координация только через интеграции)
- [ ] Конфигурация читается через `server/config/unified_config.py` (без локальных YAML/JSON)
- [ ] Все модули следуют `UniversalModuleInterface` (если затронуты модули)
- [ ] Логирование использует единый формат из `server/main.py`

### **2. gRPC совместимость** (если затронут gRPC контракт)

- [ ] Изменения в `streaming.proto`:
  - [ ] Только добавлены optional-поля (без breaking-изменений)
  - [ ] Или создан v2 сервис/метод + feature-флаг (для breaking-изменений)
  - [ ] Protobuf файлы регенерированы: `python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. modules/grpc_service/streaming.proto`
- [ ] Smoke-тест с текущим клиентом пройден (локально или в CI)
- [ ] Синхронизация с клиентской командой выполнена (если breaking-изменения)
- [ ] Документация обновлена (`Docs/GRPC_COMPATIBILITY.md` или эквивалент)

### **3. Конфигурация и секреты**

- [ ] Новые параметры добавлены в `config/unified_config_example.yaml`
- [ ] Переменные окружения задокументированы в `config.env.example`
- [ ] Секреты не закоммичены (проверен `.gitignore`)
- [ ] Описание новых параметров в `Docs/ARCHITECTURE_OVERVIEW.md`

### **4. Тестирование**

- [ ] Unit-тесты для изменённых модулей (минимум 1 happy + 1 негативный)
- [ ] Integration-тесты для изменённых интеграций
- [ ] Perf-тесты не падают (если затронута производительность):
  - [ ] p95 ≤ порога (см. `tests/perf/test_grpc_p95.py`)
  - [ ] CI выводит p95/девиацию
- [ ] Локальный запуск сервера успешен: `python -m server.main` (с `venv` и `.env`)

### **5. Документация**

- [ ] Обновлены соответствующие MD-файлы (1–2 абзаца):
  - [ ] `Docs/ARCHITECTURE_OVERVIEW.md` (если затронута архитектура)
  - [ ] `Docs/SERVER_DEVELOPMENT_RULES.md` (если изменены правила)
  - [ ] `Docs/GLOBAL_DELIVERY_PLAN.md` (если затронут деплой)
  - [ ] `Docs/SERVER_DEPLOYMENT_GUIDE.md` (если затронут деплой)
- [ ] Таблицы/секции портов/путей актуализированы (если затронута сеть)
- [ ] Ссылки на единую конфигурацию актуальны

### **6. Сеть и ingress** (если затронута сеть)

- [ ] Скрипт `scripts/check_ingress.sh` даёт `OK` по всем эндпоинтам:
  - [ ] `/health` → `200 OK`
  - [ ] `/status` → JSON с версией/конфигом
  - [ ] `/updates/health` → текущая версия обновлений
- [ ] Nginx конфигурация актуализирована (если изменены пути/порты)
- [ ] В `Docs/ARCHITECTURE_OVERVIEW.md` таблица портов/путей актуальна

### **7. Система обновлений** (если затронута система обновлений)

- [ ] Скрипт `scripts/validate_updates.sh` проходит:
  - [ ] `appcast.xml` содержит `sparkle:version` (строка)
  - [ ] `health` возвращает `latest_version` и `latest_build` (строки)
  - [ ] `build == version` (оба строки)
  - [ ] Размер DMG совпадает с GitHub CDN
  - [ ] SHA файла совпадает с GitHub релизом
- [ ] CI шаг валидации обновлений добавлен/обновлён

### **8. Feature-flags и kill-switch** (если добавлена новая функциональность)

- [ ] Новые фичи включены через `features.*` в конфиге
- [ ] Kill-switch добавлен в `kill_switches.*` (если нужно)
- [ ] Процесс rollout описан (1%→25%→100% или shadow-mode)
- [ ] Сценарий rollback описан в PR-описании

### **9. Логирование и мониторинг**

- [ ] Логи не содержат PII и секретов
- [ ] Старт/остановка сервисов логируются
- [ ] Ошибки gRPC логируются с кодом/метаданными
- [ ] Статусы модулей логируются (если затронуты модули)
- [ ] Мониторинг собирает метрики (если затронут мониторинг)

### **10. Health/Status эндпоинты**

- [ ] `GET /health` → `"OK"` (или JSON с минимальной информацией)
- [ ] `GET /status` → JSON с версией/конфигом/метриками
- [ ] `GET /updates/health` → текущая версия обновлений (если затронута система обновлений)
- [ ] Команды проверки из `Docs/GLOBAL_DELIVERY_PLAN.md` работают

---

## 🎯 Специфичные чеклисты по типу PR

### **PR-0: Валидация окружения и сетевого входа**
- [ ] Скрипт `scripts/check_ingress.sh` создан и работает
- [ ] Проверки certificate SAN, status/health/appcast проходят
- [ ] `Docs/ARCHITECTURE_OVERVIEW.md` обновлён (таблица портов/путей)

### **PR-1: Единая конфигурация**
- [ ] Все модули используют `unified_config.py`
- [ ] Нет прямых загрузок YAML/JSON вне `unified_config.py`
- [ ] `config.env.example` полный и актуальный

### **PR-2: Стандартизация модулей/интеграций**
- [ ] Модули следуют `UniversalModuleInterface`
- [ ] Интеграции координируют, не содержат бизнес-логику
- [ ] Нет прямых межмодульных вызовов

### **PR-3: gRPC совместимость**
- [ ] Изменения протокола документированы
- [ ] Smoke-тест с клиентом пройден
- [ ] Процесс регенерации protobuf документирован

### **PR-4: Логирование и мониторинг**
- [ ] Единый формат логов настроен в `main.py`
- [ ] Мониторинг gRPC ошибок подключен
- [ ] Логи не содержат PII/секретов

### **PR-5: Валидация обновлений**
- [ ] Скрипт `scripts/validate_updates.sh` создан
- [ ] CI шаг валидации добавлен
- [ ] Проверки версий/размеров/SHA работают

### **PR-6: SLO-гейты**
- [ ] Perf-тест `tests/perf/test_grpc_p95.py` создан/обновлён
- [ ] CI выводит p95/девиацию
- [ ] Пороги документированы

### **PR-7: Feature-flags**
- [ ] Секции `features.*` и `kill_switches.*` в конфиге
- [ ] Процесс rollout описан
- [ ] Сценарий rollback описан

### **PR-8: Health/Status**
- [ ] Эндпоинты `/health`, `/status`, `/updates/health` работают
- [ ] Команды проверки из гидов обновлены
- [ ] Раздел «Проверка успешного деплоя» добавлен

### **PR-9: Тест-стратегия**
- [ ] Unit/integration/scenario тесты добавлены
- [ ] CI стадии «unit», «integration», «perf», «smoke grpc» настроены
- [ ] `TEST_PLAN_TEMPLATE.md` обновлён

---

## 📝 Приложение к PR

**Обязательно приложить:**

1. **Diff** (код/скрипты/конфиги)
2. **Док-обновления** (1–2 абзаца в соответствующих MD)
3. **Скрин/лог проверок** (скрипты, тесты, smoke-тесты)
4. **Критерии приёмки** выполнены (чекбоксы выше)

**Примеры логов:**

```bash
# Проверка ingress
$ ./scripts/check_ingress.sh
✓ /health: OK
✓ /status: OK (version: 1.0.0)
✓ /updates/health: OK (latest_version: 1.0.0)

# Валидация обновлений
$ ./scripts/validate_updates.sh
✓ AppCast version: 1.0.0 (string)
✓ Health version: 1.0.0 (string)
✓ Build == version: OK
✓ DMG size matches GitHub: OK

# Perf-тест
$ python tests/perf/test_grpc_p95.py
p95: 450ms (threshold: 600ms) ✓
```

---

## 🔗 Связанные документы

- `Docs/SERVER_DEVELOPMENT_RULES.md` — правила разработки
- `Docs/ARCHITECTURE_OVERVIEW.md` — архитектура
- `Docs/GLOBAL_DELIVERY_PLAN.md` — план доставки

---

**Последнее обновление:** 3 октября 2025

