# ⚡ Анализ производительности обработки запросов

## 📊 Текущие метрики

### Из логов сервера:
- **p95 latency для StreamAudio**: ~22.4 секунды (22441ms)
- **Последний запрос**: 20.5 секунд (20468ms)
- **Медленный ответ**: 17.46s (лимит: 5.0s)
- **Error rate**: 16.7% (лимит: 5.0%)

### Проблема
Обработка запроса занимает **5-7 секунд** (по ощущениям пользователя) или **20+ секунд** (по метрикам), что превышает целевой лимит в **5 секунд**.

---

## 🔍 Анализ узких мест (Bottlenecks)

### 1. LLM обработка (TextProcessor) - **Основной bottleneck**

**Файл:** `server/modules/text_processing/providers/langchain_gemini_provider.py`  
**Метод:** `process_text_streaming()`

**Проблемы:**
- **Задержка API Gemini**: Запросы к Gemini API могут занимать **3-10 секунд** в зависимости от:
  - Сложности запроса
  - Размера контекста (memory + screenshot)
  - Загрузки API
  - Сетевой задержки

**Таймауты:**
```python
request_timeout: int = 300  # 5 минут (слишком большой)
```

**Влияние:**
- Если LLM отвечает медленно, весь запрос блокируется
- Потоковая обработка не помогает, если первый чанк приходит через 5+ секунд

---

### 2. TTS генерация (AudioProcessor) - **Вторичный bottleneck**

**Файл:** `server/modules/audio_generation/providers/edge_tts_provider.py`  
**Метод:** `process()`

**Проблемы:**
- **Edge TTS API задержка**: Запросы к Edge TTS могут занимать **1-3 секунды** на предложение
- **Конвертация MP3 → PCM**: Дополнительная обработка каждого чанка
- **Последовательная обработка**: Аудио генерируется последовательно для каждого предложения

**Влияние:**
- Если у нас 3 предложения, TTS может занять **3-9 секунд**
- Это добавляется к времени LLM обработки

---

### 3. Получение контекста памяти - **Потенциальный bottleneck**

**Файл:** `server/integrations/workflow_integrations/streaming_workflow_integration.py`  
**Метод:** `_get_memory_context_parallel()`

**Проблемы:**
- **Запрос к БД**: Если память не в кэше, запрос к PostgreSQL может занять **100-500ms**
- **Анализ памяти**: `MemoryAnalyzer` может обрабатывать данные **200-1000ms**

**Текущая оптимизация:**
```python
# Предзагрузка в фоне (не блокирует)
asyncio.create_task(
    self.memory_workflow.prefetch_memory(hardware_id)
)

# Параллельное получение (может быть медленным)
memory_context = await self._get_memory_context_parallel(hardware_id)
```

**Влияние:**
- Если память не в кэше, это добавляет **300-1500ms** к общему времени

---

### 4. Парсинг JSON - **Минимальный bottleneck**

**Файл:** `server/integrations/workflow_integrations/streaming_workflow_integration.py`  
**Метод:** `_parse_assistant_response()`

**Проблемы:**
- **Накопление JSON**: Если LLM возвращает JSON частями, мы ждем полного JSON
- **Множественные попытки парсинга**: До 10 попыток (`MAX_JSON_PARSE_ATTEMPTS = 10`)

**Влияние:**
- Обычно **<100ms**, но может быть больше для больших JSON

---

## 📈 Распределение времени обработки (пример)

Для типичного запроса **"Hello, can you help me?"**:

```
┌─────────────────────────────────────────────────────────────┐
│ Общее время: ~20 секунд                                     │
├─────────────────────────────────────────────────────────────┤
│ 1. Получение памяти:         300-1500ms  (1.5%)            │
│ 2. LLM обработка (Gemini):   5000-10000ms (50%) ⚠️         │
│ 3. Парсинг JSON:             50-200ms    (1%)              │
│ 4. Разбиение на предложения: 10-50ms     (0.2%)            │
│ 5. TTS генерация (Edge TTS): 3000-9000ms (45%) ⚠️          │
│ 6. Конвертация MP3→PCM:       500-2000ms  (10%)             │
│ 7. Сетевые задержки:          200-500ms   (2%)              │
└─────────────────────────────────────────────────────────────┘
```

**Основные узкие места:**
1. **LLM (Gemini API)**: 50% времени
2. **TTS (Edge TTS)**: 45% времени
3. **Остальное**: 5% времени

---

## 🎯 Рекомендации по оптимизации

### Приоритет 1: Оптимизация LLM обработки

#### 1.1. Использование более быстрой модели
```yaml
# config.env
LANGCHAIN_MODEL=gemini-3-flash-preview  # Текущая (быстрая)
# Или попробовать:
# LANGCHAIN_MODEL=gemini-1.5-flash  # Еще быстрее, но менее качественная
```

#### 1.2. Уменьшение max_tokens
```yaml
# config.env
TEXT_PROCESSING_MAX_TOKENS=1024  # Было 2048
```

#### 1.3. Оптимизация system prompt
- Уменьшить размер system prompt
- Убрать избыточные инструкции

#### 1.4. Параллельная обработка
- Если запрос простой, можно использовать более быструю модель
- Для сложных запросов - более качественную модель

#### 1.5. Кэширование ответов
- Кэшировать частые запросы (например, "Hello", "How are you?")
- Использовать Redis или in-memory кэш

---

### Приоритет 2: Оптимизация TTS генерации

#### 2.1. Параллельная генерация аудио
**Текущая проблема:** Аудио генерируется последовательно для каждого предложения

**Решение:** Генерировать аудио параллельно для всех предложений
```python
# Вместо:
for sentence in sentences:
    audio = await generate_audio(sentence)  # Последовательно

# Использовать:
audio_tasks = [generate_audio(s) for s in sentences]
audio_results = await asyncio.gather(*audio_tasks)  # Параллельно
```

#### 2.2. Оптимизация конвертации MP3→PCM
- Использовать более быструю библиотеку (например, `ffmpeg` вместо `pydub`)
- Кэшировать конвертированное аудио для повторяющихся фраз

#### 2.3. Предзагрузка голосов
- Предзагружать голоса Edge TTS при старте сервера
- Использовать локальный TTS для простых фраз

---

### Приоритет 3: Оптимизация памяти

#### 3.1. Улучшение кэширования
```python
# Текущая реализация уже использует кэш, но можно улучшить:
# - Увеличить TTL кэша
# - Использовать более быстрый кэш (Redis вместо in-memory)
# - Предзагружать память для активных hardware_id
```

#### 3.2. Асинхронная загрузка памяти
```python
# Уже реализовано, но можно улучшить:
# - Загружать память в фоне, не блокируя обработку
# - Использовать "stale-while-revalidate" стратегию
```

---

### Приоритет 4: Оптимизация парсинга JSON

#### 4.1. Уменьшение попыток парсинга
```python
MAX_JSON_PARSE_ATTEMPTS = 5  # Было 10
```

#### 4.2. Более быстрый парсинг
- Использовать `orjson` вместо стандартного `json`
- Парсить только необходимые поля

---

## 🚀 Быстрые улучшения (Quick Wins)

### 1. Уменьшить таймауты
```python
# config.env
REQUEST_TIMEOUT=60  # Было 300 (5 минут)
```

### 2. Уменьшить max_tokens
```python
# config.env
TEXT_PROCESSING_MAX_TOKENS=1024  # Было 2048
```

### 3. Использовать более быструю модель Gemini
```python
# config.env
LANGCHAIN_MODEL=gemini-1.5-flash  # Вместо gemini-3-flash-preview
```

### 4. Включить кэширование ответов
```python
# Добавить простой in-memory кэш для частых запросов
from functools import lru_cache

@lru_cache(maxsize=100)
def get_cached_response(text: str) -> Optional[str]:
    # Проверка кэша
    pass
```

---

## 📊 Мониторинг производительности

### Метрики для отслеживания:
1. **LLM latency**: Время от запроса до первого чанка
2. **TTS latency**: Время генерации аудио для одного предложения
3. **Memory latency**: Время получения контекста памяти
4. **Total latency**: Общее время обработки запроса

### Логирование:
```python
# Добавить детальное логирование времени на каждом этапе
logger.info(f"⏱️ LLM latency: {llm_time}ms")
logger.info(f"⏱️ TTS latency: {tts_time}ms")
logger.info(f"⏱️ Memory latency: {memory_time}ms")
logger.info(f"⏱️ Total latency: {total_time}ms")
```

---

## 🎯 Целевые метрики

### Текущие:
- p95 latency: **22.4 секунды** ❌
- Целевой лимит: **5 секунд** ✅

### После оптимизации (цель):
- p95 latency: **<5 секунд** ✅
- LLM latency: **<2 секунды** ✅
- TTS latency: **<1 секунда на предложение** ✅
- Memory latency: **<200ms** ✅

---

## 📝 План действий

### Фаза 1: Быстрые улучшения (1-2 дня)
- [ ] Уменьшить `max_tokens` до 1024
- [ ] Уменьшить `REQUEST_TIMEOUT` до 60 секунд
- [ ] Добавить детальное логирование latency
- [ ] Включить кэширование частых запросов

### Фаза 2: Оптимизация LLM (3-5 дней)
- [ ] Оптимизировать system prompt
- [ ] Реализовать параллельную обработку для простых запросов
- [ ] Добавить fallback на более быструю модель

### Фаза 3: Оптимизация TTS (3-5 дней)
- [ ] Реализовать параллельную генерацию аудио
- [ ] Оптимизировать конвертацию MP3→PCM
- [ ] Добавить кэширование аудио

### Фаза 4: Долгосрочные улучшения (1-2 недели)
- [ ] Миграция на более быстрый TTS провайдер
- [ ] Реализация полноценного кэширования (Redis)
- [ ] Оптимизация архитектуры для снижения latency

---

## 🔗 Связанные документы

- `Docs/REQUEST_PROCESSING_FLOW.md` - Полный flow обработки запросов
- `server/modules/text_processing/FULL_FLOW_DOCUMENTATION.md` - Документация обработки текста
- `server/modules/audio_generation/FULL_FLOW_DOCUMENTATION.md` - Документация генерации аудио
