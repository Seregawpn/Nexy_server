# 🔄 Переключение Azure аккаунта

**Проблема:** Текущий аккаунт использует Service Principal для GitHub Actions, не подходит для ручного развертывания.

---

## 🔍 Текущая ситуация

- **Тип аккаунта:** Service Principal
- **Имя:** `nexy-github-actions`
- **Назначение:** GitHub Actions автоматизация
- **Проблема:** Не подходит для ручного развертывания через Azure CLI

---

## ✅ Решение: Переключение на пользовательский аккаунт

### Шаг 1: Выход из текущего аккаунта

```bash
az logout
```

### Шаг 2: Вход под обычным пользователем

```bash
az login
```

Откроется браузер для авторизации. Войдите под вашим обычным Azure аккаунтом (не Service Principal).

### Шаг 3: Проверка аккаунта

```bash
az account show
```

Должно показать:
- `"type": "user"` (не `"servicePrincipal"`)
- Ваше имя пользователя (не GUID)

### Шаг 4: Выбор подписки (если несколько)

```bash
# Список доступных подписок
az account list --output table

# Выбор нужной подписки
az account set --subscription "SUBSCRIPTION_ID_OR_NAME"
```

---

## 🔍 Проверка готовности

После переключения проверьте:

```bash
# 1. Тип аккаунта
az account show --query "{Name:name, UserType:user.type, UserName:user.name}" -o json

# 2. Права доступа
az role assignment list --assignee $(az account show --query user.name -o tsv) --output table

# 3. Доступные подписки
az account list --output table
```

---

## ⚠️ Важно

- Service Principal (`nexy-github-actions`) используется только для GitHub Actions
- Для ручного развертывания нужен обычный пользовательский аккаунт
- Убедитесь, что у пользовательского аккаунта есть права **Contributor** или **Owner** на подписку

---

## 🚀 После переключения

Когда аккаунт переключен, можно запускать развертывание:

```bash
cd server/scripts
./deploy_new_azure_account.sh
```

---

**Готовы переключиться? Выполните: `az logout && az login`**
