> [!WARNING] ARCHIVE NOTICE
> Этот документ архивный и не является source of truth.
> Актуальные каноны:
> - `server/Docs/SERVER_DEPLOYMENT_GUIDE.md` (деплой кода на удаленный сервер)
> - `server/Docs/RELEASE_AND_UPDATE_GUIDE.md` (публикация DMG/PKG и update-канал)
> - `server/Docs/DEPLOY_INCIDENT_RUNBOOK.md` (инциденты, зависимости, конфиги, rollback)

# 🚀 План поставки — серверная часть

**Дата:** 2 октября 2025  
**Статус:** активный  

**См. также:** `Docs/CURRENT_STATUS_REPORT.md` — актуальный статус реализации (что уже сделано, что работает).

Системный обзор (клиент + сервер): `Docs/SYSTEM_CONCEPT.md`.

---

## 1) Цели

- Стабильный gRPC сервис на Azure VM.
- Надежные health/status endpoints.
- Update service для AppCast/артефактов.
- Мониторинг и базовые алерты.

---

## 2) Канон и источники истины

- Архитектура: `server/server/Docs/ARCHITECTURE_OVERVIEW.md`
- Flow/контракты: `server/server/Docs/FLOW_INTERACTION_SPEC.md`
- gRPC аудит: `server/server/Docs/GRPC_PROTOCOL_AUDIT.md`

---

## 3) План внедрения

1. **gRPC сервис**
   - Запуск `server/server/main.py`.
   - Проверка `grpc://127.0.0.1:50051`.
2. **Update service**
   - `/updates/` за Nginx → `127.0.0.1:8081`.
3. **Nginx/TLS**
   - Внешний `443/HTTPS` → gRPC + update endpoints.
4. **Мониторинг**
   - Метрики, health/status, базовые алерты.

---

## 4) Проверки (DoD)

- gRPC отвечает на `50051` (локально) и через `443` (внешне).
- `/health` и `/status` доступны.
- Update service отдает AppCast.
- Нагрузочный тест не деградирует latency выше допустимого.
