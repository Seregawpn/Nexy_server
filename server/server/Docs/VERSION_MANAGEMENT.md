# 📋 Централизованное управление версией

**Дата создания:** 11 января 2026  
**Статус:** ✅ Активно используется

---

## 🎯 Цель

Единая точка управления версией сервера. Изменение версии в одном месте автоматически обновляет все необходимые файлы и конфигурации.

---

## 📁 Единый источник истины

**Файл:** `VERSION` (в корне проекта)

```
1.6.0.35
```

Этот файл является единственным источником истины для версии сервера.

---

## 🔧 Использование

### Чтение текущей версии

```bash
python3 server/scripts/update_version.py --read
```

### Обновление версии

```bash
# Обновить версию до 1.6.0.36
python3 server/scripts/update_version.py 1.6.0.36
```

**Что делает скрипт:**
1. ✅ Обновляет `VERSION` файл
2. ✅ Обновляет `server/config/unified_config.yaml`
3. ✅ Обновляет `server/config/unified_config.py`
4. ✅ Обновляет `server/config.env.example`

---

## 🔄 Автоматическая синхронизация

### На локальном компьютере

При изменении версии через `update_version.py`, все файлы обновляются автоматически.

### На удаленном сервере

После обновления кода через `git pull`, версия автоматически читается из `VERSION` файла через `unified_config.py`.

**Для обновления манифеста на сервере:**

```bash
# Автоматическая синхронизация манифеста
server/scripts/sync_version_to_server.sh
```

---

## 📊 Где используется версия

### 1. Конфигурационные файлы

- `VERSION` - единый источник истины
- `server/config/unified_config.yaml` - обновляется скриптом
- `server/config/unified_config.py` - читает из VERSION файла
- `server/config.env.example` - обновляется скриптом

### 2. Runtime (во время выполнения)

- `unified_config.py` читает версию из `VERSION` файла при импорте
- Если `VERSION` файл не найден, используется переменная окружения `SERVER_VERSION`
- Если переменная окружения не задана, используется дефолтное значение

### 3. Серверные endpoints

- `/health` - возвращает `latest_version` и `latest_build`
- `/status` - возвращает `latest_version` и `latest_build`
- `/updates/health` - возвращает `version` и `latest_version`

### 4. Манифест обновлений

- `server/updates/manifests/manifest.json` - обновляется на сервере через скрипт

---

## 🚀 Workflow обновления версии

### Локально

```bash
# 1. Обновить версию
python3 server/scripts/update_version.py 1.6.0.36

# 2. Проверить изменения
git diff

# 3. Закоммитить
git add -A
git commit -m "Update version to 1.6.0.36"

# 4. Создать тег
git tag 1.6.0.36 -m "Release 1.6.0.36"

# 5. Отправить на GitHub
git push origin main
git push origin 1.6.0.36
```

### На сервере

```bash
# 1. Обновить код
cd /home/azureuser/voice-assistant
git pull origin main

# 2. Версия автоматически читается из VERSION файла
# 3. Манифест можно обновить вручную или через скрипт
server/scripts/sync_version_to_server.sh
```

---

## 🔍 Проверка версии

### Локально:

```bash
# Читать из VERSION файла
cat VERSION

# Или через скрипт
python3 server/scripts/update_version.py --read
```

### На сервере:

```bash
# Health endpoint
curl -sk https://20.63.24.187/health

# Status endpoint
curl -sk https://20.63.24.187/status

# Update health
curl -sk https://20.63.24.187/updates/health

# Манифест
cat server/updates/manifests/manifest.json | python3 -m json.tool
```

---

## ⚙️ Технические детали

### Как работает чтение версии

1. **Приоритет 1:** Файл `VERSION` (если существует и не пустой)
2. **Приоритет 2:** Переменная окружения `SERVER_VERSION`
3. **Приоритет 3:** Дефолтное значение в коде

### Функция `get_version_from_file()`

```python
def get_version_from_file() -> str:
    """
    Читает версию из VERSION файла (единый источник истины)
    """
    version_file = Path(__file__).parent.parent.parent / "VERSION"
    
    if version_file.exists():
        try:
            version = version_file.read_text().strip()
            if version:
                return version
        except Exception as e:
            logger.warning(f"Не удалось прочитать VERSION файл: {e}")
    
    return os.getenv('SERVER_VERSION', '1.6.0.35')
```

---

## ✅ Преимущества

1. **Единая точка истины** - версия хранится в одном месте
2. **Автоматическое обновление** - скрипт обновляет все файлы
3. **Синхронизация** - версия автоматически синхронизируется между локальной и серверной частями
4. **Простота** - одна команда для обновления версии
5. **Надежность** - меньше ошибок при ручном обновлении

---

## 📝 Примеры

### Обновление версии

```bash
# Обновить до 1.6.0.36
python3 server/scripts/update_version.py 1.6.0.36

# Результат:
# ✅ Версия 1.6.0.36 записана в VERSION
# ✅ Обновлен server/config/unified_config.yaml
# ✅ Обновлен server/config/unified_config.py
# ✅ Обновлен server/config.env.example
```

### Проверка версии

```bash
# Локально
python3 server/scripts/update_version.py --read
# Текущая версия: 1.6.0.35

# На сервере
curl -sk https://20.63.24.187/health
# {"status": "OK", "latest_version": "1.6.0.35", "latest_build": "1.6.0.35"}
```

---

## 🔗 Связанные файлы

- `VERSION` - единый источник истины
- `server/scripts/update_version.py` - скрипт обновления версии
- `server/scripts/sync_version_to_server.sh` - синхронизация на сервер
- `server/config/unified_config.py` - читает версию из VERSION
- `server/config/unified_config.yaml` - обновляется скриптом
- `server/config.env.example` - обновляется скриптом

---

**📞 Поддержка:** Документация в `server/Docs/`  
**🔗 Репозиторий:** `https://github.com/Seregawpn/Nexy_server`
