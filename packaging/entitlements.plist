<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
 "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Вне App Store: sandbox выключен -->
  <key>com.apple.security.app-sandbox</key><false/>
  
  <!-- Нужен, если посылаете Apple Events -->
  <key>com.apple.security.automation.apple-events</key><true/>
  
  <!-- Доступ к микрофону -->
  <key>com.apple.security.device.microphone</key><true/>
  
  <!-- Доступ к аудио входу (требуется для Hardened Runtime) -->
  <key>com.apple.security.device.audio-input</key><true/>

  <!-- Доступ к захвату экрана (Screen Recording) -->
  <key>com.apple.security.device.screen-capture</key><true/>

  <!-- Network для gRPC -->
  <key>com.apple.security.network.client</key><true/>
  
  <!-- Доступ к файловой системе -->
  <key>com.apple.security.files.user-selected.read-write</key><true/>
  <key>com.apple.security.files.downloads.read-write</key><true/>
  
  <!-- БЕЗОПАСНЫЕ флаги для PyInstaller (минимальный набор) -->
  <!-- Отключаем Library Validation ТОЛЬКО для PyInstaller onefile -->
  <key>com.apple.security.cs.disable-library-validation</key><true/>
</dict>
</plist>