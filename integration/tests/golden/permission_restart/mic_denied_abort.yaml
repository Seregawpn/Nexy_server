# Golden test: Microphone denied → ABORT
# Snapshot of axes → Expected decision
---
name: "mic_denied_abort"
description: "Microphone permission denied should abort listening"
snapshot:
  perm_mic: "denied"
  perm_screen: "granted"
  perm_accessibility: "granted"
  device_input: "default_ok"
  network: "online"
  first_run: false
  app_mode: "SLEEPING"
expected_decision: "abort"
expected_source: "listening_gateway"
expected_ctx_keys:
  - "mic"
  - "device"
  - "network"

