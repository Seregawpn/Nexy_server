# 🌐 Открытие URL на клиенте

**→ Основной документ:** `../COMPLETE_SYSTEM_LOGIC.md` раздел "Этап 7: Отправка URL на клиент"

---

## 📋 Краткое описание

Открытие URL включает:
- Команда `open_url` от сервера
- Формат `action_json`: `{"command": "open_url", "args": {"url": "..."}}`
- Реализация через macOS `open` или Python `webbrowser`
- Обработка ошибок

---

## 🔗 Полная документация

**См. `../COMPLETE_SYSTEM_LOGIC.md` раздел "Этап 7: Отправка URL на клиент для открытия браузера"** для:
- Полного описания механизма
- Формирования action_message на сервере
- Обработки на клиенте
- Примеров кода

---

## 📝 Быстрая ссылка

**Команда:** `open_url`

**Формат от сервера:**
```json
{
  "command": "open_url",
  "args": {
    "url": "https://checkout.stripe.com/..."
  }
}
```

**Реализация на клиенте:**
```python
# В ActionExecutionIntegration
if command == "open_url":
    url = args.get("url")
    subprocess.run(["open", url])  # macOS
    # или
    webbrowser.open(url)  # Python
```

**Файлы для изменения:**
- `client(Messages)/integration/integrations/action_execution_integration.py`
- Добавить `open_url` в `valid_commands`

---

**Следующий шаг:** Откройте `../COMPLETE_SYSTEM_LOGIC.md` раздел "Этап 7"

