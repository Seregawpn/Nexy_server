# Отчет о статусе сертификатов macOS для упаковки

**Дата проверки:** 25 ноября 2025

## ✅ Статус сертификатов

### 1. Developer ID Application
- **Статус:** ✅ **ВАЛИДЕН**
- **Имя:** `Developer ID Application: Sergiy Zasorin (5NKLL2CLB9)`
- **SHA-1:** `950F71B24040521A526AC2F28F37BD882585CD06`
- **Действителен с:** 8 сентября 2025
- **Действителен до:** 9 сентября 2030
- **Назначение:** Подпись .app bundle
- **Проверка:**
  ```bash
  security find-identity -v -p codesigning | grep "Developer ID Application"
  # Результат: ✅ Найден и валиден
  ```

### 2. Developer ID Installer
- **Статус:** ✅ **ВАЛИДЕН**
- **Имя:** `Developer ID Installer: Sergiy Zasorin (5NKLL2CLB9)`
- **SHA-1:** `2B7D901532ABA156D123F2C065D7A47E34DBD9BE`
- **Действителен с:** 8 сентября 2025
- **Действителен до:** 9 сентября 2030
- **Назначение:** Подпись .pkg installer
- **Проверка:**
  ```bash
  security find-identity -v | grep "Developer ID Installer"
  # Результат: ✅ Найден и валиден
  ```

## ⚠️ Настройка нотаризации

### Keychain Profile для нотаризации
- **Статус:** ❌ **НЕ НАСТРОЕН**
- **Требуемый profile:** `nexy-notary`
- **Apple ID:** `seregawpn@gmail.com`
- **Team ID:** `5NKLL2CLB9`

### Настройка keychain profile

**ВАЖНО:** Перед нотаризацией необходимо настроить keychain profile:

```bash
xcrun notarytool store-credentials "nexy-notary" \
  --apple-id "seregawpn@gmail.com" \
  --team-id "5NKLL2CLB9" \
  --password "<app-specific-password>"
```

**Где получить app-specific password:**
1. Перейти на https://appleid.apple.com
2. Войти с Apple ID: `seregawpn@gmail.com`
3. Раздел "Sign-In and Security" → "App-Specific Passwords"
4. Создать новый пароль для "Nexy Notarization"
5. Использовать этот пароль в команде выше

### Альтернатива: прямой способ (без keychain profile)

Если keychain profile не настроен, можно использовать прямой способ в скрипте:

```bash
xcrun notarytool submit "$ZIP_FILE" \
  --apple-id "seregawpn@gmail.com" \
  --password "<app-specific-password>" \
  --team-id "5NKLL2CLB9" \
  --wait
```

**Примечание:** В текущем скрипте `packaging/build_final.sh` используется keychain profile, поэтому его нужно настроить или изменить скрипт.

## 🔧 Инструменты

### notarytool
- **Версия:** 1.1.0 (39)
- **Статус:** ✅ Установлен и работает
- **Проверка:**
  ```bash
  xcrun notarytool --version
  # Результат: 1.1.0 (39)
  ```

### codesign
- **Статус:** ✅ Доступен
- **Проверка:**
  ```bash
  codesign --version
  # Результат: работает
  ```

## 📋 Чек-лист готовности к упаковке

- [x] Developer ID Application сертификат установлен и валиден
- [x] Developer ID Installer сертификат установлен и валиден
- [x] Сертификаты действительны до 2030 года (5 лет)
- [x] notarytool установлен и работает
- [ ] ⚠️ Keychain profile "nexy-notary" настроен (требуется настройка)
- [ ] App-specific password создан (требуется создание)

## 🚀 Следующие шаги

1. **Создать app-specific password:**
   - Перейти на https://appleid.apple.com
   - Создать app-specific password для нотаризации

2. **Настроить keychain profile:**
   ```bash
   xcrun notarytool store-credentials "nexy-notary" \
     --apple-id "seregawpn@gmail.com" \
     --team-id "5NKLL2CLB9" \
     --password "<app-specific-password>"
   ```

3. **Проверить настройку:**
   ```bash
   # Попробовать отправить тестовый файл (если есть)
   xcrun notarytool submit test.zip \
     --keychain-profile "nexy-notary" \
     --wait
   ```

## 📝 Использование в скриптах

Текущий скрипт `packaging/build_final.sh` использует:
- `IDENTITY="Developer ID Application: Sergiy Zasorin (5NKLL2CLB9)"` ✅
- `INSTALLER_IDENTITY="Developer ID Installer: Sergiy Zasorin (5NKLL2CLB9)"` ✅
- `--keychain-profile "nexy-notary"` ⚠️ (требует настройки)

## ✅ Итоговый статус

**Сертификаты:** ✅ **ГОТОВЫ К ИСПОЛЬЗОВАНИЮ**
- Оба сертификата валидны до 2030 года
- Правильно настроены в скриптах сборки

**Нотаризация:** ⚠️ **ТРЕБУЕТ НАСТРОЙКИ**
- Keychain profile не настроен
- Требуется создание app-specific password
- После настройки нотаризация будет работать автоматически

---

**Рекомендация:** Настроить keychain profile перед следующей сборкой для автоматической нотаризации.

