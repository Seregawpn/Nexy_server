# Руководство по тестированию Control+N

## ✅ Быстрая проверка конфигурации

Запустите скрипт быстрой проверки:

```bash
python3 scripts/test_ctrl_n_quick.py
```

Этот скрипт проверит:
- ✅ Загрузку конфигурации из `unified_config.yaml`
- ✅ Правильность параметров (`key_to_monitor: ctrl_n`)
- ✅ Импорт всех необходимых модулей
- ✅ Создание мониторов (Quartz и fallback)
- ✅ Правильность keycodes (Control: 59/62, N: 45)

## 🧪 Полное тестирование

Запустите интерактивный тест:

```bash
python3 tests/test_ctrl_n_combo.py
```

### Что тестируется:

1. **QuartzKeyboardMonitor** (macOS нативный):
   - Обработка комбинации Control+N
   - Генерация событий PRESS, SHORT_PRESS, LONG_PRESS, RELEASE
   - Подавление keyDown для N при активной комбо
   - Игнорирование автоповтора N

2. **KeyboardMonitor** (fallback/pynput):
   - Обработка комбинации через pynput
   - Та же стейт-машина, что и в Quartz

### Сценарии для тестирования:

#### ✅ Сценарий 1: Быстрый tap Control+N
1. Нажмите Control+N быстро (менее 0.6 секунды)
2. Отпустите обе клавиши
3. **Ожидаемый результат**: PRESS → SHORT_PRESS → RELEASE

#### ✅ Сценарий 2: Удержание Control+N
1. Нажмите Control+N
2. Удерживайте обе клавиши >= 0.6 секунды
3. Отпустите обе клавиши
4. **Ожидаемый результат**: PRESS → LONG_PRESS → RELEASE

#### ✅ Сценарий 3: Отпускание Control раньше N
1. Нажмите Control+N
2. Отпустите Control (N все еще зажата)
3. **Ожидаемый результат**: Комбо деактивируется, генерируется SHORT_PRESS/RELEASE или RELEASE (если был LONG_PRESS)

#### ✅ Сценарий 4: Отпускание N раньше Control
1. Нажмите Control+N
2. Отпустите N (Control все еще зажат)
3. **Ожидаемый результат**: Комбо деактивируется, генерируется SHORT_PRESS/RELEASE или RELEASE (если был LONG_PRESS)

#### ✅ Сценарий 5: Одиночные нажатия (не должны срабатывать)
1. Нажмите только Control (без N)
2. **Ожидаемый результат**: Никаких событий
3. Нажмите только N (без Control)
4. **Ожидаемый результат**: Никаких событий

#### ✅ Сценарий 6: Разные порядки нажатия
1. Нажмите Control, затем N (порядок 1)
2. **Ожидаемый результат**: PRESS когда обе клавиши зажаты
3. Нажмите N, затем Control (порядок 2)
4. **Ожидаемый результат**: PRESS когда обе клавиши зажаты

## 🔍 Проверка статуса монитора

Во время тестирования можно проверить статус монитора:

```python
status = monitor.get_status()
print(status)
```

Для комбинации Control+N статус должен содержать:
- `combo_active: bool` - активна ли комбинация
- `control_pressed: bool` - зажат ли Control
- `n_pressed: bool` - зажата ли N

## ⚠️ Требования для тестирования

### Разрешения macOS:
1. **Accessibility** (System Settings > Privacy & Security > Accessibility)
   - Добавьте Nexy.app и включите переключатель
2. **Input Monitoring** (System Settings > Privacy & Security > Input Monitoring)
   - Добавьте Nexy.app и включите переключатель

### Проверка разрешений:

```bash
# Проверить через Python
python3 -c "from ApplicationServices import AXIsProcessTrusted; print('Accessibility:', AXIsProcessTrusted())"
```

## 🐛 Отладка

### Если события не приходят:

1. **Проверьте разрешения** (см. выше)
2. **Проверьте логи** - должны быть сообщения о создании Event Tap
3. **Проверьте статус монитора** - `keyboard_available` должен быть `True`
4. **Проверьте раскладку клавиатуры** - keycode 45 соответствует N только в US/EN раскладке

### Типичные проблемы:

- **"CGEventTap вернул None"**: Не выданы разрешения Accessibility/Input Monitoring
- **"События не приходят"**: Проверьте разрешения и раскладку клавиатуры
- **"Автоповтор N срабатывает"**: Должен быть исправлен в коде (игнорирование повторных keyDown)

## 📊 Ожидаемые результаты теста

После запуска `test_ctrl_n_combo.py` вы должны увидеть:

1. **При быстром tap Control+N**:
   ```
   ✅ PRESS получен: ctrl_n, timestamp=...
   ✅ SHORT_PRESS получен: ctrl_n, duration=0.1XXs
   ✅ RELEASE получен: ctrl_n, duration=0.1XXs
   ```

2. **При удержании Control+N >= 0.6s**:
   ```
   ✅ PRESS получен: ctrl_n, timestamp=...
   ✅ LONG_PRESS получен: ctrl_n, duration=0.6XXs
   ✅ RELEASE получен: ctrl_n, duration=0.6XXs
   ```

3. **При одиночных нажатиях**:
   - Никаких событий не должно быть

## 🔄 Интеграция с основным приложением

После успешного тестирования можно запустить основное приложение:

```bash
python3 main.py
```

Приложение должно автоматически использовать `ctrl_n` из конфигурации и обрабатывать комбинацию Control+N вместо Shift.


