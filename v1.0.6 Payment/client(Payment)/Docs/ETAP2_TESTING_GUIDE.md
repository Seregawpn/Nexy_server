# 🧪 Руководство по тестированию Этапа 2: MicrophoneStateManager

## ✅ Что было реализовано

1. **MicrophoneStateManager** - централизованный менеджер состояния микрофона
2. **Интеграция с VoiceRecognitionIntegration** - все операции через MicrophoneStateManager
3. **Таймауты и автоматическое восстановление** - защита от залипания
4. **Event-driven архитектура** - события для всех операций

---

## 🧪 Сценарии для тестирования

### Сценарий 1: Нормальная работа (LONG_PRESS → RELEASE)

**Шаги:**
1. Нажмите и удерживайте Control+N (LONG_PRESS)
2. Дождитесь сигнала открытия микрофона
3. Произнесите фразу
4. Отпустите Control+N (RELEASE)

**Ожидаемое поведение:**
- ✅ Микрофон открывается через `MicrophoneStateManager.request_open()`
- ✅ Состояние: `IDLE → OPENING → ACTIVE`
- ✅ Сигнал воспроизводится
- ✅ Запись идет нормально
- ✅ При RELEASE микрофон закрывается через `MicrophoneStateManager.request_close()`
- ✅ Состояние: `ACTIVE → CLOSING → IDLE`
- ✅ Распознавание запускается

**Проверка в логах:**
```
🔄 [MIC_STATE] idle → opening (session=..., reason=recording_start)
🎤 [MIC_STATE] Запрос открытия микрофона для session ...
✅ [MIC_STATE] Микрофон успешно открыт для session ...
🔄 [MIC_STATE] opening → active (session=...)
🔄 [MIC_STATE] active → closing (session=..., reason=recording_stop)
🎤 [MIC_STATE] Запрос закрытия микрофона для session ...
✅ [MIC_STATE] Микрофон успешно закрыт для session ...
🔄 [MIC_STATE] closing → idle (session=...)
```

---

### Сценарий 2: Отмена записи (LONG_PRESS → SHORT_PRESS)

**Шаги:**
1. Нажмите и удерживайте Control+N (LONG_PRESS)
2. Дождитесь сигнала открытия микрофона
3. **БЫСТРО** нажмите Control+N еще раз (SHORT_PRESS) - отмена записи

**Ожидаемое поведение:**
- ✅ Микрофон открывается (состояние: `OPENING → ACTIVE`)
- ✅ При SHORT_PRESS микрофон **принудительно закрывается** через `MicrophoneStateManager.request_close(force=True)`
- ✅ Состояние: `ACTIVE → CLOSING → IDLE`
- ✅ Нет залипания микрофона
- ✅ Таймауты работают корректно

**Проверка в логах:**
```
🔄 [MIC_STATE] idle → opening (session=...)
🔄 [MIC_STATE] opening → active (session=...)
🔄 [MIC_STATE] active → closing (session=None, reason=recording_stop)
🎤 [MIC_STATE] Запрос закрытия микрофона для session None (force=True)
✅ [MIC_STATE] Микрофон успешно закрыт для session None
🔄 [MIC_STATE] closing → idle (session=...)
```

---

### Сценарий 3: Повторная активация (LONG_PRESS → RELEASE → LONG_PRESS)

**Шаги:**
1. Нажмите и удерживайте Control+N (LONG_PRESS)
2. Отпустите Control+N (RELEASE)
3. **СРАЗУ** нажмите и удерживайте Control+N еще раз (LONG_PRESS)

**Ожидаемое поведение:**
- ✅ Первая активация работает нормально
- ✅ Микрофон закрывается после RELEASE
- ✅ Вторая активация работает нормально
- ✅ Нет залипания микрофона
- ✅ Состояние синхронизировано между активациями

**Проверка в логах:**
```
🔄 [MIC_STATE] idle → opening (session=...)
🔄 [MIC_STATE] opening → active (session=...)
🔄 [MIC_STATE] active → closing (session=...)
🔄 [MIC_STATE] closing → idle (session=...)
🔄 [MIC_STATE] idle → opening (session=...)
🔄 [MIC_STATE] opening → active (session=...)
```

---

### Сценарий 4: Таймаут открытия микрофона

**Шаги:**
1. Отключите микрофон в системных настройках (или заблокируйте доступ)
2. Нажмите и удерживайте Control+N (LONG_PRESS)

**Ожидаемое поведение:**
- ✅ Система пытается открыть микрофон через `MicrophoneStateManager.request_open()`
- ✅ При таймауте (5 секунд) состояние переходит в `ERROR`
- ✅ Автоматическое восстановление - принудительное закрытие
- ✅ Нет залипания в состоянии `OPENING`

**Проверка в логах:**
```
🔄 [MIC_STATE] idle → opening (session=...)
🎤 [MIC_STATE] Запрос открытия микрофона для session ...
❌ [MIC_STATE] Таймаут ожидания открытия микрофона (5s) для session ...
⚠️ [MIC_STATE] Принудительное закрытие микрофона: open_timeout
🔄 [MIC_STATE] opening → idle (session=...)
```

---

### Сценарий 5: Таймаут закрытия микрофона

**Шаги:**
1. Нажмите и удерживайте Control+N (LONG_PRESS)
2. Отпустите Control+N (RELEASE)
3. Симулируйте задержку закрытия (можно добавить задержку в код для тестирования)

**Ожидаемое поведение:**
- ✅ Система пытается закрыть микрофон через `MicrophoneStateManager.request_close()`
- ✅ При таймауте (3 секунды) принудительное закрытие
- ✅ Состояние переходит в `IDLE`
- ✅ Нет залипания в состоянии `CLOSING`

**Проверка в логах:**
```
🔄 [MIC_STATE] active → closing (session=...)
🎤 [MIC_STATE] Запрос закрытия микрофона для session ...
⚠️ [MIC_STATE] Таймаут ожидания закрытия микрофона (3s) для session ...
⚠️ [MIC_STATE] Принудительное закрытие микрофона: close_timeout
🔄 [MIC_STATE] closing → idle (session=...)
```

---

### Сценарий 6: Ошибка при открытии микрофона

**Шаги:**
1. Отключите микрофон в системных настройках
2. Нажмите и удерживайте Control+N (LONG_PRESS)

**Ожидаемое поведение:**
- ✅ Система пытается открыть микрофон
- ✅ При ошибке публикуется событие `microphone.error`
- ✅ Состояние переходит в `ERROR`
- ✅ Автоматическое восстановление - принудительное закрытие
- ✅ Нет залипания в состоянии `ERROR`

**Проверка в логах:**
```
🔄 [MIC_STATE] idle → opening (session=...)
🎤 [MIC_STATE] Запрос открытия микрофона для session ...
❌ [MIC_STATE] Ошибка открытия микрофона: ...
🔄 [MIC_STATE] opening → error (session=...)
⚠️ [MIC_STATE] Принудительное закрытие микрофона: open_error
🔄 [MIC_STATE] error → idle (session=...)
```

---

### Сценарий 7: Быстрые повторные нажатия (Debounce)

**Шаги:**
1. Быстро нажмите Control+N несколько раз подряд (< 0.1s между нажатиями)

**Ожидаемое поведение:**
- ✅ Только первое нажатие обрабатывается
- ✅ Повторные нажатия игнорируются (debounce)
- ✅ Нет конфликтов состояния микрофона
- ✅ `MicrophoneStateManager` корректно обрабатывает concurrent запросы

**Проверка в логах:**
```
PRESS debounced: too rapid
```

---

## 🔍 Как проверить состояние микрофона

### В логах

Ищите строки с префиксом `🔄 [MIC_STATE]`:
```bash
grep "\[MIC_STATE\]" logs/app.log
```

### События EventBus

Проверьте события:
- `microphone.open_requested` - запрос открытия
- `microphone.opened` - успешное открытие
- `microphone.close_requested` - запрос закрытия
- `microphone.closed` - успешное закрытие
- `microphone.error` - ошибка
- `microphone.state_changed` - изменение состояния

```bash
grep "microphone\." logs/app.log
```

---

## ⚠️ Известные проблемы (если обнаружены)

### Проблема: Микрофон не открывается

**Симптомы:**
- Состояние остается в `OPENING`
- Нет события `microphone.opened`
- Таймаут через 5 секунд

**Проверка:**
1. Проверьте, что `VoiceRecognitionIntegration._on_microphone_open_requested` вызывается
2. Проверьте, что `SpeechRecognizer.start_listening()` работает
3. Проверьте логи на наличие ошибок

---

### Проблема: Микрофон не закрывается

**Симптомы:**
- Состояние остается в `CLOSING` или `ACTIVE`
- Нет события `microphone.closed`
- Таймаут через 3 секунды

**Проверка:**
1. Проверьте, что `VoiceRecognitionIntegration._on_microphone_close_requested` вызывается
2. Проверьте, что `SpeechRecognizer.stop_listening()` работает
3. Проверьте логи на наличие ошибок

---

### Проблема: Race condition при concurrent запросах

**Симптомы:**
- Несколько запросов открытия одновременно
- Состояние не синхронизировано
- Ошибки в логах

**Проверка:**
1. Проверьте, что `asyncio.Lock` используется корректно
2. Проверьте логи на наличие предупреждений о session_id mismatch
3. Проверьте, что `MicrophoneStateManager` корректно обрабатывает concurrent запросы

---

## 📊 Метрики успеха

После тестирования всех сценариев проверьте:

1. ✅ **Нет залипания микрофона** - все сценарии завершаются корректно
2. ✅ **Таймауты работают** - автоматическое восстановление при таймаутах
3. ✅ **Ошибки обрабатываются** - автоматическое восстановление при ошибках
4. ✅ **Состояние синхронизировано** - `MicrophoneStateManager` всегда отражает реальное состояние
5. ✅ **Event-driven работает** - все события публикуются корректно
6. ✅ **Нет race conditions** - concurrent запросы обрабатываются корректно

---

## 🚀 Следующие шаги после тестирования

Если все сценарии работают корректно:
1. ✅ Этап 2 завершен успешно
2. 📝 Можно удалить устаревший код из `ApplicationStateManager` (опционально)
3. 📝 Можно добавить метрики и мониторинг (опционально)

Если обнаружены проблемы:
1. 🔍 Соберите логи с проблемными сценариями
2. 📝 Опишите проблему и шаги для воспроизведения
3. 🔧 Исправьте проблему и повторите тестирование

---

## 📝 Чеклист тестирования

- [ ] Сценарий 1: Нормальная работа (LONG_PRESS → RELEASE)
- [ ] Сценарий 2: Отмена записи (LONG_PRESS → SHORT_PRESS)
- [ ] Сценарий 3: Повторная активация (LONG_PRESS → RELEASE → LONG_PRESS)
- [ ] Сценарий 4: Таймаут открытия микрофона
- [ ] Сценарий 5: Таймаут закрытия микрофона
- [ ] Сценарий 6: Ошибка при открытии микрофона
- [ ] Сценарий 7: Быстрые повторные нажатия (Debounce)

---

## ✅ Готово к тестированию!

Запустите приложение и проверьте все сценарии. Особое внимание уделите:
- **Сценарию 2** (отмена записи) - проверка принудительного закрытия
- **Сценарию 4** (таймаут открытия) - проверка автоматического восстановления
- **Сценарию 6** (ошибка) - проверка обработки ошибок


