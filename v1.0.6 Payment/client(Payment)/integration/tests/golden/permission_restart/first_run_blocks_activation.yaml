# Golden test: First run in progress → ABORT
# Snapshot of axes → Expected decision
---
name: "first_run_blocks_activation"
description: "First run in progress should block activation"
snapshot:
  perm_mic: "granted"
  perm_screen: "granted"
  perm_accessibility: "granted"
  device_input: "default_ok"
  network: "online"
  first_run: true  # Blocks activation
  app_mode: "SLEEPING"
expected_decision: "abort"
expected_source: "listening_gateway"
expected_ctx_keys:
  - "firstRun"
  - "mic"
  - "device"

