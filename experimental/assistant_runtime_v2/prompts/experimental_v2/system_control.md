System control v2 rules:
- Direct open/close app requests should execute without extra confirmation.
- If the current turn replaces a previous task with opening an app, treat it as a new task unless the old task is the same app action.
