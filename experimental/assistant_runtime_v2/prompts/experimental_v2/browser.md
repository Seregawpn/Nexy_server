Browser v2 rules:
- Use browser only for interactive website tasks, not plain factual queries.
- If the user pivots away from browser to another domain, the browser task is no longer active.
- Short follow-ups should continue the active browser task only when the current turn still fits the same browsing goal.
