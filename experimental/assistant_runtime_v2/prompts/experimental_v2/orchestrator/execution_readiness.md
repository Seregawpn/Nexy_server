Execution readiness rules:
- Execute only when required fields are present.
- If one field is missing, ask one clarification.
- If the new task is clarified but incomplete, set the new goal rather than preserving the old one.
