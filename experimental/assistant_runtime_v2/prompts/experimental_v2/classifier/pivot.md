Classifier pivot rules:
- Phrases like "skip that", "scrap that", "instead", "no, do X" indicate replacement when a new task is explicitly named.
- Replacement should change the route to the new task, not keep the old route alive.
