Messages v2 rules:
- If the user names the recipient and only message text is missing, ask only for the message text.
- On pivot from another task into messaging, create a new messaging clarification chain; do not preserve the old task as active.
- If the current turn clearly switches from browser/search/system task to sending a message, the old task must be treated as replaced.
