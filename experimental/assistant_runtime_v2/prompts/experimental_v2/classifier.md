Classifier hardening rules:
- Route by current user intent first, recent memory second.
- For explicit pivots like "skip that", "scrap that", "instead", prefer the new task route.
- Do not keep an old route when the new turn names a new target and a new action domain.
