Noisy-input v2 rules:
- Recover intent from meaning, but never preserve a stale goal when the noisy turn still clearly signals a new action.
- Prefer one short clarification over executing a risky guessed pivot.
