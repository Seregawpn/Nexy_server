# Руководство пользователя WhatsApp MCP Server

## 📋 Содержание

1. [Первоначальная настройка](#первоначальная-настройка)
2. [Активация (QR код - только один раз)](#активация-qr-код---только-один-раз)
3. [Стандартизированные запросы](#стандартизированные-запросы)
4. [Формат ответов](#формат-ответов)
5. [Примеры использования](#примеры-использования)
6. [Решение проблем](#решение-проблем)

---

## Первоначальная настройка

### Шаг 1: Установка зависимостей

```bash
cd /Users/sergiyzasorin/Whatsapp/whatsapp-mcp-python
pip3 install -r requirements.txt
```

### Шаг 2: Конфигурация MCP в Cursor

Файл: `~/Library/Application Support/Cursor/User/globalStorage/mcp.json`

```json
{
  "mcpServers": {
    "whatsapp-python": {
      "command": "python3",
      "args": [
        "/Users/sergiyzasorin/Whatsapp/whatsapp-mcp-python/server.py"
      ],
      "timeout": 30
    }
  }
}
```

### Шаг 3: Перезапуск Cursor

Полностью закройте Cursor (⌘Q) и откройте снова.

---

## Активация (QR код - только один раз)

### ⚠️ ВАЖНО: QR код нужно сканировать ТОЛЬКО ОДИН РАЗ!

После первого сканирования авторизация сохраняется автоматически в:
```
whatsapp-mcp-ready/node_modules/@iflow-mcp/whatsapp-mcp-ts/data/auth_info/
```

### Процесс активации:

1. **Первый запуск Cursor после настройки:**
   - Откройте Cursor
   - MCP сервер автоматически запустится
   - В логах Cursor появится QR код (или ссылка на него)

2. **Сканирование QR кода:**
   - Откройте WhatsApp на телефоне
   - Настройки → Связанные устройства → Связать устройство
   - Отсканируйте QR код из логов Cursor

3. **Подтверждение:**
   - После сканирования появится сообщение "Connection opened. WA user: <Ваше имя>"
   - Авторизация автоматически сохранится

4. **Готово!**
   - При следующих запусках Cursor подключение происходит автоматически
   - QR код больше не нужен!

### Если нужно переподключиться:

1. Удалите папку `auth_info/`:
   ```bash
   rm -rf /Users/sergiyzasorin/Whatsapp/whatsapp-mcp-ready/node_modules/@iflow-mcp/whatsapp-mcp-ts/data/auth_info
   ```

2. Перезапустите Cursor

3. Отсканируйте QR код заново

---

## Стандартизированные запросы

### 1. Получить последние N чатов

**Формат запроса:**
```
Получи последние 5 чатов
```

**Или через MCP:**
```json
{
  "method": "tools/call",
  "params": {
    "name": "list_chats",
    "arguments": {
      "limit": 5,
      "page": 0
    }
  }
}
```

**Параметры:**
- `limit` (integer, optional) - количество чатов (1-100, по умолчанию 20)
- `page` (integer, optional) - номер страницы, начиная с 0 (по умолчанию 0)

---

### 2. Получить сообщения из чата

**Формат запроса:**
```
Покажи сообщения из чата с Sophia
```

**Или через MCP:**
```json
{
  "method": "tools/call",
  "params": {
    "name": "list_messages",
    "arguments": {
      "chat_jid": "Sophia",
      "limit": 10,
      "page": 0
    }
  }
}
```

**Параметры:**
- `chat_jid` (string, required) - имя контакта или JID (например: `14388554334@s.whatsapp.net`)
- `limit` (integer, optional) - количество сообщений (1-100, по умолчанию 20)
- `page` (integer, optional) - номер страницы (по умолчанию 0)

---

### 3. Поиск контакта

**Формат запроса:**
```
Найди контакт Sophia
```

**Или через MCP:**
```json
{
  "method": "tools/call",
  "params": {
    "name": "search_contacts",
    "arguments": {
      "query": "Sophia"
    }
  }
}
```

**Параметры:**
- `query` (string, required) - имя контакта или номер телефона

---

### 4. Отправить сообщение

**Формат запроса:**
```
Отправь сообщение "How are you?" контакту Sophia
```

**Или через MCP:**
```json
{
  "method": "tools/call",
  "params": {
    "name": "send_message",
    "arguments": {
      "recipient": "Sophia",
      "message": "How are you?"
    }
  }
}
```

**Параметры:**
- `recipient` (string, required) - имя контакта или JID
- `message` (string, required) - текст сообщения (максимум 4096 символов)

---

### 5. Информация о чате

**Формат запроса:**
```
Покажи информацию о чате с Sophia
```

**Или через MCP:**
```json
{
  "method": "tools/call",
  "params": {
    "name": "get_chat",
    "arguments": {
      "chat_jid": "Sophia"
    }
  }
}
```

---

## Формат ответов

### Стандартизированный формат для всех ответов:

#### 1. Список чатов:
```
Чаты (страница 1):

1. <Имя контакта>
   JID: <jid>
   Телефон: <номер телефона>
   Последнее сообщение: <отправитель>: <текст>
   Время: <ISO timestamp>

2. ...
```

#### 2. Сообщения из чата:
```
Сообщения из чата: <Имя контакта> (<JID>)

[<ISO timestamp>] <Отправитель>: <Текст сообщения>
[<ISO timestamp>] <Отправитель>: <Текст сообщения>
```

#### 3. Поиск контактов:
```
Найдено контактов: <количество>

1. <Имя контакта>
   JID: <jid>
   Телефон: <номер телефона>
```

#### 4. Отправка сообщения (успех):
```
✅ Сообщение отправлено успешно
Получатель: <Имя> (<JID>)
ID сообщения: <message_id>
Время отправки: <ISO timestamp>
```

#### 5. Отправка сообщения (ошибка):
```
❌ Ошибка отправки сообщения
Получатель: <Имя или JID>
Причина: <описание ошибки>
```

#### 6. Информация о чате:
```
Информация о чате:
Имя: <Имя контакта>
JID: <jid>
Телефон: <номер телефона>
Последнее сообщение: <ISO timestamp>
Всего сообщений: <количество>
```

---

## Примеры использования

### Пример 1: Получить последние 5 чатов с именами и номерами

**Запрос:**
```
Получи последние 5 чатов
```

**Ответ:**
```
Чаты (страница 1):

1. Sophia
   JID: 14388554334@s.whatsapp.net
   Телефон: 14388554334
   Последнее сообщение: Вы: How are you?
   Время: 2025-11-30T23:50:59.000Z

2. John Doe
   JID: 1234567890@s.whatsapp.net
   Телефон: 1234567890
   Последнее сообщение: John Doe: Как дела?
   Время: 2025-11-30T22:30:15.000Z
...
```

### Пример 2: Получить сообщения из чата

**Запрос:**
```
Покажи последние 10 сообщений из чата с Sophia
```

**Ответ:**
```
Сообщения из чата: Sophia (14388554334@s.whatsapp.net)

[2025-11-30T23:50:59.000Z] Вы: How are you?
[2025-11-30T23:45:30.000Z] Sophia: Привет!
[2025-11-30T23:40:15.000Z] Вы: Как дела?
...
```

### Пример 3: Отправить сообщение

**Запрос:**
```
Отправь сообщение "How are you?" контакту Sophia
```

**Ответ:**
```
✅ Сообщение отправлено успешно
Получатель: Sophia (14388554334@s.whatsapp.net)
ID сообщения: 3EB09625E54AEB227140A4
Время отправки: 2025-11-30T23:50:59.000Z
```

---

## Решение проблем

### Проблема: "Connection not ready"

**Решение:**
1. Убедитесь, что QR код был отсканирован
2. Проверьте, что WhatsApp на телефоне подключен к интернету
3. Перезапустите Cursor

### Проблема: "Контакт не найден"

**Решение:**
1. Убедитесь, что контакт существует в WhatsApp
2. Попробуйте использовать JID вместо имени
3. Используйте `search_contacts` для поиска

### Проблема: "База данных не доступна"

**Решение:**
1. Убедитесь, что Node.js сервер был запущен хотя бы один раз
2. Проверьте путь к базе данных:
   ```
   whatsapp-mcp-ready/node_modules/@iflow-mcp/whatsapp-mcp-ts/data/whatsapp.db
   ```

### Проблема: QR код не появляется

**Решение:**
1. Проверьте логи Cursor (View → Output → MCP)
2. Убедитесь, что папка `auth_info/` пуста или удалена
3. Перезапустите Cursor

---

## Важные замечания

1. **QR код только один раз** - после первого сканирования авторизация сохраняется автоматически
2. **Стандартизированный формат** - все ответы следуют единому формату с именем, JID, телефоном
3. **Валидация параметров** - все параметры проверяются перед выполнением
4. **Автоматическое обогащение имен** - имена контактов получаются из macOS системы
5. **Поддержка имен и JID** - можно использовать как имя контакта, так и JID

---

## Контакты и поддержка

При возникновении проблем проверьте:
1. Логи Cursor (View → Output → MCP)
2. Файл `api_specification.md` для детальной спецификации API
3. Файл `SETUP_COMPLETE.md` для информации о настройке

