# Audio Migration Prototypes

**Назначение**: Изолированное тестирование критических компонентов перед полной реализацией

**Принцип**: Если прототип не работает — полная реализация не начинается.

---

## 📁 Структура

```
audio_migration_prototypes/
├── README.md (этот файл)
├── run_all_mvps.sh (скрипт для запуска всех MVP)
├── requirements.txt (зависимости)
│
├── mvp0_observability/
│   └── test_observability.py
│
├── mvp1_device_discovery/
│   └── test_device_discovery.py
│
├── mvp1b_device_identity/
│   └── test_device_identity.py
│
├── mvp2_device_mapping/
│   └── test_device_mapping.py
│
├── mvp3_storm_reconcile/
│   └── test_storm_reconcile.py
│
├── mvp4_input_stream_quality/
│   └── test_input_stream_quality.py
│
├── mvp5_input_google_sr/
│   └── test_input_google_sr_pipeline.py
│
├── mvp6_output_playback/
│   └── test_output_playback.py
│
├── mvp6b_output_recreate/
│   └── test_output_recreate_midplay.py
│
├── mvp7_permissions_gate/
│   └── test_permissions_gate.py
│
└── mvp8_end_to_end/
    └── test_end_to_end.py
```

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd audio_migration_prototypes
pip install -r requirements.txt
```

### 2. Запуск отдельного MVP

```bash
# MVP-0: Observability
python3 mvp0_observability/test_observability.py

# MVP-1: Device Discovery
python3 mvp1_device_discovery/test_device_discovery.py

# И так далее...
```

### 3. Запуск всех MVP

```bash
./run_all_mvps.sh
```

---

## 📋 Порядок выполнения MVP

### Фаза 1: Базовые компоненты (можно параллельно)

1. **MVP-0**: Observability (0.5 дня) - обязательно первым
2. **MVP-1**: Device Discovery (1-2 дня) - после MVP-0
3. **MVP-6**: Output Playback (2-3 дня) - параллельно с MVP-1

### Фаза 2: Идентичность и маппинг

4. **MVP-1b**: Device Identity (1-2 дня) - после MVP-1
5. **MVP-2**: Device Mapping (1-2 дня) - после MVP-1b

### Фаза 3: Устойчивость и качество

6. **MVP-3**: Storm/Reconcile (2-3 дня) - после MVP-2
7. **MVP-4**: Input Stream Quality (1-2 дня) - после MVP-2

### Фаза 4: Интеграция

8. **MVP-5**: Input → Google SR (2-3 дня) - после MVP-4
9. **MVP-6b**: Output Recreate (2-3 дня) - после MVP-6

### Фаза 5: Permissions и E2E

10. **MVP-7**: Permissions Gate (1 день) - после MVP-5
11. **MVP-8**: End-to-End (2-3 дня) - после MVP-5, MVP-6b

---

## ✅ Критерии успеха

Каждый MVP должен пройти свой Exit Gate перед переходом к следующему.

Подробные критерии см. в `Docs/AUDIO_MIGRATION_MVP_STRUCTURE.md`

---

## 📊 Отчеты

После выполнения каждого MVP создается отчет с:
- Результатами тестов
- Метриками
- Статусом Exit Gate

---

## 🔧 Troubleshooting

### PyObjC не доступен
```bash
pip install pyobjc-framework-AVFoundation pyobjc-framework-CoreAudio
```

### sounddevice не доступен
```bash
pip install sounddevice
```

### speech_recognition не доступен
```bash
pip install SpeechRecognition
```

---

## 📝 Важные правила

1. **Fallback всегда безопаснее** - если выбор между "умнее маппинг" и "безопаснее fallback" → всегда выбираем fallback
2. **Маленькими шашками** - не перепрыгивать через слои
3. **Exit Gate обязателен** - если Exit Gate не закрыт → следующий MVP не начинается

---

## 🎯 Следующий шаг после прототипирования

Если все MVP успешны → переходить к полной реализации согласно `Docs/AUDIO_MIGRATION_SEQUENCE_PLAN.md`

---

**Этот каталог используется только для тестирования. После успешного прохождения всех MVP прототипы не переносятся в основной проект, а используются как референс для реализации.**

