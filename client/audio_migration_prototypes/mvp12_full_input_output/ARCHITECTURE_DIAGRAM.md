# MVP-12: Архитектура подключения устройств

## 📋 Общая схема работы

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         MVP-12: Full Integration                         │
│                    (Input + Output Device Management)                    │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                          1. ИНИЦИАЛИЗАЦИЯ                               │
└─────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────────┐
    │ DeviceDiscovery     │  ← AVFoundation (NSNotificationCenter)
    │ (MVP-1)             │     • get_input_devices()
    └─────────────────────┘     • get_output_devices()
            │
            │ DeviceInfo (UID, name, transport, channels)
            ▼
    ┌─────────────────────┐
    │ DeviceMapping       │  ← PortAudio (sounddevice)
    │ (MVP-2)             │     • find_portaudio_match()
    └─────────────────────┘     • Confidence model
            │
            │ MappingResult (device_index, confidence)
            ▼
    ┌─────────────────────┐
    │ OutputPlayback      │  ← AVAudioEngine
    │ (MVP-6)             │     • initialize_engine()
    └─────────────────────┘     • play_audio_chunk()
```

---

## 🔍 Детальная схема: Подключение Input устройства

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    INPUT DEVICE CONNECTION FLOW                         │
└─────────────────────────────────────────────────────────────────────────┘

1. ОБНАРУЖЕНИЕ УСТРОЙСТВА
   ┌─────────────────────────────────────────────────────────────┐
   │ _get_current_input_device_data()                            │
   └─────────────────────────────────────────────────────────────┘
           │
           ├─→ AVFoundation Discovery (MVP-1)
           │   ┌─────────────────────────────────────┐
           │   │ DeviceDiscovery.get_input_devices()  │
           │   │ • AVAudioSession.sharedInstance()    │
           │   │ • AVAudioSessionPortDescription     │
           │   │ • Returns: List[DeviceInfo]          │
           │   └─────────────────────────────────────┘
           │           │
           │           └─→ DeviceInfo:
           │               • uid: "BuiltInMicrophoneDevice"
           │               • name: "MacBook Air Microphone"
           │               • transport: "unknown"
           │               • channels: 1
           │
           ├─→ PortAudio Default Device
           │   ┌─────────────────────────────────────┐
           │   │ sd.default.device[0]                │
           │   │ • System default input device        │
           │   │ • Returns: device_index (int)       │
           │   └─────────────────────────────────────┘
           │
           └─→ Device Mapping (MVP-2)
               ┌─────────────────────────────────────┐
               │ DeviceMapping.find_portaudio_match() │
               │ • Match by name/channels/transport  │
               │ • Confidence model                  │
               │ • Returns: MappingResult            │
               └─────────────────────────────────────┘
                       │
                       └─→ MappingResult:
                           • device_index: 1
                           • confidence: "high" | "medium" | "low"
                           • is_usable(): bool

2. ПОДКЛЮЧЕНИЕ К УСТРОЙСТВУ
   ┌─────────────────────────────────────────────────────────────┐
   │ _start_recording()                                          │
   └─────────────────────────────────────────────────────────────┘
           │
           ├─→ Получение device_index из current_input_device_data
           │   • device_index: 1
           │   • sample_rate: 48000 (из device_data)
           │
           ├─→ Fallback Samplerate Chain
           │   ┌─────────────────────────────────────┐
           │   │ candidate_srs = [                    │
           │   │     48000,  # Нативный              │
           │   │     16000,  # Для Google SR          │
           │   │     44100,  # Стандартный            │
           │   │     48000   # Альтернативный         │
           │   │ ]                                    │
           │   └─────────────────────────────────────┘
           │
           └─→ Создание sounddevice.InputStream
               ┌─────────────────────────────────────┐
               │ sd.InputStream(                      │
               │     device=device_index,             │
               │     channels=1,                      │
               │     samplerate=sr_try,               │
               │     blocksize=1024,                  │
               │     callback=_audio_callback,         │
               │     dtype=np.float32                 │
               │ )                                    │
               │                                      │
               │ input_stream.start()                 │
               └─────────────────────────────────────┘
                       │
                       └─→ Аудио данные поступают в callback
                           ┌─────────────────────────────────────┐
                           │ _audio_callback(indata, frames, ...)│
                           │ • with audio_lock:                  │
                           │   audio_buffer.append(indata)       │
                           └─────────────────────────────────────┘

3. ПЕРЕКЛЮЧЕНИЕ УСТРОЙСТВА (во время записи)
   ┌─────────────────────────────────────────────────────────────┐
   │ _monitor_devices() → _switch_input_device()                 │
   └─────────────────────────────────────────────────────────────┘
           │
           ├─→ Остановка текущего stream
           │   ┌─────────────────────────────────────┐
           │   │ if input_stream.active:              │
           │   │     input_stream.stop()             │
           │   │ input_stream.close()                │
           │   └─────────────────────────────────────┘
           │
           ├─→ Очистка буфера (избегаем смешивания данных)
           │   ┌─────────────────────────────────────┐
           │   │ with audio_lock:                     │
           │   │     audio_buffer.clear()             │
           │   └─────────────────────────────────────┘
           │
           └─→ Перезапуск на новом устройстве
               ┌─────────────────────────────────────┐
               │ input_stream = sd.InputStream(       │
               │     device=new_device_index,         │
               │     ...                              │
               │ )                                    │
               │ input_stream.start()                 │
               └─────────────────────────────────────┘
```

---

## 🔊 Детальная схема: Подключение Output устройства

```
┌─────────────────────────────────────────────────────────────────────────┐
│                   OUTPUT DEVICE CONNECTION FLOW                         │
└─────────────────────────────────────────────────────────────────────────┘

1. ОБНАРУЖЕНИЕ УСТРОЙСТВА
   ┌─────────────────────────────────────────────────────────────┐
   │ _get_current_output_device_data()                           │
   └─────────────────────────────────────────────────────────────┘
           │
           ├─→ AVFoundation Discovery (MVP-1)
           │   ┌─────────────────────────────────────┐
           │   │ DeviceDiscovery.get_output_devices()│
           │   │ • AVAudioSession.sharedInstance()   │
           │   │ • AVAudioSessionPortDescription     │
           │   │ • Returns: List[DeviceInfo]         │
           │   └─────────────────────────────────────┘
           │
           ├─→ PortAudio Default Device
           │   ┌─────────────────────────────────────┐
           │   │ sd.default.device[1]                │
           │   │ • System default output device      │
           │   │ • Returns: device_index (int)       │
           │   └─────────────────────────────────────┘
           │
           └─→ Device Mapping (MVP-2)
               ┌─────────────────────────────────────┐
               │ DeviceMapping.find_portaudio_match() │
               │ • Match by name/channels/transport  │
               │ • Returns: MappingResult             │
               └─────────────────────────────────────┘

2. ПОДКЛЮЧЕНИЕ К УСТРОЙСТВУ (Инициализация)
   ┌─────────────────────────────────────────────────────────────┐
   │ setup() → output_playback.setup_audio_session()             │
   └─────────────────────────────────────────────────────────────┘
           │
           ├─→ AVAudioSession Configuration
           │   ┌─────────────────────────────────────┐
           │   │ session = AVAudioSession.sharedInstance()│
           │   │ session.setCategory_(                │
           │   │     AVAudioSessionCategoryPlayback,  │
           │   │     mode=AVAudioSessionModeDefault,  │
           │   │     options=0                        │
           │   │ )                                    │
           │   │ session.setActive_(True)            │
           │   └─────────────────────────────────────┘
           │
           └─→ AVAudioEngine Initialization
               ┌─────────────────────────────────────┐
               │ output_playback.initialize_engine()  │
               │ • engine = AVAudioEngine.alloc().init()│
               │ • playerNode = AVAudioPlayerNode()    │
               │ • engine.attachNode_(playerNode)      │
               │ • engine.connect_(playerNode, ...)   │
               │ • engine.startAndReturnError_()       │
               └─────────────────────────────────────┘
                       │
                       └─→ AVAudioEngine автоматически использует
                           системное default output устройство

3. ПЕРЕКЛЮЧЕНИЕ УСТРОЙСТВА
   ┌─────────────────────────────────────────────────────────────┐
   │ _monitor_devices() → _switch_output_device()                │
   └─────────────────────────────────────────────────────────────┘
           │
           ├─→ Защита от race conditions (engine_lock)
           │   ┌─────────────────────────────────────┐
           │   │ with engine_lock:                    │
           │   │     # Все операции с engine         │
           │   └─────────────────────────────────────┘
           │
           ├─→ Остановка текущего engine
           │   ┌─────────────────────────────────────┐
           │   │ if engine.isRunning():               │
           │   │     engine.stop()                    │
           │   └─────────────────────────────────────┘
           │
           └─→ Пересоздание engine (автоматически использует новое устройство)
               ┌─────────────────────────────────────┐
               │ output_playback.initialize_engine()  │
               │ • engine = AVAudioEngine.alloc().init()│
               │ • playerNode = AVAudioPlayerNode()    │
               │ • engine.attachNode_(playerNode)      │
               │ • engine.connect_(playerNode, ...)   │
               │ • engine.startAndReturnError_()       │
               └─────────────────────────────────────┘
                       │
                       └─→ AVAudioEngine автоматически подключается
                           к новому системному default output устройству
```

---

## 🔄 Полный цикл: Запись → Распознавание → Воспроизведение

```
┌─────────────────────────────────────────────────────────────────────────┐
│              FULL CYCLE: INPUT → RECOGNITION → OUTPUT                   │
└─────────────────────────────────────────────────────────────────────────┘

1. PUSH-TO-TALK (Control+N)
   ┌─────────────────────────────────────────────────────────────┐
   │ start_keyboard_monitoring()                                  │
   │ • on_press: key_token() → _pressed_keys.add()               │
   │ • is_ctrl_down() && is_char_down("n")                       │
   │   → _start_recording()                                       │
   └─────────────────────────────────────────────────────────────┘
           │
           ▼
   ┌─────────────────────────────────────────────────────────────┐
   │ _start_recording()                                           │
   │ • Очистка audio_buffer                                       │
   │ • Создание sd.InputStream (fallback samplerate)             │
   │ • input_stream.start() → callback начинает получать данные  │
   └─────────────────────────────────────────────────────────────┘
           │
           ▼
   ┌─────────────────────────────────────────────────────────────┐
   │ _audio_callback(indata, frames, ...)                         │
   │ • with audio_lock:                                           │
   │     audio_buffer.append(indata)  # Накопление данных        │
   └─────────────────────────────────────────────────────────────┘

2. ОСТАНОВКА ЗАПИСИ (отпускание Control+N)
   ┌─────────────────────────────────────────────────────────────┐
   │ on_release: released_ctrl || released_n                    │
   │   → _stop_recording()                                       │
   └─────────────────────────────────────────────────────────────┘
           │
           ▼
   ┌─────────────────────────────────────────────────────────────┐
   │ _stop_recording()                                           │
   │ • input_stream.stop() + close()                             │
   │ • Объединение audio_buffer → audio_data                     │
   │ • Ресемплинг: _resample_linear(audio_data, src_sr, 16000)  │
   │ • Конвертация: audio_bytes = (audio_16k * 32767).astype(int16)│
   │ • Создание event_id и FullCycleEvent                        │
   │ • work_queue.put(("RECOGNIZE_AND_PLAY", ...))               │
   └─────────────────────────────────────────────────────────────┘
           │
           ▼
   ┌─────────────────────────────────────────────────────────────┐
   │ Worker Thread (_worker_loop)                                │
   │ • Получение задачи из work_queue                            │
   │ • _recognize_and_play_worker(event_id, audio_bytes, 16000)  │
   └─────────────────────────────────────────────────────────────┘
           │
           ├─→ Распознавание
           │   ┌─────────────────────────────────────┐
           │   │ _recognize_and_play_worker()         │
           │   │ • audio_data = sr.AudioData(        │
           │   │       audio_bytes,                  │
           │   │       sample_rate=16000             │
           │   │   )                                 │
           │   │ • recognized_text = recognizer.    │
           │   │     recognize_google(audio_data)   │
           │   │ • events_by_id[event_id].          │
           │   │     recognized_text = recognized_text│
           │   └─────────────────────────────────────┘
           │
           └─→ Воспроизведение
               ┌─────────────────────────────────────┐
               │ _play_response_worker()             │
               │ • with engine_lock:                 │
               │     # Генерация тестового звука     │
               │     audio = generate_sine_wave(440Hz)│
               │     # Конвертация numpy → PCMBuffer │
               │     pcm_buffer = numpy_to_pcm_buffer(audio)│
               │     # Воспроизведение              │
               │     playerNode.scheduleBuffer_(pcm_buffer)│
               │     engine.start()                  │
               │ • events_by_id[event_id].           │
               │     playback_success = True         │
               └─────────────────────────────────────┘
```

---

## 🔍 Мониторинг устройств (Device Monitoring)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    DEVICE MONITORING LOOP                               │
└─────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────────────────┐
    │ _monitor_devices() (запускается в отдельном thread)         │
    └─────────────────────────────────────────────────────────────┘
            │
            ├─→ Каждую секунду (time.sleep(1.0))
            │
            ├─→ Проверка Input устройства
            │   ┌─────────────────────────────────────┐
            │   │ current_input =                     │
            │   │     _get_current_input_device_data()│
            │   │ if current_input['uid'] !=          │
            │   │     last_input_device_uid:          │
            │   │     # Устройство изменилось!         │
            │   │     _switch_input_device()          │
            │   └─────────────────────────────────────┘
            │           │
            │           └─→ Если is_recording:
            │               • Остановка текущего stream
            │               • Очистка audio_buffer
            │               • Перезапуск на новом устройстве
            │
            └─→ Проверка Output устройства
                ┌─────────────────────────────────────┐
                │ current_output =                     │
                │     _get_current_output_device_data()│
                │ if current_output['uid'] !=          │
                │     last_output_device_uid:          │
                │     # Устройство изменилось!          │
                │     _switch_output_device()          │
                └─────────────────────────────────────┘
                        │
                        └─→ С engine_lock:
                            • Остановка AVAudioEngine
                            • Пересоздание engine
                            • Запуск engine (автоматически на новом устройстве)
```

---

## 🔑 Ключевые компоненты

### 1. DeviceDiscovery (MVP-1)
- **Источник**: AVFoundation (AVAudioSession)
- **Методы**: `get_input_devices()`, `get_output_devices()`
- **Возвращает**: `List[DeviceInfo]` с UID, name, transport, channels

### 2. DeviceMapping (MVP-2)
- **Источник**: PortAudio (sounddevice)
- **Метод**: `find_portaudio_match(name, channels, transport)`
- **Возвращает**: `MappingResult` с device_index и confidence

### 3. Input Connection
- **Компонент**: `sounddevice.InputStream`
- **Владелец микрофона**: Только `sounddevice` (не `sr.Microphone`)
- **Fallback**: Попытка подключения с несколькими samplerate (48000, 16000, 44100, 48000)
- **Callback**: `_audio_callback()` → накопление в `audio_buffer`

### 4. Output Connection
- **Компонент**: `AVAudioEngine` + `AVAudioPlayerNode`
- **Автоматическое устройство**: AVAudioEngine всегда использует системное default output
- **Переключение**: Пересоздание engine (автоматически подключается к новому устройству)
- **Защита**: `engine_lock` для предотвращения race conditions

### 5. Worker Thread
- **Назначение**: Тяжелые операции (распознавание, воспроизведение)
- **Очередь**: `work_queue` для задач
- **Задачи**: `("RECOGNIZE_AND_PLAY", ...)`, `("PLAY_RESPONSE", ...)`, `("PLAY_TEST_SOUND", ...)`

---

## 📊 Потоки данных

```
INPUT FLOW:
AVFoundation Discovery → DeviceInfo → DeviceMapping → device_index
                                                          │
                                                          ▼
                                    sounddevice.InputStream (device_index)
                                                          │
                                                          ▼
                                    _audio_callback → audio_buffer
                                                          │
                                                          ▼
                                    _stop_recording → audio_bytes (16k mono)
                                                          │
                                                          ▼
                                    Worker Thread → Google SR → recognized_text

OUTPUT FLOW:
AVFoundation Discovery → DeviceInfo → DeviceMapping → device_index (информационный)
                                                          │
                                                          ▼
                                    AVAudioEngine (автоматически системное default)
                                                          │
                                                          ▼
                                    Worker Thread → numpy_to_pcm_buffer → AVAudioPlayerNode
                                                          │
                                                          ▼
                                    Системное default output устройство
```

---

## 🛡️ Защита от ошибок

1. **Fallback Samplerate**: Попытка подключения с несколькими samplerate
2. **Engine Lock**: Защита AVAudioEngine от race conditions
3. **Audio Lock**: Защита audio_buffer от одновременного доступа
4. **Queue Join**: Ожидание завершения задач перед shutdown
5. **Device Mapping Confidence**: Проверка `is_usable()` перед использованием
6. **Buffer Clear**: Очистка буфера при смене устройства во время записи

---

## ✅ Результат

- **Input**: Стабильное подключение через `sounddevice.InputStream` с fallback samplerate
- **Output**: Автоматическое использование системного default через `AVAudioEngine`
- **Переключение**: Плавное переключение устройств без потери данных
- **Полный цикл**: Запись → распознавание → воспроизведение работает стабильно

