# План постепенной интеграции (MVP за MVP)

**Статус**: Обязательный план интеграции  
**Принцип**: "Маленькими шашками" — каждый MVP интегрируется, тестируется, проверяется перед следующим  
**Дата**: 2025-12-23

---

## 🎯 Общая стратегия

### Принципы постепенной интеграции

1. **Один MVP за раз** — интегрируем только один MVP, тестируем, проверяем
2. **Тестирование перед следующим** — следующий MVP не начинается, пока текущий не протестирован
3. **Проверка на конфликты** — каждый шаг проверяется на конфликты, дублирование, race conditions
4. **Feature flag для каждого MVP** — возможность отката на любом этапе
5. **Минимальные изменения** — только необходимые изменения для текущего MVP

---

## 📋 План интеграции (MVP за MVP)

### MVP-INT-1: Device Discovery (интеграция в проект)

**Цель**: Интегрировать DeviceDiscovery в основной проект как изолированный модуль

**Что интегрируем**:
- `modules/audio_route_manager/core/device_discovery.py` (из MVP-1 прототипа)
- `modules/audio_route_manager/core/contracts.py` (DeviceInfo, DeviceTransport)

**Изменения в проекте**:
1. Создать `modules/audio_route_manager/` директорию
2. Скопировать `device_discovery.py` из прототипа
3. Адаптировать импорты под структуру проекта
4. Добавить тесты `tests/test_audio_route_manager_device_discovery.py`

**Тестирование**:
- [ ] Unit тесты проходят
- [ ] Ручное тестирование: получение списка устройств
- [ ] Проверка: нет конфликтов с существующим кодом
- [ ] Проверка: нет дублирования функционала
- [ ] Проверка: нет race conditions

**Exit Gate**:
- [ ] Модуль создан и изолирован
- [ ] Тесты проходят (≥80% покрытие)
- [ ] Линтер проходит
- [ ] Ручное тестирование успешно
- [ ] Нет конфликтов/дублирования/race conditions

**Время**: 1-2 дня

**Feature Flag**: `audio_route_manager.device_discovery.enabled: false` (по умолчанию)

---

### MVP-INT-2: Device Mapping (интеграция в проект)

**Цель**: Интегрировать DeviceMapping в основной проект

**Что интегрируем**:
- `modules/audio_route_manager/core/device_mapping.py` (из MVP-2 прототипа)
- Обновить `contracts.py` (MappingResult, Confidence)

**Изменения в проекте**:
1. Добавить `device_mapping.py` в модуль
2. Обновить `contracts.py` с MappingResult
3. Добавить тесты `tests/test_audio_route_manager_device_mapping.py`

**Тестирование**:
- [ ] Unit тесты проходят
- [ ] Ручное тестирование: маппинг AVFoundation → PortAudio
- [ ] Проверка: нет конфликтов с DeviceDiscovery
- [ ] Проверка: нет дублирования функционала
- [ ] Проверка: нет race conditions

**Exit Gate**:
- [ ] Модуль интегрирован
- [ ] Тесты проходят (≥80% покрытие)
- [ ] Линтер проходит
- [ ] Ручное тестирование успешно
- [ ] Нет конфликтов/дублирования/race conditions

**Время**: 1-2 дня

**Feature Flag**: `audio_route_manager.device_mapping.enabled: false` (по умолчанию)

**Зависимости**: MVP-INT-1 (DeviceDiscovery)

---

### MVP-INT-3: State Machines (интеграция в проект)

**Цель**: Интегрировать Input/Output State Machines

**Что интегрируем**:
- `modules/audio_route_manager/core/input_state_machine.py`
- `modules/audio_route_manager/core/output_state_machine.py`

**Изменения в проекте**:
1. Добавить state machines в модуль
2. Добавить тесты `tests/test_audio_route_manager_state_machines.py`

**Тестирование**:
- [ ] Unit тесты проходят (все переходы состояний)
- [ ] Ручное тестирование: переходы состояний
- [ ] Проверка: нет конфликтов с существующими state machines
- [ ] Проверка: нет дублирования функционала
- [ ] Проверка: нет race conditions

**Exit Gate**:
- [ ] State machines интегрированы
- [ ] Тесты проходят (≥80% покрытие)
- [ ] Линтер проходит
- [ ] Ручное тестирование успешно
- [ ] Нет конфликтов/дублирования/race conditions

**Время**: 2-3 дня

**Feature Flag**: `audio_route_manager.state_machines.enabled: false` (по умолчанию)

**Зависимости**: MVP-INT-1, MVP-INT-2

---

### MVP-INT-4: Reconcile Engine (интеграция в проект)

**Цель**: Интегрировать Reconcile Engine

**Что интегрируем**:
- `modules/audio_route_manager/core/reconcile_engine.py`
- `modules/audio_route_manager/core/debounce_manager.py`

**Изменения в проекте**:
1. Добавить reconcile engine в модуль
2. Добавить тесты `tests/test_audio_route_manager_reconcile.py`

**Тестирование**:
- [ ] Unit тесты проходят (single-flight, debounce)
- [ ] Ручное тестирование: device storm (множественные события)
- [ ] Проверка: нет конфликтов с state machines
- [ ] Проверка: нет дублирования функционала
- [ ] Проверка: нет race conditions (single-flight работает)

**Exit Gate**:
- [ ] Reconcile engine интегрирован
- [ ] Тесты проходят (≥80% покрытие)
- [ ] Линтер проходит
- [ ] Ручное тестирование успешно (device storm)
- [ ] Нет конфликтов/дублирования/race conditions

**Время**: 2-3 дня

**Feature Flag**: `audio_route_manager.reconcile.enabled: false` (по умолчанию)

**Зависимости**: MVP-INT-3 (State Machines)

---

### MVP-INT-5: Input Adapter (интеграция в проект)

**Цель**: Интегрировать AVFInputAdapter (sounddevice.InputStream)

**Что интегрируем**:
- `modules/audio_route_manager/core/adapters/avf_input_adapter.py` (из MVP-5, MVP-11 прототипов)

**Изменения в проекте**:
1. Добавить adapter в модуль
2. Добавить тесты `tests/test_audio_route_manager_input_adapter.py`

**Тестирование**:
- [ ] Unit тесты проходят
- [ ] Ручное тестирование: открытие/закрытие input stream
- [ ] Проверка: нет конфликтов с существующим SpeechRecognizer
- [ ] Проверка: нет дублирования микрофона (единственный владелец)
- [ ] Проверка: нет race conditions (locks работают)

**Exit Gate**:
- [ ] Input adapter интегрирован
- [ ] Тесты проходят (≥80% покрытие)
- [ ] Линтер проходит
- [ ] Ручное тестирование успешно
- [ ] Нет конфликтов/дублирования/race conditions
- [ ] Единственный владелец микрофона подтвержден

**Время**: 2-3 дня

**Feature Flag**: `audio_route_manager.input_adapter.enabled: false` (по умолчанию)

**Зависимости**: MVP-INT-4 (Reconcile Engine)

---

### MVP-INT-6: Output Adapter (интеграция в проект)

**Цель**: Интегрировать AVFOutputAdapter (AVAudioEngine)

**Что интегрируем**:
- `modules/audio_route_manager/core/adapters/avf_output_adapter.py` (из MVP-6, MVP-12 прототипов)

**Изменения в проекте**:
1. Добавить adapter в модуль
2. Добавить тесты `tests/test_audio_route_manager_output_adapter.py`

**Тестирование**:
- [ ] Unit тесты проходят
- [ ] Ручное тестирование: воспроизведение аудио
- [ ] Проверка: нет конфликтов с существующим SequentialSpeechPlayer
- [ ] Проверка: нет дублирования output
- [ ] Проверка: нет race conditions (engine_lock работает)

**Exit Gate**:
- [ ] Output adapter интегрирован
- [ ] Тесты проходят (≥80% покрытие)
- [ ] Линтер проходит
- [ ] Ручное тестирование успешно
- [ ] Нет конфликтов/дублирования/race conditions

**Время**: 2-3 дня

**Feature Flag**: `audio_route_manager.output_adapter.enabled: false` (по умолчанию)

**Зависимости**: MVP-INT-4 (Reconcile Engine)

---

### MVP-INT-7: Route Manager Core (интеграция в проект)

**Цель**: Интегрировать RouteManager (объединение всех компонентов)

**Что интегрируем**:
- `modules/audio_route_manager/core/route_manager.py`

**Изменения в проекте**:
1. Добавить route_manager.py в модуль
2. Интегрировать все компоненты (DeviceDiscovery, DeviceMapping, StateMachines, ReconcileEngine, Adapters)
3. Добавить тесты `tests/test_audio_route_manager_route_manager.py`

**Тестирование**:
- [ ] Unit тесты проходят
- [ ] Integration тесты проходят (все компоненты работают вместе)
- [ ] Ручное тестирование: полный цикл (start input → stop input)
- [ ] Проверка: нет конфликтов между компонентами
- [ ] Проверка: нет дублирования функционала
- [ ] Проверка: нет race conditions

**Exit Gate**:
- [ ] RouteManager интегрирован
- [ ] Тесты проходят (≥80% покрытие)
- [ ] Линтер проходит
- [ ] Integration тесты успешны
- [ ] Ручное тестирование успешно
- [ ] Нет конфликтов/дублирования/race conditions

**Время**: 3-4 дня

**Feature Flag**: `audio_route_manager.route_manager.enabled: false` (по умолчанию)

**Зависимости**: MVP-INT-5, MVP-INT-6 (Adapters)

---

### MVP-INT-8: AudioRouteManagerIntegration (интеграция в проект)

**Цель**: Создать интеграцию AudioRouteManagerIntegration и добавить в SimpleModuleCoordinator

**Что интегрируем**:
- `integration/integrations/audio_route_manager_integration.py`
- Добавление в `SimpleModuleCoordinator._create_integrations()`

**Изменения в проекте**:
1. Создать `audio_route_manager_integration.py`
2. Добавить в `SimpleModuleCoordinator` (позиция 5, после PermissionRestart)
3. Добавить конфигурацию в `unified_config.yaml`
4. Добавить тесты `tests/test_audio_route_manager_integration.py`

**Тестирование**:
- [ ] Unit тесты проходят
- [ ] Integration тесты проходят (инициализация интеграции)
- [ ] Ручное тестирование: интеграция инициализируется
- [ ] Проверка: порядок инициализации правильный
- [ ] Проверка: нет конфликтов с существующими интеграциями
- [ ] Проверка: нет дублирования функционала
- [ ] Проверка: нет race conditions

**Exit Gate**:
- [ ] Интеграция создана
- [ ] Добавлена в SimpleModuleCoordinator
- [ ] Тесты проходят (≥80% покрытие)
- [ ] Линтер проходит
- [ ] Integration тесты успешны
- [ ] Ручное тестирование успешно
- [ ] Нет конфликтов/дублирования/race conditions

**Время**: 2-3 дня

**Feature Flag**: `audio_route_manager.enabled: false` (по умолчанию)

**Зависимости**: MVP-INT-7 (Route Manager Core)

---

### MVP-INT-9: EventBus интеграция (подписки и публикации)

**Цель**: Добавить подписки и публикации EventBus в AudioRouteManagerIntegration

**Что интегрируем**:
- Подписки на события: `voice.recording_start`, `voice.recording_stop`, `app.mode_changed`, `permissions.first_run_*`
- Публикации событий: `audio.input.active`, `audio.output.ready`, `audio.device.changed`

**Изменения в проекте**:
1. Добавить подписки в `audio_route_manager_integration.py`
2. Добавить публикации в `audio_route_manager_integration.py`
3. Добавить валидацию payload (из контракта EventBus)
4. Добавить тесты `tests/test_audio_route_manager_eventbus.py`

**Тестирование**:
- [ ] Unit тесты проходят (подписки/публикации)
- [ ] Integration тесты проходят (EventBus события)
- [ ] Ручное тестирование: события публикуются/подписываются
- [ ] Проверка: нет конфликтов с существующими событиями
- [ ] Проверка: нет дублирования событий
- [ ] Проверка: валидация payload работает

**Exit Gate**:
- [ ] Подписки работают
- [ ] Публикации работают
- [ ] Валидация payload работает
- [ ] Тесты проходят (≥80% покрытие)
- [ ] Линтер проходит
- [ ] Integration тесты успешны
- [ ] Ручное тестирование успешно
- [ ] Нет конфликтов/дублирования

**Время**: 2-3 дня

**Feature Flag**: `audio_route_manager.eventbus.enabled: false` (по умолчанию)

**Зависимости**: MVP-INT-8 (AudioRouteManagerIntegration)

---

### MVP-INT-10: Адаптация VoiceRecognitionIntegration (feature flag)

**Цель**: Адаптировать VoiceRecognitionIntegration для использования AudioRouteManager

**Что интегрируем**:
- Feature flag проверка в `VoiceRecognitionIntegration`
- Подписка на `audio.input.active`
- Получение аудио-данных из AudioRouteManager (вместо прямого управления микрофоном)

**Изменения в проекте**:
1. Добавить feature flag проверку в `voice_recognition_integration.py`
2. Добавить подписку на `audio.input.active`
3. Адаптировать логику получения аудио-данных
4. Сохранить fallback на старую логику
5. Добавить тесты `tests/test_voice_recognition_integration_audio_route.py`

**Тестирование**:
- [ ] Unit тесты проходят (feature flag OFF и ON)
- [ ] Integration тесты проходят (старая и новая логика)
- [ ] Ручное тестирование: push-to-talk работает (feature flag OFF)
- [ ] Ручное тестирование: push-to-talk работает (feature flag ON)
- [ ] Проверка: нет конфликтов с существующей логикой
- [ ] Проверка: нет дублирования микрофона
- [ ] Проверка: нет race conditions

**Exit Gate**:
- [ ] Feature flag работает
- [ ] Fallback на старую логику работает
- [ ] Новая логика работает (с AudioRouteManager)
- [ ] Тесты проходят (≥80% покрытие)
- [ ] Линтер проходит
- [ ] Integration тесты успешны
- [ ] Ручное тестирование успешно (оба режима)
- [ ] Нет конфликтов/дублирования/race conditions

**Время**: 3-4 дня

**Feature Flag**: `audio_route_manager.enabled: false` (по умолчанию, старая логика)

**Зависимости**: MVP-INT-9 (EventBus интеграция)

---

### MVP-INT-11: Адаптация SpeechPlaybackIntegration (feature flag)

**Цель**: Адаптировать SpeechPlaybackIntegration для использования AudioRouteManager

**Что интегрируем**:
- Feature flag проверка в `SpeechPlaybackIntegration`
- Подписка на `audio.output.ready`
- Использование AVAudioEngine через AudioRouteManager (вместо прямого управления output)

**Изменения в проекте**:
1. Добавить feature flag проверку в `speech_playback_integration.py`
2. Добавить подписку на `audio.output.ready`
3. Адаптировать логику воспроизведения
4. Сохранить fallback на старую логику
5. Добавить тесты `tests/test_speech_playback_integration_audio_route.py`

**Тестирование**:
- [ ] Unit тесты проходят (feature flag OFF и ON)
- [ ] Integration тесты проходят (старая и новая логика)
- [ ] Ручное тестирование: воспроизведение работает (feature flag OFF)
- [ ] Ручное тестирование: воспроизведение работает (feature flag ON)
- [ ] Проверка: нет конфликтов с существующей логикой
- [ ] Проверка: нет дублирования output
- [ ] Проверка: нет race conditions

**Exit Gate**:
- [ ] Feature flag работает
- [ ] Fallback на старую логику работает
- [ ] Новая логика работает (с AudioRouteManager)
- [ ] Тесты проходят (≥80% покрытие)
- [ ] Линтер проходит
- [ ] Integration тесты успешны
- [ ] Ручное тестирование успешно (оба режима)
- [ ] Нет конфликтов/дублирования/race conditions

**Время**: 3-4 дня

**Feature Flag**: `audio_route_manager.enabled: false` (по умолчанию, старая логика)

**Зависимости**: MVP-INT-9 (EventBus интеграция)

---

### MVP-INT-12: Мониторинг устройств (thread)

**Цель**: Добавить мониторинг устройств в AudioRouteManagerIntegration

**Что интегрируем**:
- Thread для мониторинга устройств (каждую секунду)
- Обнаружение изменений устройств
- Автоматическое переключение устройств

**Изменения в проекте**:
1. Добавить monitoring thread в `audio_route_manager_integration.py`
2. Добавить логику обнаружения изменений устройств
3. Добавить автоматическое переключение устройств
4. Добавить тесты `tests/test_audio_route_manager_monitoring.py`

**Тестирование**:
- [ ] Unit тесты проходят (monitoring thread)
- [ ] Integration тесты проходят (обнаружение изменений)
- [ ] Ручное тестирование: отключение/подключение устройств
- [ ] Проверка: нет конфликтов с активными потоками
- [ ] Проверка: нет дублирования мониторинга
- [ ] Проверка: нет race conditions (thread-safe)

**Exit Gate**:
- [ ] Monitoring thread работает
- [ ] Обнаружение изменений работает
- [ ] Автоматическое переключение работает
- [ ] Тесты проходят (≥80% покрытие)
- [ ] Линтер проходит
- [ ] Integration тесты успешны
- [ ] Ручное тестирование успешно
- [ ] Нет конфликтов/дублирования/race conditions

**Время**: 2-3 дня

**Feature Flag**: `audio_route_manager.monitoring.enabled: false` (по умолчанию)

**Зависимости**: MVP-INT-10, MVP-INT-11 (Адаптация интеграций)

---

### MVP-INT-13: End-to-End тестирование

**Цель**: Полное end-to-end тестирование всей системы

**Что тестируем**:
- Полный цикл: push-to-talk → распознавание → воспроизведение
- Переключение устройств во время записи
- Переключение устройств во время воспроизведения
- Device storm (множественные переключения)

**Изменения в проекте**:
1. Создать end-to-end тесты `tests/integration/test_audio_route_manager_e2e.py`
2. Ручное тестирование всех сценариев

**Тестирование**:
- [ ] End-to-end тесты проходят
- [ ] Ручное тестирование: полный цикл работает
- [ ] Ручное тестирование: переключение устройств работает
- [ ] Ручное тестирование: device storm обрабатывается корректно
- [ ] Проверка: нет регрессий в существующем функционале
- [ ] Проверка: нет конфликтов/дублирования/race conditions

**Exit Gate**:
- [ ] End-to-end тесты проходят
- [ ] Ручное тестирование успешно
- [ ] Нет регрессий
- [ ] Нет конфликтов/дублирования/race conditions
- [ ] Метрики соответствуют SLO

**Время**: 3-4 дня

**Feature Flag**: `audio_route_manager.enabled: true` (для тестирования)

**Зависимости**: MVP-INT-12 (Мониторинг устройств)

---

## 🔄 Цикл интеграции каждого MVP

### Шаг 1: Интеграция

1. Скопировать код из прототипа в основной проект
2. Адаптировать импорты под структуру проекта
3. Добавить feature flag (OFF по умолчанию)
4. Добавить тесты

### Шаг 2: Тестирование

1. Unit тесты
2. Integration тесты
3. Ручное тестирование

### Шаг 3: Проверка на проблемы

1. **Конфликты**: Проверка на конфликты с существующим кодом
2. **Дублирование**: Проверка на дублирование функционала
3. **Race conditions**: Проверка на race conditions (threads, locks)

### Шаг 4: Exit Gate

1. Все тесты проходят
2. Ручное тестирование успешно
3. Нет конфликтов/дублирования/race conditions
4. Линтер проходит

### Шаг 5: Переход к следующему MVP

Только после успешного завершения Exit Gate переходим к следующему MVP.

---

## 📊 Чек-лист проверки на каждом MVP

### Проверка конфликтов

- [ ] Нет конфликтов имен (классы, функции, переменные)
- [ ] Нет конфликтов импортов
- [ ] Нет конфликтов событий EventBus
- [ ] Нет конфликтов конфигурации

### Проверка дублирования

- [ ] Нет дублирования функционала
- [ ] Нет дублирования событий EventBus
- [ ] Нет дублирования управления устройствами
- [ ] Нет дублирования состояния

### Проверка race conditions

- [ ] Все shared state защищено locks
- [ ] Thread-safe операции
- [ ] Single-flight механизм работает
- [ ] Нет гонок между потоками

---

## 🛡️ Feature Flags для каждого MVP

```yaml
# unified_config.yaml
audio_route_manager:
  enabled: false  # Главный feature flag
  
  # Индивидуальные feature flags для каждого MVP
  device_discovery:
    enabled: false
  device_mapping:
    enabled: false
  state_machines:
    enabled: false
  reconcile:
    enabled: false
  input_adapter:
    enabled: false
  output_adapter:
    enabled: false
  route_manager:
    enabled: false
  eventbus:
    enabled: false
  monitoring:
    enabled: false
```

---

## 📋 Итоговый чек-лист готовности

### Перед началом интеграции

- [x] Все прототипы MVP-0..MVP-12 протестированы
- [x] Проверка совместимости выполнена
- [x] Идеальная архитектура определена
- [x] Контракт EventBus создан
- [ ] Impact Map создан для MVP-INT-1

### Во время интеграции

- [ ] Каждый MVP интегрирован
- [ ] Каждый MVP протестирован
- [ ] Каждый MVP проверен на конфликты/дублирование/race conditions
- [ ] Exit Gate пройден для каждого MVP

### После интеграции

- [ ] End-to-end тесты проходят
- [ ] Ручное тестирование успешно
- [ ] Нет регрессий
- [ ] Метрики соответствуют SLO

---

## 🎯 Следующие шаги

1. **Создать Impact Map** для MVP-INT-1 (Device Discovery)
2. **Начать интеграцию** MVP-INT-1
3. **Тестировать и проверять** MVP-INT-1
4. **Перейти к следующему** MVP только после успешного завершения

---

**Статус**: ✅ Готово к постепенной интеграции

