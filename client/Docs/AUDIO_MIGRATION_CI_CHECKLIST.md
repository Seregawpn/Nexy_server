# CI Checklist для миграции аудиосистемы

**Статус**: Обязательный чек-лист для CI/CD  
**Версия**: 1.0  
**Дата**: 2025-01-XX

**Примечание**: Этот документ является детальным CI-чеклистом. Главный нормативный документ: `Docs/AUDIO_MIGRATION_MASTER_SPECIFICATION.md`

---

## 📋 Pre-commit проверки

### Линтинг и форматирование

- [ ] `ruff check` проходит без ошибок
- [ ] `ruff format --check` проходит без ошибок
- [ ] `mypy` проходит без ошибок (если настроен)
- [ ] Нет прямого доступа к состоянию (кроме selectors/gateways)

**Команда**:
```bash
ruff check modules/voice_recognition/core/avfoundation/ integration/integrations/audio_route_manager_integration.py
ruff format --check modules/voice_recognition/core/avfoundation/ integration/integrations/audio_route_manager_integration.py
```

---

### Валидация схем

- [ ] `unified_config.yaml` валиден
- [ ] `interaction_matrix.yaml` валиден
- [ ] Схемы соответствуют конфигурации

**Команда**:
```bash
python3 scripts/validate_schemas.py
```

---

### Проверка feature flags

- [ ] Все feature flags зарегистрированы в `FEATURE_FLAGS.md`
- [ ] Kill-switches зарегистрированы
- [ ] Значения по умолчанию корректны (все `false`)

**Команда**:
```bash
python3 scripts/verify_feature_flags.py
```

---

## 🧪 Unit тесты

### Покрытие компонентов

- [ ] `test_avfoundation_contracts.py` - покрытие ≥80%
- [ ] `test_avfoundation_mapping.py` - покрытие ≥80%
- [ ] `test_avfoundation_state_machines.py` - покрытие ≥80%
- [ ] `test_avfoundation_route_manager.py` - покрытие ≥80%
- [ ] `test_avfoundation_adapters.py` - покрытие ≥80%

**Команда**:
```bash
pytest tests/test_avfoundation_*.py --cov=modules/voice_recognition/core/avfoundation --cov-report=term-missing --cov-report=html
```

**Требования**:
- Минимум 80% покрытие для каждого файла
- Все edge cases покрыты
- Негативные тесты присутствуют

---

### Pairwise тесты

- [ ] `test_avfoundation_route_manager.py` содержит ≥12 pairwise тестов
- [ ] ≥2 негативных теста
- [ ] Decision-логи проверяются в каноническом формате

**Команда**:
```bash
pytest tests/test_avfoundation_route_manager.py -v
```

---

## 🔗 Интеграционные тесты

### Route Manager интеграция

- [ ] `test_audio_route_manager.py` - все сценарии проходят
  - [ ] Happy path
  - [ ] Device changed
  - [ ] Blocking conditions (first_run, restart_pending, update_in_progress)
  - [ ] Fallback (AVFoundation недоступен)
  - [ ] Mapping failures (LOW/NONE confidence)
  - [ ] Network offline (degrade)
  - [ ] Device busy (retry)

**Команда**:
```bash
pytest tests/integration/test_audio_route_manager.py -v
```

---

### Порядок инициализации

- [ ] `test_init_order.py` - порядок инициализации корректен
- [ ] `AudioRouteManagerIntegration` инициализируется после `speech_playback`
- [ ] Зависимости проверяются

**Команда**:
```bash
pytest tests/test_init_order.py -v
```

---

## 📊 Метрики и SLO

### Проверка метрик

- [ ] Метрики зарегистрированы в `registry.md`
- [ ] SLO пороги определены
- [ ] Источники метрик указаны

**Команда**:
```bash
grep -A 10 "Audio Route Manager Metrics" client/metrics/registry.md
```

---

### SLO тесты

- [ ] `test_slo.py` содержит тесты для новых метрик
- [ ] Пороги проверяются автоматически

**Команда**:
```bash
pytest tests/perf/test_slo.py -v
```

---

## 🔍 Статические проверки

### Запрет прямого доступа к состоянию

- [ ] `verify_no_direct_state_access.py` проходит
- [ ] Только selectors/gateways читают состояние

**Команда**:
```bash
python3 scripts/verify_no_direct_state_access.py
```

---

### Валидация 4-артефактного инварианта

- [ ] `STATE_CATALOG.md` обновлен (если нужно)
- [ ] `interaction_matrix.yaml` содержит правила RouteManager
- [ ] `gateways.py` содержит `decide_route_manager_reconcile`
- [ ] Тесты gateways обновлены

**Команда**:
```bash
# Проверка вручную или через скрипт
python3 scripts/verify_4_artifact_invariant.py  # Если есть
```

---

### Проверка контрактов EventBus

- [ ] Все события документированы
- [ ] Payload схемы валидны
- [ ] Обратная совместимость соблюдена

**Команда**:
```bash
# Проверка вручную или через скрипт
python3 scripts/verify_eventbus_contracts.py  # Если есть
```

---

## 📝 Документация

### Обновление документации

- [ ] `AUDIO_MIGRATION_SPECIFICATION.md` соответствует реализации
- [ ] `modules/voice_recognition/core/avfoundation/README.md` создан
- [ ] `INTEGRATION_GUIDE.md` обновлен (если нужно)
- [ ] `AUDIO_SYSTEM_ARCHITECTURE.md` обновлен (если нужно)

**Проверка**:
```bash
# Проверка вручную
ls -la modules/voice_recognition/core/avfoundation/README.md
```

---

## 🚀 Pre-build Gate

### Полная проверка перед сборкой

- [ ] `pre_build_gate.sh` проходит успешно

**Команда**:
```bash
./scripts/pre_build_gate.sh
```

**Что проверяет**:
- Линтеры и форматтеры
- Unit-тесты
- Статические проверки
- Валидация схем
- Проверка feature flags
- Проверка 4-артефактного инварианта

---

## 🔄 Release Suite

### Полный цикл проверок

- [ ] `run_release_suite.py` проходит успешно

**Команда**:
```bash
python3 scripts/run_release_suite.py
```

**Что проверяет**:
- Pre-build gate
- Сборка dev-билда
- Проверка логов
- Интеграционные тесты
- Проверки сервера
- Сверка требований

---

## 📦 Packaging проверки

### Сборка и упаковка

- [ ] PyInstaller сборка проходит успешно
- [ ] `.app` создается корректно
- [ ] PKG создается корректно
- [ ] Нотарификация проходит (если требуется)

**Команда**:
```bash
./packaging/rebuild_from_scratch.sh
./packaging/build_final.sh
```

---

### Entitlements проверка

- [ ] `entitlements.plist` содержит необходимые разрешения
- [ ] AVFoundation не требует дополнительных entitlements
- [ ] Hardened Runtime совместим

**Проверка**:
```bash
cat packaging/entitlements.plist | grep -i microphone
cat packaging/entitlements.plist | grep -i audio
```

---

## 🎯 Критерии готовности к мерджу

### Обязательные проверки

- [ ] Все pre-commit проверки пройдены
- [ ] Все unit тесты проходят (≥80% покрытие)
- [ ] Все интеграционные тесты проходят
- [ ] Pairwise тесты созданы (≥12 + 2 негативных)
- [ ] Decision-логи в каноническом формате
- [ ] Feature flags зарегистрированы
- [ ] Метрики зарегистрированы
- [ ] Правила в interaction_matrix.yaml добавлены
- [ ] ADR создан
- [ ] Change Impact создан
- [ ] Документация обновлена
- [ ] Pre-build gate проходит
- [ ] Release Suite проходит (опционально для PR)

---

### Опциональные проверки (для релиза)

- [ ] Release Suite проходит полностью
- [ ] Packaging проверки пройдены
- [ ] Нотарификация пройдена
- [ ] Валидация бандла пройдена

---

## 🔧 CI/CD интеграция

### GitHub Actions workflow

**Файл**: `.github/workflows/ci.yml`

**Добавить job**:
```yaml
audio_migration_checks:
  runs-on: macos-latest
  steps:
    - uses: actions/checkout@v3
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
    - name: Run pre-build gate
      run: ./scripts/pre_build_gate.sh
    - name: Run audio migration tests
      run: |
        pytest tests/test_avfoundation_*.py --cov --cov-report=xml
    - name: Run integration tests
      run: |
        pytest tests/integration/test_audio_route_manager.py -v
```

---

## 📊 Метрики CI

### Отслеживание в CI

- [ ] Время выполнения тестов
- [ ] Покрытие кода
- [ ] Количество проваленных тестов
- [ ] Время сборки

---

## ✅ Итоговый чек-лист перед мерджем

### Критично (блокирует мердж)

- [ ] Все линтеры проходят
- [ ] Все unit тесты проходят (≥80% покрытие)
- [ ] Все интеграционные тесты проходят
- [ ] Pairwise тесты созданы
- [ ] Decision-логи в каноническом формате
- [ ] Feature flags зарегистрированы
- [ ] Правила в interaction_matrix.yaml добавлены
- [ ] Pre-build gate проходит

### Важно (рекомендуется)

- [ ] Release Suite проходит
- [ ] Документация обновлена
- [ ] ADR создан
- [ ] Change Impact создан

### Опционально (для релиза)

- [ ] Packaging проверки пройдены
- [ ] Нотарификация пройдена

---

**Этот чек-лист обязателен для проверки перед каждым PR и мерджем.**

