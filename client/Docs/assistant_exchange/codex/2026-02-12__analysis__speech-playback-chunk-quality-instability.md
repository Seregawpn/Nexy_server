## Контекст
- Симптом: периодически «грязный/нечистый» звук TTS-чанков, чаще в сценариях с сообщениями.
- Логи: `nexy-dev.log` (фрагмент от `2026-02-12 11:09:40`).

## Ключевые наблюдения
- В `SpeechPlaybackIntegration` включен `tts_auto_gain_enabled`, при этом gain фиксируется на сессию:
  - `integration/integrations/speech_playback_integration.py` (`_on_audio_chunk`, блок `_tts_gain_by_session`).
- В логах почти постоянный `gain=6.00`:
  - `🔊 [AUDIO_GAIN] ... gain=6.00 ...`
- После gain амплитуда часто упирается в лимитер:
  - `AUDIO_PIPELINE ... peak=0.9800`
  - в коде применен `np.clip(..., -0.98, 0.98)`.
- Это приводит к жесткому clipping на части чанков (особенно когда исходный `peak_before` меняется от 0.01 до 0.33 в рамках одной сессии).

## Вывод
- Основная причина артефактов: переусиление + клиппинг из-за session-pinned gain (`max 6.0`) при изменяющейся динамике потока.
- Архитектурно владелец loudness-политики уже корректный: `SpeechPlaybackIntegration` (оставляем централизацию, без переноса в `AVFoundationPlayer`/`GrpcClientIntegration`).

## Рекомендованное направление фикса
1. Перестать фиксировать gain на всю сессию; рассчитывать мягко по чанкам с EMA-сглаживанием.
2. Ограничить максимальный gain (например, 2.0–3.0 вместо 6.0).
3. Добавить headroom-guard: если после gain ожидаемый пик > 0.9, автоматически уменьшать gain.
4. Добавить telemetry для `% clipped samples` на чанк; DoD: clipping < 1% в обычных ответах.

## Риск/совместимость
- Низкий риск архитектурно (изменение только в одном владельце политики громкости).
- Высокий эффект на субъективное качество и стабильность тембра.
