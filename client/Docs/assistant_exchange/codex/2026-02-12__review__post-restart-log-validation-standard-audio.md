## Проверенный лог
- `~/Library/Logs/Nexy/nexy-dev.log`
- Окно проверки: актуальные записи после перезапуска (в т.ч. `2026-02-12 11:21:51`–`11:24:37`)

## Вывод по целевому изменению
- `AUDIO_GAIN` в новых сессиях отсутствует (последние такие записи только около `11:09:40`).
- В `AUDIO_PIPELINE phase=before_queue` пики не зажимаются к `0.9800` как раньше; значения распределены естественно (например 0.23, 0.44, 0.67, 0.06, 0.00 и т.д.).
- По архитектуре поток завершения/отмены корректен: `interrupt.request -> grpc.request_cancel -> playback.cancelled -> mode.request -> sleeping`.

## Найденный риск
- Повторяются `ERROR`:
  - `modules.speech_playback.core.avf_player - ❌ [QUEUE_EXTRACT] Audio data is ZERO after extraction from queue!`
  - встречается и в свежем запуске (например `11:21:52.812`, `11:21:53.329` и далее).
- Это похоже на диагностический шум для тишины/хвостовых чанков, а не на функциональный сбой playback.

## Рекомендация
- Понизить этот кейс из `ERROR` в `DEBUG/WARNING` при валидных silent-tail сценариях, чтобы не маскировать реальные ошибки.
