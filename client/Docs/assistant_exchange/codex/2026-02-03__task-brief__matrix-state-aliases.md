# Interaction Matrix vs State Catalog Aliases

Goal: document predicate aliases to reduce confusion between canonical axes and rule keys.

## Changes
- Added predicate alias notes in `Docs/STATE_CATALOG.md` for perm.*, app.*, update.in_progress.
- Added note in `config/interaction_matrix.yaml` about predicate aliases.

## Files
- `Docs/STATE_CATALOG.md`
- `config/interaction_matrix.yaml`
