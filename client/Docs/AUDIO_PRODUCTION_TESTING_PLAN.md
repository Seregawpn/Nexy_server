# Audio Playback System — Production Testing Plan

**Дата:** 2025-12-24  
**Статус:** Готов к выполнению

---

## 📋 Общая структура тестирования

### Уровни тестирования

1. **Unit-тесты** — проверка отдельных компонентов
2. **Integration-тесты** — проверка взаимодействия компонентов
3. **Stress-тесты** — проверка под нагрузкой
4. **Production-тесты** — проверка в реальных условиях

### Быстрый старт

**Автоматизированные тесты:**
```bash
# Запуск всех production-тестов
python scripts/test_audio_production.py

# Проверка метрик
python scripts/test_audio_metrics.py
```

**Ручные тесты:**
- См. разделы ниже для детальных инструкций

---

## 1. Unit-тесты (критические компоненты)

### 1.1 Тест: Lock-инварианты

**Цель:** Проверить, что все операции с shared state выполняются под lock.

**Шаги:**
1. Создать mock `SequentialSpeechPlayer`
2. Симулировать параллельные вызовы `add_audio_data()` из 10 потоков
3. Проверить, что нет race conditions

**Ожидаемый результат:**
- ✅ Нет ошибок доступа к `_audio_stream` без lock
- ✅ Все операции с shared state под `_stream_lock`
- ✅ Нет deadlock'ов

**Критерий успеха:**
- Все операции завершаются без ошибок
- Нет нарушений lock-инвариантов

---

### 1.2 Тест: Generation counter (closure)

**Цель:** Проверить, что generation работает через closure и защищает от устаревших callback'ов.

**Шаги:**
1. Создать stream с `stream_gen=1`
2. Пересоздать stream (увеличить generation до 2)
3. Вызвать старый callback
4. Проверить, что старый callback не пишет данные

**Ожидаемый результат:**
- ✅ Старый callback заполняет `outdata[:] = 0`
- ✅ `_callback_gen_mismatch_count` увеличивается
- ✅ Новый callback работает корректно

**Критерий успеха:**
- Старый callback не пишет данные
- Generation изолирован для каждого callback

---

### 1.3 Тест: Нормализация int16 → float32

**Цель:** Проверить симметричную нормализацию.

**Шаги:**
1. Создать массив `[-32768, 0, 32767]` (int16)
2. Нормализовать: `/ 32768.0`
3. Конвертировать обратно: `* 32768.0`
4. Проверить восстановление значений

**Ожидаемый результат:**
- ✅ `-32768 → -1.0` (симметрично)
- ✅ `32767 → ~0.99997` (в пределах точности)
- ✅ Обратная конвертация восстанавливает значения

**Критерий успеха:**
- Нет микроклиппинга
- Симметричная нормализация

---

### 1.4 Тест: Resampling (round vs int)

**Цель:** Проверить, что `round()` устраняет систематический дрейф.

**Шаги:**
1. Создать последовательность чанков (10 чанков по 1000 samples, 24kHz → 48kHz)
2. Ресемплировать с `int()` и с `round()`
3. Сравнить общую длину

**Ожидаемый результат:**
- ✅ С `round()`: общая длина ближе к ожидаемой
- ✅ С `int()`: систематический дрейф (короче)
- ✅ Разница: `round()` точнее на ~0.1-0.5%

**Критерий успеха:**
- `round()` даёт более точную длину
- Нет накопления ошибки

---

### 1.5 Тест: Mono/Stereo mix

**Цель:** Проверить правильный downmix/upmix.

**Шаги:**
1. Mono → Stereo: создать моно данные, проверить дублирование
2. Stereo → Mono: создать стерео данные, проверить среднее (L+R)/2
3. Проверить, что `outdata[:] = 0` в начале

**Ожидаемый результат:**
- ✅ Mono → Stereo: оба канала одинаковые
- ✅ Stereo → Mono: среднее (L+R)/2
- ✅ `outdata` обнулён перед записью

**Критерий успеха:**
- Правильный downmix/upmix
- Нет мусора в каналах

---

## 2. Integration-тесты (критические сценарии)

### 2.1 Тест: Двухфазный recreate

**Цель:** Проверить, что recreate работает корректно без deadlock'ов.

**Шаги:**
1. Запустить playback
2. Симулировать изменение device (вызвать `resync_output_device()`)
3. Проверить, что recreate выполняется в 3 фазы
4. Проверить, что нет deadlock'ов

**Ожидаемый результат:**
- ✅ Фаза 1: решение под lock
- ✅ Фаза 2: I/O операции вне lock
- ✅ Фаза 3: финализация под lock
- ✅ Нет deadlock'ов
- ✅ Generation обновлён после активации stream

**Критерий успеха:**
- Recreate выполняется без ошибок
- Нет deadlock'ов
- Stream работает после recreate

---

### 2.2 Тест: Fallback SR (content_sr → device_sr)

**Цель:** Проверить, что fallback на device_sr работает с ресемплингом.

**Шаги:**
1. Установить device с 48kHz (не поддерживает 24kHz)
2. Отправить аудио 24kHz
3. Проверить, что stream создан с 48kHz (fallback)
4. Проверить, что ресемплинг работает в callback
5. Проверить скорость звука

**Ожидаемый результат:**
- ✅ Stream создан с device_sr (48kHz)
- ✅ Ресемплинг выполняется в callback
- ✅ Скорость звука корректная (не ускорена)
- ✅ Нет underrun

**Критерий успеха:**
- Fallback работает
- Ресемплинг корректный
- Скорость звука нормальная

---

### 2.3 Тест: Device switching (AirPods)

**Цель:** Проверить быструю реакцию на переключение устройства.

**Шаги:**
1. Запустить playback на built-in output
2. Подключить AirPods (route change)
3. Проверить, что переключение обработано сразу (без cooldown)
4. Проверить, что звук не пропадает надолго

**Ожидаемый результат:**
- ✅ Переключение обработано сразу (игнорируется cooldown)
- ✅ Звук не пропадает надолго (< 500ms)
- ✅ `_last_valid_device_id` обновлён
- ✅ Нет потери данных

**Критерий успеха:**
- Быстрая реакция на переключение
- Нет длительной тишины

---

### 2.4 Тест: Device query failure-path

**Цель:** Проверить, что при failure используется кэш и cooldown.

**Шаги:**
1. Запустить playback
2. Симулировать failure device query (вернуть None)
3. Проверить, что используется `_last_valid_device_id`
4. Проверить, что cooldown применяется
5. Проверить, что буферизация продолжается

**Ожидаемый результат:**
- ✅ Используется кэш `_last_valid_device_id`
- ✅ Cooldown применяется (≤ 2 запроса/сек)
- ✅ Буферизация продолжается
- ✅ Нет recreate без device_id

**Критерий успеха:**
- Кэш работает
- Cooldown применяется
- Нет потери данных

---

## 3. Stress-тесты (под нагрузкой)

### 3.1 Тест: Concurrency storm

**Цель:** Проверить, что система работает под высокой нагрузкой.

**Шаги:**
1. Создать 10 потоков
2. Каждый поток вызывает `add_audio_data()` параллельно
3. Проверить, что нет race conditions
4. Проверить, что `_recreating_stream` всегда возвращается в `False`

**Ожидаемый результат:**
- ✅ Нет race conditions
- ✅ `_recreating_stream` всегда возвращается в `False`
- ✅ Нет "залипло"
- ✅ `_stream_recreate_count` разумный (≤ 10)

**Критерий успеха:**
- Система стабильна под нагрузкой
- Нет deadlock'ов или залипаний

---

### 3.2 Тест: Быстрое переключение устройств

**Цель:** Проверить, что система справляется с частыми переключениями.

**Шаги:**
1. Подключить/отключить AirPods 10 раз за 30 секунд
2. Проверить, что каждый раз recreate выполняется корректно
3. Проверить, что нет накопления ошибок

**Ожидаемый результат:**
- ✅ Каждое переключение обработано
- ✅ `_stream_recreate_count` ≤ 10
- ✅ Нет накопления ошибок
- ✅ Звук работает после каждого переключения

**Критерий успеха:**
- Система справляется с частыми переключениями
- Нет деградации производительности

---

### 3.3 Тест: Длительный playback (2-3 минуты)

**Цель:** Проверить стабильность на длинной сессии.

**Шаги:**
1. Запустить playback на 2-3 минуты
2. Мониторить метрики:
   - `callback_underrun_count`
   - `callback_resample_ms` (p95)
   - `_stream_recreate_count`
3. Проверить, что нет накопления ошибок

**Ожидаемый результат:**
- ✅ `callback_underrun_count ≈ 0-единицы`
- ✅ `callback_resample_ms` (p95) < 5-8ms
- ✅ Нет crackles
- ✅ Нет накопления ошибок

**Критерий успеха:**
- Стабильная работа на длинной сессии
- Метрики в пределах нормы

---

## 4. Production-тесты (реальные условия)

### 4.1 Тест: AirPods / BT (Release Acceptance Criteria)

**Цель:** Проверить работу с Bluetooth устройствами.

**Шаги:**
1. Подключить AirPods
2. Запустить playback на 2-3 минуты
3. Проверить метрики:
   - `callback_underrun_count`
   - Нет тишины при route change
4. Переключить на built-in output и обратно (5-10 раз)

**Ожидаемый результат:**
- ✅ `callback_underrun_count ≈ 0-единицы`
- ✅ Нет тишины при route change
- ✅ Переключение чувствуется сразу
- ✅ Нет потери данных

**Критерий успеха:**
- Работа с Bluetooth стабильна
- Нет проблем с переключением

---

### 4.2 Тест: Fallback SR (Release Acceptance Criteria)

**Цель:** Проверить, что fallback на device_sr работает корректно.

**Шаги:**
1. Установить device с 48kHz (не поддерживает 24kHz)
2. Отправить аудио 24kHz
3. Проверить скорость и тон звука
4. Проверить, что ресемплинг работает

**Ожидаемый результат:**
- ✅ Скорость звука корректная (не ускорена)
- ✅ Тон корректный (не выше)
- ✅ Ресемплинг работает автоматически
- ✅ Нет underrun

**Критерий успеха:**
- Fallback работает корректно
- Звук нормальной скорости и тона

---

### 4.3 Тест: Concurrency storm (Release Acceptance Criteria)

**Цель:** Проверить, что система справляется с параллельными вызовами.

**Шаги:**
1. Создать 10 потоков
2. Каждый поток вызывает `add_audio_data()` параллельно
3. Проверить, что `_recreating_stream` всегда возвращается в `False`
4. Проверить, что нет race conditions

**Ожидаемый результат:**
- ✅ `_recreating_stream` всегда возвращается в `False`
- ✅ Нет race conditions
- ✅ Нет "залипло"
- ✅ Система стабильна

**Критерий успеха:**
- Система справляется с параллельными вызовами
- Нет deadlock'ов или залипаний

---

### 4.4 Тест: p95 resample latency (Release Acceptance Criteria)

**Цель:** Проверить, что ресемплинг не создаёт задержек.

**Шаги:**
1. Запустить playback с ресемплингом (24kHz → 48kHz)
2. Собрать метрики `callback_resample_ms_history`
3. Вычислить p95
4. Проверить, что p95 < 5-8ms

**Ожидаемый результат:**
- ✅ p95 < 5-8ms
- ✅ Нет underrun
- ✅ Нет crackles

**Критерий успеха:**
- Ресемплинг не создаёт задержек
- Если p95 высокий → перенести ресемпл в producer

---

## 5. Метрики и Tripwires

### 5.1 Мониторинг метрик

**Метрики для отслеживания:**

1. **Счётчики:**
   - `callback_underrun_count` — должно быть ≈ 0-единицы
   - `callback_gen_mismatch_count` — должно быть минимально
   - `callback_shape_mismatch_count` — должно быть 0
   - `callback_error_count` — должно быть 0
   - `resample_error_count` — должно быть 0
   - `device_query_failure_count` — должно быть минимально
   - `stream_open_fail_count` — должно быть минимально

2. **Таймеры:**
   - `recreate_total_ms` (p95) — должно быть < 100ms
   - `callback_resample_ms` (p95) — должно быть < 5-8ms

3. **Счётчики операций:**
   - `_stream_recreate_count` — должно быть разумным (≤ 10 за сессию)

---

### 5.2 Проверка Tripwires

**Автоматические проверки:**

1. **Underrun:**
   - Если `callback_underrun_count > 10` → предупреждение
   - Если `callback_underrun_count > 50` → ошибка

2. **Shape mismatch:**
   - Если `callback_shape_mismatch_count > 0` → предупреждение
   - Если `callback_shape_mismatch_count > 10` → ошибка

3. **Recreate timeout:**
   - Если `recreate_elapsed_ms > 5000` → предупреждение
   - Если `recreate_elapsed_ms > 10000` → ошибка

4. **Resample latency:**
   - Если `callback_resample_ms` (p95) > 8ms → предупреждение
   - Если `callback_resample_ms` (p95) > 15ms → ошибка

---

## 6. Чек-лист перед релизом

### 6.1 Обязательные проверки

- [ ] **Lock-инварианты:** Все операции с shared state под lock
- [ ] **Generation counter:** Closure работает, старые callback'ы не пишут данные
- [ ] **Callback безопасность:** Нет assert, нет логов, нет np.vstack
- [ ] **Инварианты данных:** `outdata.shape[0] == frames`, всегда обнулён
- [ ] **Нормализация:** Симметричная int16 → float32 (32768.0)
- [ ] **Каналы:** Правильный mono/stereo mix
- [ ] **Ресемплинг:** `round()` вместо `int()`, dtype guard
- [ ] **Device handling:** Pure detection, cooldown только на failure
- [ ] **Tripwires:** Все счётчики работают, логи вне callback

---

### 6.2 Release Acceptance Criteria

- [ ] **AirPods / BT:** `callback_underrun_count ≈ 0`, нет тишины при route change
- [ ] **Fallback SR:** Скорость и тон корректны, ресемплинг работает
- [ ] **Concurrency storm:** `_recreating_stream` всегда возвращается в `False`
- [ ] **p95 resample latency:** < 5-8ms

---

### 6.3 Метрики

- [ ] `callback_underrun_count` ≈ 0-единицы за 2-3 минуты
- [ ] `callback_resample_ms` (p95) < 5-8ms
- [ ] `_stream_recreate_count` разумный (≤ 10 за сессию)
- [ ] Нет накопления ошибок

---

## 7. Скрипты для автоматизации

### 7.1 Скрипт: Проверка метрик

```python
# scripts/check_audio_metrics.py
def check_audio_metrics(player):
    """Проверка метрик audio playback"""
    metrics = {
        'underrun_count': player._callback_underrun_count,
        'gen_mismatch_count': player._callback_gen_mismatch_count,
        'shape_mismatch_count': player._callback_shape_mismatch_count,
        'error_count': player._callback_error_count,
        'resample_error_count': player._resample_error_count,
        'recreate_count': player._stream_recreate_count,
    }
    
    # Проверка tripwires
    if metrics['underrun_count'] > 10:
        logger.warning(f"⚠️ Underrun count high: {metrics['underrun_count']}")
    
    if metrics['shape_mismatch_count'] > 0:
        logger.error(f"❌ Shape mismatch detected: {metrics['shape_mismatch_count']}")
    
    # Проверка p95 resample latency
    if len(player._callback_resample_ms_history) > 0:
        p95 = np.percentile(player._callback_resample_ms_history, 95)
        if p95 > 8:
            logger.warning(f"⚠️ Resample p95 high: {p95:.2f}ms")
    
    return metrics
```

---

### 7.2 Скрипт: Stress-тест

```python
# scripts/stress_test_audio.py
def stress_test_concurrency(player, num_threads=10, duration=30):
    """Stress-тест параллельных вызовов"""
    import threading
    import time
    
    def add_audio_worker():
        for _ in range(100):
            audio_data = np.random.randint(-32768, 32767, 1000, dtype=np.int16)
            player.add_audio_data(audio_data)
            time.sleep(0.01)
    
    threads = []
    for _ in range(num_threads):
        t = threading.Thread(target=add_audio_worker)
        threads.append(t)
        t.start()
    
    for t in threads:
        t.join()
    
    # Проверка метрик
    assert player._recreating_stream == False, "Recreating stream залипло"
    assert player._stream_recreate_count <= 20, f"Слишком много recreate: {player._stream_recreate_count}"
```

---

### 7.3 Скрипт: Проверка инвариантов

```python
# scripts/check_audio_invariants.py
def check_audio_invariants(player):
    """Проверка инвариантов audio playback"""
    with player._stream_lock:
        # Проверка lock-инвариантов
        assert player._audio_stream is None or isinstance(player._audio_stream, sd.OutputStream)
        assert isinstance(player._stream_started, bool)
        assert player._stream_sample_rate is None or isinstance(player._stream_sample_rate, int)
        assert player._current_stream_gen >= 0
        
        # Проверка generation
        assert player._current_stream_gen <= player._stream_gen
        
        # Проверка recreate
        assert player._recreating_stream == False or player._recreating_stream_start_time is not None
```

---

## 8. Порядок выполнения тестов

### Этап 1: Unit-тесты (быстрые)

1. Тест: Lock-инварианты
2. Тест: Generation counter
3. Тест: Нормализация int16 → float32
4. Тест: Resampling (round vs int)
5. Тест: Mono/Stereo mix

**Время:** ~5-10 минут  
**Критерий:** Все тесты зелёные

---

### Этап 2: Integration-тесты (средние)

1. Тест: Двухфазный recreate
2. Тест: Fallback SR
3. Тест: Device switching
4. Тест: Device query failure-path

**Время:** ~15-20 минут  
**Критерий:** Все тесты зелёные

---

### Этап 3: Stress-тесты (длительные)

1. Тест: Concurrency storm
2. Тест: Быстрое переключение устройств
3. Тест: Длительный playback (2-3 минуты)

**Время:** ~5-10 минут  
**Критерий:** Метрики в пределах нормы

---

### Этап 4: Production-тесты (реальные условия)

1. Тест: AirPods / BT
2. Тест: Fallback SR
3. Тест: Concurrency storm
4. Тест: p95 resample latency

**Время:** ~10-15 минут  
**Критерий:** Все Release Acceptance Criteria выполнены

---

## 9. Критерии успеха

### 9.1 Обязательные (блокеры)

- ✅ Все unit-тесты зелёные
- ✅ Все integration-тесты зелёные
- ✅ Все stress-тесты зелёные
- ✅ Все Release Acceptance Criteria выполнены
- ✅ Нет нарушений lock-инвариантов
- ✅ Нет race conditions
- ✅ Нет deadlock'ов

---

### 9.2 Желательные (не блокеры)

- ✅ Метрики в пределах нормы
- ✅ Нет накопления ошибок
- ✅ Производительность стабильна

---

## 10. Отчёт о тестировании

### 10.1 Формат отчёта

```json
{
  "test_date": "2025-12-24",
  "test_results": {
    "unit_tests": {
      "lock_invariants": "PASS",
      "generation_counter": "PASS",
      "normalization": "PASS",
      "resampling": "PASS",
      "mono_stereo_mix": "PASS"
    },
    "integration_tests": {
      "two_phase_recreate": "PASS",
      "fallback_sr": "PASS",
      "device_switching": "PASS",
      "device_query_failure": "PASS"
    },
    "stress_tests": {
      "concurrency_storm": "PASS",
      "rapid_device_switching": "PASS",
      "long_playback": "PASS"
    },
    "production_tests": {
      "airpods_bt": "PASS",
      "fallback_sr": "PASS",
      "concurrency_storm": "PASS",
      "p95_resample_latency": "PASS"
    }
  },
  "metrics": {
    "callback_underrun_count": 0,
    "callback_resample_ms_p95": 3.2,
    "stream_recreate_count": 2,
    "recreate_total_ms_p95": 45.0
  },
  "tripwires": {
    "all_clear": true
  }
}
```

---

## ✅ Итог

**План тестирования готов к выполнению.**

Все критические сценарии покрыты:
- ✅ Unit-тесты (быстрые проверки)
- ✅ Integration-тесты (взаимодействие компонентов)
- ✅ Stress-тесты (под нагрузкой)
- ✅ Production-тесты (реальные условия)

**Готово к тестированию!**

