# Ручной тест First-Run Flow

## Цель теста
Проверить, что система первого запуска корректно:
1. Запрашивает разрешения последовательно
2. Блокирует запуск остальных интеграций до получения всех разрешений
3. Ждёт решения пользователя без таймаута
4. Перезапускается после выдачи критических разрешений

## Подготовка

### 1. Сброс TCC разрешений
```bash
# Сброс всех разрешений для bundle ID
sudo tccutil reset Microphone com.nexy.assistant
sudo tccutil reset Accessibility com.nexy.assistant
sudo tccutil reset ScreenCapture com.nexy.assistant
sudo tccutil reset ListenEvent com.nexy.assistant
```

### 2. Очистка флагов первого запуска
```bash
# Использовать скрипт очистки
python scripts/clear_first_run_flags.py

# Или вручную:
rm -f ~/Library/Application\ Support/Nexy/*.flag
```

### 3. Проверка конфигурации
Убедитесь, что в `config/unified_config.yaml`:
```yaml
permissions:
  first_run:
    enabled: true
```

## Выполнение теста

### 1. Запуск приложения
```bash
python client/main.py
```

### 2. Ожидаемое поведение

#### Шаг 1: Блокировка интеграций
- Приложение должно заблокировать запуск остальных интеграций
- В логах должно быть: `[FIRST_RUN_PERMISSIONS] Проверка разрешений при запуске`
- В логах должно быть: `[PERMISSIONS] Некоторые разрешения не выданы — начинаем запрос`

#### Шаг 2: Последовательный запрос разрешений
Порядок запроса:
1. **Input Monitoring** (требует перезапуска)
2. **Accessibility** (требует перезапуска)
3. **Microphone** (не требует перезапуска)
4. **Screen Capture** (требует перезапуска)

Для каждого разрешения:
- Должен появиться системный диалог macOS
- В логах: `[PERMISSIONS] <тип> не выдан — запрашиваем...`
- В логах: `⏳ [<тип>] Активация запроса разрешения...`
- После активации: `📋 [<тип>] Открываем настройки и ждём выдачи...`

#### Шаг 3: Ожидание решения пользователя
- **Важно**: Приложение ждёт БЕЗ таймаута (бесконечный цикл проверки)
- Каждые 10 секунд в логах: `⏳ [<тип>] Ожидание разрешения... (status=...)`
- После выдачи: `✅ [<тип>] Разрешение получено!`

#### Шаг 4: Перезапуск (если требуется)
Если были выданы Accessibility, Input Monitoring или Screen Capture:
- В логах: `🔄 [PERMISSIONS] Требуется перезапуск для активации Accessibility/Input Monitoring`
- В логах: `[FIRST_RUN_PERMISSIONS] Установка restart_completed.flag`
- Приложение должно перезапуститься автоматически

#### Шаг 5: Пост-перезапуск
После перезапуска:
- В логах нового процесса: `✅ [FIRST_RUN_PERMISSIONS] Перезапуск после first_run завершён успешно`
- В логах: `RESTART_FLAG seen_ts=...`
- Публикуется событие: `permissions.first_run_completed`

## Проверка результатов

### Логи
Проверьте наличие следующих логов:
```
[FIRST_RUN_PERMISSIONS] Проверка разрешений при запуске
[PERMISSIONS] Некоторые разрешения не выданы — начинаем запрос
⌨️ [PERMISSIONS] Input Monitoring не выдан — запрашиваем...
♿ [PERMISSIONS] Accessibility не выдан — запрашиваем...
🎙️ [PERMISSIONS] Microphone не выдан — запрашиваем...
📺 [PERMISSIONS] Screen Capture не выдан — запрашиваем...
✅ [PERMISSIONS] Все разрешения получены!
🔄 [PERMISSIONS] Требуется перезапуск...
```

### Флаги
После завершения должны быть созданы:
- `~/Library/Application Support/Nexy/permissions_first_run_completed.flag`
- `~/Library/Application Support/Nexy/restart_completed.flag` (после перезапуска)

### События EventBus
Проверьте публикацию событий:
- `permissions.first_run_started`
- `permissions.status_checked` (для каждого разрешения)
- `permissions.first_run_restart_pending` (если требуется перезапуск)
- `permissions.first_run_completed` (после перезапуска)

## Известные проблемы

### Проблема: Приложение не блокирует интеграции
**Решение**: Проверьте, что `first_run.enabled: true` в конфиге и флаг `permissions_first_run_completed.flag` отсутствует.

### Проблема: Диалоги не появляются
**Решение**: 
- Убедитесь, что TCC разрешения сброшены: `sudo tccutil reset <тип> com.nexy.assistant`
- Проверьте, что приложение запущено не из sandbox (для dev-режима)

### Проблема: Приложение не перезапускается
**Решение**: 
- Проверьте логи на наличие `restart_completed.flag`
- Убедитесь, что `NEXY_DISABLE_AUTO_RESTART` не установлена в env
- Проверьте, что `PermissionRestartIntegration` включена в конфиге

## Дополнительные проверки

### Тест с частичными разрешениями
1. Выдайте только Microphone
2. Откажите в остальных
3. Проверьте, что приложение ждёт выдачи всех разрешений

### Тест без перезапуска
1. Выдайте только Microphone (не требует перезапуска)
2. Проверьте, что приложение продолжает работу без перезапуска
3. В логах должно быть: `permissions.first_run_completed` с `restart_needed: false`

## Автоматизация

Для автоматического тестирования используйте:
```bash
# Скрипт очистки флагов
python scripts/clear_first_run_flags.py

# Скрипт проверки состояния
python scripts/check_first_run_state.py
```
