# Завершение валидации состояния: First-Run Permissions

## Метаданные
- Ассистент: cursor
- Тип: task-brief
- Дата: 2026-01-13
- Статус: completed

## Цель

Оставить разделение offline/runtime, убедиться, что offline режим не импортирует ApplicationStateManager, и явно указать в QA-документах, что state валиден только при активном приложении.

## Выполненные изменения

### 1. Проверка offline режима ✅

**Проверка импортов:**
- ✅ Offline режим **НЕ импортирует** `ApplicationStateManager` в начале файла
- ✅ Импорт `ApplicationStateManager` только в runtime режиме (lazy import внутри `check_runtime_state()`)
- ✅ Offline режим использует только `get_user_data_dir` для проверки флага и чтения логов

**Импорты в файле:**
```python
# Только базовые импорты
import sys
import os
import re
from pathlib import Path
from typing import Optional
from integration.utils.resource_path import get_user_data_dir

# ApplicationStateManager импортируется ТОЛЬКО в check_runtime_state()
```

### 2. Обновлены QA-документы ✅

**Обновлён `2026-01-13__task-brief__first-run-qa-plan.md`:**
- ✅ Добавлено явное предупреждение: "State валиден только при активном приложении"
- ✅ Указано, что скрипт создаёт новый экземпляр ApplicationStateManager
- ✅ Указано, что для фактического состояния используется offline режим

**Обновлён `2026-01-13__task-brief__first-run-qa-results.md`:**
- ✅ Добавлено: "Offline режим НЕ импортирует ApplicationStateManager"
- ✅ Добавлено: "State валиден только при активном приложении"
- ✅ Убрана трактовка "нового ApplicationStateManager" как фактического состояния
- ✅ Чётко указано, что единственный источник истины в offline режиме: флаг + логи

### 3. Обновлён `check_first_run_state.py` ✅

**Изменения в предупреждении:**
- ✅ Изменено: "Это состояние из нового ApplicationStateManager" → "State валиден только при активном приложении"
- ✅ Добавлено: "Это состояние из нового ApplicationStateManager, созданного скриптом"
- ✅ Уточнено: "Оно отражает runtime-состояние только если интеграции инициализированы"

## Результаты проверки

### Offline режим (default)

**Команда:**
```bash
python3 scripts/check_first_run_state.py
```

**Проверка импортов:**
- ✅ Нет импорта `ApplicationStateManager` в начале файла
- ✅ Нет импорта `ApplicationStateManager` в `check_offline_state()`
- ✅ Только проверка флага и логов

**Вывод:**
```
=== First Run State Check (OFFLINE MODE) ===
ℹ️  StateManager не проверяется в offline режиме (создаёт новый экземпляр)
    Единственный источник истины: флаг и логи

📋 Флаг first-run:
   ✅ First-run завершён (флаг существует)

📋 События в логах:
   ✅ permissions.first_run_started: 3 вхождений
   ✅ permissions.first_run_completed: 12 вхождений
   ...
```

### Runtime режим (opt-in)

**Команда:**
```bash
python3 scripts/check_first_run_state.py --runtime
```

**Проверка импортов:**
- ✅ Импорт `ApplicationStateManager` только внутри `check_runtime_state()` (lazy import)
- ✅ Предупреждение о валидности state

**Вывод:**
```
=== First Run State Check (RUNTIME MODE) ===
⚠️  Runtime режим требует работающее приложение
    StateManager проверяется только если интеграции инициализированы

📋 StateManager состояние:
   first_run_in_progress: False
   first_run_required: None
   first_run_completed: False
   ...

⚠️  ВАЖНО: State валиден только при активном приложении
    Это состояние из нового ApplicationStateManager, созданного скриптом
    Оно отражает runtime-состояние только если интеграции инициализированы в работающем приложении
    Для проверки фактического состояния используйте offline режим (флаг + логи)
```

## Соответствие требованиям

### Step 1 — Offline режим ✅
- ✅ Не импортирует `ApplicationStateManager`
- ✅ Проверяет только флаг и логи
- ✅ Единственный источник истины: `permissions_first_run_completed.flag` и события в логах

### Step 2 — Runtime режим ✅
- ✅ Lazy import `ApplicationStateManager` только в runtime режиме
- ✅ Предупреждение: "State валиден только при активном приложении"
- ✅ Указание использовать offline режим для фактического состояния

### Step 3 — QA-документы ✅
- ✅ Явно указано: "State валиден только при активном приложении"
- ✅ Убрана трактовка "нового ApplicationStateManager" как фактического состояния
- ✅ Чётко разделены источники истины для offline и runtime режимов

### Step 4 — Перепроверка ✅
- ✅ Offline режим не импортирует `ApplicationStateManager`
- ✅ Runtime режим показывает state с предупреждением о валидности

## Verification (DoD)

### Steps ✅
- ✅ `check_first_run_state.py` → offline вывод (флаг+логи) без импорта ApplicationStateManager
- ✅ `check_first_run_state.py --runtime` → предупреждение + state только при запущенном приложении

### Expected behavior/logs ✅
- ✅ Offline не читает state_manager (нет импорта)
- ✅ Runtime явно предупреждает о валидности state

### Regression checks ✅
- ✅ `test_first_run_integration.sh` — работает
- ✅ `test_golden_first_run_logs.py` — работает

## Выводы

- ✅ **Offline режим:** не импортирует ApplicationStateManager, только флаг и логи
- ✅ **Runtime режим:** lazy import с предупреждением о валидности state
- ✅ **QA-документы:** явно указано, что state валиден только при активном приложении
- ✅ **Валидация:** больше не даёт ложных несоответствий
- ✅ **Источники истины:** чётко разделены (offline: флаг+логи, runtime: state с предупреждением)

## Критерии: "стало проще/стабильнее"

- ✅ Валидация больше не даёт ложных несоответствий
- ✅ Источники истины чётко разделены
- ✅ Offline режим не зависит от ApplicationStateManager
- ✅ Runtime режим явно предупреждает о валидности state
- ✅ QA-документы чётко описывают ограничения каждого режима
