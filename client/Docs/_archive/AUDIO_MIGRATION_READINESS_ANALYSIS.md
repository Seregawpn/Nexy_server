# Анализ готовности к миграции аудиосистемы

**Статус**: Инструмент для предварительного анализа  
**Версия**: 1.0  
**Дата**: 2025-01-XX

---

## Назначение

Скрипт `scripts/analyze_audio_migration_readiness.py` выполняет **полный анализ готовности** к миграции аудиосистемы **БЕЗ фактического внедрения** новой системы.

**Что анализируется**:
- ✅ Все feature flags и их использование в коде
- ✅ Все модули и интеграции, взаимодействующие с аудио
- ✅ Все события EventBus, связанные с аудио
- ✅ Все зависимости и потенциальные конфликты
- ✅ Конфигурация и настройки
- ✅ Порядок инициализации модулей

---

## Использование

### Базовый запуск

```bash
cd client
python scripts/analyze_audio_migration_readiness.py
```

### С опциями

```bash
# Указать корневую директорию проекта
python scripts/analyze_audio_migration_readiness.py --project-root /path/to/project

# Сохранить только JSON отчет (без вывода в консоль)
python scripts/analyze_audio_migration_readiness.py --json-only

# Указать путь для JSON отчета
python scripts/analyze_audio_migration_readiness.py --output custom_report.json
```

---

## Что анализирует скрипт

### 1. Feature Flags

**Источники**:
- `Docs/FEATURE_FLAGS.md` — реестр всех флагов
- Код проекта — поиск упоминаний флагов в файлах

**Анализируется**:
- Имя флага
- Тип (feature_flag / kill_switch)
- Путь в конфигурации
- Места использования в коде
- Значение по умолчанию
- Назначение

**Пример вывода**:
```
📋 FEATURE FLAGS:
  • NEXY_FEATURE_AVFOUNDATION_AUDIO_V2 (feature_flag)
    Config: unified_config.yaml: audio_system.avfoundation_enabled
    Locations: integration/integrations/audio_route_manager_integration.py
```

---

### 2. События EventBus

**Источники**:
- Все файлы в `integration/integrations/`
- Поиск паттернов `event_bus.publish()` и `event_bus.subscribe()`

**Анализируется**:
- Имя события
- Кто публикует событие
- Кто подписывается на событие
- Приоритет подписки (если указан)

**Пример вывода**:
```
📡 СОБЫТИЯ EVENTBUS (аудио-связанные):
  • voice.recording_start
    Публикуют: input_processing
    Подписываются: voice_recognition, mode_management, audio_route_manager
```

---

### 3. Модули

**Источники**:
- Все директории в `modules/`
- Анализ Python файлов в модулях

**Анализируется**:
- Имя модуля
- Связь с аудио (по ключевым словам)
- Зависимости (импорты)
- События, которые модуль публикует
- События, на которые модуль подписывается

**Пример вывода**:
```
🧩 МОДУЛИ (аудио-связанные):
  • voice_recognition
    События: 5 публикует, 3 подписывается
```

---

### 4. Интеграции

**Источники**:
- Все файлы в `integration/integrations/`
- `SimpleModuleCoordinator` для порядка инициализации

**Анализируется**:
- Имя интеграции
- Связь с аудио
- Зависимости
- События (публикация/подписка)
- Порядок инициализации

**Пример вывода**:
```
🔗 ИНТЕГРАЦИИ (аудио-связанные):
  • voice_recognition (порядок: #8)
    События: 6 публикует, 5 подписывается
```

---

### 5. Конфигурация

**Источники**:
- `config/unified_config.yaml`

**Анализируется**:
- Аудио-связанные секции:
  - `voice_recognition`
  - `speech_playback`
  - `input_processing`
  - `permission_restart`
  - `first_run_permissions`
  - `default_audio`
- Секция `integrations` для аудио-модулей

---

### 6. Порядок инициализации

**Источники**:
- `integration/core/simple_module_coordinator.py`
- Поиск `startup_order` в коде

**Анализируется**:
- Последовательность инициализации модулей
- Позиция аудио-связанных модулей
- Зависимости между модулями

**Пример вывода**:
```
🔄 ПОРЯДОК ИНИЦИАЛИЗАЦИИ:
  🎤 8. voice_recognition
  🎤 8.5. audio_route_manager
  🎤 13. speech_playback
```

---

### 7. Потенциальные конфликты

**Проверяется**:
- Дублирование публикации событий (несколько компонентов публикуют одно событие)
- Отсутствующие feature flags в конфигурации
- Циклические зависимости между интеграциями
- Аудио-модули без feature flags

**Пример вывода**:
```
⚠️ ПОТЕНЦИАЛЬНЫЕ КОНФЛИКТЫ:
  ⚠️ Событие 'voice.recording_start' публикуется несколькими компонентами: input_processing, audio_route_manager
  ⚠️ Feature flag 'NEXY_FEATURE_AVFOUNDATION_AUDIO_V2' указан в конфигурации, но путь 'audio_system.avfoundation_enabled' может отсутствовать
```

---

### 8. Расчет готовности

**Формула**:
```
readiness_score = 100.0
- (количество критических конфликтов × 5)
- (количество информационных конфликтов × 1)
- (количество отсутствующих флагов × 10)
- (штрафы за недостаточное количество компонентов)
```

**Уровни готовности**:
- 🟢 **85-100%**: Высокий уровень готовности - можно начинать миграцию
- 🟡 **70-84%**: Средний уровень готовности - рекомендуется доработка
- 🔴 **0-69%**: Критический уровень готовности - требуется доработка

---

## Формат отчета

### Консольный вывод

Скрипт выводит структурированный отчет в консоль с разделами:
1. Общая готовность (процент)
2. Feature Flags
3. События EventBus
4. Модули
5. Интеграции
6. Порядок инициализации
7. Потенциальные конфликты
8. Рекомендации

### JSON отчет

Скрипт сохраняет детальный JSON отчет со всеми данными:

```json
{
  "readiness_score": 85.5,
  "feature_flags": [...],
  "events": [...],
  "modules": [...],
  "integrations": [...],
  "config_entries": {...},
  "initialization_order": [...],
  "potential_conflicts": [...],
  "recommendations": [...]
}
```

---

## Exit codes

- `0` — Готовность ≥85% (можно начинать миграцию)
- `1` — Готовность <70% (критический уровень)
- `2` — Готовность 70-84% (средний уровень)

---

## Пример использования

### Перед началом миграции

```bash
# Запустить анализ
python scripts/analyze_audio_migration_readiness.py

# Проверить готовность
if [ $? -eq 0 ]; then
    echo "✅ Готовность высокая, можно начинать миграцию"
elif [ $? -eq 2 ]; then
    echo "⚠️ Готовность средняя, рекомендуется доработка"
else
    echo "❌ Готовность низкая, требуется доработка"
fi
```

### В CI/CD

```yaml
# .github/workflows/ci.yml
- name: Analyze audio migration readiness
  run: |
    python scripts/analyze_audio_migration_readiness.py --json-only --output readiness_report.json
    # Проверка готовности (exit code 0 = готов, 1-2 = требуется доработка)
```

---

## Интерпретация результатов

### Высокая готовность (85-100%)

**Что означает**:
- Все необходимые feature flags присутствуют
- Конфликтов не обнаружено или они незначительны
- Порядок инициализации корректен
- Все зависимости определены

**Действия**:
- ✅ Можно начинать миграцию
- ✅ Следовать плану миграции из `Docs/AUDIO_SYSTEM_MIGRATION_PLAN.md`

---

### Средняя готовность (70-84%)

**Что означает**:
- Большинство компонентов готовы
- Есть незначительные конфликты или отсутствующие флаги
- Требуется доработка перед миграцией

**Действия**:
- ⚠️ Исправить обнаруженные конфликты
- ⚠️ Добавить отсутствующие feature flags
- ⚠️ Повторить анализ после исправлений

---

### Низкая готовность (0-69%)

**Что означает**:
- Критические проблемы обнаружены
- Много отсутствующих компонентов или флагов
- Требуется значительная доработка

**Действия**:
- ❌ НЕ начинать миграцию
- ❌ Исправить все критические проблемы
- ❌ Добавить все необходимые компоненты
- ❌ Повторить анализ после исправлений

---

## Интеграция с планом миграции

Скрипт анализа дополняет план миграции (`Docs/AUDIO_SYSTEM_MIGRATION_PLAN.md`):

1. **Перед Этапом 1**: Запустить анализ для проверки готовности
2. **После каждого этапа**: Запустить анализ для проверки прогресса
3. **Перед мерджем**: Запустить анализ для финальной проверки

---

## Ограничения

**Что скрипт НЕ делает**:
- ❌ Не проверяет runtime поведение (только статический анализ)
- ❌ Не проверяет тесты (только структуру кода)
- ❌ Не проверяет документацию (только код и конфигурацию)
- ❌ Не проверяет зависимости PyObjC/AVFoundation (только структуру)

**Что скрипт делает**:
- ✅ Анализирует структуру кода
- ✅ Проверяет наличие компонентов
- ✅ Ищет потенциальные конфликты
- ✅ Проверяет конфигурацию
- ✅ Рассчитывает готовность

---

## Заключение

Скрипт `analyze_audio_migration_readiness.py` — это **инструмент для предварительного анализа** готовности к миграции без фактического внедрения новой системы.

**Используйте его**:
- Перед началом миграции
- После каждого этапа миграции
- Перед мерджем изменений
- В CI/CD для автоматической проверки

**Результаты анализа** помогают:
- Определить готовность к миграции
- Выявить потенциальные проблемы заранее
- Планировать этапы миграции
- Избежать конфликтов при внедрении

