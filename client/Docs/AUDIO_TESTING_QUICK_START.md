# Audio Testing — Quick Start Guide

**Дата:** 2025-12-24  
**Цель:** Быстрый запуск тестирования для проверки всех применённых фиксов

---

## 🚀 Быстрый запуск

### 1. Автоматизированные тесты

```bash
# Из корня проекта
cd /Users/sergiyzasorin/Fix_new/client

# Запуск всех production-тестов
python scripts/test_audio_production.py

# Проверка метрик (после длительного тестирования)
python scripts/test_audio_metrics.py
```

**Ожидаемый результат:**
- ✅ Все тесты зелёные
- ✅ Метрики в пределах нормы

---

## 2. Ручные тесты (критические сценарии)

### 2.1 Тест: AirPods / BT (2-3 минуты)

**Шаги:**
1. Подключить AirPods
2. Запустить приложение: `python main.py`
3. Воспроизвести речь (2-3 минуты)
4. Проверить логи:
   ```bash
   grep "underrun" logs.md | wc -l  # Должно быть ≈ 0-единицы
   ```

**Ожидаемый результат:**
- ✅ `callback_underrun_count ≈ 0-единицы`
- ✅ Нет crackles
- ✅ Звук нормальной скорости

---

### 2.2 Тест: Переключение устройств (10 раз)

**Шаги:**
1. Запустить playback
2. Подключить/отключить AirPods 10 раз за 30 секунд
3. Проверить логи:
   ```bash
   grep "Смена устройства" logs.md | wc -l  # Должно быть ≈ 10
   grep "RECREATE" logs.md | wc -l  # Должно быть ≤ 10
   ```

**Ожидаемый результат:**
- ✅ Каждое переключение обработано
- ✅ Нет длительной тишины
- ✅ Звук работает после каждого переключения

---

### 2.3 Тест: Fallback SR

**Шаги:**
1. Установить device с 48kHz (не поддерживает 24kHz)
2. Отправить аудио 24kHz
3. Проверить логи:
   ```bash
   grep "fallback" logs.md  # Должно быть при fallback
   grep "Ресемплинг будет выполняться" logs.md  # Должно быть
   ```
4. Проверить звук: скорость и тон должны быть корректными

**Ожидаемый результат:**
- ✅ Stream создан с device_sr (48kHz)
- ✅ Ресемплинг работает
- ✅ Скорость звука нормальная (не ускорена)

---

### 2.4 Тест: Concurrency storm

**Шаги:**
1. Запустить автоматизированный тест:
   ```bash
   python scripts/test_audio_production.py
   ```
2. Проверить метрики:
   - `_recreating_stream` всегда возвращается в `False`
   - `_stream_recreate_count` разумный (≤ 10)

**Ожидаемый результат:**
- ✅ Нет race conditions
- ✅ Нет "залипло"
- ✅ Система стабильна

---

## 3. Проверка метрик

### 3.1 После длительного тестирования

```bash
# Проверка метрик
python scripts/test_audio_metrics.py
```

**Ожидаемые значения:**
- `callback_underrun_count`: ≈ 0-единицы
- `callback_resample_ms` (p95): < 5-8ms
- `_stream_recreate_count`: ≤ 10 за сессию
- `callback_shape_mismatch_count`: 0

---

## 4. Чек-лист перед релизом

### 4.1 Обязательные проверки

- [ ] **Автоматизированные тесты:** Все зелёные
- [ ] **AirPods / BT:** `underrun_count ≈ 0` за 2-3 минуты
- [ ] **Переключение устройств:** 10 раз без проблем
- [ ] **Fallback SR:** Скорость и тон корректны
- [ ] **Concurrency storm:** Нет race conditions
- [ ] **Метрики:** Все в пределах нормы

---

### 4.2 Release Acceptance Criteria

- [ ] **AirPods / BT:** `callback_underrun_count ≈ 0`, нет тишины при route change
- [ ] **Fallback SR:** Скорость и тон корректны, ресемплинг работает
- [ ] **Concurrency storm:** `_recreating_stream` всегда возвращается в `False`
- [ ] **p95 resample latency:** < 5-8ms

---

## 5. Интерпретация результатов

### 5.1 Если тесты провалены

**Lock-инварианты:**
- Проверить, что все операции с shared state под lock
- Проверить, что I/O операции вне lock

**Generation counter:**
- Проверить, что callback использует closure
- Проверить, что generation обновляется после активации stream

**Нормализация:**
- Проверить, что используется 32768.0 (не 32767.0)
- Проверить симметричную конвертацию

---

### 5.2 Если метрики вне нормы

**High underrun count:**
- Увеличить `blocksize`
- Проверить CPU нагрузку
- Проверить, что ресемплинг не в callback (если p95 высокий)

**High resample p95:**
- Перенести ресемплинг в producer
- Или увеличить `blocksize`

**High recreate count:**
- Проверить, что нет thrash (частое пересоздание)
- Проверить cooldown на device queries

---

## ✅ Итог

**План тестирования готов к выполнению.**

Все критические сценарии покрыты:
- ✅ Автоматизированные тесты
- ✅ Ручные тесты
- ✅ Проверка метрик
- ✅ Release Acceptance Criteria

**Готово к тестированию!**


