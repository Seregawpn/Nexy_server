# Почему появилась проблема с преждевременным срабатыванием callback

## 🔍 История изменений

### Этап 1: Изначальная попытка (не работала)

**Проблема**: `objc.ObjCBlock` не существует в этой версии PyObjC, `objc.callbackFor` не работает с `void (^)(void)`

```python
# Попытка использовать objc.ObjCBlock (не существует)
_AVF_COMPLETION_CALLBACK = objc.ObjCBlock(playback_completion_handler_impl, signature=b"v")
# ❌ Ошибка: module 'objc' has no attribute 'ObjCBlock'

# Попытка использовать objc.callbackFor (не работает с void (^)(void))
callback_decorator = objc.callbackFor(AVAudioPlayerNode.scheduleBuffer_atTime_options_completionHandler_)
_AVF_COMPLETION_CALLBACK = callback_decorator(playback_completion_handler_impl)
# ❌ Ошибка: objc.BadPrototypeError или callback не вызывается
```

**Результат**: Callback не создавался, событие `audio.playback.completed` не публиковалось

---

### Этап 2: Упрощение (текущая реализация) - **ЗДЕСЬ ПОЯВИЛАСЬ ПРОБЛЕМА**

**Решение**: Передать Python функцию напрямую, предполагая, что PyObjC автоматически преобразует её в Objective-C блок

```python
# Попытка 1: Передать функцию напрямую
_AVF_COMPLETION_CALLBACK = playback_completion_handler_impl
# ✅ Успешно сохраняется
# ✅ Передаётся в scheduleBuffer без ошибок
# ❌ НО: Вызывается СЛИШКОМ РАНО (через 0.89s вместо 5.28s)
```

**Проблема**: 
- PyObjC **НЕ преобразует** Python функцию в правильный Objective-C блок
- Вместо этого, PyObjC может вызывать функцию **сразу после `scheduleBuffer`**, а не после реального завершения воспроизведения
- Это происходит потому, что Python функция не правильно обёрнута в Objective-C блок с правильной сигнатурой `void (^)(void)`

---

## 🔧 Почему это происходит

### Техническая причина

1. **PyObjC автоматическое преобразование работает не всегда**:
   - Для простых случаев (например, `int → int`) PyObjC автоматически преобразует типы
   - Для сложных случаев (например, `Python function → Objective-C block`) требуется явное создание блока
   - `void (^)(void)` - это особый случай, который требует правильной обёртки

2. **Python функция передаётся как есть**:
   - Когда мы передаём `playback_completion_handler_impl` напрямую, PyObjC может интерпретировать её как обычный Python callable
   - Objective-C runtime может вызывать её **сразу после планирования буфера**, а не после завершения воспроизведения
   - Это происходит потому, что Python функция не правильно связана с Objective-C блоком

3. **Отсутствие правильной обёртки**:
   - Objective-C блок должен быть создан через правильный API PyObjC
   - Просто передача Python функции не создаёт правильный блок
   - Блок должен быть создан с правильной сигнатурой `void (^)(void)`

---

## ✅ Решение проблемы

### Вариант 1: Использовать только fallback таймер (текущее рабочее решение)

**Плюсы**:
- Работает надёжно
- Не зависит от PyObjC преобразования
- Событие публикуется всегда

**Минусы**:
- Неточность (таймер может сработать с задержкой)
- Дублирование событий (callback + fallback)

### Вариант 2: Правильно создать Objective-C блок

**Требуется**:
- Использовать правильный API PyObjC для создания блока
- Или использовать альтернативный способ отслеживания завершения

**Пример** (теоретический):
```python
# Правильное создание Objective-C блока (требует исследования PyObjC API)
from objc import ObjCBlock
block = ObjCBlock(playback_completion_handler_impl, signature=b"v")
_AVF_COMPLETION_CALLBACK = block
```

### Вариант 3: Использовать NSNotification для отслеживания завершения

**Требуется**:
- Подписка на события AVAudioEngine
- Отслеживание состояния AVAudioPlayerNode
- Альтернативный способ определения завершения воспроизведения

---

## 📝 Вывод

**Проблема появилась из-за упрощения**: 
- Мы убрали попытки правильного создания Objective-C блока
- Начали передавать Python функцию напрямую
- PyObjC не преобразует её правильно, вызывая callback преждевременно

**Текущее состояние**:
- Callback срабатывает, но слишком рано (через 0.89s вместо 5.28s)
- Fallback таймер компенсирует это и публикует событие корректно
- Система работает, но не оптимально

**Рекомендация**: 
- Использовать fallback таймер как основной механизм
- Игнорировать преждевременный callback
- В будущем исследовать правильное создание Objective-C блока
