# Анализ неиспользуемого кода (Dead Code)

## 🔍 Найденные проблемы

### 1. ⚠️ **Неиспользуемая переменная: `_google_listening_thread`**

**Местоположение:** Строка 105
```python
self._google_listening_thread: Optional[threading.Thread] = None
```

**Проблема:**
- Объявлена в `__init__`, но **никогда не используется**
- Раньше использовалась для `recognizer.listen()` в отдельном потоке
- После перехода на `listen_in_background()` больше не нужна

**Использование:** ❌ Не используется нигде

**Рекомендация:** ✅ Удалить

---

### 2. ⚠️ **Неиспользуемая переменная: `_google_listening_event`**

**Местоположение:** Строки 105, 770, 831, 843, 1049, 1062

**Проблема:**
- Объявлена и устанавливается, но **никогда не используется активно**
- Раньше использовалась для синхронизации с потоком `recognizer.listen()`
- После перехода на `listen_in_background()` больше не нужна

**Использование:** ❌ Только устанавливается/очищается, но не используется

**Рекомендация:** ✅ Удалить

---

### 3. ⚠️ **Неиспользуемый метод: `_on_mic_data_ready`**

**Местоположение:** Строки 1298-1356

**Проблема:**
- Метод существует и реализован
- **Подписка на событие закомментирована** (строка 318: "✅ УДАЛЕНО: Подписка на voice.mic_data_ready")
- Метод никогда не вызывается, так как нет подписки
- **НО:** `_publish_mic_data_ready` всё ещё публикует событие `voice.mic_data_ready` (строка 1134), но никто его не обрабатывает

**Использование:** ❌ Не вызывается (нет подписки на событие)

**Рекомендация:** ✅ Удалить (событие публикуется, но не обрабатывается - это мёртвый код)

---

### 4. ⚠️ **Неиспользуемый метод: `_publish_mic_data_ready`**

**Местоположение:** Строки 1517-1561

**Проблема:**
- Метод существует и реализован
- Вызывается из `_on_recording_stop` (строка 1134) - публикует событие `voice.mic_data_ready`
- Но событие **никто не обрабатывает** (подписка на `_on_mic_data_ready` удалена)
- Это означает, что событие публикуется в пустоту

**Использование:** ❌ Вызывается, но событие не обрабатывается (мёртвый код)

**Рекомендация:** ✅ Удалить (событие публикуется, но не обрабатывается)

---

### 5. ⚠️ **Неиспользуемая переменная: `_processed_mic_data_sessions`**

**Местоположение:** Используется только в `_on_mic_data_ready`

**Проблема:**
- Используется только в `_on_mic_data_ready` (строки 1310, 1315, 1337, 1340, 1351)
- Если `_on_mic_data_ready` не вызывается, то переменная не нужна

**Использование:** ❌ Не используется (так как `_on_mic_data_ready` не вызывается)

**Рекомендация:** ✅ Удалить (вместе с `_on_mic_data_ready`)

---

### 6. ⚠️ **Legacy код: `_audio_buffer` и связанные переменные**

**Местоположение:** Строки 95-96, 108-109, используется в `_on_recording_stop`

**Проблема:**
- Используется в legacy fallback пути (строки 1136-1172)
- Комментарий говорит: "✅ LEGACY FALLBACK: Если result.data пустой, используем буфер"
- Это fallback на случай, если AVF не вернул данные

**Использование:** ✅ Используется как fallback (если `result.data` пустой)

**Рекомендация:** ⚠️ Оставить (используется как fallback), но можно упростить логику

---

### 7. ⚠️ **Метод: `_recognize_avf_audio` (частично используется)**

**Местоположение:** Вызывается в двух местах

**Проблема:**
- Вызывается из legacy fallback пути (строка 1173) - ✅ **ИСПОЛЬЗУЕТСЯ**
- Вызывается из `_on_mic_data_ready` (строка 1329) - ❌ **НЕ ИСПОЛЬЗУЕТСЯ** (метод не вызывается)

**Использование:** ⚠️ Частично используется (только legacy fallback)

**Рекомендация:** ⚠️ Оставить (используется в legacy fallback), но можно упростить, удалив вызов из `_on_mic_data_ready`

---

## 📊 Итоговая статистика

### Критичные (точно не используются):
1. ✅ `_google_listening_thread` - не используется
2. ✅ `_google_listening_event` - не используется активно
3. ✅ `_processed_mic_data_sessions` - не используется (зависит от `_on_mic_data_ready`)
4. ✅ `_recognize_avf_audio` - не используется (зависит от `_on_mic_data_ready`)

### Требуют проверки:
5. ⚠️ `_on_mic_data_ready` - не вызывается (нет подписки)
6. ⚠️ `_publish_mic_data_ready` - вызывается, но событие не обрабатывается
7. ⚠️ `_audio_buffer` и связанные - используется в legacy пути

---

## ✅ Рекомендации по очистке

### Фаза 1: Безопасное удаление (точно не используется)

```python
# Удалить из __init__:
self._google_listening_thread: Optional[threading.Thread] = None  # ❌ Удалить
self._google_listening_event: Optional[threading.Event] = None    # ❌ Удалить

# Удалить все установки/очистки:
self._google_listening_event = threading.Event()  # ❌ Удалить (строка 770)
self._google_listening_event = None               # ❌ Удалить (строки 831, 843, 1049, 1062)
```

### Фаза 2: Удаление legacy пути (если не используется)

```python
# Удалить методы:
async def _on_mic_data_ready(self, event: Dict[str, Any]):  # ❌ Удалить (строки 1298-1356)
async def _publish_mic_data_ready(self, result, session_id: str):  # ❌ Удалить (строки 1517-1561)
async def _recognize_avf_audio(...):  # ❌ Удалить (если не используется)

# Удалить переменные:
self._processed_mic_data_sessions = set()  # ❌ Удалить (если не используется)
```

### Фаза 3: Проверка legacy пути `_audio_buffer`

**Вопрос:** Используется ли legacy путь с `_audio_buffer`?

**Проверка:**
- Если `_use_avf=True` и используется новая логика Google записи, то legacy путь не нужен
- Если legacy путь не используется, удалить:
  - `self._audio_buffer: List[bytes] = []`
  - `self._audio_buffer_bytes: int = 0`
  - `self._audio_buffer_sample_rate: Optional[int] = None`
  - `self._audio_buffer_channels: Optional[int] = None`
  - Метод `_clear_audio_buffer()` (или оставить, если используется)
  - Код в `_on_recording_stop` (строки 1136-1172)

---

## 📝 Чек-лист очистки

- [ ] **Фаза 1:** Удалить `_google_listening_thread` и `_google_listening_event`
- [ ] **Фаза 2:** Проверить, используется ли `_on_mic_data_ready` (нет подписки)
- [ ] **Фаза 2:** Удалить `_on_mic_data_ready`, `_publish_mic_data_ready`, `_recognize_avf_audio`
- [ ] **Фаза 2:** Удалить `_processed_mic_data_sessions`
- [ ] **Фаза 3:** Проверить, используется ли legacy путь с `_audio_buffer`
- [ ] **Фаза 3:** Если не используется - удалить `_audio_buffer` и связанные переменные

---

## ⚠️ Внимание

Перед удалением кода нужно убедиться:
1. ✅ Legacy путь действительно не используется
2. ✅ Нет других мест, где вызываются эти методы
3. ✅ Нет тестов, которые используют этот код
4. ✅ Удаление не сломает обратную совместимость






