# 🔍 Стратегический анализ: Централизованное управление состоянием микрофона

## 📊 Текущая ситуация: Проблемы и корневые причины

### 🐛 Проблема 1: Разбросанное состояние микрофона (КРИТИЧЕСКАЯ)

**Текущее состояние:**
- `_mic_active` в `InputProcessingIntegration` (управляется через события `voice.mic_opened/closed`)
- `is_listening` в `SpeechRecognizer` (управляется через `start_listening/stop_listening`)
- `_recording_active` в `VoiceRecognitionIntegration` (управляется через события `voice.recording_start/stop`)
- `_stream_state` в `SpeechRecognizer` (внутреннее состояние аудио потока)

**Проблемы:**
1. **Нет единого источника истины** - состояние микрофона разбросано по 3+ модулям
2. **Race conditions** - разные модули могут иметь разные представления о состоянии
3. **Десинхронизация** - `_mic_active=True`, но `is_listening=False` (залипание)
4. **Сложность отладки** - невозможно определить реальное состояние микрофона

**Пример проблемы:**
```python
# InputProcessingIntegration
self._mic_active = True  # Установлено через voice.mic_opened

# SpeechRecognizer
self.is_listening = False  # Поток не запущен (ошибка при старте)

# Результат: залипание - _mic_active=True, но микрофон не работает
```

---

### 🐛 Проблема 2: Отсутствие гарантии синхронизации (КРИТИЧЕСКАЯ)

**Текущая логика:**
1. `InputProcessingIntegration` публикует `voice.recording_start`
2. `VoiceRecognitionIntegration` получает событие и открывает микрофон
3. `VoiceRecognitionIntegration` публикует `voice.mic_opened`
4. `InputProcessingIntegration` получает событие и устанавливает `_mic_active=True`

**Проблемы:**
1. **Асинхронность** - события могут прийти в неправильном порядке
2. **Потеря событий** - если `voice.mic_opened` не придет, `_mic_active` останется `False`
3. **Нет гарантии** - если микрофон не открылся, `_mic_active` может остаться `True`
4. **Нет механизма восстановления** - при ошибке состояние остается несинхронизированным

**Пример проблемы:**
```python
# LONG_PRESS → voice.recording_start
# VoiceRecognitionIntegration пытается открыть микрофон → ошибка
# voice.mic_opened НЕ публикуется
# _mic_active остается False, но микрофон может быть частично открыт
```

---

### 🐛 Проблема 3: Игнорирование событий с session_id=None (КРИТИЧЕСКАЯ)

**Текущая логика:**
```python
# VoiceRecognitionIntegration._on_recording_stop
if session_id is None or active_session_id != session_id:
    logger.debug("VOICE: recording_stop ignored (session mismatch)")
    return
```

**Проблемы:**
1. **Невозможность принудительной остановки** - если `session_id=None`, микрофон не останавливается
2. **Залипание при отмене** - при SHORT_PRESS микрофон остается активным
3. **Нет механизма восстановления** - невозможно исправить несинхронизированное состояние

**Пример проблемы:**
```python
# SHORT_PRESS отменяет pending session → session_id=None
# Публикуется voice.recording_stop с session_id=None
# VoiceRecognitionIntegration игнорирует событие
# Микрофон остается активным → залипание
```

---

### 🐛 Проблема 4: Отсутствие централизованного управления (ВЫСОКАЯ)

**Текущая архитектура:**
- Каждый модуль управляет своим состоянием независимо
- Нет единого места для управления микрофоном
- Нет механизма принудительной остановки
- Нет гарантии согласованности состояния

**Проблемы:**
1. **Сложность поддержки** - изменения в одном модуле могут сломать другой
2. **Дублирование логики** - каждый модуль реализует свою логику управления
3. **Нет единой точки входа** - невозможно централизованно управлять микрофоном
4. **Сложность тестирования** - невозможно изолированно протестировать управление микрофоном

---

## 🎯 Стратегическое решение: Централизованный MicrophoneStateManager

### 📐 Архитектурный подход

**Принципы:**
1. **Единый источник истины** - `MicrophoneStateManager` как централизованный менеджер состояния
2. **Интеграция с ApplicationStateManager** - использование существующей архитектуры
3. **Гарантия синхронизации** - все модули используют одно и то же состояние
4. **Механизм принудительной остановки** - возможность восстановления при ошибках
5. **Защита от race conditions** - использование locks и атомарных операций

---

### 🏗️ Архитектура MicrophoneStateManager

```
┌─────────────────────────────────────────────────────────────┐
│           MicrophoneStateManager                            │
│  (Единый источник истины для состояния микрофона)          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Состояние:                                                 │
│  - microphone_state: MicrophoneState (IDLE/OPENING/ACTIVE/ │
│                      CLOSING/ERROR)                         │
│  - current_session_id: Optional[str]                         │
│  - last_state_change: float (timestamp)                     │
│  - error_count: int                                         │
│                                                             │
│  Методы:                                                    │
│  - request_open(session_id) → bool                          │
│  - request_close(session_id) → bool                         │
│  - force_close(reason) → bool                               │
│  - get_state() → MicrophoneState                            │
│  - is_active() → bool                                       │
│                                                             │
│  События (публикует):                                       │
│  - microphone.state_changed                                 │
│  - microphone.opened                                         │
│  - microphone.closed                                         │
│  - microphone.error                                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
         │                    │                    │
         ▼                    ▼                    ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ InputProc    │    │ VoiceRecog   │    │ SpeechRecog  │
│ Integration  │    │ Integration  │    │ nizer       │
└──────────────┘    └──────────────┘    └──────────────┘
```

---

### 📋 Детальная спецификация

#### 1. MicrophoneState Enum

```python
class MicrophoneState(Enum):
    """Состояние микрофона (централизованное)"""
    IDLE = "idle"              # Микрофон закрыт, нет активных операций
    OPENING = "opening"        # Микрофон открывается (асинхронная операция)
    ACTIVE = "active"          # Микрофон активен, запись идет
    CLOSING = "closing"        # Микрофон закрывается (асинхронная операция)
    ERROR = "error"            # Ошибка, требуется восстановление
```

#### 2. MicrophoneStateManager API

```python
class MicrophoneStateManager:
    """Централизованный менеджер состояния микрофона"""
    
    def __init__(self, event_bus: EventBus, state_manager: ApplicationStateManager):
        self._event_bus = event_bus
        self._state_manager = state_manager
        self._state = MicrophoneState.IDLE
        self._current_session_id: Optional[str] = None
        self._state_lock = asyncio.Lock()
        self._last_state_change = 0.0
        self._error_count = 0
        
    async def request_open(self, session_id: str, timeout: float = 5.0) -> bool:
        """
        Запросить открытие микрофона.
        
        Args:
            session_id: ID сессии для которой открывается микрофон
            timeout: Таймаут ожидания открытия (секунды)
            
        Returns:
            True если микрофон успешно открыт, False в противном случае
        """
        async with self._state_lock:
            if self._state == MicrophoneState.ACTIVE:
                if self._current_session_id == session_id:
                    return True  # Уже открыт для этой сессии
                else:
                    # Другая сессия - сначала закрываем
                    await self._force_close_internal("session_mismatch")
            
            if self._state not in [MicrophoneState.IDLE, MicrophoneState.ERROR]:
                logger.warning(f"⚠️ [MIC_STATE] Невозможно открыть микрофон в состоянии {self._state}")
                return False
            
            # Переходим в OPENING
            await self._set_state(MicrophoneState.OPENING, session_id)
            
            # Публикуем событие запроса открытия
            await self._event_bus.publish("microphone.open_requested", {
                "session_id": session_id,
                "timeout": timeout
            })
            
            # Ждем подтверждения открытия (через microphone.opened)
            # TODO: Реализовать ожидание с таймаутом
            
        return True
    
    async def request_close(self, session_id: Optional[str] = None, force: bool = False) -> bool:
        """
        Запросить закрытие микрофона.
        
        Args:
            session_id: ID сессии (None для принудительного закрытия)
            force: Принудительное закрытие (игнорирует session_id)
            
        Returns:
            True если микрофон успешно закрыт, False в противном случае
        """
        async with self._state_lock:
            if self._state == MicrophoneState.IDLE:
                return True  # Уже закрыт
            
            if not force and session_id is not None:
                if self._current_session_id != session_id:
                    logger.warning(f"⚠️ [MIC_STATE] session_id mismatch: {self._current_session_id} != {session_id}")
                    return False
            
            # Переходим в CLOSING
            await self._set_state(MicrophoneState.CLOSING, None)
            
            # Публикуем событие запроса закрытия
            await self._event_bus.publish("microphone.close_requested", {
                "session_id": session_id,
                "force": force
            })
            
        return True
    
    async def force_close(self, reason: str = "unknown") -> bool:
        """
        Принудительно закрыть микрофон (восстановление при ошибках).
        
        Args:
            reason: Причина принудительного закрытия
            
        Returns:
            True если микрофон успешно закрыт
        """
        async with self._state_lock:
            logger.warning(f"⚠️ [MIC_STATE] Принудительное закрытие микрофона: {reason}")
            await self._force_close_internal(reason)
        return True
    
    async def _set_state(self, new_state: MicrophoneState, session_id: Optional[str] = None):
        """Внутренний метод для изменения состояния (с защитой от race conditions)"""
        old_state = self._state
        if old_state != new_state:
            self._state = new_state
            self._current_session_id = session_id
            self._last_state_change = time.monotonic()
            
            # Публикуем событие изменения состояния
            await self._event_bus.publish("microphone.state_changed", {
                "old_state": old_state.value,
                "new_state": new_state.value,
                "session_id": session_id,
                "timestamp": self._last_state_change
            })
            
            logger.info(f"🔄 [MIC_STATE] {old_state.value} → {new_state.value} (session={session_id})")
    
    def get_state(self) -> MicrophoneState:
        """Получить текущее состояние микрофона"""
        return self._state
    
    def is_active(self) -> bool:
        """Проверить, активен ли микрофон"""
        return self._state == MicrophoneState.ACTIVE
    
    def get_current_session_id(self) -> Optional[str]:
        """Получить текущий session_id микрофона"""
        return self._current_session_id
```

---

### 🔄 Интеграция с существующими модулями

#### 1. InputProcessingIntegration

**Изменения:**
- Удалить `_mic_active` - использовать `MicrophoneStateManager.is_active()`
- Использовать `MicrophoneStateManager.request_open/close()` вместо прямых событий
- Подписаться на `microphone.state_changed` для синхронизации состояния

**До:**
```python
self._mic_active = False
# ...
await self.event_bus.publish("voice.recording_start", {...})
# ...
if self._mic_active:
    # ...
```

**После:**
```python
# Инициализация
self._mic_manager = MicrophoneStateManager(self.event_bus, self.state_manager)

# Открытие микрофона
if await self._mic_manager.request_open(session_id):
    # Микрофон открыт
    pass

# Проверка состояния
if self._mic_manager.is_active():
    # Микрофон активен
    pass

# Закрытие микрофона
await self._mic_manager.request_close(session_id)
```

#### 2. VoiceRecognitionIntegration

**Изменения:**
- Подписаться на `microphone.open_requested/close_requested`
- Публиковать `microphone.opened/closed` после успешного открытия/закрытия
- Использовать `MicrophoneStateManager` для синхронизации состояния

**До:**
```python
async def _on_recording_start(self, event):
    # ...
    await self._recognizer.start_listening()
    await self.event_bus.publish("voice.mic_opened", {...})
```

**После:**
```python
async def _on_microphone_open_requested(self, event):
    session_id = event.get("data", {}).get("session_id")
    try:
        await self._recognizer.start_listening()
        # Уведомляем MicrophoneStateManager об успешном открытии
        await self.event_bus.publish("microphone.opened", {
            "session_id": session_id
        })
    except Exception as e:
        # Уведомляем об ошибке
        await self.event_bus.publish("microphone.error", {
            "session_id": session_id,
            "error": str(e)
        })
```

#### 3. SpeechRecognizer

**Изменения:**
- Минимальные - только публикация событий `microphone.opened/closed` при успешном открытии/закрытии
- Использование существующего `is_listening` как внутреннего состояния

---

### 🛡️ Защита от race conditions

#### 1. Атомарные операции

```python
async def request_open(self, session_id: str) -> bool:
    async with self._state_lock:  # Защита от concurrent запросов
        if self._state == MicrophoneState.ACTIVE:
            # Проверка и обработка в одной атомарной операции
            if self._current_session_id == session_id:
                return True
            else:
                await self._force_close_internal("session_mismatch")
        # ...
```

#### 2. Гарантия синхронизации

```python
async def _on_microphone_opened(self, event):
    """Обработчик успешного открытия микрофона"""
    async with self._state_lock:
        if self._state == MicrophoneState.OPENING:
            await self._set_state(MicrophoneState.ACTIVE, session_id)
        else:
            logger.warning(f"⚠️ [MIC_STATE] Неожиданное состояние при opened: {self._state}")
```

#### 3. Таймауты и восстановление

```python
async def request_open(self, session_id: str, timeout: float = 5.0) -> bool:
    # ...
    # Ждем подтверждения открытия с таймаутом
    opened_event = await asyncio.wait_for(
        self._wait_for_microphone_opened(session_id),
        timeout=timeout
    )
    
    if opened_event:
        return True
    else:
        # Таймаут - принудительно закрываем
        await self._force_close_internal("open_timeout")
        return False
```

---

### 📊 Преимущества централизованного подхода

1. **Единый источник истины** - все модули используют одно и то же состояние
2. **Гарантия синхронизации** - состояние всегда согласовано между модулями
3. **Защита от race conditions** - атомарные операции с locks
4. **Механизм восстановления** - принудительное закрытие при ошибках
5. **Упрощение отладки** - одно место для логирования состояния
6. **Упрощение тестирования** - можно изолированно протестировать управление состоянием
7. **Расширяемость** - легко добавить новые состояния или операции

---

### 🚀 План внедрения (поэтапный)

#### Этап 1: Создание MicrophoneStateManager (1-2 дня)
- [ ] Создать `modules/microphone_state/core/microphone_state_manager.py`
- [ ] Реализовать базовую логику управления состоянием
- [ ] Добавить интеграцию с EventBus
- [ ] Написать unit-тесты

#### Этап 2: Интеграция с InputProcessingIntegration (1 день)
- [ ] Заменить `_mic_active` на `MicrophoneStateManager`
- [ ] Обновить логику открытия/закрытия микрофона
- [ ] Подписаться на `microphone.state_changed`
- [ ] Протестировать с существующими сценариями

#### Этап 3: Интеграция с VoiceRecognitionIntegration (1 день)
- [ ] Подписаться на `microphone.open_requested/close_requested`
- [ ] Публиковать `microphone.opened/closed` после операций
- [ ] Обновить обработку ошибок
- [ ] Протестировать с существующими сценариями

#### Этап 4: Интеграция с SpeechRecognizer (0.5 дня)
- [ ] Публиковать `microphone.opened/closed` при успешном открытии/закрытии
- [ ] Обновить обработку ошибок
- [ ] Протестировать с существующими сценариями

#### Этап 5: Удаление устаревшего кода (0.5 дня)
- [ ] Удалить `_mic_active` из `InputProcessingIntegration`
- [ ] Удалить дублирующую логику управления состоянием
- [ ] Обновить документацию

#### Этап 6: Тестирование и валидация (1 день)
- [ ] Интеграционное тестирование всех сценариев
- [ ] Тестирование edge cases (SHORT_PRESS, ошибки, таймауты)
- [ ] Проверка отсутствия залипания
- [ ] Проверка производительности

**Общее время: ~5-6 дней**

---

### ⚠️ Риски и митигации

#### Риск 1: Нарушение существующей функциональности
**Митигация:**
- Поэтапное внедрение с тестированием на каждом этапе
- Сохранение обратной совместимости через события
- Откат к предыдущей версии при критических ошибках

#### Риск 2: Увеличение сложности
**Митигация:**
- Простой и понятный API
- Подробная документация
- Примеры использования

#### Риск 3: Производительность
**Митигация:**
- Минимальные накладные расходы (только проверки состояния)
- Асинхронные операции не блокируют event loop
- Оптимизация критических путей

---

### 📝 Выводы и рекомендации

1. **Централизация критична** - текущая разбросанность состояния является корневой причиной проблем
2. **Единый источник истины** - `MicrophoneStateManager` решит проблемы синхронизации
3. **Поэтапное внедрение** - минимизирует риски и позволит тестировать на каждом этапе
4. **Сохранение архитектуры** - интеграция с существующими компонентами (EventBus, ApplicationStateManager)
5. **Защита от ошибок** - механизм принудительного закрытия предотвратит залипание

---

## 🎯 Следующие шаги

1. **Создать MicrophoneStateManager** - базовая реализация с тестами
2. **Интегрировать с InputProcessingIntegration** - заменить `_mic_active`
3. **Интегрировать с VoiceRecognitionIntegration** - подписка на события
4. **Протестировать** - все сценарии использования
5. **Документировать** - обновить архитектурную документацию

---

## 🚀 Альтернативное решение: Быстрое исправление (без полной переделки)

Если полная переделка занимает слишком много времени, можно реализовать **упрощенное решение**, которое решит основные проблемы:

### Упрощенный подход: Расширение ApplicationStateManager

**Идея:** Добавить управление состоянием микрофона в существующий `ApplicationStateManager` вместо создания нового модуля.

**Преимущества:**
- Минимальные изменения в архитектуре
- Использование существующей инфраструктуры
- Быстрое внедрение (1-2 дня)
- Решает основные проблемы залипания

**Реализация:**

```python
# В ApplicationStateManager добавить:
class ApplicationStateManager:
    def __init__(self):
        # ... существующий код ...
        # ✅ НОВОЕ: Состояние микрофона
        self._microphone_state = "idle"  # idle/opening/active/closing/error
        self._microphone_session_id: Optional[str] = None
        self._microphone_lock = threading.Lock()
    
    def set_microphone_state(self, state: str, session_id: Optional[str] = None):
        """Установить состояние микрофона (thread-safe)"""
        with self._microphone_lock:
            old_state = self._microphone_state
            self._microphone_state = state
            self._microphone_session_id = session_id
            logger.info(f"🔄 [MIC_STATE] {old_state} → {state} (session={session_id})")
    
    def get_microphone_state(self) -> tuple[str, Optional[str]]:
        """Получить состояние микрофона (thread-safe)"""
        with self._microphone_lock:
            return (self._microphone_state, self._microphone_session_id)
    
    def is_microphone_active(self) -> bool:
        """Проверить, активен ли микрофон"""
        with self._microphone_lock:
            return self._microphone_state == "active"
```

**Изменения в модулях:**

1. **InputProcessingIntegration:**
   - Заменить `self._mic_active` на `self.state_manager.is_microphone_active()`
   - Использовать `state_manager.set_microphone_state()` для изменения состояния

2. **VoiceRecognitionIntegration:**
   - При открытии микрофона: `state_manager.set_microphone_state("active", session_id)`
   - При закрытии микрофона: `state_manager.set_microphone_state("idle", None)`
   - Обрабатывать `voice.recording_stop` с `session_id=None` для принудительного закрытия

3. **SpeechRecognizer:**
   - Публиковать `microphone.state_changed` при изменении состояния потока

**Время реализации: 1-2 дня**

**Решает:**
- ✅ Единый источник истины для состояния микрофона
- ✅ Защита от race conditions через lock
- ✅ Возможность принудительного закрытия
- ✅ Синхронизация состояния между модулями

**Не решает (требует полной переделки):**
- ❌ Централизованное управление операциями открытия/закрытия
- ❌ Таймауты и автоматическое восстановление
- ❌ Детальное логирование всех операций

---

## 📋 Рекомендация: Двухэтапный подход

### Этап 1: Быстрое исправление (1-2 дня) - РЕКОМЕНДУЕТСЯ СНАЧАЛА

**Цель:** Решить проблему залипания микрофона с минимальными изменениями

**Действия:**
1. Расширить `ApplicationStateManager` методами управления состоянием микрофона
2. Обновить `VoiceRecognitionIntegration` для обработки `voice.recording_stop` с `session_id=None`
3. Обновить `InputProcessingIntegration` для использования `state_manager` вместо `_mic_active`
4. Добавить принудительное закрытие микрофона при SHORT_PRESS

**Результат:**
- ✅ Решает проблему залипания микрофона
- ✅ Минимальные изменения в архитектуре
- ✅ Быстрое внедрение и тестирование

### Этап 2: Полная централизация (5-6 дней) - ДОЛГОСРОЧНОЕ РЕШЕНИЕ

**Цель:** Создать полноценный `MicrophoneStateManager` для долгосрочной стабильности

**Действия:**
1. Создать `MicrophoneStateManager` как отдельный модуль
2. Интегрировать с существующими модулями
3. Добавить таймауты и автоматическое восстановление
4. Удалить устаревший код

**Результат:**
- ✅ Полная централизация управления микрофоном
- ✅ Защита от всех типов ошибок
- ✅ Расширяемость для будущих улучшений

---

## 🎯 Итоговая рекомендация

**Для немедленного решения проблемы:**
1. Реализовать **Этап 1 (быстрое исправление)** - решает проблему залипания за 1-2 дня
2. Протестировать все сценарии использования
3. При необходимости перейти к **Этапу 2 (полная централизация)** для долгосрочной стабильности

**Преимущества двухэтапного подхода:**
- Быстрое решение проблемы (1-2 дня)
- Минимальный риск (маленькие изменения)
- Возможность отката при проблемах
- Долгосрочное решение можно реализовать позже без спешки

