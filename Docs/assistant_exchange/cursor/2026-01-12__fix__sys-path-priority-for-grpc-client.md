# Fix: sys.path Priority для GrpcClient - гарантия полной версии

**Дата:** 2026-01-12  
**Тип:** fix  
**Ассистент:** cursor  
**Статус:** ✅ Завершено

---

## Executive Summary

Исправлен порядок путей в `sys.path` в `main.py`, чтобы `client/modules` загружался раньше `modules`, гарантируя использование полной версии `GrpcClient`. Добавлена диагностика для выявления проблем с кэшем и устаревшими версиями.

---

## Проблема

**Симптомы:**
- В runtime всё ещё возникает `TypeError: missing 1 required positional argument: 'session_id'`
- Python может загружать неполную версию `GrpcClient` из `modules/grpc_client` вместо полной из `client/modules/grpc_client`

**Root Cause:**
- В `main.py` путь `client/modules` не был добавлен в `sys.path`
- Python ищет модули по порядку в `sys.path`
- Если `modules/grpc_client` найден раньше `client/modules/grpc_client`, используется неполная версия
- Python кэш может содержать устаревшие версии модулей

---

## Решение

### 1. Исправлен порядок путей в sys.path

**Файл:** `main.py` (строки 16-20)

**Изменения:**
- Добавлен путь `client/modules` в `sys.path` перед `modules`
- Это гарантирует, что полная версия `GrpcClient` загружается раньше неполной

**Код:**
```python
# Добавляем пути к модулям (централизованно)
CLIENT_ROOT = Path(__file__).parent
sys.path.insert(0, str(CLIENT_ROOT))
# КРИТИЧНО: client/modules должен быть ПЕРЕД modules, чтобы полная версия GrpcClient загружалась раньше неполной
if (CLIENT_ROOT / "client" / "modules").exists():
    sys.path.insert(0, str(CLIENT_ROOT / "client" / "modules"))
sys.path.insert(0, str(CLIENT_ROOT / "modules"))
sys.path.insert(0, str(CLIENT_ROOT / "integration"))
```

**Порядок путей (от первого к последнему):**
1. `CLIENT_ROOT / "integration"` (самый приоритетный)
2. `CLIENT_ROOT / "modules"` (неполная версия, но ниже приоритет)
3. `CLIENT_ROOT / "client" / "modules"` (полная версия, высокий приоритет)
4. `CLIENT_ROOT` (корень проекта)

### 2. Добавлена диагностика при инициализации

**Файл:** `integration/integrations/grpc_client_integration.py` (строки 162-190)

**Изменения:**
- Логирование модуля и файла загруженного класса
- Логирование сигнатуры `stream_audio` с параметрами
- Явная проверка наличия `session_id` в сигнатуре

**Ожидаемые логи:**
```
🔍 [DIAG] GrpcClient загружен из модуля: client.modules.grpc_client.core.grpc_client
🔍 [DIAG] GrpcClient файл: /Users/sergiyzasorin/Fix_new/client/modules/grpc_client/core/grpc_client.py
🔍 [DIAG] stream_audio сигнатура: ['prompt', 'screenshot_base64', 'screen_info', 'hardware_id', 'session_id']
🔍 [DIAG] stream_audio имеет session_id: True
✅ GrpcClient инициализирован корректно: stream_audio имеет параметр session_id
```

### 3. Добавлена диагностика перед вызовом

**Файл:** `integration/integrations/grpc_client_integration.py` (строки 498-507)

**Изменения:**
- Логирование параметров перед вызовом `stream_audio`
- Логирование класса и модуля перед вызовом
- Подтверждение, что `session_id` передается

**Ожидаемые логи:**
```
🔍 [DIAG] Вызов stream_audio: session_id=<uuid>, hardware_id=<id>..., prompt_len=<int>
🔍 [DIAG] GrpcClient класс: GrpcClient, модуль: client.modules.grpc_client.core.grpc_client
🔍 [DIAG] GrpcClient файл: /Users/sergiyzasorin/Fix_new/client/modules/grpc_client/core/grpc_client.py
```

### 4. Создан скрипт для очистки кэша

**Файл:** `scripts/clear_python_cache.sh`

**Назначение:**
- Удаление всех `__pycache__` директорий
- Удаление `.pyc` и `.pyo` файлов
- Специфичная очистка кэша `grpc_client` модулей

**Использование:**
```bash
./scripts/clear_python_cache.sh
```

---

## Инструкция по применению

### Шаг 1: Остановить текущий процесс

**ВАЖНО:** Полностью остановить приложение, чтобы освободить память от старого кода.

```bash
# Найти процесс
ps aux | grep "python.*main.py"

# Остановить процесс (если запущен)
pkill -f "python.*main.py"
```

### Шаг 2: Очистить Python кэш

```bash
cd /Users/sergiyzasorin/Fix_new
./scripts/clear_python_cache.sh
```

**Или вручную:**
```bash
# Удалить все __pycache__
find . -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null || true

# Удалить .pyc файлы
find . -type f -name "*.pyc" -delete 2>/dev/null || true

# Специфичная очистка grpc_client
rm -rf client/modules/grpc_client/core/__pycache__ 2>/dev/null || true
rm -rf modules/grpc_client/core/__pycache__ 2>/dev/null || true
rm -rf integration/integrations/__pycache__ 2>/dev/null || true
```

### Шаг 3: Перезапустить приложение

```bash
cd /Users/sergiyzasorin/Fix_new
python main.py
```

### Шаг 4: Проверить диагностические логи

**При инициализации ожидаются логи:**
```
✅ Используется GrpcClient из client/modules/grpc_client (полная версия с зависимостями)
🔍 [DIAG] GrpcClient загружен из модуля: client.modules.grpc_client.core.grpc_client
🔍 [DIAG] GrpcClient файл: /Users/sergiyzasorin/Fix_new/client/modules/grpc_client/core/grpc_client.py
🔍 [DIAG] stream_audio сигнатура: ['prompt', 'screenshot_base64', 'screen_info', 'hardware_id', 'session_id']
🔍 [DIAG] stream_audio имеет session_id: True
✅ GrpcClient инициализирован корректно: stream_audio имеет параметр session_id
```

**При вызове stream_audio ожидаются логи:**
```
🔍 [DIAG] Вызов stream_audio: session_id=<uuid>, hardware_id=<id>..., prompt_len=<int>
🔍 [DIAG] GrpcClient класс: GrpcClient, модуль: client.modules.grpc_client.core.grpc_client
TRACE phase=grpc.start ts=<timestamp> session=<uuid> ...
```

### Шаг 5: Выполнить голосовой запрос

- Нажать PTT (push-to-talk)
- Произнести команду
- Проверить отсутствие `TypeError`

---

## Диагностика проблем

### Если логи показывают неправильный модуль

**Проблема:** `🔍 [DIAG] GrpcClient загружен из модуля: modules.grpc_client.core.grpc_client`

**Решение:**
1. Проверить, что `client/modules` добавлен в `sys.path` в `main.py`
2. Убедиться, что путь добавлен ПЕРЕД `modules`
3. Очистить кэш и перезапустить

### Если сигнатура не содержит session_id

**Проблема:** `🔍 [DIAG] stream_audio имеет session_id: False`

**Решение:**
1. Проверить, что `client/modules/grpc_client/core/grpc_client.py` содержит `session_id: str` в сигнатуре
2. Очистить кэш и перезапустить
3. Проверить, что нет конфликта с другими версиями класса

### Если TypeError всё ещё возникает

**Проблема:** `TypeError: missing 1 required positional argument: 'session_id'`

**Диагностика:**
1. Проверить логи `🔍 [DIAG] Вызов stream_audio: session_id=...` - должен быть валидный UUID
2. Проверить логи `🔍 [DIAG] GrpcClient класс: ...` - должен быть `GrpcClient` из `client.modules.grpc_client.core.grpc_client`
3. Если `session_id=None` или пустой - проблема в `final_session_id`
4. Если класс из другого модуля - проблема с импортом или порядком `sys.path`

---

## Критерии успеха

✅ **Используется актуальная реализация GrpcClient**
- Логи показывают модуль: `client.modules.grpc_client.core.grpc_client`
- Файл: `/Users/sergiyzasorin/Fix_new/client/modules/grpc_client/core/grpc_client.py`
- Сигнатура содержит `session_id`

✅ **Ошибка сигнатуры не воспроизводится**
- Нет `TypeError: missing 1 required positional argument: 'session_id'`
- Нет `invalid_client_signature` в событиях
- gRPC запрос успешно отправляется

---

## Заключение

**Статус:** ✅ **Исправлено**

Исправлен порядок путей в `sys.path` для гарантии загрузки полной версии `GrpcClient`. Добавлена диагностика для выявления проблем с кэшем и устаревшими версиями. Создан скрипт для очистки кэша.

**Следующий шаг:** Выполнить инструкцию по применению (остановить процесс → очистить кэш → перезапустить → проверить логи).
