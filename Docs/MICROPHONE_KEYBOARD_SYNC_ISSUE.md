# Проблема синхронизации микрофона и клавиатуры

## Дата создания
2025-11-30

## Проблема

Микрофон продолжает работать после `RELEASE`, несмотря на то, что `voice.recording_stop` публикуется. В логах видно противоречие:

```
🔄 RELEASE: was_recording=False (_recording_started=False, mic_active=False)
🛑 RELEASE: микрофон активен (mic_active=False) или запись начата (_recording_started=False) или есть сессия (session=1764516201.630065) - принудительно останавливаем
```

**Проблема**: Микрофон физически активен (callback работает), но `state_manager.is_microphone_active()` возвращает `False`.

## Анализ потока событий

### 1. LONG_PRESS → voice.recording_start

**Файл**: `integration/integrations/input_processing_integration.py:1288`

```python
self._recording_started = True  # Устанавливается ПОСЛЕ публикации voice.recording_start
```

**Проблема**: `_recording_started` устанавливается в `True`, но в логах видно `_recording_started=False` при LONG_PRESS. Это означает, что:
- Либо код не доходит до строки 1288
- Либо `_recording_started` сбрасывается где-то между LONG_PRESS и RELEASE

### 2. voice.recording_start → microphone.opened

**Файл**: `integration/integrations/voice_recognition_integration.py:310`

```python
async def _on_recording_start(self, event: Dict[str, Any]):
    self._recording_active = True
    # ... публикует microphone.open_requested
```

**Проблема**: `_recording_active` устанавливается в `True`, но это локальная переменная в `VoiceRecognitionIntegration`, а не в `state_manager`.

### 3. microphone.opened → state_manager

**Файл**: `modules/microphone_state/core/microphone_state_manager.py`

```python
async def _on_microphone_opened(self, event):
    # Обновляет state_manager через state_manager.set_microphone_active(True)
```

**Проблема**: Если `microphone.opened` не публикуется или не обрабатывается, `state_manager` не знает, что микрофон активен.

### 4. RELEASE → voice.recording_stop

**Файл**: `integration/integrations/input_processing_integration.py:1364`

```python
was_recording = self._recording_started or self.state_manager.is_microphone_active()
```

**Проблема**: 
- `_recording_started` может быть `False` (если сброшен)
- `state_manager.is_microphone_active()` может быть `False` (если `microphone.opened` не обработано)
- Но микрофон физически активен (callback работает)

## Корневая причина

**Рассинхронизация состояний**:
1. `_recording_started` в `InputProcessingIntegration` - локальный флаг
2. `_recording_active` в `VoiceRecognitionIntegration` - локальный флаг
3. `state_manager.is_microphone_active()` - централизованное состояние
4. Физическое состояние потока (`_current_stream.active`) - реальное состояние

Эти 4 источника истины могут быть рассинхронизированы.

## Решение

### Вариант 1: Единый источник истины через state_manager

**Идея**: Использовать только `state_manager.is_microphone_active()` как единственный источник истины.

**Изменения**:
1. Удалить `_recording_started` из `InputProcessingIntegration`
2. Удалить `_recording_active` из `VoiceRecognitionIntegration`
3. Все проверки делать через `state_manager.is_microphone_active()`
4. Обновлять `state_manager` при каждом изменении состояния потока

**Плюсы**:
- Единый источник истины
- Нет рассинхронизации

**Минусы**:
- Требует рефакторинга всех проверок
- Может быть медленнее (дополнительные вызовы)

### Вариант 2: Синхронизация при каждом изменении

**Идея**: Синхронизировать все источники при каждом изменении состояния.

**Изменения**:
1. При `voice.recording_start` → устанавливать `_recording_started = True` И обновлять `state_manager`
2. При `microphone.opened` → обновлять `state_manager` И устанавливать `_recording_started = True`
3. При `voice.recording_stop` → сбрасывать все флаги синхронно
4. При `microphone.closed` → сбрасывать все флаги синхронно

**Плюсы**:
- Минимальные изменения
- Сохраняет существующую логику

**Минусы**:
- Все еще есть несколько источников истины
- Риск рассинхронизации при ошибках

### Вариант 3: Проверка физического состояния потока

**Идея**: При проверке `mic_active` проверять не только `state_manager`, но и физическое состояние потока.

**Изменения**:
```python
def is_microphone_actually_active(self) -> bool:
    """Проверяет реальное состояние микрофона (поток активен)"""
    if self._recognizer is None:
        return False
    if not hasattr(self._recognizer, '_current_stream'):
        return False
    if self._recognizer._current_stream is None:
        return False
    return self._recognizer._current_stream.active
```

**Плюсы**:
- Проверяет реальное состояние
- Не требует рефакторинга

**Минусы**:
- Прямой доступ к внутреннему состоянию `SpeechRecognizer`
- Нарушает инкапсуляцию

## Рекомендация

**Рекомендую Вариант 2 + Вариант 3**:
1. Синхронизировать все флаги при каждом изменении (Вариант 2)
2. Добавить проверку физического состояния потока как fallback (Вариант 3)

Это даст:
- Синхронизацию состояний
- Проверку реального состояния как последнюю линию защиты
- Минимальные изменения в коде

## Реализованное решение

### ✅ Шаг 1: Проверка физического состояния потока

**Файл**: `integration/integrations/voice_recognition_integration.py:505-525`

**Что сделано**:
- Добавлена проверка физического состояния потока (`_current_stream.active`) ПЕРЕД вызовом `request_close`
- Если поток физически активен, он принудительно останавливается немедленно
- Это предотвращает залипание микрофона, даже если `state_manager` рассинхронизирован

**Код**:
```python
# ✅ КРИТИЧНО: ПРОВЕРЯЕМ ФИЗИЧЕСКОЕ СОСТОЯНИЕ ПОТОКА ПЕРЕД ВСЕМ ОСТАЛЬНЫМ
stream_was_active = False
if self._recognizer is not None and hasattr(self._recognizer, '_current_stream') and self._recognizer._current_stream:
    with getattr(self._recognizer, '_stream_lock', threading.RLock()):
        if self._recognizer._current_stream and self._recognizer._current_stream.active:
            stream_was_active = True
            logger.warning("🛑 VOICE: Поток физически активен - принудительно останавливаем ПЕРЕД request_close")
            self._recognizer._current_stream.stop()
```

### ✅ Шаг 2: Улучшенное логирование

**Что сделано**:
- Добавлено логирование после каждого шага в `_on_recording_stop`
- Логируется состояние потока, результат `request_close`, и все исключения
- Это поможет понять, где именно застревает код

### ✅ Шаг 3: Обработка исключений при request_close

**Что сделано**:
- Добавлена обработка исключений при вызове `request_close`
- Если `request_close` падает, используется fallback через `state_manager.force_close_microphone()`

## Следующие шаги

1. ✅ Добавить проверку физического состояния потока в `_on_recording_stop` - **СДЕЛАНО**
2. ✅ Добавить логирование всех изменений состояния микрофона - **СДЕЛАНО**
3. ⏳ Синхронизировать все флаги при каждом изменении (требует дополнительного тестирования)
4. ⏳ Добавить таймаут для принудительной остановки потока (если проблема останется)

