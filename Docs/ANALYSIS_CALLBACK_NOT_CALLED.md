# Анализ проблемы: Callback не вызывается при записи

## Дата создания
2025-12-12

## Проблема

При нажатии клавиши и попытке записи команды callback от `speech_recognition.listen_in_background` **НИ РАЗУ не вызывается**, что приводит к ошибке:
```
❌ [Google] Callback НИ РАЗУ не был вызван - проблема с активацией микрофона или разрешениями!
⚠️ [Google] Нет накопленных чанков после остановки записи
⚠️ [Google] Нет данных от записи (нулевая длительность или ошибка записи)
```

## Анализ логов

### Последовательность событий

1. **Строка 2**: Переход из `PROCESSING` в `LISTENING`
2. **Строки 44-135**: Множество `KeyDown` событий для клавиши N (автоповтор), все подавляются
3. **Строка 137**: `KeyUp` для N
4. **Строка 141**: Генерируется `SHORT_PRESS` (duration=8.740s - **очень долго!**)
5. **Строка 152**: `SHORT_PRESS` обрабатывается
6. **Строка 155**: "SHORT_PRESS: запись начата=True, режим=AppMode.LISTENING"
7. **Строка 175**: `RELEASE` обрабатывается (duration=8740ms)
8. **Строка 177**: "RELEASE: микрофон активен (mic_active=True)"
9. **Строка 181**: "RELEASE: публикуем voice.recording_stop"
10. **Строка 265**: `_on_recording_stop ВХОД`
11. **Строка 304**: **КРИТИЧЕСКАЯ ПРОБЛЕМА**: "❌ [Google] Callback НИ РАЗУ не был вызван"

### Ключевые наблюдения

1. **Нет LONG_PRESS**: Клавиша была зажата 8.7 секунд, но `LONG_PRESS` не сработал. Вместо этого сработал `SHORT_PRESS` после отпускания.

2. **Нет voice.recording_start**: В логах нет публикации `voice.recording_start`, что означает, что `_handle_long_press` не был вызван (или не опубликовал событие).

3. **Нет активации Google записи**: В логах нет строки "🎤 [Google] Активация микрофона через speech_recognition...", что означает, что `_on_recording_start` либо не вызывается, либо выходит раньше.

4. **RELEASE останавливает несуществующую запись**: `RELEASE` публикует `voice.recording_stop`, но запись еще не началась.

## Корневая причина

### Проблема 1: LONG_PRESS не срабатывает

**Симптомы**:
- Клавиша зажата 8.7 секунд (намного больше порога LONG_PRESS 0.6 сек)
- Но `LONG_PRESS` не генерируется
- Вместо этого генерируется `SHORT_PRESS` после отпускания

**Возможные причины**:
1. `_run_hold_monitor` не успевает проверить `duration >= long_press_threshold` до отпускания клавиши
2. `_combo_active` или `_combo_start_time` не установлены правильно
3. `_run_hold_monitor` не работает (поток не запущен или заблокирован)

**Проверка**:
- В логах нет строки "🔑 PTT: LONG_PRESS triggered!" (строка 797 в `quartz_monitor.py`)
- Это означает, что `_run_hold_monitor` не обнаружил LONG_PRESS

### Проблема 2: voice.recording_start не публикуется

**Симптомы**:
- `SHORT_PRESS` обрабатывается, но не публикует `voice.recording_start`
- `voice.recording_start` публикуется только в `_handle_long_press` (строка 1534 в `input_processing_integration.py`)

**Причина**:
- `SHORT_PRESS` не публикует `voice.recording_start` - он только переводит в `PROCESSING` режим
- `voice.recording_start` публикуется только в `_handle_long_press`

### Проблема 3: _on_recording_start не активирует Google запись

**Симптомы**:
- В логах нет строки "🎤 [Google] Активация микрофона через speech_recognition..."
- Это означает, что код не доходит до строки 783 в `voice_recognition_integration.py`

**Возможные причины**:
1. `_on_recording_start` не вызывается (событие `voice.recording_start` не публикуется)
2. `_on_recording_start` выходит раньше (например, из-за `_first_run_in_progress` или другой проверки)
3. `_use_avf` или `_avf_engine` не установлены правильно

## Решения

### Решение 1: Исправить LONG_PRESS detection

**Проблема**: `_run_hold_monitor` может не успевать проверить LONG_PRESS до отпускания клавиши.

**Решение**:
1. Уменьшить `hold_check_interval` для более частых проверок
2. Добавить проверку LONG_PRESS в `_update_combo_state` при деактивации
3. Добавить логирование в `_run_hold_monitor` для диагностики

### Решение 2: Публиковать voice.recording_start из SHORT_PRESS

**Проблема**: `SHORT_PRESS` не публикует `voice.recording_start`, но должен активировать микрофон.

**Решение**:
- Если запись еще не начата, `SHORT_PRESS` должен публиковать `voice.recording_start` перед переходом в `PROCESSING`
- Или `SHORT_PRESS` должен проверять, была ли запись начата через `LONG_PRESS`, и если нет - публиковать `voice.recording_start`

### Решение 3: Улучшить диагностику

**Проблема**: Недостаточно логов для диагностики проблемы.

**Решение**:
1. Добавить логирование в `_run_hold_monitor` для отслеживания проверок LONG_PRESS
2. Добавить логирование в `_on_recording_start` для отслеживания входа в метод
3. Добавить логирование в `_handle_short_press` для отслеживания публикации событий

## Рекомендации

1. **Немедленно**: Добавить диагностическое логирование в `_run_hold_monitor` и `_on_recording_start`
2. **Краткосрочно**: Исправить LONG_PRESS detection для более надежной работы
3. **Долгосрочно**: Пересмотреть логику SHORT_PRESS vs LONG_PRESS для более предсказуемого поведения

## Связанные файлы

- `modules/input_processing/keyboard/mac/quartz_monitor.py` - логика определения LONG_PRESS
- `integration/integrations/input_processing_integration.py` - обработка SHORT_PRESS и LONG_PRESS
- `integration/integrations/voice_recognition_integration.py` - активация Google записи

