# 📚 Полная инструкция: Система планирования и валидации изменений

**ЦЕЛЬ**: Полная инструкция для ассистента о том, как правильно использовать обновленную систему планирования и валидации изменений.

**ВАЖНО**: Эта инструкция описывает все изменения, которые были внесены, и как теперь работает система планирования.

**Дата создания**: 2025-01-XX  
**Версия**: 1.0

---

## 🎯 Что было изменено

### Проблема, которую мы решали

**Исходная проблема**: При планировании и разработке не учитывались все нюансы:
- Конфигурации и feature flags
- Архитектурные ограничения и зависимости
- Существующий функционал
- Дублирование кода
- Конфликты имен/состояний
- Race conditions

**Результат**: Ассистент генерировал изменения, которые выглядели логично локально, но ломали часть текущей системы.

**Пример проблемы**: В аудиосистеме изменения привели к тому, что устройство по умолчанию не определялось и не переключалось корректно из-за неправильного учета логики и конфигурации.

---

### Решение: Многоуровневая система планирования и валидации

Создана система из **двух этапов**:

1. **Этап планирования** (5 обязательных чек-листов) — выполняется один раз перед началом работы
2. **Автоматическая валидация** (при каждом изменении кода) — выполняется автоматически при генерации любого кода

---

## 📋 Этап 1: Планирование (5 обязательных чек-листов)

### Порядок выполнения (ОБЯЗАТЕЛЕН!)

```
1. CONTEXT_AWARE_PLANNING_GUIDE.md (ПЕРВЫЙ)
   ↓
2. ARCHITECTURE_ANALYSIS_CHECKLIST.md (ВТОРОЙ)
   ↓
3. CONFIGURATION_ANALYSIS_CHECKLIST.md (ТРЕТИЙ)
   ↓
4. PRE_CHANGE_PLANNING_CHECKLIST.md (ЧЕТВЕРТЫЙ)
   ↓
5. DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md (ПЯТЫЙ)
   ↓
   ГОТОВНОСТЬ К ГЕНЕРАЦИИ КОДА
```

**ВАЖНО**: Порядок выполнения обязателен. Каждый этап опирается на результаты предыдущего.

---

### Этап 1: CONTEXT_AWARE_PLANNING_GUIDE.md

**Цель**: Контекстный анализ (ЧТО учесть)

**Что делать**:
1. Определить область изменения (какие модули/интеграции/файлы затрагиваются)
2. Анализировать конфигурации и флаги (высокоуровневый обзор)
3. Анализировать архитектурные ограничения
4. Анализировать существующий функционал
5. Создать детальную Impact Map

**Результат**: Детальная Impact Map, полное понимание контекста изменения.

**Время**: 30-60 минут

**Документ**: `Docs/CONTEXT_AWARE_PLANNING_GUIDE.md`

---

### Этап 2: ARCHITECTURE_ANALYSIS_CHECKLIST.md

**Цель**: Анализ архитектуры (КАК устроено)

**Что делать**:
1. Изучить структуру проекта (модули, интеграции, workflows, core)
2. Анализировать взаимодействия (EventBus события, workflows, selectors/gateways)
3. Анализировать зависимости (прямые, обратные, внешние)
4. Понять инварианты (архитектурные правила, правила взаимодействия)

**Результат**: Полное понимание архитектуры системы.

**Время**: 45-90 минут

**Документ**: `Docs/ARCHITECTURE_ANALYSIS_CHECKLIST.md`

---

### Этап 3: CONFIGURATION_ANALYSIS_CHECKLIST.md

**Цель**: Анализ конфигураций (КАКИЕ параметры)

**Что делать**:
1. Идентифицировать все конфигурации, связанные с изменениями
2. Анализировать параметры (значения, зависимости, логика использования)
3. Анализировать флаги (feature flags, kill-switches)
4. Анализировать зависимости между конфигурациями
5. Создать таблицу анализа конфигураций

**Результат**: Полное понимание всех конфигураций и их зависимостей.

**Время**: 30-60 минут

**Документ**: `Docs/CONFIGURATION_ANALYSIS_CHECKLIST.md`

---

### Этап 4: PRE_CHANGE_PLANNING_CHECKLIST.md

**Цель**: Планирование изменений (КАК изменить)

**Что делать**:
1. Понимать задачу и контекст
2. Анализировать зависимости и влияние (модули, EventBus, конфигурации, оси состояния)
3. Анализировать риски и инварианты
4. Планировать реализацию (Impact Map, change_impact.yaml, план изменений)
5. Проверять перед началом кода (документация, конфигурация, тесты, мониторинг)

**Результат**: Полный план изменений, Impact Map, change_impact.yaml.

**Время**: 60-120 минут

**Документ**: `Docs/PRE_CHANGE_PLANNING_CHECKLIST.md`

---

### Этап 5: DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md

**Цель**: Проверка дублирования/конфликтов/race conditions (ЧТО предотвратить)

**Что делать**:
1. Проверять дублирование кода (поиск существующей функциональности)
2. Проверять конфликты (конфликты с существующим кодом, архитектурой, конфигурациями)
3. Проверять race conditions (thread-safety, async/await паттерны)
4. Проверять состояние и синхронизацию (единый источник истины, атомарность)

**ВАЖНО**: Использовать конкретные методы из `DETECTION_GUIDE_DUPLICATES_CONFLICTS_RACES.md` для правильного выявления.

**Результат**: Таблица проверки дублирования/конфликтов/race conditions, готовность к генерации кода.

**Время**: 20-40 минут

**Документ**: `Docs/DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md`

**Инструкция по выявлению**: `Docs/DETECTION_GUIDE_DUPLICATES_CONFLICTS_RACES.md`

---

## ⚡ Этап 2: Автоматическая валидация при каждом изменении кода

### Как работает

**ВАЖНО**: Эти проверки выполняются **АВТОМАТИЧЕСКИ** при КАЖДОМ изменении кода, независимо от этапа планирования.

### Правило 1: Проверка дублирования перед созданием нового кода

**Когда срабатывает**: Перед созданием любой функции/класса/метода

**Что делать**:
1. Извлечь имя функции/метода из генерируемого кода
2. Выполнить поиск по кодовой базе:
   ```bash
   grep -r "function_name" . --include="*.py"
   codebase_search("How is functionality implemented?")
   ```
3. Если найдено → использовать существующую функцию
4. Если не найдено → создать новую функцию

**Инструкция**: См. `DETECTION_GUIDE_DUPLICATES_CONFLICTS_RACES.md` раздел 1

---

### Правило 2: Проверка конфликтов перед изменением имен/состояний

**Когда срабатывает**: Перед созданием/изменением любой переменной/функции/класса

**Что делать**:
1. Извлечь имя переменной из генерируемого кода
2. Выполнить поиск по кодовой базе:
   ```bash
   grep -r "variable_name\|function_name\|ClassName" . --include="*.py"
   ```
3. Если найдено в том же контексте → использовать существующую переменную
4. Если найдено в другом контексте → использовать уникальное имя

**Инструкция**: См. `DETECTION_GUIDE_DUPLICATES_CONFLICTS_RACES.md` раздел 2

---

### Правило 3: Проверка race conditions перед доступом к общим данным

**Когда срабатывает**: Перед доступом к любым данным из нескольких потоков

**Что делать**:
1. Определить, доступны ли данные из нескольких потоков:
   ```bash
   grep -r "callback\|threading\|async def" . --include="*.py"
   ```
2. Если да → проверить наличие блокировок:
   ```bash
   grep -r "threading\.Lock\|asyncio\.Lock\|Lock()" . --include="*.py"
   ```
3. Если блокировок нет → добавить блокировки
4. Если блокировки есть → использовать существующие

**Инструкция**: См. `DETECTION_GUIDE_DUPLICATES_CONFLICTS_RACES.md` раздел 3

---

### Правило 4: Проверка async/await паттернов

**Когда срабатывает**: Перед созданием async задачи

**Что делать**:
1. Проверить, отслеживаются ли задачи:
   ```bash
   grep -r "asyncio\.create_task\|create_task" . --include="*.py"
   grep -r "_task\s*=\|\.task\s*=" . --include="*.py"
   ```
2. Если есть предыдущая задача → отменить перед созданием новой
3. Проверить наличие таймаутов:
   ```bash
   grep -r "asyncio\.wait_for\|wait_for" . --include="*.py"
   ```

**Инструкция**: См. `DETECTION_GUIDE_DUPLICATES_CONFLICTS_RACES.md` раздел 3.2

---

## 🔄 Полный процесс работы системы

### Сценарий: Пользователь просит изменить код

```
1. Пользователь: "Добавь функцию для определения Bluetooth устройств"
   ↓
2. Ассистент: "Перед началом изменений мне нужно пройти процесс планирования"
   ↓
3. ЭТАП ПЛАНИРОВАНИЯ (5 чек-листов):
   
   Шаг 1: CONTEXT_AWARE_PLANNING_GUIDE.md
   - Определяет область изменения
   - Анализирует конфигурации и флаги
   - Создает Impact Map
   
   Шаг 2: ARCHITECTURE_ANALYSIS_CHECKLIST.md
   - Изучает структуру проекта
   - Анализирует взаимодействия
   - Поняет инварианты
   
   Шаг 3: CONFIGURATION_ANALYSIS_CHECKLIST.md
   - Идентифицирует все конфигурации
   - Анализирует параметры и флаги
   - Создает таблицу анализа
   
   Шаг 4: PRE_CHANGE_PLANNING_CHECKLIST.md
   - Планирует реализацию
   - Создает Impact Map и change_impact.yaml
   - Проверяет готовность
   
   Шаг 5: DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md
   - Проверяет дублирование (используя DETECTION_GUIDE_DUPLICATES_CONFLICTS_RACES.md)
   - Проверяет конфликты
   - Проверяет race conditions
   ↓
4. ГОТОВНОСТЬ К ГЕНЕРАЦИИ КОДА подтверждена
   ↓
5. Ассистент начинает генерировать код
   ↓
6. АВТОМАТИЧЕСКАЯ ВАЛИДАЦИЯ (при каждом изменении):
   
   Перед созданием функции:
   - Автоматически ищет: grep -r "is_bluetooth_device" .
   - Находит: device_utils.is_bluetooth_device()
   - Использует существующую функцию вместо создания новой
   
   Перед созданием переменной:
   - Автоматически проверяет конфликты имен
   - Использует уникальное имя (если нужно)
   
   Перед доступом к данным:
   - Автоматически проверяет thread-safety
   - Добавляет блокировки (если нужно)
   ↓
7. Код сгенерирован с использованием существующей функции
   ↓
8. Проверка: Нет дублирования, конфликтов, race conditions
   ↓
9. Готово!
```

---

## 📋 Пошаговая инструкция для ассистента

### Шаг 1: Получить запрос на изменение

**Когда**: Пользователь просит изменить код

**Что делать**:
1. Сказать: "Перед началом изменений мне нужно пройти процесс планирования"
2. НЕ начинать генерацию кода сразу
3. Начать с этапа планирования

---

### Шаг 2: Пройти этап планирования (5 чек-листов)

**Порядок выполнения (ОБЯЗАТЕЛЕН!)**:

#### 2.1 CONTEXT_AWARE_PLANNING_GUIDE.md (ПЕРВЫЙ)

**Действия**:
1. Открыть `Docs/CONTEXT_AWARE_PLANNING_GUIDE.md`
2. Пройти все этапы 0-6
3. Создать детальную Impact Map
4. Зафиксировать результаты

**Проверка**: Все этапы 0-6 выполнены, Impact Map создана

---

#### 2.2 ARCHITECTURE_ANALYSIS_CHECKLIST.md (ВТОРОЙ)

**Действия**:
1. Открыть `Docs/ARCHITECTURE_ANALYSIS_CHECKLIST.md`
2. Пройти все этапы 0-6
3. Изучить структуру проекта, взаимодействия, зависимости
4. Зафиксировать результаты

**Проверка**: Все этапы 0-6 выполнены, архитектура понята

---

#### 2.3 CONFIGURATION_ANALYSIS_CHECKLIST.md (ТРЕТИЙ)

**Действия**:
1. Открыть `Docs/CONFIGURATION_ANALYSIS_CHECKLIST.md`
2. Пройти все этапы 1-6
3. Идентифицировать все конфигурации, проанализировать параметры и флаги
4. Создать таблицу анализа конфигураций
5. Зафиксировать результаты

**Проверка**: Все этапы 1-6 выполнены, таблица анализа создана

---

#### 2.4 PRE_CHANGE_PLANNING_CHECKLIST.md (ЧЕТВЕРТЫЙ)

**Действия**:
1. Открыть `Docs/PRE_CHANGE_PLANNING_CHECKLIST.md`
2. Пройти все этапы 1-6
3. Создать Impact Map (если еще не создана)
4. Создать change_impact.yaml
5. Зафиксировать результаты

**Проверка**: Все этапы 1-6 выполнены, Impact Map и change_impact.yaml созданы

---

#### 2.5 DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md (ПЯТЫЙ)

**Действия**:
1. Открыть `Docs/DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md`
2. Открыть `Docs/DETECTION_GUIDE_DUPLICATES_CONFLICTS_RACES.md` (ИНСТРУКЦИЯ)
3. Пройти все этапы 1-5, используя конкретные методы из инструкции:
   - Этап 1: Проверка дублирования (использовать команды из DETECTION_GUIDE раздел 1)
   - Этап 2: Проверка конфликтов (использовать команды из DETECTION_GUIDE раздел 2)
   - Этап 3: Проверка race conditions (использовать команды из DETECTION_GUIDE раздел 3)
4. Заполнить таблицу проверки
5. Зафиксировать результаты

**Проверка**: Все этапы 1-5 выполнены, таблица проверки заполнена

---

### Шаг 3: Проверить готовность к генерации кода

**Чек-лист готовности**:
- [ ] Все 5 чек-листов выполнены в правильном порядке
- [ ] Impact Map создана и заполнена
- [ ] change_impact.yaml создан и заполнен
- [ ] Таблица проверки дублирования/конфликтов/race conditions заполнена
- [ ] Документация обновлена (если нужно)
- [ ] Конфигурация обновлена (если нужно)

**Если все готово** → перейти к шагу 4  
**Если не готово** → вернуться к соответствующему этапу планирования

---

### Шаг 4: Генерировать код с автоматической валидацией

**ВАЖНО**: При каждом изменении кода выполнять автоматические проверки.

#### 4.1 Перед созданием функции/метода

**Действия**:
1. Извлечь имя функции/метода из генерируемого кода
2. Выполнить поиск:
   ```bash
   grep -r "function_name" . --include="*.py"
   codebase_search("How is functionality implemented?")
   ```
3. Если найдено → использовать существующую функцию
4. Если не найдено → создать новую функцию

**Пример**:
```python
# Пользователь просит: "Создай функцию для определения Bluetooth устройств"
# 1. Поиск: grep -r "is_bluetooth_device" . --include="*.py"
# 2. Результат: Найдено device_utils.is_bluetooth_device()
# 3. Действие: Использовать существующую функцию

# ✅ ХОРОШО:
from modules.audio_system.utils.device_utils import is_bluetooth_device
is_bluetooth = is_bluetooth_device(device_name)

# ❌ ПЛОХО:
def _is_bluetooth_device(self, name: str) -> bool:
    return "bluetooth" in name.lower()
```

---

#### 4.2 Перед созданием переменной

**Действия**:
1. Извлечь имя переменной из генерируемого кода
2. Выполнить поиск:
   ```bash
   grep -r "variable_name" . --include="*.py"
   ```
3. Если найдено в том же контексте → использовать существующую переменную
4. Если найдено в другом контексте → использовать уникальное имя

**Пример**:
```python
# Пользователь просит: "Создай переменную _mic_state"
# 1. Поиск: grep -r "_mic_state\|mic.*state" . --include="*.py"
# 2. Результат: Найдено MicrophoneStateManager._state
# 3. Действие: Использовать существующую переменную через MicrophoneStateManager

# ✅ ХОРОШО:
if self._mic_state_manager:
    state = await self._mic_state_manager.get_state()

# ❌ ПЛОХО:
self._mic_state = "opening"  # Конфликт с существующим состоянием
```

---

#### 4.3 Перед доступом к данным

**Действия**:
1. Определить, доступны ли данные из нескольких потоков:
   ```bash
   grep -r "callback\|threading\|async def" . --include="*.py"
   ```
2. Если да → проверить наличие блокировок:
   ```bash
   grep -r "threading\.Lock\|asyncio\.Lock\|Lock()" . --include="*.py"
   ```
3. Если блокировок нет → добавить блокировки
4. Если блокировки есть → использовать существующие

**Пример**:
```python
# Пользователь просит: "Добавь данные в _google_audio_chunks из callback"
# 1. Проверка: Доступен ли из нескольких потоков?
# 2. Результат: Да, из callback потока и основного async потока
# 3. Проверка блокировок: grep -r "_google_audio_chunks_lock\|Lock" .
# 4. Результат: Блокировки не найдены
# 5. Действие: Добавить блокировку

# ✅ ХОРОШО:
self._google_audio_chunks_lock = threading.Lock()

def callback(recognizer, audio):
    with self._google_audio_chunks_lock:
        self._google_audio_chunks.append(audio)

async def process_audio():
    with self._google_audio_chunks_lock:
        if self._google_audio_chunks:
            self._google_audio_data = self._google_audio_chunks[-1]
        self._google_audio_chunks = []

# ❌ ПЛОХО:
def callback(recognizer, audio):
    self._google_audio_chunks.append(audio)  # Race condition!

async def process_audio():
    self._google_audio_chunks = []  # Race condition!
```

---

#### 4.4 Перед созданием async задачи

**Действия**:
1. Проверить, отслеживаются ли задачи:
   ```bash
   grep -r "asyncio\.create_task\|create_task" . --include="*.py"
   grep -r "_task\s*=\|\.task\s*=" . --include="*.py"
   ```
2. Если есть предыдущая задача → отменить перед созданием новой
3. Проверить наличие таймаутов:
   ```bash
   grep -r "asyncio\.wait_for\|wait_for" . --include="*.py"
   ```

**Пример**:
```python
# Пользователь просит: "Создай async задачу для прослушивания"
# 1. Проверка: Есть ли уже запущенная задача?
# 2. Результат: Да, есть предыдущая задача
# 3. Действие: Отменить предыдущую перед созданием новой

# ✅ ХОРОШО:
self._listen_task: Optional[asyncio.Task] = None

async def start_listening():
    if self._listen_task and not self._listen_task.done():
        self._listen_task.cancel()
        try:
            await self._listen_task
        except asyncio.CancelledError:
            pass
    self._listen_task = asyncio.create_task(self._listen_loop())

# ❌ ПЛОХО:
async def start_listening():
    asyncio.create_task(self._listen_loop())  # Может создать дубликаты!
```

---

### Шаг 5: Проверить результат

**После генерации кода проверить**:
- [ ] Нет дублирования кода (использованы существующие функции где возможно)
- [ ] Нет конфликтов имен/состояний (использованы уникальные имена или единый источник истины)
- [ ] Нет race conditions (общие данные защищены блокировками)
- [ ] Все async задачи отслеживаются и имеют таймауты

---

## 📚 Ключевые документы

### Обязательные чек-листы (порядок выполнения обязателен!)

1. `Docs/CONTEXT_AWARE_PLANNING_GUIDE.md` — контекстный анализ (ПЕРВЫЙ этап)
2. `Docs/ARCHITECTURE_ANALYSIS_CHECKLIST.md` — анализ архитектуры (ВТОРОЙ этап)
3. `Docs/CONFIGURATION_ANALYSIS_CHECKLIST.md` — анализ конфигураций (ТРЕТИЙ этап)
4. `Docs/PRE_CHANGE_PLANNING_CHECKLIST.md` — планирование изменений (ЧЕТВЕРТЫЙ этап)
5. `Docs/DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md` — проверка дублирования/конфликтов/race conditions (ПЯТЫЙ этап)

### Инструкции по выявлению

- `Docs/DETECTION_GUIDE_DUPLICATES_CONFLICTS_RACES.md` — **ИНСТРУКЦИЯ** по выявлению дублирования, конфликтов и race conditions (ОБЯЗАТЕЛЬНО использовать!)
- `Docs/DETECTION_GUIDE_QUICK_REFERENCE.md` — быстрая справка с командами

### Правила валидации

- `.cursorrules` (раздел 11.2) — обязательные проверки при каждом изменении кода
- `Docs/CODE_CHANGE_VALIDATION_RULES.md` — детальные правила валидации изменений кода
- `Docs/REAL_TIME_CODE_VALIDATION.md` — автоматическая валидация при генерации кода

### Другие документы

- `Docs/PLANNING_PROCESS_ORDER.md` — единый источник истины для порядка выполнения всех чек-листов
- `Docs/HOW_SYSTEM_WORKS_NOW.md` — как работает система
- `Docs/UNIVERSAL_SYSTEM_APPLICATION.md` — универсальное применение системы

---

## ✅ Чек-лист для ассистента

### Перед началом работы

- [ ] Прочитана эта инструкция (`COMPLETE_PLANNING_SYSTEM_GUIDE.md`)
- [ ] Понят порядок выполнения (5 этапов планирования + автоматическая валидация)
- [ ] Открыт `DETECTION_GUIDE_DUPLICATES_CONFLICTS_RACES.md` для справки по методам выявления

### Этап планирования

- [ ] Пройден CONTEXT_AWARE_PLANNING_GUIDE.md (этапы 0-6)
- [ ] Пройден ARCHITECTURE_ANALYSIS_CHECKLIST.md (этапы 0-6)
- [ ] Пройден CONFIGURATION_ANALYSIS_CHECKLIST.md (этапы 1-6)
- [ ] Пройден PRE_CHANGE_PLANNING_CHECKLIST.md (этапы 1-6)
- [ ] Пройден DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md (этапы 1-5)
- [ ] Использованы методы из DETECTION_GUIDE_DUPLICATES_CONFLICTS_RACES.md для проверок
- [ ] Создана Impact Map
- [ ] Создан change_impact.yaml
- [ ] Заполнена таблица проверки дублирования/конфликтов/race conditions

### Генерация кода

- [ ] Перед созданием функции → выполнен поиск существующей функциональности
- [ ] Перед созданием переменной → выполнена проверка конфликтов имен
- [ ] Перед доступом к данным → выполнена проверка thread-safety
- [ ] Перед созданием async задачи → выполнена проверка отслеживания задач

### После генерации кода

- [ ] Проверено: нет дублирования кода
- [ ] Проверено: нет конфликтов имен/состояний
- [ ] Проверено: нет race conditions
- [ ] Проверено: все async задачи отслеживаются

---

## 🎯 Примеры работы системы

### Пример 1: Простое изменение

**Запрос**: "Измени функцию определения Bluetooth устройств"

**Процесс**:
1. Планирование (80-110 минут):
   - Быстрый анализ контекста (15-20 минут)
   - Фокус на затронутых модулях (20-30 минут)
   - Фокус на затронутых конфигурациях (15-20 минут)
   - Планирование простого изменения (30-40 минут)
   - Быстрая проверка дублирования/конфликтов/race conditions (10-15 минут)

2. Генерация кода:
   - Автоматическая проверка: функция уже существует
   - Использование существующей функции
   - Нет дублирования, конфликтов, race conditions

**Результат**: Изменение выполнено без проблем.

---

### Пример 2: Среднее изменение

**Запрос**: "Добавь поддержку нового типа устройств"

**Процесс**:
1. Планирование (185-250 минут):
   - Полный анализ контекста (30-40 минут)
   - Полный анализ архитектуры (45-60 минут)
   - Полный анализ конфигураций (30-40 минут)
   - Планирование среднего изменения (60-80 минут)
   - Полная проверка дублирования/конфликтов/race conditions (20-30 минут)

2. Генерация кода:
   - Автоматическая проверка при каждом изменении
   - Использование существующих функций где возможно
   - Защита общих данных блокировками
   - Нет дублирования, конфликтов, race conditions

**Результат**: Изменение выполнено без проблем.

---

## ⚠️ Частые ошибки (избегай их!)

- ❌ Начинать код без прохождения всех 5 этапов планирования
- ❌ Пропускать этапы планирования или менять их порядок
- ❌ Не использовать DETECTION_GUIDE_DUPLICATES_CONFLICTS_RACES.md для проверок
- ❌ Создавать дублирующий код вместо использования существующего
- ❌ Не проверять конфликты имен/состояний перед изменениями
- ❌ Не проверять race conditions в многопоточном/async коде
- ❌ Генерировать код без автоматической валидации

---

## 📊 Время выполнения

### Простое изменение (1-2 файла)
- Планирование: 80-110 минут
- Генерация кода: с автоматической валидацией
- **Общее время**: 90-125 минут

### Среднее изменение (несколько модулей)
- Планирование: 185-250 минут
- Генерация кода: с автоматической валидацией
- **Общее время**: 3-4 часа

### Большое изменение (функциональность)
- Планирование: 270-370 минут
- Генерация кода: с автоматической валидацией
- **Общее время**: 4.5-6 часов

---

## 🎯 Итоговая логика работы

```
ПОЛЬЗОВАТЕЛЬ ПРОСИТ ИЗМЕНИТЬ КОД
         ↓
ЭТАП ПЛАНИРОВАНИЯ (5 чек-листов)
  - Контекстный анализ
  - Анализ архитектуры
  - Анализ конфигураций
  - Планирование изменений
  - Проверка дублирования/конфликтов/race conditions
         ↓
ГОТОВНОСТЬ К ГЕНЕРАЦИИ КОДА
         ↓
ГЕНЕРАЦИЯ КОДА
  - Автоматическая проверка дублирования
  - Автоматическая проверка конфликтов
  - Автоматическая проверка race conditions
  - Автоматическая проверка async/await паттернов
         ↓
КОД СГЕНЕРИРОВАН БЕЗ ПРОБЛЕМ
```

---

## 📚 Связанные документы

**Обязательные чек-листы**:
- `CONTEXT_AWARE_PLANNING_GUIDE.md` — контекстный анализ (ПЕРВЫЙ этап)
- `ARCHITECTURE_ANALYSIS_CHECKLIST.md` — анализ архитектуры (ВТОРОЙ этап)
- `CONFIGURATION_ANALYSIS_CHECKLIST.md` — анализ конфигураций (ТРЕТИЙ этап)
- `PRE_CHANGE_PLANNING_CHECKLIST.md` — планирование изменений (ЧЕТВЕРТЫЙ этап)
- `DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md` — проверка дублирования/конфликтов/race conditions (ПЯТЫЙ этап)

**Инструкции**:
- `DETECTION_GUIDE_DUPLICATES_CONFLICTS_RACES.md` — **ИНСТРУКЦИЯ** по выявлению (ОБЯЗАТЕЛЬНО использовать!)
- `DETECTION_GUIDE_QUICK_REFERENCE.md` — быстрая справка

**Правила**:
- `.cursorrules` (раздел 11.2) — обязательные проверки при каждом изменении кода
- `CODE_CHANGE_VALIDATION_RULES.md` — детальные правила валидации
- `REAL_TIME_CODE_VALIDATION.md` — автоматическая валидация

**Другие**:
- `PLANNING_PROCESS_ORDER.md` — единый источник истины для порядка выполнения
- `HOW_SYSTEM_WORKS_NOW.md` — как работает система
- `UNIVERSAL_SYSTEM_APPLICATION.md` — универсальное применение системы

**Для улучшения системы**:
- `HOW_TO_IMPROVE_PLANNING_SYSTEM.md` — **ИНСТРУКЦИЯ** по улучшению системы планирования (для другого ассистента)
- `IMPROVEMENT_PROCESS_SUMMARY.md` — краткое резюме процесса улучшения

---

**Версия**: 1.0  
**Дата создания**: 2025-01-XX


