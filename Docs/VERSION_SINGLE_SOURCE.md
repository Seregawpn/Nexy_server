# ✅ Единый источник истины для версий

**Дата:** 11 января 2026  
**Статус:** ✅ Централизовано

---

## 🎯 Ответ: **ДА, изменения происходят в ОДНОМ месте!**

**Единый источник истины:** Файл `VERSION` (в корне проекта)

---

## 📍 Где нужно изменять версию

### **✅ ОДНО место для изменения:**

**Файл:** `VERSION` (в корне проекта)

```bash
# Изменить версию
echo "1.6.0.36" > VERSION
```

**Или через скрипт:**
```bash
python3 server/scripts/update_version.py 1.6.0.36
```

---

## 🔄 Автоматическая синхронизация

При изменении `VERSION` файла, **все компоненты автоматически** получат новую версию:

### **Компоненты, которые используют VERSION файл:**

1. ✅ **UpdateConfig** → `get_version_from_file()`
2. ✅ **UpdateServiceConfig** → `get_version_from_file()`
3. ✅ **ServerMetadataConfig** → `get_version_from_file()` (если нет SERVER_VERSION в env)
4. ✅ **main.py** → через `server_metadata.version`

**Все используют один источник!**

---

## ⚠️ Важное замечание

### **Переменная окружения `SERVER_VERSION`**

Если в `config.env` установлена переменная `SERVER_VERSION`, она **переопределяет** VERSION файл в `ServerMetadataConfig.from_env()`.

**Приоритет:**
1. Переменная окружения `SERVER_VERSION` (если установлена)
2. VERSION файл (fallback)

**Рекомендация:**
- ✅ Используйте только VERSION файл (удалите SERVER_VERSION из config.env)
- ИЛИ синхронизируйте SERVER_VERSION в config.env с VERSION файлом

---

## 📊 Текущее состояние

**VERSION файл:** `1.6.0.35` ✅

**Компоненты:**
- ✅ UpdateConfig: `1.6.0.35` (из VERSION файла)
- ⚠️ ServerMetadataConfig: `1.0.3` (из config.env SERVER_VERSION)

**Решение:** Синхронизировать `SERVER_VERSION` в `config.env` с VERSION файлом.

---

## ✅ Итог

**Да, изменения происходят в ОДНОМ месте:**

1. **Измените VERSION файл** → все компоненты получат новую версию автоматически
2. **Убедитесь, что в config.env нет SERVER_VERSION** (или синхронизируйте её)
3. **Манифесты обновляются отдельно** (через скрипты при деплое)

**Система полностью централизована!** 🎉
