# 🔍 Комплексный анализ аудиосистемы: текущее состояние и проблемы

## 📋 Содержание
1. [Текущая архитектура](#текущая-архитектура)
2. [Выявленные проблемы](#выявленные-проблемы)
3. [Как должно работать](#как-должно-работать)
4. [Рекомендации по исправлению](#рекомендации-по-исправлению)

---

## 🏗️ Текущая архитектура

### Компоненты аудиосистемы

#### 1. **AVFAudioEngine** (`modules/audio_avf/core/avf_audio_engine.py`)
- **Назначение**: Нативная аудиосистема на основе AVFoundation для macOS
- **Функции**:
  - `start_input()` - начало записи с микрофона
  - `stop_input()` - остановка записи
  - Мониторинг устройств (INPUT/OUTPUT)
  - Получение диагностики устройства (формат, sample_rate, device_info)

#### 2. **VoiceRecognitionIntegration** (`integration/integrations/voice_recognition_integration.py`)
- **Назначение**: Координация распознавания речи
- **Ответственность**:
  - Обработка событий `voice.recording_start/stop`
  - Активация микрофона через AVF (диагностика) и Google Speech Recognition (запись)
  - Управление состоянием записи
  - Публикация событий `microphone.opened/closed`

#### 3. **InputProcessingIntegration** (`integration/integrations/input_processing_integration.py`)
- **Назначение**: Обработка ввода с клавиатуры
- **Ответственность**:
  - Обработка нажатий клавиш (Ctrl+N)
  - Публикация `voice.recording_start/stop`
  - Отслеживание состояния микрофона через `state_manager.is_microphone_active()`

#### 4. **ApplicationStateManager** (`integration/core/state_manager.py`)
- **Назначение**: Централизованное управление состоянием приложения
- **Функции микрофона**:
  - `set_microphone_state(state, session_id, reason)` - установка состояния
  - `is_microphone_active()` - проверка активности
  - `force_close_microphone(reason)` - принудительное закрытие

#### 5. **AudioCoreIntegration** (`integration/integrations/audio_core_integration.py`)
- **Назначение**: Мониторинг аудиоустройств
- **Ответственность**:
  - Отслеживание подключения/отключения устройств
  - Предоставление `InputStreamManager` и `OutputStreamManager`

---

## 🔄 Текущий поток активации микрофона

### Последовательность при `voice.recording_start`:

```
1. InputProcessingIntegration
   └─► Публикует voice.recording_start (session_id, source="keyboard")

2. VoiceRecognitionIntegration._on_recording_start()
   ├─► Проверка блокировок (first_run, PROCESSING mode)
   ├─► Установка сессии: _set_session_id(session_id)
   ├─► _recording_active = True
   │
   ├─► ШАГ 1: AVF диагностика (~1 секунда)
   │   ├─► AVFAudioEngine.start_input()
   │   ├─► Записывает ~1 секунду
   │   ├─► Получает: device_info, input_format, diagnostics
   │   └─► AVFAudioEngine.stop_input() ✅ (дезактивирует)
   │
   ├─► ШАГ 2: Пауза 0.2 секунды (гарантия деактивации AVF)
   │
   └─► ШАГ 3: Google Speech Recognition активация
       ├─► Создаёт sr.Recognizer() и sr.Microphone()
       ├─► Настраивает device_index и sample_rate от AVF
       ├─► recognizer.adjust_for_ambient_noise()
       ├─► recognizer.listen_in_background(microphone, callback)
       ├─► state_manager.set_microphone_state("active", session_id)
       └─► Публикует microphone.opened
```

### Последовательность при `voice.recording_stop`:

```
1. InputProcessingIntegration
   └─► Публикует voice.recording_stop (session_id, duration)

2. VoiceRecognitionIntegration._on_recording_stop()
   ├─► Проверка session_id (active_session_id == request_session_id)
   ├─► _recording_active = False
   │
   ├─► ШАГ 1: Остановка Google записи
   │   ├─► _google_recording_active = False
   │   ├─► await asyncio.sleep(0.1) (завершение callback'ов)
   │   ├─► _google_stop_listening(wait_for_stop=False)
   │   └─► await asyncio.sleep(0.5) (ожидание последних callback'ов)
   │
   ├─► ШАГ 2: Получение данных
   │   ├─► Объединение чанков из _google_audio_chunks
   │   └─► Создание sr.AudioData
   │
   ├─► ШАГ 3: Распознавание
   │   └─► recognizer.recognize_google(audio_data, language)
   │
   └─► ШАГ 4: Закрытие микрофона
       ├─► state_manager.set_microphone_state("idle", None)
       └─► Публикует microphone.closed
```

---

## 🐛 Выявленные проблемы

### Проблема 1: Разбросанное состояние микрофона (КРИТИЧЕСКАЯ)

**Описание:**
Состояние микрофона отслеживается в нескольких местах одновременно:
- `_recording_active` в `VoiceRecognitionIntegration`
- `_google_recording_active` в `VoiceRecognitionIntegration`
- `_google_stop_listening` в `VoiceRecognitionIntegration`
- `is_listening` в `SpeechRecognizer` (legacy)
- `_microphone_state` в `ApplicationStateManager`
- `_mic_active` (удалено, но логика осталась)

**Последствия:**
1. **Race conditions** - разные модули могут иметь разные представления о состоянии
2. **Десинхронизация** - `state_manager.is_microphone_active() == True`, но `_google_stop_listening == None`
3. **Залипание микрофона** - микрофон остается активным после ошибки
4. **Сложность отладки** - невозможно определить реальное состояние микрофона

**Пример проблемы:**
```python
# VoiceRecognitionIntegration
self._google_recording_active = True
self._google_stop_listening = None  # Ошибка при активации

# ApplicationStateManager
self._microphone_state = "active"  # Установлено ПЕРЕД listen_in_background

# Результат: залипание - микрофон "активен" в state_manager, но не записывает
```

---

### Проблема 2: Конфликт доступа к микрофону между AVF и Google (КРИТИЧЕСКАЯ)

**Описание:**
AVF и Google Speech Recognition пытаются использовать микрофон одновременно:
1. AVF активирует микрофон для диагностики (~1 секунда)
2. AVF должен деактивировать микрофон
3. Google активирует микрофон через `listen_in_background()`

**Проблемы:**
1. **Неполная деактивация AVF** - `is_input_active` может остаться `True` после `stop_input()`
2. **Конфликт доступа** - PyAudio (внутри speech_recognition) не может открыть микрофон, если AVF его держит
3. **Отсутствие гарантии освобождения** - нет механизма проверки, что AVF действительно освободил микрофон

**Код проблемы:**
```python
# voice_recognition_integration.py:819
device_info = await self._get_device_info_via_avf()  # AVF активирует микрофон
await self._avf_engine.stop_input()  # Должен деактивировать
await asyncio.sleep(0.2)  # Пауза для гарантии деактивации

# Проверка (строка 843-847)
if hasattr(self._avf_engine, 'is_input_active') and self._avf_engine.is_input_active:
    logger.warning("⚠️ [AVF] AVF все еще активен после stop_input()")
    await asyncio.sleep(0.5)  # Дополнительная пауза

# Проблема: даже после дополнительной паузы AVF может остаться активным
```

---

### Проблема 3: Неправильная обработка session_id=None (ВЫСОКАЯ)

**Описание:**
При SHORT_PRESS после LONG_PRESS сессия отменяется (`session_id=None`), но микрофон может остаться активным.

**Текущая логика:**
```python
# voice_recognition_integration.py:1320
if session_id is None:
    # Проверяем активность микрофона
    if google_mic_active or legacy_mic_active:
        # Принудительно останавливаем
        ...
    return
```

**Проблемы:**
1. **Поздняя проверка** - проверка `session_id is None` происходит ПОСЛЕ установки состояния
2. **Неполная очистка** - состояние может остаться несинхронизированным
3. **Отсутствие гарантии** - нет механизма восстановления при ошибке

---

### Проблема 4: Callback от Google Speech Recognition обрабатывается после остановки (ВЫСОКАЯ)

**Описание:**
Callback от `listen_in_background()` может быть вызван ПОСЛЕ того, как `_google_recording_active = False`.

**Текущая защита:**
```python
# voice_recognition_integration.py:948
if not self._google_recording_active:
    logger.warning("🔒 [Google] Callback игнорирован: запись уже остановлена")
    return
```

**Проблемы:**
1. **Race condition** - callback может быть вызван между проверкой и установкой флага
2. **Накопление чанков** - чанки могут накапливаться после остановки
3. **Утечка памяти** - `_google_audio_chunks` не очищается при игнорировании callback'ов

---

### Проблема 5: Отсутствие единого источника истины для выбора устройства (СРЕДНЯЯ)

**Описание:**
Выбор аудиоустройства происходит в нескольких местах:
- AVF использует системное default устройство
- Google Speech Recognition ищет устройство по имени от AVF
- `AudioCoreIntegration` отслеживает устройства отдельно

**Проблемы:**
1. **Несоответствие имен** - имя устройства от AVF может не совпадать с именем в PyAudio
2. **Отсутствие синхронизации** - при переключении устройства (например, AirPods) система не обновляется
3. **Fallback на default** - если устройство не найдено, используется default без уведомления

**Код проблемы:**
```python
# voice_recognition_integration.py:879-897
if device_info and device_info.get('device_info') and device_info['device_info'].get('name'):
    device_name = device_info['device_info']['name']
    # Ищем индекс по имени
    for idx, name in enumerate(mic_names):
        if device_name.lower() in name.lower() or name.lower() in device_name.lower():
            device_index = idx
            break
    
    if device_index is None:
        logger.warning("⚠️ [Google] Не удалось найти device_index, используем default")
        # Проблема: используется default без уведомления пользователя
```

---

### Проблема 6: Неправильная обработка ошибок активации (СРЕДНЯЯ)

**Описание:**
При ошибке активации микрофона состояние может остаться несинхронизированным.

**Текущая логика:**
```python
# voice_recognition_integration.py:1198-1220
except Exception as e:
    logger.error(f"❌ [Google] Ошибка активации микрофона: {e}")
    # Восстанавливаем состояние
    if self.state_manager.is_microphone_active():
        self.state_manager.set_microphone_state("idle", None, reason="google_mic_activate_exception")
        await self.event_bus.publish("microphone.closed", {"session_id": session_id})
    # Очищаем состояние
    self._google_recording_active = False
    ...
```

**Проблемы:**
1. **Частичная очистка** - не все флаги сбрасываются при ошибке
2. **Отсутствие гарантии** - нет механизма проверки, что состояние действительно обновлено
3. **Дублирование логики** - логика очистки дублируется в нескольких местах

---

## ✅ Как должно работать

### Правильная архитектура управления микрофоном

```
┌─────────────────────────────────────────────────────────────┐
│         ApplicationStateManager                             │
│  (Единый источник истины для состояния микрофона)          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Состояние:                                                 │
│  - microphone_state: "idle" | "opening" | "active" |      │
│                        "closing" | "error"                  │
│  - microphone_session_id: Optional[str]                    │
│  - microphone_lock: threading.Lock                         │
│                                                             │
│  Методы:                                                    │
│  - set_microphone_state(state, session_id, reason)          │
│  - is_microphone_active() → bool                            │
│  - force_close_microphone(reason)                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
         │                    │                    │
         ▼                    ▼                    ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ InputProc    │    │ VoiceRecog   │    │ AVF/Google   │
│ Integration  │    │ Integration  │    │ Engines      │
└──────────────┘    └──────────────┘    └──────────────┘
```

### Правильная последовательность активации

```
1. voice.recording_start
   │
   └─► VoiceRecognitionIntegration._on_recording_start()
       ├─► Проверка блокировок
       ├─► state_manager.set_microphone_state("opening", session_id)
       │
       ├─► ШАГ 1: AVF диагностика (с гарантией освобождения)
       │   ├─► AVFAudioEngine.start_input()
       │   ├─► Записывает ~1 секунду
       │   ├─► Получает диагностику
       │   ├─► AVFAudioEngine.stop_input()
       │   └─► ✅ ГАРАНТИЯ: Ждём пока is_input_active == False
       │       └─► Проверка каждые 100мс, максимум 2 секунды
       │
       ├─► ШАГ 2: Google активация (только после гарантии освобождения AVF)
       │   ├─► Проверка: AVF.is_input_active == False
       │   ├─► Создаём sr.Microphone() с device_index от AVF
       │   ├─► recognizer.listen_in_background()
       │   ├─► ✅ ГАРАНТИЯ: Ждём первый callback (максимум 2 секунды)
       │   └─► state_manager.set_microphone_state("active", session_id)
       │
       └─► Публикует microphone.opened
```

### Правильная последовательность деактивации

```
1. voice.recording_stop
   │
   └─► VoiceRecognitionIntegration._on_recording_stop()
       ├─► Проверка session_id
       ├─► state_manager.set_microphone_state("closing", session_id)
       │
       ├─► ШАГ 1: Остановка Google записи
       │   ├─► _google_recording_active = False (atomic)
       │   ├─► await asyncio.sleep(0.1) (завершение текущих callback'ов)
       │   ├─► _google_stop_listening(wait_for_stop=False)
       │   └─► await asyncio.sleep(0.5) (ожидание последних callback'ов)
       │
       ├─► ШАГ 2: Получение данных
       │   └─► Объединение чанков (с защитой от race conditions)
       │
       ├─► ШАГ 3: Распознавание
       │   └─► recognizer.recognize_google()
       │
       └─► ШАГ 4: Закрытие микрофона
           ├─► state_manager.set_microphone_state("idle", None)
           └─► Публикует microphone.closed
```

---

## 🔧 Рекомендации по исправлению

### Приоритет 1: КРИТИЧЕСКИЕ проблемы

#### 1.1 Гарантия освобождения микрофона AVF

**Проблема:** AVF может не освободить микрофон после `stop_input()`

**Решение:**
```python
async def _wait_for_avf_deactivation(self, timeout: float = 2.0) -> bool:
    """Ждём пока AVF полностью деактивирует микрофон"""
    start_time = time.monotonic()
    check_interval = 0.1  # Проверяем каждые 100мс
    
    while time.monotonic() - start_time < timeout:
        if not (hasattr(self._avf_engine, 'is_input_active') and self._avf_engine.is_input_active):
            logger.info("✅ [AVF] Микрофон освобожден")
            return True
        await asyncio.sleep(check_interval)
    
    logger.error(f"❌ [AVF] Таймаут ожидания деактивации ({timeout}s)")
    return False

# Использование:
device_info = await self._get_device_info_via_avf()
await self._avf_engine.stop_input()
avf_deactivated = await self._wait_for_avf_deactivation()
if not avf_deactivated:
    raise RuntimeError("AVF не освободил микрофон")
```

#### 1.2 Единый источник истины для состояния микрофона

**Проблема:** Состояние разбросано по нескольким местам

**Решение:**
- Использовать ТОЛЬКО `ApplicationStateManager` для состояния микрофона
- Удалить все локальные флаги (`_recording_active`, `_google_recording_active`)
- Все проверки состояния через `state_manager.is_microphone_active()`

```python
# УДАЛИТЬ:
self._recording_active = True
self._google_recording_active = True

# ИСПОЛЬЗОВАТЬ:
self.state_manager.set_microphone_state("active", session_id)
if self.state_manager.is_microphone_active():
    ...
```

#### 1.3 Атомарная установка флагов

**Проблема:** Race conditions при установке флагов

**Решение:**
```python
# Использовать lock для атомарных операций
async def _set_google_recording_active(self, active: bool, session_id: Optional[str] = None):
    async with self._google_recording_lock:
        self._google_recording_active = active
        if active:
            self.state_manager.set_microphone_state("active", session_id)
        else:
            self.state_manager.set_microphone_state("idle", None)
```

---

### Приоритет 2: ВЫСОКИЕ проблемы

#### 2.1 Правильная обработка session_id=None

**Решение:**
```python
# Проверка session_id ПЕРЕД установкой состояния
if session_id is None:
    # Принудительно останавливаем микрофон
    await self._force_stop_microphone(reason="session_cancelled")
    return

# Только после проверки устанавливаем состояние
self.state_manager.set_microphone_state("opening", session_id)
```

#### 2.2 Защита callback'ов от обработки после остановки

**Решение:**
```python
# Использовать atomic flag с проверкой в callback
def callback(recognizer, audio):
    # Атомарная проверка
    if not self._is_recording_allowed():
        return
    
    # Обработка аудио
    ...

def _is_recording_allowed(self) -> bool:
    """Проверка разрешения записи (atomic)"""
    with self._recording_lock:
        return (
            self._google_recording_active and
            self.state_manager.is_microphone_active() and
            self._google_stop_listening is not None
        )
```

---

### Приоритет 3: СРЕДНИЕ проблемы

#### 3.1 Единый источник истины для выбора устройства

**Решение:**
- Использовать `AudioCoreIntegration` для получения текущего устройства
- Кэшировать соответствие имен устройств (AVF ↔ PyAudio)
- Обновлять кэш при изменении устройств

#### 3.2 Улучшенная обработка ошибок

**Решение:**
```python
async def _activate_microphone_safe(self, session_id: str) -> bool:
    """Безопасная активация микрофона с гарантией очистки при ошибке"""
    try:
        # Активация
        ...
        return True
    except Exception as e:
        # Гарантированная очистка
        await self._cleanup_microphone_state(session_id, reason=f"activation_error: {e}")
        return False
    finally:
        # Дополнительная проверка состояния
        if not self._verify_microphone_state():
            await self._force_cleanup_microphone()
```

---

## 📊 Сравнительная таблица: текущее vs правильное

| Аспект | Текущее состояние | Правильное состояние |
|--------|-------------------|----------------------|
| **Источник истины состояния** | Разбросан по 3+ местам | Только `ApplicationStateManager` |
| **Гарантия освобождения AVF** | Пауза 0.2-0.5 сек (ненадежно) | Активное ожидание `is_input_active == False` |
| **Обработка session_id=None** | Поздняя проверка | Ранняя проверка ПЕРЕД установкой состояния |
| **Защита callback'ов** | Флаг `_google_recording_active` | Атомарная проверка с lock |
| **Выбор устройства** | Поиск по имени (ненадежно) | Кэшированное соответствие через `AudioCoreIntegration` |
| **Обработка ошибок** | Частичная очистка | Гарантированная очистка в finally |

---

## 🎯 План действий

### Этап 1: Критические исправления (1-2 дня)
1. ✅ Реализовать `_wait_for_avf_deactivation()` с активным ожиданием
2. ✅ Удалить локальные флаги состояния, использовать только `ApplicationStateManager`
3. ✅ Добавить атомарные операции с lock для установки флагов

### Этап 2: Высокие исправления (1 день)
1. ✅ Ранняя проверка `session_id=None` ПЕРЕД установкой состояния
2. ✅ Улучшенная защита callback'ов от обработки после остановки

### Этап 3: Средние улучшения (1 день)
1. ✅ Единый источник истины для выбора устройства через `AudioCoreIntegration`
2. ✅ Улучшенная обработка ошибок с гарантированной очисткой

---

## 📝 Выводы

### Основные проблемы:
1. **Разбросанное состояние** - нет единого источника истины
2. **Конфликт доступа** - AVF и Google пытаются использовать микрофон одновременно
3. **Отсутствие гарантий** - нет механизмов проверки освобождения микрофона
4. **Race conditions** - несинхронизированные операции с состоянием

### Критичность:
- **КРИТИЧЕСКАЯ**: Проблемы 1, 2 (блокируют работу микрофона)
- **ВЫСОКАЯ**: Проблемы 3, 4 (приводят к залипанию)
- **СРЕДНЯЯ**: Проблемы 5, 6 (влияют на стабильность)

### Рекомендуемый порядок исправления:
1. Сначала исправить критические проблемы (гарантия освобождения AVF, единый источник истины)
2. Затем высокие проблемы (обработка session_id=None, защита callback'ов)
3. В конце средние улучшения (выбор устройства, обработка ошибок)
