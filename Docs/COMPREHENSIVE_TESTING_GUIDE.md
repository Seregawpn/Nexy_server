# 🧪 Комплексное руководство по тестированию серверной части

**Дата:** 11 января 2026  
**Версия:** 1.0  
**Статус:** ✅ Активно используется

---

## 📋 Обзор

Этот документ описывает полный процесс тестирования серверной части Nexy для обеспечения работоспособности всех компонентов.

---

## 🎯 Уровни тестирования

### **1. Unit тесты** (Модульные тесты)
- Тестирование отдельных компонентов изолированно
- Быстрые, детерминированные
- Покрытие: модули, адаптеры, утилиты

### **2. Integration тесты** (Интеграционные тесты)
- Тестирование взаимодействия между компонентами
- Проверка интеграций между модулями
- Тестирование workflow интеграций

### **3. E2E тесты** (End-to-End тесты)
- Полный цикл от клиента до ответа
- Реальные gRPC запросы
- Проверка всего пайплайна

### **4. Smoke тесты** (Дымовые тесты)
- Быстрая проверка базовой работоспособности
- Проверка критических путей
- Используется для CI/CD

### **5. Contract тесты** (Контрактные тесты)
- Проверка соответствия gRPC контракту
- Валидация форматов данных
- Проверка версий protobuf

---

## 🚀 Быстрый старт

### **Полное тестирование (рекомендуется):**

```bash
# Запуск всех тестов автоматически
python server/scripts/run_all_tests.py
```

### **Пошаговое тестирование:**

```bash
# 1. Unit тесты
pytest server/tests/ -v

# 2. Smoke тесты
python server/scripts/grpc_smoke.py nexy-server.canadacentral.cloudapp.azure.com 443

# 3. Health checks
python server/scripts/check_grpc_health.py nexy-server.canadacentral.cloudapp.azure.com 443

# 4. E2E тесты
python server/scripts/test_full_pipeline_e2e.py
```

---

## 📊 Детальный план тестирования

### **1. Unit тесты (pytest)**

#### **Запуск:**
```bash
# Все unit тесты
pytest server/tests/ -v

# Конкретный файл
pytest server/tests/test_pr2_1_coordinator.py -v

# Конкретный тест
pytest server/tests/test_pr2_1_coordinator.py::TestModuleCoordinatorIntegration::test_coordinator_registration -v
```

#### **Что тестируется:**

**test_pr2_1_coordinator.py:**
- ✅ Регистрация модулей в ModuleCoordinator
- ✅ Получение модулей по capability
- ✅ Статус координатора
- ✅ Очистка модулей

**test_pr2_module_interface.py:**
- ✅ UniversalModuleInterface реализация
- ✅ Инициализация модулей
- ✅ Обработка запросов
- ✅ Статус модулей

**test_config_validation.py:**
- ✅ Валидация unified_config
- ✅ Проверка обязательных полей
- ✅ Проверка типов данных
- ✅ Проверка диапазонов значений

**test_config_drift.py:**
- ✅ Проверка расхождений конфигурации
- ✅ Синхронизация версий
- ✅ Проверка соответствия YAML и Python конфигов

**test_assistant_response_parser.py:**
- ✅ Парсинг JSON ответов от LLM
- ✅ Извлечение команд и текста
- ✅ Валидация форматов
- ✅ Обработка ошибок парсинга

**test_grpc_mcp_integration.py:**
- ✅ Интеграция MCP команд
- ✅ Формирование command_payload
- ✅ Передача через gRPC

**test_streaming_workflow_mcp.py:**
- ✅ Streaming workflow с MCP
- ✅ Потоковая обработка команд
- ✅ Интеграция с модулями

**test_memory_single_call_smoke.py:**
- ✅ Проверка единого вызова памяти
- ✅ Отсутствие дублирования
- ✅ Правильная передача hardware_id

**test_secret_masking.py:**
- ✅ Маскирование секретов в логах
- ✅ Безопасность конфигурации

---

### **2. Smoke тесты**

#### **gRPC Smoke Test:**
```bash
# Production сервер
python server/scripts/grpc_smoke.py nexy-server.canadacentral.cloudapp.azure.com 443

# Локальный сервер
python server/scripts/grpc_smoke.py localhost 50051
```

**Что проверяет:**
- ✅ Подключение к gRPC серверу
- ✅ RPC метод `InterruptSession`
- ✅ RPC метод `StreamAudio`
- ✅ Базовый streaming функционал
- ✅ Валидация ответов

**Ожидаемый результат:**
```
✅ Подключение к nexy-server.canadacentral.cloudapp.azure.com:443 установлено
🧪 Тестирование InterruptSession...
✅ InterruptSession успешен: success=True
🧪 Тестирование StreamAudio...
   → Получен ответ: text_chunk
   → Получен ответ: audio_chunk
✅ StreamAudio успешен: получено 3 ответов
✅ Все smoke-тесты прошли успешно
```

---

### **3. Health/Status Checks**

#### **Запуск:**
```bash
# Production сервер
python server/scripts/check_grpc_health.py nexy-server.canadacentral.cloudapp.azure.com 443

# Локальный сервер
python server/scripts/check_grpc_health.py localhost 8080
```

**Что проверяет:**
- ✅ `/health` endpoint (HTTP 200)
- ✅ `/status` endpoint (HTTP 200)
- ✅ Согласованность версий
- ✅ Формат версий (строки)
- ✅ `/updates/health` endpoint
- ✅ Доступность портов

**Ожидаемый результат:**
```
✅ Health endpoint доступен: HTTP 200
✅ Health содержит версии: 1.6.0.35
✅ Status endpoint доступен: HTTP 200
✅ Status возвращает JSON
✅ Версии согласованы: 1.6.0.35
✅ Updates health endpoint доступен: HTTP 200
✅ Все проверки пройдены успешно!
```

---

### **4. E2E тесты (End-to-End)**

#### **Полный пайплайн:**
```bash
python server/scripts/test_full_pipeline_e2e.py
```

**Что проверяет:**
- ✅ gRPC запрос → обработка
- ✅ LLM ответ → текст
- ✅ Edge TTS → аудио
- ✅ gRPC ответ → клиент
- ✅ Формат данных
- ✅ Потоковая передача

#### **Полная цепочка:**
```bash
python server/scripts/test_full_chain_client.py
```

**Что проверяет:**
- ✅ Обычный текст → клиент
- ✅ JSON команда → клиент
- ✅ Формат данных для gRPC
- ✅ Обработка session_id

#### **MCP интеграция:**
```bash
python server/scripts/test_mcp_full_integration.py
```

**Что проверяет:**
- ✅ Парсинг MCP команд
- ✅ Формирование command_payload
- ✅ Передача через gRPC
- ✅ Feature flag проверка

---

### **5. Специализированные тесты**

#### **Backpressure:**
```bash
python server/scripts/test_backpressure.py nexy-server.canadacentral.cloudapp.azure.com 443
```

**Что проверяет:**
- ✅ Лимит на стримы (max_concurrent_streams)
- ✅ Rate limiting (max_message_rate_per_second)
- ✅ Idle timeout
- ✅ Правильные коды ошибок

#### **Interceptor:**
```bash
python server/scripts/test_interceptor_errors.py nexy-server.canadacentral.cloudapp.azure.com 443
```

**Что проверяет:**
- ✅ Обработка ошибок в interceptor
- ✅ Логирование ошибок
- ✅ Метрики ошибок
- ✅ Правильные gRPC статусы

#### **Welcome Audio:**
```bash
python server/scripts/test_welcome_audio.py
```

**Что проверяет:**
- ✅ Генерация приветственного аудио
- ✅ Формат аудио данных
- ✅ Метаданные (sample_rate, channels, dtype)

---

### **6. Contract тесты**

#### **gRPC Contract:**
```bash
python server/scripts/grpc_contract_tests.py nexy-server.canadacentral.cloudapp.azure.com 443
```

**Что проверяет:**
- ✅ Соответствие protobuf контракту
- ✅ Валидация типов данных
- ✅ Обязательные поля
- ✅ Версии protobuf

---

### **7. Chaos тесты**

#### **Chaos Smoke:**
```bash
python server/scripts/chaos_smoke.py nexy-server.canadacentral.cloudapp.azure.com 443
```

**Что проверяет:**
- ✅ Устойчивость к ошибкам
- ✅ Обработка таймаутов
- ✅ Восстановление после ошибок
- ✅ Graceful degradation

---

## 🔧 Автоматизированное тестирование

### **Скрипт для полного тестирования:**

Создан скрипт `server/scripts/run_all_tests.py` который автоматически запускает все тесты в правильном порядке.

**Использование:**
```bash
# Полное тестирование
python server/scripts/run_all_tests.py

# Тестирование конкретного уровня
python server/scripts/run_all_tests.py --level unit
python server/scripts/run_all_tests.py --level integration
python server/scripts/run_all_tests.py --level e2e
```

---

## 📊 Чеклист тестирования перед релизом

### **Обязательные проверки:**

- [ ] **Unit тесты:** Все тесты проходят (`pytest server/tests/ -v`)
- [ ] **Smoke тесты:** gRPC работает (`grpc_smoke.py`)
- [ ] **Health checks:** Все endpoints доступны (`check_grpc_health.py`)
- [ ] **E2E тесты:** Полный пайплайн работает (`test_full_pipeline_e2e.py`)
- [ ] **Backpressure:** Лимиты работают (`test_backpressure.py`)
- [ ] **Contract тесты:** Контракт соблюдается (`grpc_contract_tests.py`)
- [ ] **Версии:** Все версии синхронизированы
- [ ] **Логи:** Нет критических ошибок в логах

### **Рекомендуемые проверки:**

- [ ] **Chaos тесты:** Устойчивость к ошибкам
- [ ] **Performance:** Время ответа в пределах нормы
- [ ] **Memory:** Нет утечек памяти
- [ ] **Load:** Нагрузочное тестирование

---

## 🎯 Приоритеты тестирования

### **Критический путь (обязательно):**
1. ✅ Health/Status endpoints
2. ✅ gRPC подключение
3. ✅ StreamAudio метод
4. ✅ InterruptSession метод
5. ✅ Версии синхронизированы

### **Важный путь (рекомендуется):**
1. ✅ Полный E2E пайплайн
2. ✅ Backpressure лимиты
3. ✅ MCP интеграция
4. ✅ Welcome audio

### **Дополнительный путь (опционально):**
1. ⚪ Chaos тесты
2. ⚪ Performance тесты
3. ⚪ Load тесты

---

## 📝 Примеры использования

### **Быстрая проверка:**
```bash
# Только smoke тесты (1-2 минуты)
python server/scripts/grpc_smoke.py nexy-server.canadacentral.cloudapp.azure.com 443
python server/scripts/check_grpc_health.py nexy-server.canadacentral.cloudapp.azure.com 443
```

### **Полная проверка:**
```bash
# Все тесты (10-15 минут)
python server/scripts/run_all_tests.py
```

### **Проверка перед деплоем:**
```bash
# Критический путь
pytest server/tests/ -v
python server/scripts/grpc_smoke.py nexy-server.canadacentral.cloudapp.azure.com 443
python server/scripts/check_grpc_health.py nexy-server.canadacentral.cloudapp.azure.com 443
python server/scripts/test_backpressure.py nexy-server.canadacentral.cloudapp.azure.com 443
```

---

## 🔍 Интерпретация результатов

### **✅ Успешный результат:**
- Все тесты проходят
- Нет ошибок в логах
- Health checks возвращают 200 OK
- Версии согласованы

### **⚠️ Предупреждения:**
- Некоторые необязательные тесты не прошли
- Предупреждения в логах (не критично)
- Rate limiting (нормально для free tier)

### **❌ Критические ошибки:**
- Health checks не проходят
- gRPC подключение не устанавливается
- Критические ошибки в логах
- Несоответствие версий

---

## 🚨 Troubleshooting

### **Проблемы с подключением:**
- Проверьте, что сервер запущен
- Проверьте firewall/security groups
- Для HTTPS (443) проверьте SSL сертификат

### **Проблемы с импортами:**
```bash
# Установите зависимости
pip install -r server/requirements.txt

# Проверьте PYTHONPATH
export PYTHONPATH="${PYTHONPATH}:$(pwd)/server"
```

### **Проблемы с protobuf:**
```bash
# Регенерируйте protobuf файлы
cd server/modules/grpc_service
python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. streaming.proto
```

---

## 📚 Дополнительные ресурсы

- **Документация тестов:** `server/tests/RUN_TESTS.md`
- **gRPC Protocol:** `server/Docs/GRPC_PROTOCOL_AUDIT.md`
- **Architecture:** `server/Docs/ARCHITECTURE_OVERVIEW.md`
- **Development Rules:** `server/Docs/SERVER_DEVELOPMENT_RULES.md`

---

## ✅ Итог

**Для полного тестирования серверной части:**

1. ✅ Запустите unit тесты (`pytest server/tests/ -v`)
2. ✅ Запустите smoke тесты (`grpc_smoke.py`)
3. ✅ Проверьте health endpoints (`check_grpc_health.py`)
4. ✅ Запустите E2E тесты (`test_full_pipeline_e2e.py`)
5. ✅ Проверьте backpressure (`test_backpressure.py`)

**Или используйте автоматический скрипт:**
```bash
python server/scripts/run_all_tests.py
```
