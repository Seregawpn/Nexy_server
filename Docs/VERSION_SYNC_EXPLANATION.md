# ✅ Автоматическая синхронизация версий - Объяснение

**Дата:** 11 января 2026

---

## 🎯 Ответ: **ДА, версия будет корректна автоматически!**

При изменении версии в **одном месте** (`VERSION` файл), все компоненты автоматически получат новую версию при следующем запуске сервера.

---

## 📍 Единый источник истины

**Файл:** `VERSION` (в корне проекта)  
**Текущая версия:** `1.6.0.35`

---

## 🔄 Как работает автоматическая синхронизация

### **При запуске сервера:**

Все конфигурации **динамически** читают версию из `VERSION` файла:

1. **UpdateConfig** → использует `get_version_from_file()` ✅
2. **UpdateServiceConfig** → использует `get_version_from_file()` ✅  
3. **ServerMetadataConfig** → использует `get_version_from_file()` ✅
4. **main.py** → получает из `server_metadata.version` ✅

**Все компоненты синхронизированы автоматически!**

---

## ⚠️ Важное замечание о приоритетах

### **Приоритет получения версии:**

1. **VERSION файл** (если существует) - основной источник ✅
2. Переменная окружения `SERVER_VERSION` (для переопределения)
3. Дефолтное значение (только если ничего не найдено)

### **Если в `config.env` указана `SERVER_VERSION`:**

- `ServerMetadataConfig.from_env()` будет использовать переменную окружения (приоритет выше)
- `UpdateConfig` всегда использует `get_version_from_file()`, который сначала проверяет VERSION файл

**Рекомендация:** Синхронизируйте `SERVER_VERSION` в `config.env` с VERSION файлом, или используйте только VERSION файл.

---

## ✅ Что обновляется автоматически

### **При изменении VERSION файла:**

✅ **UpdateConfig.default_version** - автоматически  
✅ **UpdateConfig.default_build** - автоматически  
✅ **UpdateServiceConfig.default_version** - автоматически  
✅ **UpdateServiceConfig.default_build** - автоматически  
✅ **ServerMetadataConfig.version** - автоматически (если нет SERVER_VERSION в env)  
✅ **ServerMetadataConfig.build** - автоматически (если нет SERVER_BUILD в env)  
✅ **main.py SERVER_VERSION** - автоматически  
✅ **main.py SERVER_BUILD** - автоматически  

### **Требуют ручного обновления:**

⚠️ **Манифесты** (`server/updates/manifests/manifest.json`)  
   - Обновляются через `sync_version_to_server.sh` или при деплое  
   - Содержат дополнительную информацию (URL, размер, SHA256)

⚠️ **YAML и example файлы**  
   - Обновляются через `update_version.py`  
   - Используются только как примеры, не критично для работы

---

## 🚀 Как изменить версию

### **Вариант 1: Прямое изменение VERSION файла**

```bash
echo "1.6.0.36" > VERSION
```

**Результат:**
- ✅ Все конфигурации получат новую версию при следующем запуске
- ⚠️ Манифесты нужно обновить отдельно

### **Вариант 2: Использовать скрипт (рекомендуется)**

```bash
python3 server/scripts/update_version.py 1.6.0.36
```

**Результат:**
- ✅ VERSION файл обновлен
- ✅ unified_config.yaml обновлен
- ✅ unified_config.py обновлен
- ✅ config.env.example обновлен
- ⚠️ Манифесты нужно обновить отдельно

---

## 📊 Проверка синхронизации

```bash
# Проверить версию в VERSION файле
cat VERSION

# Проверить версию в конфигурациях
python3 -c "
from server.config.unified_config import get_config
from server.modules.update.config import UpdateConfig

config = get_config()
update_config = UpdateConfig()

print(f'VERSION файл: 1.6.0.35')
print(f'ServerMetadata: {config.server.version}')
print(f'UpdateServiceConfig: {config.get_update_service_config().default_version}')
print(f'UpdateConfig: {update_config.default_version}')
"
```

---

## 🎯 Итог

**Ответ:** ✅ **ДА!**

При изменении версии в `VERSION` файле:
- ✅ Все конфигурации автоматически получат новую версию **при следующем запуске сервера**
- ✅ Не нужно вручную обновлять конфигурации
- ✅ Все компоненты синхронизированы через `get_version_from_file()`
- ⚠️ Манифесты обновляются отдельно (но это нормально)

**Система полностью централизована и работает автоматически!** 🎉
