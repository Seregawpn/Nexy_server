# 📋 Следующие шаги для решения проблемы gRPC

**Дата:** 13 января 2026  
**Статус:** 🔴 Проблема требует дополнительного исследования

---

## ✅ Что уже сделано

1. ✅ Исправлен порядок location блоков в Nginx
2. ✅ Nginx пересобран с модулем grpc
3. ✅ Зависимости обновлены до последних версий:
   - `grpcio>=1.76.0` ✅
   - `grpcio-tools>=1.76.0` ✅
   - `protobuf>=6.33.3,<7.0.0` ✅ (установлено 6.33.4)
4. ✅ Protobuf файлы пересгенерированы
5. ✅ Сервис перезапущен

---

## ❌ Текущая проблема

**Ошибка:** `Trying to connect an http1.x server (HTTP status 400)`

**В логах Nginx:**
```
"PRI * HTTP/2.0" 400 157
```

**Что это значит:**
- gRPC клиент отправляет HTTP/2 connection preface
- Nginx возвращает 400 ошибку
- Запрос не доходит до gRPC сервера

---

## 🔍 Возможные причины (требуют проверки)

### 1. Проблема с обработкой HTTP/2 preface на корневом пути `/`

**Гипотеза:** Nginx может неправильно обрабатывать HTTP/2 preface для gRPC на корневом пути.

**Решение:** Использовать отдельный путь для gRPC (например, `/grpc/`)

### 2. Проблема с SSL/TLS handshake

**Гипотеза:** Клиент использует `insecure_channel`, но сервер может требовать правильный TLS handshake.

**Решение:** Проверить, нужен ли secure_channel вместо insecure_channel

### 3. Проблема с версией gRPC клиента

**Гипотеза:** Несовместимость версий gRPC библиотек между клиентом и сервером.

**Решение:** Убедиться, что клиент использует те же версии библиотек

### 4. Проблема с конфигурацией upstream

**Гипотеза:** Backend сервер недоступен или неправильно настроен.

**Решение:** Проверить доступность `127.0.0.1:50051` напрямую

---

## 🎯 Рекомендуемые следующие шаги

### Шаг 1: Проверить прямую доступность gRPC сервера

```bash
# На сервере
curl -v http://127.0.0.1:50051
# или
grpcurl -plaintext 127.0.0.1:50051 list
```

### Шаг 2: Попробовать использовать secure_channel вместо insecure_channel

```python
# В тестовом скрипте
credentials = grpc.ssl_channel_credentials()
channel = aio.secure_channel("nexy-prod-sergiy.canadacentral.cloudapp.azure.com:443", credentials)
```

### Шаг 3: Использовать отдельный путь для gRPC

Если корневой путь `/` вызывает проблемы, использовать `/grpc/` или другой путь.

### Шаг 4: Проверить логи gRPC сервера

```bash
sudo journalctl -u voice-assistant.service -n 100 | grep -i grpc
```

### Шаг 5: Использовать специализированный gRPC прокси

Если Nginx продолжает вызывать проблемы, рассмотреть:
- Envoy Proxy
- HAProxy с поддержкой gRPC
- Прямое подключение к gRPC серверу через VPN/SSH туннель

---

## 📝 Альтернативные решения

### Вариант 1: Прямое подключение к gRPC серверу

Если Nginx не может правильно обработать gRPC, можно:
- Открыть порт 50051 напрямую (не рекомендуется для production)
- Использовать SSH туннель для доступа
- Использовать VPN для безопасного доступа

### Вариант 2: Использовать Envoy Proxy

Envoy Proxy имеет отличную поддержку gRPC и может работать перед Nginx или вместо него.

### Вариант 3: Использовать отдельный поддомен для gRPC

Настроить отдельный поддомен (например, `grpc.example.com`) специально для gRPC трафика.

---

## 🔗 Полезные ссылки

- [Nginx gRPC Module Documentation](https://nginx.org/en/docs/http/ngx_http_grpc_module.html)
- [gRPC over HTTP/2](https://grpc.io/docs/guides/http2/)
- [Troubleshooting gRPC](https://grpc.io/docs/guides/troubleshooting/)

---

**Статус:** 🟡 Требуется дополнительное исследование и тестирование альтернативных подходов
