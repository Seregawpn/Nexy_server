# Описание CRM системы Nexy

**Версия:** 2.3  
**Статус:** Active Control Enabled (Federated)  
**Дата обновления:** 2026-01-08

---

## 📋 Содержание

1. [Общее описание](#общее-описание)
2. [Архитектура системы](#архитектура-системы)
3. [Компоненты системы](#компоненты-системы)
4. [Источники истины](#источники-истины)
5. [Workflow и процессы](#workflow-и-процессы)
6. [Federated Workspaces](#federated-workspaces)
7. [Безопасность](#безопасность)
8. [API и скрипты](#api-и-скрипты)
9. [Интеграция с ассистентами](#интеграция-с-ассистентами)

---

## Общее описание

**Nexy CRM** — это локальная система управления задачами (Kanban), разработанная специально для проекта Nexy. Система предназначена для:

- **Управления задачами** проекта через Kanban-доску
- **Отслеживания прогресса** выполнения задач через чек-листы
- **Координации работы** нескольких AI-ассистентов (Cursor, Codex, Antigravity)
- **Автоматизации релизов** с генерацией CHANGELOG
- **Децентрализованного управления** задачами через Federated Workspaces

### Ключевые особенности

- ✅ **Локальная работа**: Все данные хранятся локально, без внешних зависимостей
- ✅ **Web UI**: Современный интерфейс на базе HTML/JavaScript
- ✅ **Автоматическая синхронизация**: Фоновый процесс каждые 5 минут
- ✅ **Безопасность**: Защита от path traversal и shell injection
- ✅ **Federated Workspaces**: Поддержка децентрализованного управления задачами
- ✅ **Assistant Tracking**: Отслеживание авторов и редакторов задач

---

## Архитектура системы

### Общая схема

```
┌─────────────────────────────────────────────────────────────┐
│                    CRM System Architecture                   │
└─────────────────────────────────────────────────────────────┘

┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│   Client    │         │    Server    │         │    Master    │
│  Workspace  │         │  Workspace   │         │  Workspace   │
│             │         │              │         │              │
│ .crm/       │         │ .crm/        │         │ Docs/        │
│ TASKS.json  │         │ TASKS.json   │         │ PROJECT_     │
│ (CLI-XXX)   │         │ (SRV-XXX)    │         │ KANBAN.json  │
└──────┬───────┘         └──────┬───────┘         └──────┬───────┘
       │                       │                        │
       │                       │                        │
       └───────────────────────┼────────────────────────┘
                               │
                    ┌──────────▼──────────┐
                    │  task_aggregator.py  │
                    │  (Merge Workspaces)  │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │  PROJECT_KANBAN.json│
                    │   (Master Data)     │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │    server.py        │
                    │  (Bridge Server)    │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │   Browser UI        │
                    │  localhost:8000     │
                    └─────────────────────┘
```

### Компоненты

1. **Bridge Server** (`scripts/server.py`): HTTP-сервер, обслуживающий UI и API
2. **Task Aggregator** (`scripts/task_aggregator.py`): Синхронизация задач из workspace
3. **Progress Collector** (`scripts/kanban_progress_collector.py`): Обновление прогресса из чек-листов
4. **Web UI** (`Docs/PROJECT_KANBAN.html`): Интерфейс Kanban-доски
5. **Master Data** (`Docs/PROJECT_KANBAN.json`): Единый источник истины для задач

---

## Компоненты системы

### 1. Bridge Server (`scripts/server.py`)

**Назначение**: HTTP-сервер, обеспечивающий взаимодействие между UI и данными.

**Функции**:
- Обслуживание Web UI (`/`)
- API для чтения данных (`/api/data`)
- API для сохранения данных (`/api/save`)
- API для открытия файлов (`/api/open_file`)
- API для создания релизов (`/api/create_release`)
- Автоматическая синхронизация через `task_aggregator.py` (каждые 5 минут)

**Порт**: 8000  
**Запуск**: `python3 scripts/server.py`

### 2. Task Aggregator (`scripts/task_aggregator.py`)

**Назначение**: Объединение задач из разных workspace в мастер-план.

**Правила слияния**:
1. **Ключ**: `id` + `workspace`
2. **Обновление**: Если локальный `updated_at` > мастер → обновить мастер
3. **Создание**: Новые ID добавляются
4. **Защита**: Мастер-карточки без workspace защищены от удаления
5. **Изоляция**: Каждый workspace влияет только на свои карточки

**Workspaces**:
- `client/.crm/TASKS.json` → префикс `CLI-`
- `server/.crm/TASKS.json` → префикс `SRV-`
- Master (`.crm/TASKS.json` или `Docs/PROJECT_KANBAN.json`) → префикс `NEXY-`

### 3. Progress Collector (`scripts/kanban_progress_collector.py`)

**Назначение**: Автоматическое обновление прогресса задач из чек-листов в документах.

**Механизм работы**:
1. Читает `CRM_INSTRUCTION_REGISTRY.md` для получения маппинга `INS-XXX` → путь к файлу
2. Для каждой задачи с полем `registry_ref` (например, `INS-007`):
   - Находит соответствующий файл документа
   - Подсчитывает выполненные (`[x]`) и невыполненные (`[ ]`) чек-листы
   - Обновляет поле `progress` (процент) и `evidence` (список выполненных пунктов)

**Формат чек-листа**:
```markdown
- [x] Шаг 1 выполнен
- [x] Шаг 2 выполнен
- [ ] Шаг 3 в работе
```

### 4. Web UI (`Docs/PROJECT_KANBAN.html`)

**Назначение**: Визуальный интерфейс для управления задачами.

**Функции**:
- Kanban-доска с колонками статусов
- Фильтрация по workspace (All, Client, Server, Master)
- Фильтрация по Epic
- Открытие файлов через кнопку "Open File"
- Создание и редактирование задач
- Автоматизация релизов ("Ship Release")

**Статусы задач**:
- `plan` (План) - 📋
- `in_progress` (В процессе) - 🔨
- `testing` (Тестируется) - 🧪
- `released` (Готово к релизу) - ✅

**Приоритеты**:
- `P0` (Critical) - 🟣
- `P1` (High) - 🔴
- `P2` (Medium) - 🟡
- `P3` (Low) - 🟢

### 5. Master Data (`Docs/PROJECT_KANBAN.json`)

**Назначение**: Единый источник истины для всех задач проекта.

**Структура**:
```json
{
  "meta": {
    "version": "2.1.0",
    "updated_at": "2026-01-08T10:14:13.812403"
  },
  "config": {
    "blocking_mode": "warning",
    "statuses": [...],
    "priorities": {...}
  },
  "epics": [...],
  "cards": [...],
  "releases": [...]
}
```

**Обязательные поля**:
- `meta` - метаданные версии
- `config` - конфигурация статусов и приоритетов
- `epics` - список Epic'ов
- `cards` - список задач (карточек)
- `releases` - история релизов

---

## Источники истины

| Артефакт | Путь | Назначение |
|:---------|:-----|:-----------|
| **Master Data** | `Docs/PROJECT_KANBAN.json` | Epics, Cards, Releases |
| **Registry** | `Docs/CRM_INSTRUCTION_REGISTRY.md` | Индекс инструкций (INS-XXX) |
| **Standard** | `Docs/CRM_MASTER_INSTRUCTION.md` | Шаблон документации |
| **Assistant Guide** | `Docs/CRM_ASSISTANT_INSTRUCTIONS.md` | Как ассистенты создают задачи |
| **Consolidated Rules** | `Docs/CRM_CONSOLIDATED_RULES.md` | Единый свод правил |
| **User Guide** | `Docs/CRM_USER_GUIDE.md` | Руководство пользователя |

### Иерархия приоритетов

1. **CRM_CONSOLIDATED_RULES.md** — единый свод правил (высший приоритет)
2. **CODEX_PROMPT.md / ANTIGRAVITY_PROMPT.md** — правила для конкретных ассистентов
3. **CRM_ASSISTANT_INSTRUCTIONS.md** — краткая инструкция (entrypoint)

---

## Workflow и процессы

### 1. Создание задачи

**Процесс**:
1. Ассистент создает задачу в соответствующем workspace:
   - `client/.crm/TASKS.json` для клиентских задач (префикс `CLI-`)
   - `server/.crm/TASKS.json` для серверных задач (префикс `SRV-`)
   - `.crm/TASKS.json` или `Docs/PROJECT_KANBAN.json` для мастер-задач (префикс `NEXY-`)
2. Заполняет обязательные поля:
   - `id` - уникальный ID с префиксом
   - `created_by` - имя ассистента
   - `modified_by` - имя ассистента
   - `updated_at` - ISO 8601 datetime
3. Опциональные поля:
   - `workspace` - территория кода
   - `epicId` - ссылка на Epic
   - `priority` - приоритет (P0-P3)
   - `status` - статус (plan, in_progress, testing, released)
   - `description` - описание проблемы и прогресса
   - `registry_ref` - ссылка на INS-XXX для синхронизации прогресса
   - `file_path` - путь к файлу для "Open File"
   - `subtasks` - массив подзадач
   - `responsible_docs` - ответственные документы

**Минимальный формат**:
```json
{
  "id": "CLI-002",
  "title": "Краткое описание",
  "status": "plan",
  "created_by": "Codex",
  "modified_by": "Codex",
  "updated_at": "2026-01-05T19:47:00-05:00",
  "workspace": "client"
}
```

### 2. Обновление статуса

**ОБЯЗАТЕЛЬНО при изменении**:
1. Обновить `status` при переходе
2. Обновить `modified_by`: имя ассистента
3. Обновить `updated_at`: текущее время
4. **НЕ менять** `created_by`

**Переходы статусов**:
- `plan` → `in_progress` - начало работы
- `in_progress` → `testing` - готово к тестированию
- `testing` → `released` - готово к релизу
- `released` → архив (через "Ship Release")

### 3. Описание проблемы и прогресса

**Формат в поле `description`**:
```markdown
## Проблема

[Описание проблемы или контекста]

## Текущий прогресс

- [x] Шаг 1 выполнен
- [x] Шаг 2 выполнен
- [ ] Шаг 3 в работе

## Блокеры

- Заблокировано задачей NEXY-001
- Ожидаем решение по архитектуре

## Следующие шаги

1. Реализовать X
2. Протестировать Y
3. Интегрировать с Z
```

### 4. Синхронизация прогресса

**Процесс**:
1. Задача содержит поле `registry_ref` (например, `INS-007`)
2. `kanban_progress_collector.py` читает `CRM_INSTRUCTION_REGISTRY.md`
3. Находит соответствующий файл документа
4. Подсчитывает чек-листы (`[x]` vs `[ ]`)
5. Обновляет `progress` (%) и `evidence` (список выполненных пунктов)

**Запуск**:
```bash
python3 scripts/kanban_progress_collector.py
```

### 5. Автоматизация релизов

**Процесс**:
1. Все задачи в колонке "Готово к релизу" (Released)
2. Нажать кнопку `🚀 Ship Release` в UI
3. Ввести версию (например, `v1.2.0`)
4. **Автоматически**:
   - Все карточки перемещаются в архив `PROJECT_KANBAN.json`
   - Создается запись в `Docs/CHANGELOG.md`

---

## Federated Workspaces

### Концепция

Система поддерживает **децентрализованное управление задачами** через отдельные workspace для разных частей проекта.

### Структура

```
Fix_new/
├── client/.crm/TASKS.json   ← CLI-XXX tasks
├── server/.crm/TASKS.json   ← SRV-XXX tasks
└── Docs/PROJECT_KANBAN.json ← Master (aggregated)
```

### Правила

1. **Префиксы ID**:
   - Master: `NEXY-`
   - Client: `CLI-`
   - Server: `SRV-`

2. **Изоляция**: Каждый workspace влияет только на свои карточки

3. **Авто-агрегация**: Сервер раз в 5 минут запускает `task_aggregator.py`

4. **Синхронизация**: Изменения из UI синхронизируются обратно в workspace через поле `owner`

5. **Фильтрация**: В UI можно фильтровать задачи по workspace (All, Client, Server, Master)

### Merge Rules

1. **Ключ**: `id` + `workspace`
2. **Обновление**: Если локальный `updated_at` > мастер → обновить мастер
3. **Создание**: Новые ID добавляются
4. **Защита**: Мастер-карточки без workspace защищены от удаления
5. **Изоляция**: Каждый workspace только влияет на свои карточки

---

## Безопасность

### Правила безопасности

| Правило | Реализация |
|:--------|:-----------|
| **No Shell Execution** | `shlex.split()` + `subprocess.run(list)` |
| **Path Traversal Block** | `os.path.normpath()` + `startswith(ROOT)` |
| **JSON Validation** | Schema check before `/api/save` |
| **No `/api/exec`** | Disabled by policy |

### Active Control ("Open File")

**Безопасность**:
- Открываются только файлы внутри папки проекта (Path Traversal Protection)
- Команды запускаются **без shell** (`subprocess.run(list)`), что защищает от инъекций
- Используется переменная `CRM_EDITOR_CMD` для выбора редактора

**Настройка**:
```bash
export CRM_EDITOR_CMD="code"  # или "cursor"
python3 scripts/server.py
```

---

## API и скрипты

### API Endpoints (`server.py`)

| Endpoint | Method | Description |
|:---------|:-------|:------------|
| `/` | GET | Serve UI |
| `/api/data` | GET | Return master JSON |
| `/api/save` | POST | Validate & save JSON |
| `/api/open_file` | POST | Open file in `CRM_EDITOR_CMD` |
| `/api/create_release` | POST | Archive cards, generate CHANGELOG |

### Скрипты

#### 1. `server.py` (Bridge Server)
- **Назначение**: HTTP-сервер для UI и API
- **Запуск**: `python3 scripts/server.py`
- **Порт**: 8000
- **Функции**: Обслуживание UI, API, автоматическая синхронизация

#### 2. `task_aggregator.py` (Task Aggregator)
- **Назначение**: Объединение задач из workspace в мастер-план
- **Запуск**: `python3 scripts/task_aggregator.py`
- **Автоматически**: Запускается сервером каждые 5 минут

#### 3. `kanban_progress_collector.py` (Progress Collector)
- **Назначение**: Обновление прогресса из чек-листов
- **Запуск**: `python3 scripts/kanban_progress_collector.py`
- **Механизм**: Читает `registry_ref` → находит файл → подсчитывает чек-листы

---

## Интеграция с ассистентами

### Assistant Tracking (Phase 5.1)

Каждая задача отслеживает:
- `created_by` - ассистент, создавший задачу
- `modified_by` - ассистент, последний раз редактировавший задачу

В UI карточки отображается значок ассистента (например, `🤖 Ant`).

### Правила работы для ассистентов

**Источник истины**: `Docs/CRM_ASSISTANT_INSTRUCTIONS.md`

**Основные правила**:
1. **Выбор workspace**: Master для задач всего проекта, Client/Server для специфичных задач
2. **Префикс ID**: Соответствует workspace (`NEXY-`, `CLI-`, `SRV-`)
3. **Не редактировать**: Задачи другого workspace без согласования
4. **Всегда указывать**: `created_by` и `modified_by`
5. **Инкрементировать**: `next_id` в файле после создания задачи
6. **Epic**: Создается только через Fix_new (главный CRM)
7. **Описывать прогресс**: При каждом обновлении задачи
8. **Использовать подзадачи**: Для разбиения больших задач
9. **Распределять подзадачи**: Между workspace для параллельной работы
10. **Проверять блокеры**: Перед началом работы над задачей
11. **Указывать ответственные документы**: Для понимания контекста и следования правилам

### Workflow: Начало работы над задачей

1. **Просмотр активных задач**:
   - Открыть UI: `http://localhost:8000/`
   - Или проверить `client/.crm/TASKS.json` / `server/.crm/TASKS.json` / `.crm/TASKS.json`

2. **Выбор задачи**:
   - Найти задачу со статусом `in_progress` или `plan`
   - Проверить блокеры (`blockedBy`) — не начинать заблокированные задачи
   - Прочитать описание (`description`) для понимания контекста
   - **Изучить ответственные документы** (`responsible_docs`)

3. **Обновление статуса**:
   - При начале работы: `plan` → `in_progress`
   - Обновить `description` с описанием того, что начали делать
   - Обновить `modified_by` и `updated_at`

4. **Работа над задачей**:
   - Создать подзадачи для разбиения работы
   - Обновлять `description` при каждом значимом прогрессе
   - Отмечать выполненные подзадачи

5. **Завершение**:
   - Завершить все подзадачи
   - Обновить `description` с итоговым описанием выполненной работы
   - Перевести статус: `in_progress` → `testing` или `released`

---

## Контракты и форматы

### JSON Schema (`PROJECT_KANBAN.json`)

```json
{
  "meta": {},      // Required
  "config": {},    // Required
  "epics": [],     // Required
  "cards": [],     // Required
  "releases": []   // Required
}
```

### Registry Format (`CRM_INSTRUCTION_REGISTRY.md`)

```markdown
| **INS-XXX** | Title | [File](./path) | Description |
```

- ID: `INS-XXX` (bold или plain)
- Path: Markdown link относительно `Docs/`

### Card Fields

| Field | Type | Required | Description |
|:------|:-----|:---------|:------------|
| `id` | string | ✅ | Unique ID (NEXY-XXX, CLI-XXX, SRV-XXX) |
| `workspace` | string | ❌ | Code territory: `client` / `server` / `null` (master) |
| `created_by` | string | ✅ | Assistant who created: `Antigravity` / `Cursor` / `Codex` |
| `modified_by` | string | ✅ | Assistant who last modified |
| `updated_at` | ISO date | ✅ | Last modification timestamp |
| `registry_ref` | string | ❌ | Link to INS-XXX for progress sync |
| `file_path` | string | ❌ | Relative path for "Open File" |
| `epicId` | string | ❌ | Link to Epic |
| `priority` | string | ❌ | P0 (Critical) / P1 (High) / P2 (Medium) / P3 (Low) |
| `status` | string | ❌ | plan / in_progress / testing / released |
| `description` | string | ❌ | Markdown description with problem/progress/blockers |
| `subtasks` | array | ❌ | Array of subtasks |
| `progress` | int | ❌ | Progress percentage (0-100) |
| `evidence` | array | ❌ | List of completed checklist items |
| `blockedBy` | array | ❌ | Array of blocking task IDs |
| `responsible_docs` | array | ❌ | Array of responsible documents |

---

## Troubleshooting

### 1. Сервер возвращает Error 400 при сохранении

**Причина**: Поврежденный JSON или отсутствие обязательных полей.

**Решение**: Проверить структуру данных согласно JSON Schema.

### 2. Open File не открывает редактор

**Причина**: Не установлена переменная `CRM_EDITOR_CMD`.

**Решение**: 
```bash
export CRM_EDITOR_CMD="code"  # или "cursor"
python3 scripts/server.py
```

### 3. Прогресс не обновляется

**Причина**: Неправильный формат `registry_ref` или отсутствие файла.

**Решение**: 
- Проверить, что `registry_ref` указан в задаче (например, `INS-007`)
- Проверить, что `INS-007` существует в `CRM_INSTRUCTION_REGISTRY.md`
- Проверить, что файл документа содержит чек-листы в формате `- [x]` / `- [ ]`

### 4. Задачи не синхронизируются между workspace

**Причина**: `task_aggregator.py` не запускается автоматически.

**Решение**: 
- Проверить, что сервер запущен (`python3 scripts/server.py`)
- Запустить вручную: `python3 scripts/task_aggregator.py`
- Проверить логи сервера на наличие ошибок

---

## Связанные документы

- **CRM_CONSOLIDATED_RULES.md** — единый свод правил
- **CRM_USER_GUIDE.md** — руководство пользователя
- **CRM_ASSISTANT_INSTRUCTIONS.md** — инструкция для ассистентов
- **CRM_INSTRUCTION_REGISTRY.md** — реестр инструкций
- **CRM_MASTER_INSTRUCTION.md** — шаблон документации

---

## История версий

- **v2.3** (2026-01-08): Active Control Enabled, Federated Workspaces, Assistant Tracking
- **v2.1.0**: Базовая функциональность Kanban, Progress Sync, Release Automation

---

**Примечание**: Этот документ описывает текущее состояние CRM системы. При изменении системы документ должен быть обновлен.
