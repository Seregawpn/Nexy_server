# Impact-Gate Checklist (автоматизированные проверки)

**Цель**: Автоматизировать проверки, чтобы изменения не проходили без соответствия правилам.

**См. также**: `.cursorrules` раздел 11 "Чеклист изменений" и `PRODUCTION_RULES_SUMMARY.md`.

---

## ✅ Автоматические проверки (CI)

### 1. Инвариант 4 артефактов ✅ РАБОТАЕТ

**Скрипт**: `scripts/verify_4_artifacts_invariant.py`  
**CI job**: `impact-gate` → `Check 4-artifacts invariant`

**Проверка**: Любое изменение оси состояния → обновлены:
1. ✅ `Docs/STATE_CATALOG.md`
2. ✅ `config/interaction_matrix.yaml`
3. ✅ `integration/core/gateways.py` (или `gateways/**/*.py`)
4. ✅ `tests/test_gateways.py` (≥8–14 pairwise + 2 негативных)

**Использование**:
```bash
python scripts/verify_4_artifacts_invariant.py update_in_progress restart_pending
```

**Ошибка**: Если ось не найдена в любом из 4 артефактов → CI фейлит.

**Ограничения**: Проверяет наличие по имени, но не семантическую связность (правило → gateway логика).

---

### 2. Покрытие правил тестами ✅ РАБОТАЕТ

**Скрипт**: `scripts/verify_rule_coverage.py`  
**CI job**: `impact-gate` → `Check rule coverage`

**Проверка**: Все правила из `interaction_matrix.yaml`:
- ✅ Имеют gateway (функция существует)
- ✅ Покрыты тестами (gateway упоминается в test_gateways.py)
- ✅ Приоритет соответствует тестам (hard_stop → abort, graceful → retry/degrade)

**Ошибка**: Если правило не покрыто тестом → CI фейлит.

**Ограничения**: Проверяет наличие gateway и упоминания в тестах, но не полную логику теста.

---

### 3. Feature Flags регистрация ✅ РАБОТАЕТ

**Скрипт**: `scripts/verify_feature_flags.py`  
**CI job**: `impact-gate` → `Check feature flags registration`

**Проверка**: Все флаги для first-run/permission-restart зарегистрированы в `Docs/FEATURE_FLAGS.md`:
- `NEXY_FEATURE_FIRST_RUN_V2`
- `NEXY_FEATURE_PERMISSION_RESTART_V2`
- `NEXY_KS_PERMISSION_RESTART_V2`
- `NEXY_DISABLE_AUTO_RESTART`

**Ошибка**: Если флаг не зарегистрирован → CI фейлит.

---

### 4. Запрет прямого доступа к состоянию ✅ РАБОТАЕТ

**Скрипт**: `scripts/verify_no_direct_state_access.py`  
**CI job**: `lint` → `Check state access`

**Проверка**: Нет прямого доступа к осям состояния вне selectors/gateways.

**Исключения**: `**/selectors.py`, `**/gateways.py`, `**/gateways/**/*.py`, `**/test*.py`

**Ошибка**: Если найден прямой доступ → CI фейлит.

**Ограничения**: Проверяет только строковые паттерны, не динамический доступ через переменные.

---

### 5. Валидация схем ✅ РАБОТАЕТ

**Тесты**: `tests/test_schemas.py`  
**CI job**: `schema-validate`

**Проверка**: `unified_config.yaml` и `interaction_matrix.yaml` соответствуют JSON Schema.

**Ошибка**: Если схема невалидна → CI фейлит.

---

### 6. Decision-логи в каноническом формате ✅ РАБОТАЕТ

**Тесты**: `tests/test_gateways.py::TestGatewayDecisionLogs` + `TestDecisionLogFormatValidation`  
**CI job**: `contracts-test` → `Run gateway tests`

**Проверка**: Все gateways логируют в формате:
```
decision=<start|abort|retry|degrade> ctx={mic=...,screen=...,device=...,network=...,firstRun=...,appMode=...} source=<domain> duration_ms=<int>
```

**Негативные тесты**: Проверяют, что тесты **падают** при нарушении формата.

**Ошибка**: Если формат нарушен → тесты фейлят.

---

### 7. Порядок инициализации интеграций ✅ РАБОТАЕТ

**Тесты**: `tests/test_init_order.py`  
**CI job**: `contracts-test` → `Test initialization order`

**Проверка**: 
- `first_run_permissions` на позиции 3 (0-based: 2)
- `permission_restart` на позиции 4 (0-based: 3), сразу после `first_run_permissions`

**Ошибка**: Если порядок нарушен → тесты фейлят.

---

### 8. Permission Restart Gateway тесты ✅ РАБОТАЕТ

**Тесты**: `tests/test_gateways.py::TestPermissionRestartGateway` + `TestPermissionRestartWithUpdates`  
**CI job**: `contracts-test` → `Run gateway tests`

**Проверка**:
- ✅ `update_in_progress=true` → `Decision.ABORT` (graceful)
- ✅ `update_in_progress=false` → `Decision.START` (happy path)
- ✅ `first_run_restart_pending=true` → `Decision.ABORT` (hard_stop)
- ✅ `update_available=true + respect_updates=true` → blocked (hard_stop)
- ✅ `update_in_progress=true` → delayed (graceful)
- ✅ `appMode!=SLEEPING + respect_active_sessions=true` → delayed (graceful)

**Ошибка**: Если логика нарушена → тесты фейлят.

---

### 9. Golden-лог тест ⚠️ ЧАСТИЧНО РАБОТАЕТ

**Тесты**: `tests/test_golden_first_run_logs.py`  
**CI job**: `golden-tests`

**Проверка**: Структура expected sequence определена корректно.

**TODO**: Реализовать валидацию реального лог-файла (требует log file).

**Ошибка**: Пока только проверяет структуру, не реальные логи.

---

### 10. SLO метрики ⚠️ ЧАСТИЧНО РАБОТАЕТ

**Тесты**: `tests/perf/test_slo_smoke.py`  
**CI job**: `perf-slo`

**Проверка**: SLO thresholds определены корректно.

**TODO**: Реализовать извлечение метрик из логов (требует log file).

**Ошибка**: Пока только проверяет структуру thresholds, не реальные метрики.

---

## 📋 Ручные проверки (перед мерджем)

### 11. Сверка со спеком логов первого запуска

**Документ**: `Docs/NEXY_FIRST_RUN_LOG_EXPECTED.md`

**Чек-лист**:
- [ ] Логи сверены со спецификацией
- [ ] Нет «проигрывания приветствия до рестарта» в старом процессе
- [ ] Нет публикации `permissions.first_run_completed` в старом процессе (только `restart_pending`)
- [ ] Нет логов CoreAudio о записи микрофона до перезапуска
- [ ] В новом процессе есть `Перезапуск после first_run завершён успешно`
- [ ] Приветствие играет **ТОЛЬКО** после перезапуска (в новом процессе)

**TODO**: Создать golden-лог тест для автоматизации.

---

### 12. Метрики и SLO

**Метрики** (цели):
- `tcc_prompt_duration_ms` (p95 ≤ 5 сек)
- `permission_flow_success` (≥ 99%)
- `permission_restart_success_rate` (≥ 98%)
- `permission_restart_latency_ms` (p95 ≤ 15 сек)

**TODO**: Создать smoke-тест для проверки метрик из логов.

---

### 13. Change Impact YAML

**Проверка**: Если изменены `interaction_matrix.yaml` или `gateways.py` → должен быть `change_impact.yaml`.

**CI job**: `impact-gate` → `Check change_impact.yaml`

**Ошибка**: Если файл отсутствует → CI фейлит.

---

## 🎯 Быстрые проверки (quick audit)

### ✅ Проверено автоматически

1. **Порядок инициализации**: тест `test_init_order.py` проверяет позиции
2. **Правила в матрице**: скрипт `verify_4_artifacts_invariant.py` проверяет наличие
3. **Покрытие правил**: скрипт `verify_rule_coverage.py` проверяет gateway и тесты
4. **Decision-логи**: негативные тесты проверяют формат
5. **Feature flags**: скрипт `verify_feature_flags.py` проверяет регистрацию
6. **Permission restart с обновлениями**: тесты `TestPermissionRestartWithUpdates` покрывают все сценарии

### ⚠️ Требует ручной проверки

1. **Golden-лог тест**: нужно реализовать валидацию реального лог-файла
2. **Smoke-тест метрик**: нужно реализовать извлечение метрик из логов
3. **Семантическая связность**: нужно улучшить проверку (правило → gateway логика)

---

## 📊 Статус проверок

| Проверка | Статус | Блокирует мердж | Критичность |
|---------|--------|-----------------|-------------|
| Инвариант 4 артефактов | ✅ Работает | ✅ Да | **КРИТИЧНО** |
| Покрытие правил тестами | ✅ Работает | ✅ Да | **КРИТИЧНО** |
| Feature flags | ✅ Работает | ✅ Да | **КРИТИЧНО** |
| Прямой доступ к состоянию | ✅ Работает | ✅ Да | **КРИТИЧНО** |
| Валидация схем | ✅ Работает | ✅ Да | **КРИТИЧНО** |
| Decision-логи | ✅ Работает | ✅ Да | **КРИТИЧНО** |
| Порядок инициализации | ✅ Работает | ✅ Да | **КРИТИЧНО** |
| Permission restart тесты | ✅ Работает | ✅ Да | **КРИТИЧНО** |
| Golden-лог тест | ⚠️ Частично | ❌ Нет | Средняя |
| SLO метрики | ⚠️ Частично | ❌ Нет | Средняя |

---

## 🚨 Критические точки

### 1. Семантическая связность (частично)

**Проблема**: Скрипт `verify_4_artifacts_invariant.py` проверяет наличие оси по имени, но не проверяет, что gateway реально использует ось в логике.

**Решение**: Скрипт `verify_rule_coverage.py` проверяет наличие gateway, но не полную логику. Нужно улучшить проверку.

### 2. Golden-лог тест (TODO)

**Проблема**: Тест проверяет только структуру expected sequence, но не реальные логи.

**Решение**: Реализовать валидацию реального лог-файла (требует log file).

### 3. SLO метрики (TODO)

**Проблема**: Тест проверяет только структуру thresholds, но не реальные метрики.

**Решение**: Реализовать извлечение метрик из логов (требует log file).

---

## 📝 Следующие шаги

1. **Реализовать golden-лог тест** для автоматизации проверки логов первого запуска
2. **Реализовать smoke-тест метрик** для проверки SLO из логов
3. **Улучшить проверку семантической связности** (правило → gateway логика)

---

**Владелец**: Tech Lead клиента  
**Последнее обновление**: 2025-01-30
