# Nexy CRM User Guide (v2.3)

**System Status:** Active Control Enabled (Federated)
**Background Sync:** Active (5 min interval)
**Architecture:** Local Bridge Server + Browser UI + Python Scripts

---

## 🚀 Quick Start

### 1. Запуск системы (с редактором)
Чтобы кнопка "Open File" открывала VS Code (или Cursor):
```bash
export CRM_EDITOR_CMD="code"  # или "cursor"
python3 scripts/server.py
```
Откройте: [http://localhost:8000/](http://localhost:8000/)

### 2. Синхронизация прогресса
```bash
python3 scripts/kanban_progress_collector.py
```

---

## 📜 System Contracts (Правила игры)

Чтобы система работала предсказуемо, мы следуем этим контрактам:

### 1. Data Contract (`PROJECT_KANBAN.json`)
Файл `Docs/PROJECT_KANBAN.json` — **Единый источник истины**.
Сервер `server.py` валидирует данные при сохранении. JSON **обязан** содержать ключи:
*   `meta`
*   `config`
*   `epics`
*   `cards`
*   `releases`

Любой запрос `/api/save` без этих полей будет отклонен с ошибкой 400.

### 2. Registry Contract (`CRM_INSTRUCTION_REGISTRY.md`)
Коллектор (`kanban_progress_collector.py`) ожидает строгого формата таблицы реестра:
`| **INS-XXX** | Name | Link | Desc |`
*   ID должен быть в первой колонке.
*   Путь к файлу должен быть Markdown-ссылкой относительно `Docs/`.

### 3. Evidence Strategy
*   Коллектор **сохраняет** список выполненных пунктов в поле `evidence` задачи.
*   Это позволяет проследить, *почему* прогресс именно 28% (какие именно пункты закрыты).

---

## 🧩 Основные функции

### 1. Active Control ("Open File")
*   **Безопасность**:
    *   Открываются только файлы внутри папки проекта (Path Traversal Protection).
    *   Команды запускаются **без shell** (`subprocess.run(list)`), что защищает от инъекций.
*   **Редактор**: Используется переменная `CRM_EDITOR_CMD`. Если не задана — системный `open`.

### 2. Progress Sync
*   Связь "Факт → Прогресс" работает через `registry_ref` в задаче.
*   Пример: Задача `NEXY-003` ссылается на `INS-007`.

### 3. Release Automation ("Ship It")
*   Когда все задачи в колонке **"Готово к релизу"** (Released), нажмите кнопку `🚀 Ship Release`.
*   Система попросит ввести версию (например, `v1.2.0`).
*   **Автоматически**:
    *   Все карточки переместятся в архив `PROJECT_KANBAN.json`.
    *   Создастся запись в `Docs/CHANGELOG.md`.

### 4. Federated Workspaces (Phase 5)
*   Система поддерживает децентрализованное управление задачами.
*   **Локальные файлы**:
    *   `client/.crm/TASKS.json`
    *   `server/.crm/TASKS.json`
*   **Авто-агрегация**: Сервер раз в 5 минут (или при старте) запускает `task_aggregator.py`, который объединяет задачи из всех workspace в мастер-план.
*   **Фильтрация**: В UI (Sidebar) можно фильтровать задачи по их происхождению (All, Client, Server, Master).

### 5. Assistant Tracking (Phase 5.1)
*   Каждая задача отслеживает автора и последнего редактора через поля `created_by` и `modified_by`.
*   В UI карточки отображается значок ассистента (`🤖 Ant`).
*   Правила работы для ассистентов описаны в `Docs/CRM_ASSISTANT_INSTRUCTIONS.md`.

---

## 🛠 Troubleshooting

**1. Сервер возвращает Error 400 при сохранении**
*   Вы пытаетесь сохранить поврежденный JSON. Проверьте структуру данных.

**2. Open File не открывает редактор**
*   Перезапустите сервер с `export CRM_EDITOR_CMD="code"`.
