# Руководство по тестированию распознавания речи

## Дата: 2025-12-09

## Последовательность проверки

### Шаг 1: Убедиться, что AVF действительно пишет голос

**Цель**: Проверить, что в буфер попадает реальный голос, а не только звук клавиатуры/тишина.

**Действия:**
1. Включить сохранение аудио:
   ```bash
   export NEXY_DEBUG_SAVE_AUDIO=true
   python main.py
   ```

2. Зажать Ctrl+N и произнести короткую английскую фразу (например, "Hello")
3. Отпустить Ctrl+N
4. Проверить файл в `/tmp/nexy_debug_session_{session_id}.wav`
5. Прослушать файл:
   - Если слышен голос → переходим к шагу 2
   - Если только клавиатура/тишина → проблема аппаратная:
     - Проверить системные настройки macOS → Звук → Вход → выбран правильный микрофон
     - Проверить `config/audio_config.py` → `input_device` (если указан)

**Логи для проверки:**
- `💾 [AVF] Аудио сохранено для диагностики (ДО распознавания): /tmp/nexy_debug_session_{session_id}.wav`
- `🔍 [AVF] Характеристики аудио перед распознаванием: RMS=..., Первые 10 samples: ...`

---

### Шаг 2: Очистка буфера и борьба с дублями

**Цель**: Убедиться, что буфер очищается правильно и нет дублирования событий.

**Проверка:**
1. Перезапустить приложение
2. Зажать Ctrl+N, произнести фразу, отпустить
3. Проверить логи:
   - ✅ `🧹 [VOICE] Буфер очищен в finally блоке _on_recording_stop` — один раз после записи
   - ✅ `🚫 [DEBOUNCE] Игнорируем дублирующий voice.recording_stop` — при повторных событиях
   - ✅ `🔒 [QUARTZ] Комбо уже завершено, игнорируем повторную деактивацию` — при повторных KeyUp/FlagsChanged
   - ✅ Нет повторных `[AVF] stop_input вернул None`

**Если есть проблемы:**
- Дублирование `voice.recording_stop` → проверить `_publish_recording_stop_with_debounce()` (debounce 200мс)
- Буфер не очищается → проверить `finally` блок в `_on_recording_stop`
- Повторные события от Quartz → проверить `_combo_completed` флаг

---

### Шаг 3: Включить локальный стриминг (SFSpeech)

**Цель**: Использовать локальное распознавание через SFSpeechRecognizer вместо Google Speech API.

**Проверка зависимостей:**
```bash
python3 -c "from Speech import SFSpeechRecognizer; print('✅ SFSpeechRecognizer доступен')"
```

Если ошибка:
```bash
pip3 install pyobjc-framework-Speech --break-system-packages
```

**Проверка конфигурации:**
- `config/unified_config.yaml` → `speech_recognition.streaming.enabled: true` (строка 533)
- `config/unified_config.yaml` → `speech_recognition.streaming.on_device: true` (строка 534)

**Проверка разрешений macOS:**
- Системные настройки → Конфиденциальность → Микрофон → Nexy включен
- Системные настройки → Конфиденциальность → Распознавание речи → Nexy включен

**Логи для проверки:**
- `✅ [SFSpeech] Стриминговое распознавание включено: language=..., on_device=...`
- `🎤 [SFSpeech] Запуск стримингового распознавания для session ...`
- `🔄 [SFSpeech] Промежуточный: '...'` — при удержании Ctrl+N
- `✅ [SFSpeech] Финальный результат: '...'` — после отпускания

**Если SFSpeech не работает:**
- Проверить логи на наличие ошибок импорта
- Проверить разрешения macOS (см. выше)
- Проверить, что `pyobjc-framework-Speech` установлен

---

### Шаг 4: Тест распознавания на английском

**Цель**: Убедиться, что распознавание работает корректно.

**Действия:**
1. Перезапустить приложение
2. Зажать Ctrl+N
3. Произнести короткий английский запрос (например, "Hello" или "What time is it")
4. Отпустить Ctrl+N
5. Проверить логи:
   - ✅ `voice.recognition_completed` должен публиковаться с текстом
   - ✅ `duration (original)` должна соответствовать фактическому удержанию
   - ✅ Нет `UnknownValueError`
   - ✅ Нет повторных `[AVF] stop_input вернул None`

**Если `UnknownValueError`:**
- Проверить сохраненный WAV файл (если `NEXY_DEBUG_SAVE_AUDIO=true`) — есть ли голос
- Проверить RMS в логах — должен быть > 100 (не тишина)
- Проверить длительность — должна быть > 0.5s
- Проверить язык в `unified_config.yaml` → `speech_recognition.language: "en-US"`

---

### Шаг 5: Улучшение сигнала при ошибке

**Текущее состояние:**
- Сигнал при `voice.recognition_failed` уже публикуется через `SignalIntegration`
- Лог улучшен: теперь показывает тип события, ошибку и session_id

**Проверка:**
- При `UnknownValueError` должен быть слышен ERROR сигнал (если не подавлен suppression window)
- В логах: `⚠️ Signals: ERROR (failure event: voice.recognition_failed, error=..., session=...)`

---

## Диагностика

### Характеристики аудио в логах

При каждом распознавании в логах выводятся:
- `RMS` — средняя мощность сигнала (должен быть > 100 для голоса)
- `Первые 10 samples` — первые значения аудио (для проверки формата)
- `Min/Max/Mean` — статистика по аудио

**Интерпретация:**
- RMS < 100 → тишина или очень тихий сигнал
- RMS > 10000 → перегрузка/искажение
- Первые samples близки к 0 → возможно тишина в начале записи

### Сохранение WAV файлов

При `NEXY_DEBUG_SAVE_AUDIO=true` файлы сохраняются в `/tmp/nexy_debug_session_{session_id}.wav`.

**Проверка:**
1. Прослушать файл в любом аудио-плеере
2. Проверить:
   - Есть ли голос в файле?
   - Есть ли только звук клавиш?
   - Есть ли только тишина?
   - Качество звука (искажения, шум)?

**Если проблема аппаратная:**
- Проверить системные настройки macOS → Звук → Вход
- Проверить `config/audio_config.py` → `input_device`
- Попробовать другой микрофон

---

## Следующие шаги

После успешного тестирования базового распознавания:

1. **Включить SFSpeechRecognizer** (если еще не включен)
2. **Протестировать стриминговое распознавание** (промежуточные результаты)
3. **Оптимизировать параметры** (таймауты, sample_rate, language)
4. **Рассмотреть альтернативные backends** (если Google Speech API не подходит):
   - `recognize_google_cloud` (требуется API key)
   - Whisper (локальное распознавание)
   - Azure Speech Services

---

## Готово к тестированию! 🚀
