# 🔄 Логика работы системы планирования изменений

**ЦЕЛЬ**: Описать как работает обновленная система планирования изменений после всех исправлений.

**Дата создания**: 2025-01-XX  
**Версия**: 1.0

---

## 🎯 Общая логика работы

### Принцип работы

Система работает по принципу **последовательного углубления анализа**:
1. **Контекстный анализ** (ЧТО учесть) — высокоуровневый обзор
2. **Архитектурный анализ** (КАК устроено) — понимание структуры
3. **Анализ конфигураций** (КАКИЕ параметры) — детальный анализ параметров
4. **Планирование изменений** (КАК изменить) — конкретный план реализации
5. **Проверка дублирования/конфликтов/race conditions** (ЧТО предотвратить) — финальная проверка перед кодом

Каждый этап опирается на результаты предыдущего и углубляет понимание.

---

## 📋 Детальная логика каждого этапа

### Этап 1: CONTEXT_AWARE_PLANNING_GUIDE.md (ПЕРВЫЙ)

**Цель**: Обеспечить учет всех конфигураций, флагов, архитектурных особенностей и существующего функционала ДО генерации кода.

**Что происходит**:

1. **Подготовка (Этап 0)**:
   - Определяется область изменения (какие модули/интеграции/файлы затрагиваются)
   - Находятся похожие изменения в истории проекта
   - Изучаются связанные проблемы из документации
   - Определяются ключевые слова для поиска

2. **Анализ конфигураций и флагов (Этап 1)**:
   - **unified_config.yaml**: Читается полностью, не только раздел, связанный с изменением
   - **Feature Flags**: Проверяется реестр, логика включения/выключения, приоритеты
   - **Схемы конфигурации**: Изучаются схемы валидации

3. **Анализ архитектурных ограничений (Этап 2)**:
   - **Порядок инициализации**: Изучается `SimpleModuleCoordinator._create_integrations()`, понимаются зависимости
   - **EventBus события**: Находятся ВСЕ подписки и публикации (grep по коду)
   - **Selectors/Gateways**: Находятся ВСЕ использования, проверяются правила из interaction_matrix.yaml

4. **Анализ существующего функционала (Этап 3)**:
   - **Поиск похожих реализаций**: Поиск по коду, документам, git log
   - **Анализ текущей реализации**: Читается ВЕСЬ код изменяемых модулей полностью
   - **Анализ зависимостей**: Находятся ВСЕ прямые, обратные, внешние зависимости

5. **Проверка архитектурных ограничений (Этап 4)**:
   - **Инварианты системы**: Проверяются ВСЕ инварианты (архитектурные, модульные, состояния, workflows)
   - **Правила взаимодействия**: Проверяются ВСЕ правила из interaction_matrix.yaml
   - **Ограничения платформы**: Проверяются особенности macOS API, PyObjC, потоков, event loops

6. **Создание детальной Impact Map (Этап 5)**:
   - Создается расширенная Impact Map со ВСЕМИ затронутыми компонентами
   - Указываются зависимости, инварианты, риски, проверки

7. **Финальная проверка готовности (Этап 6)**:
   - Проверяется выполнение всех этапов
   - Ответы на критические вопросы перед переходом к следующему этапу

**Результат**: Детальная Impact Map, полное понимание контекста изменения.

**Время выполнения**: 30-60 минут

---

### Этап 2: ARCHITECTURE_ANALYSIS_CHECKLIST.md (ВТОРОЙ)

**Цель**: Обеспечить полный анализ текущей архитектуры, структуры, взаимодействий ДО создания плана изменений.

**Что происходит**:

1. **Изучение ключевых архитектурных документов (Этап 0)**:
   - Читаются обязательные документы: ARCHITECTURE_OVERVIEW.md, STATE_CATALOG.md, interaction_matrix.yaml, PROJECT_REQUIREMENTS.md
   - Читаются дополнительные документы (если релевантно)

2. **Анализ структуры проекта (Этап 1)**:
   - Изучается структура каталогов (модули, интеграции, workflows, core)
   - Изучается порядок инициализации в `SimpleModuleCoordinator`
   - Изучается модульная структура (границы, интерфейсы, зависимости)

3. **Анализ взаимодействий и коммуникаций (Этап 2)**:
   - Изучаются EventBus события (входные/выходные, контракты, подписки, публикации)
   - Изучаются Workflows и FSM (ListeningWorkflow, ProcessingWorkflow, режимы, переходы)
   - Изучаются Selectors и Gateways (чистые функции, логика принятия решений)
   - Изучается State Manager (логика управления состоянием, оси состояния)

4. **Анализ конфигурации (Этап 3)**:
   - Изучается структура `unified_config.yaml` (общий обзор)
   - Изучаются Feature Flags и Kill-Switches (общий обзор)
   - Изучаются схемы конфигурации (общий обзор)

5. **Анализ зависимостей (Этап 4)**:
   - Определяются прямые зависимости (импорты, модули, интеграции)
   - Определяются обратные зависимости (кто зависит от изменяемого кода)
   - Определяются внешние зависимости (библиотеки, версии, системные зависимости)

6. **Анализ инвариантов и правил (Этап 5)**:
   - Определяются инварианты системы (архитектурные, модульные, состояния, workflows)
   - Изучаются правила взаимодействия из interaction_matrix.yaml
   - Определяются ограничения и правила (порядок инициализации, режимов, событий)

7. **Анализ текущего состояния (Этап 6)**:
   - Изучается текущая реализация (код, логика, проблемы, технический долг)
   - Изучаются тесты (существующие тесты, покрытие, тестовые сценарии)
   - Изучаются метрики и мониторинг (метрики, SLO, логирование)

**Результат**: Полное понимание архитектуры системы.

**Время выполнения**: 45-90 минут

---

### Этап 3: CONFIGURATION_ANALYSIS_CHECKLIST.md (ТРЕТИЙ)

**Цель**: Обеспечить полный анализ всех конфигураций, флагов и параметров, влияющих на изменяемый функционал.

**Что происходит**:

1. **Идентификация затронутых конфигураций (Этап 1)**:
   - Определяется домен системы (audio, permissions, network, и т.д.)
   - Определяются разделы `unified_config.yaml`, связанные с изменением
   - Определяются схемы в `config/schemas/`
   - Определяются модули, читающие/пишущие конфигурации
   - Находятся ВСЕ связанные параметры (прямые, косвенные, по умолчанию, условные)

2. **Детальный анализ конфигураций (Этап 2)**:
   - Анализируется структура конфигурации (иерархия, зависимости, взаимоисключающие параметры)
   - Анализируются значения и диапазоны (допустимые значения, валидация, fallback)
   - Анализируется логика использования (где читается, как используется, когда применяется, порядок применения)

3. **Анализ Feature Flags и Kill-Switches (Этап 3)**:
   - Идентифицируются флаги (реестр, связанные флаги, kill-switches, shadow-mode)
   - Анализируется логика флагов (условия активации, приоритеты, rollout стратегия, откат)
   - Анализируется влияние на изменения (блокировка, активация, совместимость)

4. **Анализ зависимостей между конфигурациями (Этап 4)**:
   - Анализируются зависимости внутри домена (прямые, косвенные, циклические, группы параметров)
   - Анализируются зависимости между доменами (междоменные, обратные, совместное использование)
   - Анализируются риски зависимостей (поломка, рассинхронизация, деградация)

5. **Анализ влияния изменений на конфигурации (Этап 5)**:
   - Определяются новые параметры (необходимость, место в иерархии, значения по умолчанию, валидация)
   - Определяются изменения существующих параметров (обратная совместимость, миграция, deprecation)
   - Определяется удаление параметров (необходимость, зависимости, миграция)

6. **Проверка конфигураций перед изменениями (Этап 6)**:
   - Проверяется готовность (все параметры идентифицированы, зависимости проанализированы)
   - Обновляется документация (unified_config.yaml, схемы, FEATURE_FLAGS.md)
   - Определяется тестирование конфигураций (валидация, зависимости, миграция)

**Результат**: Таблица анализа конфигураций, понимание всех параметров и их зависимостей.

**Время выполнения**: 30-60 минут

---

### Этап 4: PRE_CHANGE_PLANNING_CHECKLIST.md (ЧЕТВЕРТЫЙ)

**Цель**: Убедиться, что все нюансы, зависимости и риски учтены ДО начала написания кода.

**Что происходит**:

1. **Проверка выполнения предыдущих этапов (Этап 0)**:
   - Проверяется выполнение CONTEXT_AWARE_PLANNING_GUIDE.md (детальная Impact Map создана)
   - Проверяется выполнение ARCHITECTURE_ANALYSIS_CHECKLIST.md (архитектура понятна)
   - Проверяется выполнение CONFIGURATION_ANALYSIS_CHECKLIST.md (таблица анализа конфигураций создана)

2. **Понимание задачи и контекста (Этап 1)**:
   - Анализируется задача (цель, scope, границы, примеры)
   - Изучается текущая реализация (ключевые файлы, архитектура, зависимости, инварианты)
   - Проверяется документация (PROJECT_REQUIREMENTS.md, STATE_CATALOG.md, ARCHITECTURE_OVERVIEW.md)

3. **Анализ зависимостей и влияния (Этап 2)**:
   - Анализируются модули и интеграции (затронутые модули/интеграции, прямые/обратные зависимости, порядок инициализации)
   - Анализируются EventBus события и контракты (входные/выходные события, изменение контрактов, версионирование)
   - Анализируется конфигурация (новые параметры, существующие параметры, feature flags, kill-switches, схемы)
   - Анализируются оси состояния (затронутые оси, новые оси, владельцы, источники истины, метрики)
   - Анализируются правила взаимодействия (затронутые правила, новые правила, приоритеты, guards)

4. **Анализ рисков и инвариантов (Этап 3)**:
   - Анализируются инварианты системы (список инвариантов, проверка инвариантов, новые инварианты)
   - Анализируются риски (регрессия, производительность, безопасность, совместимость, UX)
   - Определяется митигация рисков (feature flags, kill-switches, тестирование, мониторинг)

5. **Планирование реализации (Этап 4)**:
   - Создается Impact Map (все оси/модули/события/конфиги учтены)
   - Создается change_impact.yaml (все разделы заполнены, тест-планы указаны, метрики указаны)
   - Определяется план изменений (последовательность шагов, точки проверки, откат, миграция)

6. **Проверка перед началом кода (Этап 5)**:
   - Обновляется документация (STATE_CATALOG.md, interaction_matrix.yaml, PROJECT_REQUIREMENTS.md, FEATURE_FLAGS.md)
   - Обновляется конфигурация (unified_config.yaml, схемы, feature flags)
   - Определяется тестирование (тест-стратегия, unit/integration/pairwise тесты)
   - Определяется мониторинг и логирование (метрики, логирование, алерты)

7. **Финальная проверка готовности (Этап 6)**:
   - Проверяется выполнение всех этапов 1-5
   - Проверяется наличие Impact Map и change_impact.yaml
   - Проверяется обновление документации и конфигурации
   - Проверяется согласование с владельцами модулей/осей

**Результат**: Полный план изменений, Impact Map, change_impact.yaml.

**Время выполнения**: 60-120 минут

---

### Этап 5: DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md (ПЯТЫЙ)

**Цель**: Обеспечить отсутствие дублирования кода, конфликтов и race conditions при создании или внесении изменений.

**Что происходит**:

1. **Проверка дублирования кода (Этап 1)**:
   - Поиск существующей функциональности (grep, codebase_search, git log)
   - Проверка дублирования логики (функции, классы, паттерны)
   - Проверка дублирования конфигураций (параметры, значения, схемы)

2. **Проверка конфликтов (Этап 2)**:
   - Проверка конфликтов с существующим кодом (имена, импорты, зависимости, состояния)
   - Проверка конфликтов с архитектурой (порядок инициализации, EventBus, workflows)
   - Проверка конфликтов с конфигурациями (параметры, значения, схемы)

3. **Проверка race conditions (Этап 3)**:
   - Проверка thread-safety (блокировки, атомарные операции, deadlock риски)
   - Проверка async/await паттернов (отслеживание задач, отмена дубликатов, таймауты)
   - Проверка смешивания async и threading (run_coroutine_threadsafe, правильный event loop)

4. **Проверка состояния и синхронизации (Этап 4)**:
   - Проверка единого источника истины (единое место управления состоянием)
   - Проверка атомарности операций (TOCTOU защита, транзакционность)

5. **Чек-лист перед генерацией кода (Этап 5)**:
   - Проверка дублирования (поиск выполнен, дублирование не найдено)
   - Проверка конфликтов (конфликты не найдены)
   - Проверка race conditions (race conditions предотвращены)

**Результат**: Таблица проверки дублирования/конфликтов/race conditions, готовность к генерации кода без проблем.

**Время выполнения**: 20-40 минут

---

## 🔄 Полный цикл работы системы

### Сценарий 1: Простое изменение (1-2 файла)

```
1. CONTEXT_AWARE_PLANNING_GUIDE.md
   - Быстрый анализ контекста (15-20 минут)
   - Создание упрощенной Impact Map
   
2. ARCHITECTURE_ANALYSIS_CHECKLIST.md
   - Фокус на затронутых модулях (20-30 минут)
   - Понимание архитектурных ограничений
   
3. CONFIGURATION_ANALYSIS_CHECKLIST.md
   - Фокус на затронутых конфигурациях (15-20 минут)
   - Таблица анализа конфигураций
   
4. PRE_CHANGE_PLANNING_CHECKLIST.md
   - Планирование простого изменения (30-40 минут)
   - Impact Map, change_impact.yaml
   
5. DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md
   - Быстрая проверка дублирования/конфликтов/race conditions (10-15 минут)
   - Таблица проверки
   
6. ГЕНЕРАЦИЯ КОДА
```

**Общее время**: 90-125 минут

---

### Сценарий 2: Среднее изменение (несколько модулей)

```
1. CONTEXT_AWARE_PLANNING_GUIDE.md
   - Полный анализ контекста (30-40 минут)
   - Создание детальной Impact Map
   
2. ARCHITECTURE_ANALYSIS_CHECKLIST.md
   - Полный анализ архитектуры (45-60 минут)
   - Понимание всех взаимодействий
   
3. CONFIGURATION_ANALYSIS_CHECKLIST.md
   - Полный анализ конфигураций (30-40 минут)
   - Детальная таблица анализа
   
4. PRE_CHANGE_PLANNING_CHECKLIST.md
   - Планирование среднего изменения (60-80 минут)
   - Детальная Impact Map, change_impact.yaml
   
5. DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md
   - Полная проверка дублирования/конфликтов/race conditions (20-30 минут)
   - Детальная таблица проверки
   
6. ГЕНЕРАЦИЯ КОДА
```

**Общее время**: 185-250 минут (3-4 часа)

---

### Сценарий 3: Большое изменение (функциональность)

```
1. CONTEXT_AWARE_PLANNING_GUIDE.md
   - Глубокий анализ контекста (45-60 минут)
   - Создание расширенной Impact Map
   - Поиск похожих реализаций
   
2. ARCHITECTURE_ANALYSIS_CHECKLIST.md
   - Глубокий анализ архитектуры (60-90 минут)
   - Полное понимание системы
   
3. CONFIGURATION_ANALYSIS_CHECKLIST.md
   - Глубокий анализ конфигураций (45-60 минут)
   - Полная таблица анализа с зависимостями
   
4. PRE_CHANGE_PLANNING_CHECKLIST.md
   - Планирование большого изменения (90-120 минут)
   - Расширенная Impact Map, change_impact.yaml
   - План миграции (если нужен)
   
5. DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md
   - Глубокая проверка дублирования/конфликтов/race conditions (30-40 минут)
   - Полная таблица проверки со всеми рисками
   
6. ГЕНЕРАЦИЯ КОДА
```

**Общее время**: 270-370 минут (4.5-6 часов)

---

## ✅ Как система предотвращает проблемы

### Проблема: Не учтены конфигурации

**Как предотвращается**:
- **Этап 1 (CONTEXT_AWARE)**: Высокоуровневый анализ unified_config.yaml и feature flags
- **Этап 3 (CONFIGURATION)**: Детальный анализ всех конфигураций, создание таблицы анализа
- **Результат**: Все конфигурации идентифицированы и проанализированы до начала кода

### Проблема: Не учтены архитектурные ограничения

**Как предотвращается**:
- **Этап 1 (CONTEXT_AWARE)**: Анализ архитектурных ограничений (порядок инициализации, EventBus, selectors/gateways)
- **Этап 2 (ARCHITECTURE)**: Полный анализ архитектуры, инвариантов, правил взаимодействия
- **Результат**: Все архитектурные ограничения понятны и учтены

### Проблема: Не учтен существующий функционал

**Как предотвращается**:
- **Этап 1 (CONTEXT_AWARE)**: Поиск похожих реализаций, анализ текущей логики, анализ зависимостей
- **Результат**: Существующий функционал изучен, похожие проблемы найдены и учтены

### Проблема: Не учтены зависимости

**Как предотвращается**:
- **Этап 2 (ARCHITECTURE)**: Анализ прямых, обратных, внешних зависимостей
- **Этап 3 (CONFIGURATION)**: Анализ зависимостей между конфигурациями
- **Результат**: Все зависимости идентифицированы и проанализированы

### Проблема: Дублирование кода

**Как предотвращается**:
- **Этап 5 (DUPLICATE_CONFLICT_RACE)**: Поиск существующей функциональности, проверка дублирования логики
- **Результат**: Использование существующей реализации вместо создания новой

### Проблема: Конфликты

**Как предотвращается**:
- **Этап 5 (DUPLICATE_CONFLICT_RACE)**: Проверка конфликтов имен/состояний/архитектуры
- **Результат**: Уникальные имена, единый источник истины, совместимость с архитектурой

### Проблема: Race conditions

**Как предотвращается**:
- **Этап 5 (DUPLICATE_CONFLICT_RACE)**: Проверка thread-safety, async/await паттернов, смешивания async и threading
- **Результат**: Все общие данные защищены блокировками, все async операции имеют таймауты

---

## 🎯 Практический пример

### Задача: Изменить логику выбора аудио устройства

**Этап 1: CONTEXT_AWARE_PLANNING_GUIDE.md**

```
1. Подготовка:
   - Область: modules/audio_system/core/device_manager.py
   - Похожие изменения: AUDIO_DEVICE_SWITCHING_ANALYSIS.md
   - Ключевые слова: device_mode, default_device, fallback

2. Анализ конфигураций:
   - unified_config.yaml: audio_system.device_mode, default_device.*, fallback.*
   - Feature flags: audio.avf.enabled, audio.ks_avf.enabled
   - Схемы: audio_system_hybrid_schema.yaml

3. Архитектурные ограничения:
   - Порядок инициализации: AudioSystemIntegration после VoiceRecognition
   - EventBus: audio.device.output_changed, audio.device.input_changed
   - Selectors/Gateways: decide_start_listening проверяет device.input

4. Существующий функционал:
   - Похожие реализации: HYBRID_MODE_LOGIC_FLOW.md
   - Текущая логика: device_mode="auto" → default first, fallback на explicit
   - Зависимости: DeviceManager, HybridStreamManager, SpeechPlayback

5. Инварианты:
   - player_node_connected должен быть синхронизирован
   - _output_state должен быть синхронизирован с engine.isRunning()

6. Impact Map:
   - Ось: device.input/output
   - Модули: DeviceManager, HybridStreamManager
   - События: audio.device.*
   - Конфиги: audio_system.device_mode, default_device.*, fallback.*
```

**Этап 2: ARCHITECTURE_ANALYSIS_CHECKLIST.md**

```
1. Архитектурные документы:
   - ARCHITECTURE_OVERVIEW.md: модули, интеграции, workflows
   - STATE_CATALOG.md: device.input/output оси
   - interaction_matrix.yaml: правила для device.input/output

2. Структура проекта:
   - Модули: audio_system, audio_avf, speech_playback
   - Интеграции: AudioSystemIntegration, SpeechPlaybackIntegration
   - Порядок инициализации: AudioSystemIntegration после VoiceRecognition

3. Взаимодействия:
   - EventBus: audio.device.* события
   - Workflows: ListeningWorkflow использует device.input
   - Selectors/Gateways: decide_start_listening проверяет device.input

4. Зависимости:
   - Прямые: DeviceManager → PortAudio, CoreAudio
   - Обратные: SpeechPlayback → DeviceManager
   - Внешние: sounddevice, PyObjC
```

**Этап 3: CONFIGURATION_ANALYSIS_CHECKLIST.md**

```
1. Идентификация конфигураций:
   - Домен: audio
   - Разделы: audio_system.device_mode, default_device.*, fallback.*
   - Схемы: audio_system_hybrid_schema.yaml
   - Модули: DeviceManager, HybridStreamManager

2. Детальный анализ:
   - device_mode: "default" | "explicit" | "auto" (enum, default: "auto")
   - default_device.enabled: true/false (зависит от device_mode)
   - fallback.enabled: true/false (зависит от device_mode="auto")

3. Feature Flags:
   - audio.avf.enabled: влияет на использование AVFoundation
   - audio.ks_avf.enabled: kill-switch для AVFoundation

4. Зависимости:
   - device_mode="auto" → требует default_device.enabled=true и fallback.enabled=true
   - Риск: Изменение device_mode может сломать логику выбора устройств

5. Таблица анализа:
   | Параметр | Раздел | Схема | Модули | Зависимости | Риски |
   | device_mode | audio_system.device_mode | hybrid_schema | DeviceManager | default_device.* | Изменение может сломать логику |
```

**Этап 4: PRE_CHANGE_PLANNING_CHECKLIST.md**

```
1. Проверка предыдущих этапов:
   - ✅ CONTEXT_AWARE выполнен (Impact Map создана)
   - ✅ ARCHITECTURE выполнен (архитектура понятна)
   - ✅ CONFIGURATION выполнен (таблица анализа создана)

2. Понимание задачи:
   - Цель: Изменить логику выбора устройства с "auto" на "default"
   - Scope: DeviceManager, HybridStreamManager
   - Границы: Не менять fallback логику

3. Анализ зависимостей:
   - Модули: DeviceManager, HybridStreamManager
   - События: audio.device.* (не изменяются)
   - Конфиги: device_mode (изменяется), default_device.* (не изменяется)
   - Оси: device.input/output (не изменяются)

4. Риски:
   - Регрессия: Может сломать fallback логику
   - Митигация: Feature flag для постепенного rollout

5. План реализации:
   - Шаг 1: Добавить feature flag NEXY_FEATURE_DEVICE_MODE_DEFAULT
   - Шаг 2: Изменить логику в HybridStreamManager
   - Шаг 3: Обновить тесты
   - Шаг 4: Rollout через feature flag

6. Проверка готовности:
   - ✅ Документация обновлена
   - ✅ Конфигурация обновлена (feature flag добавлен)
   - ✅ Тесты определены
   - ✅ Метрики определены
```

**Результат**: Полное понимание контекста, архитектуры, конфигураций. План изменений готов. Код генерируется с учетом всех нюансов.

---

## 📚 Связанные документы

- `PLANNING_PROCESS_ORDER.md` — единый источник истины для порядка выполнения всех чек-листов
- `CONTEXT_AWARE_PLANNING_GUIDE.md` — контекстный анализ (ПЕРВЫЙ этап)
- `ARCHITECTURE_ANALYSIS_CHECKLIST.md` — анализ архитектуры (ВТОРОЙ этап)
- `CONFIGURATION_ANALYSIS_CHECKLIST.md` — анализ конфигураций (ТРЕТИЙ этап)
- `PRE_CHANGE_PLANNING_CHECKLIST.md` — планирование изменений (ЧЕТВЕРТЫЙ этап)
- `DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md` — проверка дублирования/конфликтов/race conditions (ПЯТЫЙ этап)
- `.cursorrules` (раздел 11.1) — правила изменений

---

**Версия**: 1.1  
**Дата создания**: 2025-01-XX  
**Последнее обновление**: 2025-01-XX (добавлен этап 5: проверка дублирования/конфликтов/race conditions)



