# Инструкция для AI-ассистентов: Управление задачами CRM

> **Для**: Cursor, Codex, и других AI-ассистентов работающих в `client/` или `server/`

---

## 🎯 Цель

Каждый ассистент должен создавать и обновлять задачи в `.crm/TASKS.json` своего workspace, чтобы главный CRM (Fix_new) мог агрегировать и отслеживать прогресс.

---

## 📂 Где создавать задачи

| Если работаете в... | Файл задач |
|:--------------------|:-----------|
| `client/` | `client/.crm/TASKS.json` |
| `server/` | `server/.crm/TASKS.json` |

---

## 📋 Формат задачи

```json
{
    "id": "CLI-002",
    "epicId": "EPIC-MVP",
    "title": "Краткое описание задачи",
    "status": "in_progress",
    "priority": "P1",
    "created_by": "Cursor",
    "modified_by": "Cursor",
    "description": "Детальное описание",
    "updated_at": "2026-01-05T19:47:00-05:00"
}
```

### Обязательные поля:
| Поле | Формат | Пример |
|:-----|:-------|:-------|
| `id` | `CLI-XXX` или `SRV-XXX` | `CLI-002`, `SRV-005` |
| `status` | `plan` / `in_progress` / `testing` / `released` | `in_progress` |
| `created_by` | Имя ассистента | `Cursor`, `Codex`, `Antigravity` |
| `modified_by` | Имя ассистента (кто последний изменил) | `Cursor` |
| `updated_at` | ISO 8601 datetime | `2026-01-05T19:47:00-05:00` |

### Опциональные поля:
| Поле | Описание |
|:-----|:---------|
| `epicId` | Ссылка на Epic из Fix_new (например `EPIC-MVP`) |
| `priority` | `P0` (Critical) / `P1` (High) / `P2` (Medium) / `P3` (Low) |
| `file_path` | Путь к файлу для "Open File" |
| `registry_ref` | Ссылка на INS-XXX для синхронизации прогресса |

---

## 🔄 Workflow

### 1. Создание новой задачи
```json
// Добавить в tasks[] в TASKS.json:
{
    "id": "CLI-002",
    "title": "Fix login button",
    "status": "plan",
    "priority": "P2",
    "created_by": "Cursor",
    "modified_by": "Cursor",
    "updated_at": "2026-01-05T19:47:00-05:00"
}
```

### 2. Обновление статуса
При изменении задачи **обязательно** обновить:
- `status` — новый статус
- `modified_by` — ваше имя
- `updated_at` — текущее время

### 3. Синхронизация с Fix_new
После изменений в TASKS.json, директор (Fix_new) запустит:
```bash
python3 scripts/task_aggregator.py
```

---

## 📏 Правила

1. **Префикс ID** соответствует workspace: `CLI-` для client, `SRV-` для server
2. **Не редактируйте** задачи другого workspace
3. **Всегда указывайте** `created_by` и `modified_by`
4. **Инкрементируйте** `next_id` в файле после создания задачи
5. **Epic** создаётся только через Fix_new (главный CRM)

---

## ✅ Пример полного TASKS.json

```json
{
    "workspace": "client",
    "id_prefix": "CLI",
    "next_id": 3,
    "tasks": [
        {
            "id": "CLI-001",
            "epicId": "EPIC-MVP",
            "title": "Fix UI styles",
            "status": "released",
            "priority": "P2",
            "created_by": "Cursor",
            "modified_by": "Antigravity",
            "updated_at": "2026-01-05T18:00:00-05:00"
        },
        {
            "id": "CLI-002",
            "epicId": "EPIC-MVP",
            "title": "Add login validation",
            "status": "in_progress",
            "priority": "P1",
            "created_by": "Cursor",
            "modified_by": "Cursor",
            "updated_at": "2026-01-05T19:47:00-05:00"
        }
    ]
}
```
