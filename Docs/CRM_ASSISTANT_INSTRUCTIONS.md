# Инструкция для AI-ассистентов: Управление задачами CRM

> **Для**: Cursor, Codex, Antigravity и других AI-ассистентов

---

## 📚 Иерархия источников (Source of Truth)

**ВАЖНО**: Этот документ — краткий entrypoint. Полные правила находятся в источниках истины:

| Приоритет | Документ | Назначение |
|:----------|:---------|:-----------|
| **1. Общий SoT** | [`CRM_CONSOLIDATED_RULES.md`](./CRM_CONSOLIDATED_RULES.md) | Единый свод правил, форматы, контракты |
| **2. Ассистент-специфика** | [`CODEX_PROMPT.md`](./CODEX_PROMPT.md) (раздел 8) | Правила для Codex |
| **2. Ассистент-специфика** | [`ANTIGRAVITY_PROMPT.md`](./ANTIGRAVITY_PROMPT.md) (раздел 8) | Правила для Antigravity |
| **3. Entrypoint** | Этот документ | Краткая инструкция + ссылки на SoT |

**Правило**: При конфликте правил приоритет у **CRM_CONSOLIDATED_RULES.md**.

---

## 🎯 Цель

Каждый ассистент должен:
- **Видеть** активные задачи и их прогресс
- **Описывать** текущую проблему и прогресс работы
- **Создавать** задачи и подзадачи
- **Вносить** изменения и корректировки

---

## 🔒 CRM v2.1 — Обязательные правила

> **КРИТИЧНО**: Эти правила обязательны для всех ассистентов. Нарушение = создание `problem` задачи.

### 1. Обязательное чтение Snapshot

**ПЕРЕД ЛЮБОЙ РАБОТОЙ** прочитайте `project_state` из `PROJECT_KANBAN.json`:

```json
{
  "project_state": {
    "focus": "master",
    "active_goals": ["NEXY-003"],
    "current_problems": [],
    "top_blockers": [{"task": "NEXY-002", "blocked_by": "NEXY-001"}],
    "wip_status": {"master": {"in_progress": 1, "testing": 0}}
  }
}
```

**Проверьте:**
- Есть ли активные goals/problems?
- Есть ли blockers, влияющие на вашу работу?
- Не превышен ли WIP-лимит (max 1 in_progress per workspace)?

### 2. Noise Control — когда НЕ создавать задачу

**НЕ создавайте задачу если:**
- ❌ Исправление опечатки / форматирования
- ❌ Рутинный рефакторинг без изменения поведения
- ❌ Обновление документации без изменения логики
- ❌ Действие завершается за < 5 минут

**СОЗДАВАЙТЕ задачу если:**
- ✅ Изменение влияет на API, контракты, поведение
- ✅ Нужна координация между workspaces
- ✅ Есть риск/неопределённость
- ✅ Действие требует review или тестирования

### 3. HTTP 409 — обработка конфликтов

При сохранении через API может вернуться HTTP 409:

```json
{
  "error": "conflict",
  "message": "Revision conflict. Your: 5, Current: 6",
  "current_revision": 6
}
```

**Ваши действия:**
1. **Перечитать** `GET /api/data` (свежий snapshot)
2. **Оценить** что изменилось
3. **Решить**: повторить с учётом изменений или отказаться
4. **Никогда** не пытаться "перезаписать силой"

### 4. Запрет "тихих" фиксов

**НЕЛЬЗЯ** молча исправлять ошибки в данных CRM:

```
❌ Увидел дублирующую задачу → просто удалил
❌ Нашёл битую ссылку → просто починил
❌ WIP-лимит превышен → просто вернул в plan
```

**Правильный подход:**
```
✅ Создать NEXY-* type: problem
✅ Описать что обнаружено
✅ Дождаться решения или исправить через задачу
```

### 5. Handoff между workspaces

При передаче задачи в другой workspace:

```json
{
  "id": "NEXY-001",
  "handoff": {
    "from": "Antigravity",
    "to": "Codex",
    "reason": "Server implementation needed",
    "context": "Design complete, see §3 in description"
  }
}
```

**Обязательно:**
- Указать `from`, `to`, `reason`
- Дать достаточный контекст в `description`
- Обновить `modified_by`

---

## 📂 Где создавать задачи

**Источник истины**: [`CRM_CONSOLIDATED_RULES.md`](./CRM_CONSOLIDATED_RULES.md), раздел E "Federated Workspaces"

| Workspace | Файл задач | Префикс ID | Когда использовать |
|:----------|:-----------|:-----------|:-------------------|
| **Master** | `.crm/TASKS.json` | `NEXY-` | Задачи всего проекта (архитектура, документация, общие процессы) |
| **Client** | `client/.crm/TASKS.json` | `CLI-` | Задачи клиентской части |
| **Server** | `server/.crm/TASKS.json` | `SRV-` | Задачи серверной части |

**Правила workspace**:
- **Codex**: работает в `client/` или `server/`, создаёт задачи только в соответствующих `.crm/TASKS.json` (см. [`CODEX_PROMPT.md`](./CODEX_PROMPT.md), раздел 8.1)
- **Antigravity**: может создавать master-задачи в `.crm/TASKS.json` (см. [`ANTIGRAVITY_PROMPT.md`](./ANTIGRAVITY_PROMPT.md), раздел 8)
- **Cursor**: по аналогии с Codex, в зависимости от workspace

---

## 📋 Формат задачи

**Источник истины**: [`CRM_CONSOLIDATED_RULES.md`](./CRM_CONSOLIDATED_RULES.md), раздел B "Contracts" → "Card Fields"

### Обязательные поля

| Поле | Формат | Пример | Источник |
|:-----|:-------|:-------|:---------|
| `id` | `NEXY-XXX`, `CLI-XXX`, `SRV-XXX` | `NEXY-001`, `CLI-002` | [Consolidated Rules §B](./CRM_CONSOLIDATED_RULES.md#b-contracts) |
| `created_by` | Имя ассистента | `Cursor`, `Codex`, `Antigravity` | [Consolidated Rules §B](./CRM_CONSOLIDATED_RULES.md#b-contracts) |
| `modified_by` | Имя ассистента | `Cursor` | [Consolidated Rules §B](./CRM_CONSOLIDATED_RULES.md#b-contracts) |
| `updated_at` | ISO 8601 datetime | `2026-01-05T19:47:00-05:00` | [Consolidated Rules §B](./CRM_CONSOLIDATED_RULES.md#b-contracts) |

### Опциональные поля

**Источник**: [`CRM_CONSOLIDATED_RULES.md`](./CRM_CONSOLIDATED_RULES.md), раздел B "Card Fields"

- `workspace`: `master`, `client`, `server` (автоматически определяется по префиксу ID)
- `epicId`: Ссылка на Epic
- `priority`: `P0` (Critical) / `P1` (High) / `P2` (Medium) / `P3` (Low)
- `status`: `plan`, `in_progress`, `testing`, `released` (см. [CODEX_PROMPT.md §8.2](./CODEX_PROMPT.md#82-жизненный-цикл-задачи))
- `description`: Детальное описание проблемы, прогресса, блокеров
- `file_path`: Путь к файлу для "Open File"
- `registry_ref`: Ссылка на INS-XXX для синхронизации прогресса
- `subtasks`: Массив подзадач (см. раздел "Подзадачи" ниже)
- `progress`: Процент выполнения (0-100), обновляется автоматически
- `evidence`: Список выполненных пунктов
- `blockedBy`: Массив ID задач-блокеров
- `responsible_docs`: Массив ответственных документов (см. раздел "Ответственные документы")

---

## 🔄 Workflow

**Источник истины**: [`CODEX_PROMPT.md`](./CODEX_PROMPT.md), раздел 8 "CRM Task Management"

### 1. Создание задачи

**Когда**: Начинаете новую работу, получили запрос, обнаружили баг

**Минимальный формат**:
```json
{
    "id": "CLI-002",
    "title": "Краткое описание",
    "status": "plan",
    "created_by": "Codex",
    "modified_by": "Codex",
    "updated_at": "2026-01-05T19:47:00-05:00",
    "workspace": "client"
}
```

**Полный формат**: см. [CODEX_PROMPT.md §8.3](./CODEX_PROMPT.md#83-создание-задачи)

### 2. Обновление статуса

**ОБЯЗАТЕЛЬНО при изменении**:
1. Обновить `status` при переходе
2. Обновить `modified_by`: ваше имя
3. Обновить `updated_at`: текущее время
4. **НЕ менять** `created_by`

**Статусы и переходы**: см. [CODEX_PROMPT.md §8.2](./CODEX_PROMPT.md#82-жизненный-цикл-задачи)

### 3. Описание проблемы и прогресса

**ОБЯЗАТЕЛЬНО** при обновлении задачи описывать:
- **Текущую проблему** (если есть)
- **Что уже сделано** (прогресс)
- **Блокеры** (если есть)
- **Следующие шаги**

**Формат в поле `description`**:
```markdown
## Проблема

[Описание проблемы или контекста]

## Текущий прогресс

- [x] Шаг 1 выполнен
- [x] Шаг 2 выполнен
- [ ] Шаг 3 в работе

## Блокеры

- Заблокировано задачей NEXY-001
- Ожидаем решение по архитектуре

## Следующие шаги

1. Реализовать X
2. Протестировать Y
3. Интегрировать с Z
```

**Когда обновлять**:
- При переходе `plan` → `in_progress`: опишите, что начали делать
- При обнаружении проблемы: опишите проблему и план решения
- При `in_progress` → `testing`: опишите, что сделано и что нужно протестировать
- При каждом значимом прогрессе: обновите список выполненных шагов

---

## 📝 Подзадачи (Subtasks)

**Источник**: [CODEX_PROMPT.md §8.5](./CODEX_PROMPT.md#85-подзадачи-subtasks)

Для разбиения большой задачи на мелкие шаги используйте подзадачи. **Важно**: подзадачи можно распределять между разными workspace.

**Минимальный формат**:
```json
{
    "subtasks": [
        {
            "title": "Шаг 1",
            "status": "released"
        },
        {
            "title": "Шаг 2",
            "status": "in_progress"
        }
    ]
}
```

**Расширенный формат с распределением**:
```json
{
    "id": "NEXY-001",
    "title": "Implement authentication system",
    "workspace": "master",
    "subtasks": [
        {
            "title": "Design auth architecture",
            "status": "in_progress",
            "workspace": "master",
            "assigned_to": "Antigravity"
        },
        {
            "title": "Implement client-side auth",
            "status": "plan",
            "workspace": "client",
            "assigned_to": "Cursor"
        },
        {
            "title": "Implement server-side auth API",
            "status": "plan",
            "workspace": "server",
            "assigned_to": "Codex"
        }
    ]
}
```

**Поля подзадачи**:
- `title` — название (обязательно)
- `status` — `plan`, `in_progress`, `testing`, `released`
- `workspace` — `master`, `client`, `server` (опционально, наследуется от родителя)
- `assigned_to` — ассистент, ответственный (опционально)
- `description` — описание (опционально)

**Правила**:
- Подзадачи создаются для задач со статусом `in_progress`
- Подзадачи могут быть распределены между разными workspace
- Если `workspace` не указан, подзадача наследует workspace родительской задачи
- Статус главной задачи может автоматически обновляться на основе подзадач

---

## 📊 Ответственные документы

**ОБЯЗАТЕЛЬНО** указывать документы, которые контролируют и отвечают за текущее направление работы.

**Формат**:
```json
{
    "responsible_docs": [
        {
            "path": "Docs/STATE_CATALOG.md",
            "type": "source_of_truth",
            "description": "Источник истины для осей состояния"
        },
        {
            "path": "config/interaction_matrix.yaml",
            "type": "control",
            "description": "Правила взаимодействия осей"
        }
    ]
}
```

**Типы документов**:
- `source_of_truth` — источник истины (STATE_CATALOG.md, unified_config.yaml)
- `control` — контролирующие документы (interaction_matrix.yaml, PROJECT_REQUIREMENTS.md)
- `reference` — справочные документы (ARCHITECTURE_OVERVIEW.md, инструкции)
- `instruction` — инструкции (INS-XXX из реестра, можно использовать `registry_ref`)

**Когда указывать**:
- При создании задачи: укажите все релевантные документы
- При изменении логики: обновите список, если добавились новые документы
- При работе с осями состояния: обязательно `STATE_CATALOG.md` и `interaction_matrix.yaml`
- При изменении требований: обязательно `PROJECT_REQUIREMENTS.md`

**Примеры по типам задач**:

| Тип задачи | Обязательные документы |
|:-----------|:----------------------|
| Изменение осей состояния | `STATE_CATALOG.md`, `interaction_matrix.yaml`, `gateways.py` |
| Изменение требований | `PROJECT_REQUIREMENTS.md`, `REQUIREMENTS_SOURCE_MAP.md` |
| Изменение архитектуры | `ARCHITECTURE_OVERVIEW.md`, `ASSISTANT_COORDINATION_PROTOCOL.md` |
| Packaging/Release | `PACKAGING_FINAL_GUIDE.md`, `PRE_PACKAGING_VERIFICATION.md` |
| Интеграция модуля | `ARCHITECTURE_OVERVIEW.md`, инструкция модуля (INS-XXX) |

**Часто используемые документы**:

| Документ | Тип | Когда использовать |
|:---------|:----|:-------------------|
| `Docs/STATE_CATALOG.md` | source_of_truth | Работа с осями состояния, разрешениями, режимами |
| `config/interaction_matrix.yaml` | control | Правила взаимодействия осей, приоритеты решений |
| `Docs/PROJECT_REQUIREMENTS.md` | control | Изменение логики, добавление функциональности |
| `Docs/ARCHITECTURE_OVERVIEW.md` | reference | Понимание архитектуры, интеграция модулей |
| `Docs/AGENTS.md` | reference | Правила работы ассистентов, процессы |
| `integration/core/gateways.py` | control | Реализация логики принятия решений |
| `config/unified_config.yaml` | source_of_truth | Конфигурация системы, feature flags |

---

## 🔍 Просмотр активных задач

**Как увидеть активные задачи**:

1. **Через UI** (рекомендуется):
   ```bash
   python3 scripts/server.py
   # Открыть http://localhost:8000/
   ```
   - Фильтр: "In Progress" показывает все задачи в работе
   - Фильтр по workspace: "Client" / "Server" / "Master" / "All"
   - Фильтр по Epic: выберите нужный Epic

2. **Через файл TASKS.json**:
   ```bash
   cat client/.crm/TASKS.json | jq '.tasks[] | select(.status == "in_progress")'
   ```

3. **Через мастер-план**:
   ```bash
   cat Docs/PROJECT_KANBAN.json | jq '.cards[] | select(.status == "in_progress")'
   ```

**Что видит ассистент**:
- Все задачи со статусом `in_progress` в своем workspace
- Подзадачи текущей задачи
- Прогресс выполнения (поле `progress`)
- Блокеры (поле `blockedBy`)
- Последние изменения (`updated_at`, `modified_by`)

---

## 🔄 Синхронизация

**Источник истины**: [`CRM_CONSOLIDATED_RULES.md`](./CRM_CONSOLIDATED_RULES.md), раздел D "Scripts & APIs" → `task_aggregator.py`

После изменений в TASKS.json, директор (Fix_new) запустит:
```bash
python3 scripts/task_aggregator.py
```

**Автоматическая синхронизация**:
- Сервер `server.py` автоматически запускает агрегатор каждые 5 минут
- Изменения из UI синхронизируются обратно в workspace через поле `owner`

**Правила синхронизации**: см. [CRM_CONSOLIDATED_RULES.md §E](./CRM_CONSOLIDATED_RULES.md#e-federated-workspaces-phase-5)

---

## 📊 Отслеживание прогресса через чек-листы

**Источник истины**: [`CRM_CONSOLIDATED_RULES.md`](./CRM_CONSOLIDATED_RULES.md), раздел D "Scripts & APIs" → `kanban_progress_collector.py`

Если задача связана с инструкцией через `registry_ref`, прогресс автоматически обновляется из чек-листа:

1. **В задаче указать**:
```json
{
    "id": "CLI-002",
    "registry_ref": "INS-007"
}
```

2. **В инструкции** использовать чек-лист:
```markdown
- [x] Шаг 1 выполнен
- [x] Шаг 2 выполнен
- [ ] Шаг 3 в работе
```

3. **Запустить коллектор**:
```bash
python3 scripts/kanban_progress_collector.py
```

Прогресс автоматически обновится в задаче (поле `progress` и `evidence`).

---

## 🔍 Workflow: Начало работы над задачей

1. **Просмотр активных задач**:
   - Откройте UI: `http://localhost:8000/`
   - Или проверьте `client/.crm/TASKS.json` / `server/.crm/TASKS.json` / `.crm/TASKS.json`

2. **Выбор задачи**:
   - Найдите задачу со статусом `in_progress` или `plan`
   - Проверьте блокеры (`blockedBy`) — не начинайте заблокированные задачи
   - Прочитайте описание (`description`) для понимания контекста
   - **Изучите ответственные документы** (`responsible_docs`) — это источники истины и правила

3. **Обновление статуса**:
   - При начале работы: `plan` → `in_progress`
   - Обновите `description` с описанием того, что начали делать
   - Обновите `modified_by` и `updated_at`

4. **Работа над задачей**:
   - Создайте подзадачи для разбиения работы
   - Обновляйте `description` при каждом значимом прогрессе
   - Отмечайте выполненные подзадачи

5. **Завершение**:
   - Завершите все подзадачи
   - Обновите `description` с итоговым описанием выполненной работы
   - Переведите статус: `in_progress` → `testing` или `released`

---

## 📏 Правила

**Источник истины**: [`CRM_CONSOLIDATED_RULES.md`](./CRM_CONSOLIDATED_RULES.md), раздел E "Federated Workspaces"

1. **Префикс ID** соответствует workspace: `NEXY-` для master, `CLI-` для client, `SRV-` для server
2. **Выбор workspace**: Master для задач всего проекта, Client/Server для специфичных задач
3. **Не редактируйте** задачи другого workspace без согласования
4. **Всегда указывайте** `created_by` и `modified_by`
5. **Инкрементируйте** `next_id` в файле после создания задачи
6. **Epic** создаётся только через Fix_new (главный CRM)
7. **Описывайте прогресс** при каждом обновлении задачи
8. **Используйте подзадачи** для разбиения больших задач
9. **Распределяйте подзадачи** между workspace для параллельной работы
10. **Проверяйте блокеры** перед началом работы над задачей
11. **Указывайте ответственные документы** для понимания контекста и следования правилам

---

## ✅ Примеры TASKS.json

### Пример 1: Master задача с распределенными подзадачами

```json
{
    "workspace": "master",
    "id_prefix": "NEXY",
    "next_id": 2,
    "tasks": [
        {
            "id": "NEXY-001",
            "epicId": "EPIC-MVP",
            "title": "Implement authentication system",
            "status": "in_progress",
            "priority": "P1",
            "workspace": "master",
            "created_by": "Antigravity",
            "modified_by": "Antigravity",
            "updated_at": "2026-01-07T10:00:00-05:00",
            "description": "## Проблема\n\nНужна единая система аутентификации для клиента и сервера.\n\n## Текущий прогресс\n\n- [x] Проектирование архитектуры\n- [ ] Реализация клиентской части\n- [ ] Реализация серверной части\n\n## Следующие шаги\n\n1. Завершить клиентскую часть\n2. Завершить серверную часть",
            "subtasks": [
                {
                    "title": "Design auth architecture",
                    "status": "released",
                    "workspace": "master",
                    "assigned_to": "Antigravity"
                },
                {
                    "title": "Implement client-side auth",
                    "status": "in_progress",
                    "workspace": "client",
                    "assigned_to": "Cursor"
                },
                {
                    "title": "Implement server-side auth API",
                    "status": "plan",
                    "workspace": "server",
                    "assigned_to": "Codex"
                }
            ],
            "responsible_docs": [
                {
                    "path": "Docs/ARCHITECTURE_OVERVIEW.md",
                    "type": "reference",
                    "description": "Архитектура системы"
                }
            ]
        }
    ]
}
```

### Пример 2: Client задача

**Полный пример**: см. [CODEX_PROMPT.md §8.3](./CODEX_PROMPT.md#83-создание-задачи)

---

## 📚 Ссылки на источники истины

- **Общие правила**: [`CRM_CONSOLIDATED_RULES.md`](./CRM_CONSOLIDATED_RULES.md)
- **Правила для Codex**: [`CODEX_PROMPT.md`](./CODEX_PROMPT.md), раздел 8
- **Правила для Antigravity**: [`ANTIGRAVITY_PROMPT.md`](./ANTIGRAVITY_PROMPT.md), раздел 8
- **Реестр инструкций**: [`CRM_INSTRUCTION_REGISTRY.md`](./CRM_INSTRUCTION_REGISTRY.md)
- **Мастер-стандарт**: [`CRM_MASTER_INSTRUCTION.md`](./CRM_MASTER_INSTRUCTION.md)

---

**Примечание**: Этот документ — entrypoint. При конфликте правил приоритет у [`CRM_CONSOLIDATED_RULES.md`](./CRM_CONSOLIDATED_RULES.md).
