# 🔧 Исправление проблемы с Google Speech Recognition callback

**Дата:** 2025-12-13  
**Статус:** Внедрено

## 🔍 Проблема

Из логов видно:
- **Строка 87-101**: Google Speech Recognition callback НЕ вызван через 2 секунды после `listen_in_background`
- **Строка 103**: Используется LEGACY путь, который блокирует микрофон для других приложений
- **Строка 421**: `_use_avf=True`, но `_google_recognizer=None` и `_google_microphone=None`

**Корневая причина**: При `_use_avf=True` код пытается использовать Google Speech Recognition, но callback не вызывается (возможно, из-за конфликта доступа к микрофону или проблем с PyAudio). В результате система переходит на LEGACY путь, который блокирует микрофон.

## ✅ Решение

### 1. Добавлено детальное логирование диагностики callback
- Логирование состояния `_google_recognizer` и `_google_microphone`
- Логирование всех параметров микрофона перед вызовом `listen_in_background`

### 2. Реализован автоматический переход на LEGACY путь при ошибке callback
- Если callback не вызван через 2 секунды → выбрасывается `RuntimeError` с сообщением "callback not called - falling back to LEGACY path"
- Исключение перехватывается, устанавливается флаг `use_legacy_fallback = True`
- Код продолжает выполнение и переходит на LEGACY путь вместо возврата из функции

### 3. Улучшена обработка исключений
- Разделена обработка `RuntimeError` о callback (fallback на LEGACY) и других ошибок
- При ошибке callback код НЕ возвращается из функции, а продолжает выполнение для перехода на LEGACY путь

## 📊 Изменения в коде

### Файл: `integration/integrations/voice_recognition_integration.py`

1. **Строка 831**: Добавлен флаг `use_legacy_fallback = False` для контроля перехода на LEGACY путь

2. **Строки 1237-1252**: Добавлено детальное логирование диагностики callback:
   - `_google_recognizer` и `_google_microphone` состояния
   - Все параметры микрофона

3. **Строки 1253-1254**: При ошибке callback:
   - Останавливается Google запись
   - Очищается состояние
   - Закрывается микрофон
   - Выбрасывается `RuntimeError` для перехода на LEGACY путь

4. **Строки 1259-1271**: Обработка `RuntimeError` о callback:
   - Если это ошибка callback → устанавливается флаг `use_legacy_fallback = True`
   - Код НЕ возвращается, а продолжает выполнение

5. **Строки 1391-1400**: Изменена логика перехода на LEGACY путь:
   - Проверяется флаг `use_legacy_fallback` ИЛИ условие `not (self._use_avf and self._avf_engine is not None)`
   - Добавлено логирование причины перехода на LEGACY путь

## 🎯 Ожидаемое поведение

1. При активации микрофона через Shortcut (Ctrl+N):
   - Система пытается использовать AVF→Google путь
   - Если callback не вызывается через 2 секунды → автоматический переход на LEGACY путь
   - LEGACY путь активирует микрофон, но может блокировать его для других приложений

2. В логах будет видно:
   - Детальную диагностику состояния перед вызовом `listen_in_background`
   - Причину перехода на LEGACY путь (callback_failed или другие причины)
   - Состояние `_google_recognizer` и `_google_microphone` при остановке записи

## ⚠️ Известные ограничения

1. **LEGACY путь блокирует микрофон**: Это известная проблема, которая требует дальнейшего исправления
2. **Callback не вызывается**: Требуется дополнительная диагностика причин (конфликт доступа, PyAudio, разрешения)

## 🔄 Следующие шаги

1. **Проверить логи после внедрения**: Убедиться, что переход на LEGACY путь происходит корректно
2. **Диагностировать причину отсутствия callback**: Проверить конфликты доступа к микрофону, PyAudio, разрешения
3. **Исправить проблему с callback**: После выявления причины исправить проблему, чтобы callback вызывался корректно

