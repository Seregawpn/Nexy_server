# 🚀 Создание развертывания Azure OpenAI

**Проблема:** Развертывание `OpenAICreate-20260120103424` не найдено в ресурсе `nexy-ai-core-01`

---

## ✅ Решение: Создать новое развертывание

Есть два способа создать развертывание:

---

## 📋 Способ 1: Через Azure Portal (рекомендуется)

### Шаг 1: Откройте Azure Portal

1. Перейдите на: https://portal.azure.com
2. Войдите в свой аккаунт

### Шаг 2: Найдите ресурс Azure OpenAI

1. В поиске введите: `nexy-ai-core-01`
2. Или перейдите: **All resources** → найдите ресурс типа **"Cognitive Services"** или **"Azure OpenAI"**

### Шаг 3: Откройте раздел развертываний

1. Откройте ресурс `nexy-ai-core-01`
2. В меню слева найдите: **"Model deployments"** или **"Deployments"**
3. Или откройте: [Azure OpenAI Studio](https://oai.azure.com/) → выберите ресурс → **"Deployments"**

### Шаг 4: Создайте новое развертывание

1. Нажмите кнопку **"Create"** или **"Deploy model"** или **"+ Create"**
2. Заполните форму:

   **Deployment name:**
   ```
   gpt-4
   ```
   (или любое другое имя, например: `gpt-35-turbo`, `my-deployment`)

   **Model:**
   - Выберите модель из списка (например: `gpt-4`, `gpt-35-turbo`, `gpt-4o`)
   - Если модели нет в списке, выберите доступную

   **Other settings:**
   - Оставьте значения по умолчанию или настройте по необходимости

3. Нажмите **"Create"** или **"Deploy"**

### Шаг 5: Дождитесь готовности

1. Дождитесь, пока статус изменится на **"Succeeded"** (может занять 5-10 минут)
2. Скопируйте **точное имя** развертывания (то, что вы указали в Deployment name)

### Шаг 6: Протестируйте

После того, как развертывание готово, запустите тест:

```bash
source client/.venv/bin/activate
python3 scripts/find_azure_deployment.py \
    "https://nexy-ai-core-01.openai.azure.com" \
    "4rj11kQNEfSXwmOUldmRuEdpK6ObU5MqqQiWgiAKyzwEzGj3NluTJQQJ99CAACBsN54XJ3w3AAABACOGjJx5" \
    "gpt-4"
```

(Замените `gpt-4` на имя, которое вы указали при создании)

---

## 💻 Способ 2: Через Azure CLI

### Предварительные требования

1. Установите Azure CLI: https://docs.microsoft.com/cli/azure/install-azure-cli
2. Выполните вход: `az login`

### Создание развертывания

```bash
# Установите переменные
RESOURCE_NAME="nexy-ai-core-01"
RESOURCE_GROUP="NetworkWatcherRG"  # Замените на вашу группу ресурсов
DEPLOYMENT_NAME="gpt-4"
MODEL_NAME="gpt-4"  # Или другая доступная модель

# Создайте развертывание
az cognitiveservices account deployment create \
    --name "$RESOURCE_NAME" \
    --resource-group "$RESOURCE_GROUP" \
    --deployment-name "$DEPLOYMENT_NAME" \
    --model-name "$MODEL_NAME" \
    --model-version "0613" \
    --model-format "OpenAI"
```

### Проверка статуса

```bash
# Проверьте статус развертывания
az cognitiveservices account deployment show \
    --name "$RESOURCE_NAME" \
    --resource-group "$RESOURCE_GROUP" \
    --deployment-name "$DEPLOYMENT_NAME"
```

Дождитесь статуса `"provisioningState": "Succeeded"`

---

## 🧪 После создания развертывания

### Тест 1: Проверка через скрипт поиска

```bash
source client/.venv/bin/activate
python3 scripts/find_azure_deployment.py \
    "https://nexy-ai-core-01.openai.azure.com" \
    "4rj11kQNEfSXwmOUldmRuEdpK6ObU5MqqQiWgiAKyzwEzGj3NluTJQQJ99CAACBsN54XJ3w3AAABACOGjJx5" \
    "ВАШЕ_ИМЯ_РАЗВЕРТЫВАНИЯ"
```

### Тест 2: Полный тест всех форматов

```bash
python3 scripts/test_azure_openai_config.py \
    "https://nexy-ai-core-01.openai.azure.com" \
    "ВАШЕ_ИМЯ_РАЗВЕРТЫВАНИЯ" \
    "4rj11kQNEfSXwmOUldmRuEdpK6ObU5MqqQiWgiAKyzwEzGj3NluTJQQJ99CAACBsN54XJ3w3AAABACOGjJx5"
```

### Тест 3: Интерактивная настройка

```bash
python3 scripts/azure_openai_interactive_setup.py
```

---

## 📝 Рекомендуемые имена развертываний

- `gpt-4` — для GPT-4
- `gpt-35-turbo` — для GPT-3.5 Turbo
- `gpt-4o` — для GPT-4o (если доступно)
- `gpt-4o-mini` — для GPT-4o Mini (если доступно)

**Важно:** Используйте простые имена без пробелов и специальных символов (кроме дефиса и подчеркивания).

---

## ✅ Чек-лист

- [ ] Открыт Azure Portal
- [ ] Найден ресурс `nexy-ai-core-01`
- [ ] Открыт раздел "Model deployments"
- [ ] Создано новое развертывание
- [ ] Статус развертывания "Succeeded"
- [ ] Скопировано точное имя развертывания
- [ ] Запущен тест с правильным именем
- [ ] Тест прошел успешно

---

## 🔗 Полезные ссылки

- [Azure Portal](https://portal.azure.com)
- [Azure OpenAI Studio](https://oai.azure.com/)
- `Docs/AZURE_OPENAI_INTERACTIVE_GUIDE.md` — интерактивная настройка
- `scripts/find_azure_deployment.py` — поиск правильного имени

---

**Следующий шаг:** Создайте развертывание в Azure Portal и протестируйте с правильным именем!
