# 🧪 Руководство по тестированию Этапа 1: Централизованное управление состоянием микрофона

## ✅ Что было реализовано

1. **ApplicationStateManager** - добавлены методы управления состоянием микрофона:
   - `set_microphone_state(state, session_id, reason)` - установка состояния
   - `get_microphone_state()` - получение состояния
   - `is_microphone_active()` - проверка активности
   - `force_close_microphone(reason)` - принудительное закрытие

2. **VoiceRecognitionIntegration** - синхронизация состояния микрофона:
   - Обновление состояния при `recording_start` → "opening" → "active"
   - Обновление состояния при `recording_stop` → "closing" → "idle"
   - Обработка `recording_stop` с `session_id=None` для принудительного закрытия
   - Обновление состояния при ошибках → "error"

3. **InputProcessingIntegration** - использование централизованного состояния:
   - Все проверки `_mic_active` заменены на `state_manager.is_microphone_active()`
   - Принудительное закрытие микрофона при SHORT_PRESS
   - Синхронизация состояния во всех сценариях

---

## 🧪 Сценарии для тестирования

### Сценарий 1: Нормальная работа (LONG_PRESS → RELEASE)

**Шаги:**
1. Нажмите и удерживайте Control+N (LONG_PRESS)
2. Дождитесь сигнала открытия микрофона
3. Произнесите фразу
4. Отпустите Control+N (RELEASE)

**Ожидаемое поведение:**
- ✅ Микрофон открывается (состояние: "opening" → "active")
- ✅ Сигнал воспроизводится
- ✅ Запись идет нормально
- ✅ При RELEASE микрофон закрывается (состояние: "closing" → "idle")
- ✅ Распознавание запускается
- ✅ В логах видно синхронизацию состояния через `state_manager`

**Проверка в логах:**
```
🔄 [MIC_STATE] idle → opening (session=..., reason=recording_start)
🔄 [MIC_STATE] opening → active (session=..., reason=microphone_opened)
🔄 [MIC_STATE] active → closing (session=..., reason=recording_stop)
🔄 [MIC_STATE] closing → idle (session=..., reason=microphone_closed)
```

---

### Сценарий 2: Отмена записи (LONG_PRESS → SHORT_PRESS)

**Шаги:**
1. Нажмите и удерживайте Control+N (LONG_PRESS)
2. Дождитесь сигнала открытия микрофона
3. **БЫСТРО** нажмите Control+N еще раз (SHORT_PRESS) - отмена записи

**Ожидаемое поведение:**
- ✅ Микрофон открывается (состояние: "opening" → "active")
- ✅ При SHORT_PRESS микрофон **принудительно закрывается** (состояние: "active" → "idle")
- ✅ Нет залипания микрофона
- ✅ В логах видно принудительное закрытие

**Проверка в логах:**
```
🔄 [MIC_STATE] idle → opening (session=..., reason=recording_start)
🔄 [MIC_STATE] opening → active (session=..., reason=microphone_opened)
⚠️ VOICE: recording_stop с session_id=None, но микрофон активен - принудительно останавливаем микрофон
⚠️ [MIC_STATE] Принудительное закрытие микрофона: recording_stop_no_session
🔄 [MIC_STATE] active → idle (session=None, reason=recording_stop_no_session)
```

---

### Сценарий 3: Повторная активация (LONG_PRESS → RELEASE → LONG_PRESS)

**Шаги:**
1. Нажмите и удерживайте Control+N (LONG_PRESS)
2. Отпустите Control+N (RELEASE)
3. **СРАЗУ** нажмите и удерживайте Control+N еще раз (LONG_PRESS)

**Ожидаемое поведение:**
- ✅ Первая активация работает нормально
- ✅ Микрофон закрывается после RELEASE
- ✅ Вторая активация работает нормально
- ✅ Нет залипания микрофона
- ✅ Состояние синхронизировано между активациями

**Проверка в логах:**
```
🔄 [MIC_STATE] idle → opening (session=..., reason=recording_start)
🔄 [MIC_STATE] opening → active (session=..., reason=microphone_opened)
🔄 [MIC_STATE] active → closing (session=..., reason=recording_stop)
🔄 [MIC_STATE] closing → idle (session=..., reason=microphone_closed)
🔄 [MIC_STATE] idle → opening (session=..., reason=recording_start)
🔄 [MIC_STATE] opening → active (session=..., reason=microphone_opened)
```

---

### Сценарий 4: Ошибка при открытии микрофона

**Шаги:**
1. Отключите микрофон в системных настройках (или заблокируйте доступ)
2. Нажмите и удерживайте Control+N (LONG_PRESS)

**Ожидаемое поведение:**
- ✅ Система пытается открыть микрофон
- ✅ При ошибке состояние переходит в "error"
- ✅ Нет залипания в состоянии "opening"
- ✅ Система восстанавливается (переход на симуляцию или повторная попытка)

**Проверка в логах:**
```
🔄 [MIC_STATE] idle → opening (session=..., reason=recording_start)
❌ VOICE: failed to start listening after 3 attempts
🔄 [MIC_STATE] opening → error (session=None, reason=recording_failed)
```

---

### Сценарий 5: Быстрые повторные нажатия (Debounce)

**Шаги:**
1. Быстро нажмите Control+N несколько раз подряд (< 0.1s между нажатиями)

**Ожидаемое поведение:**
- ✅ Только первое нажатие обрабатывается
- ✅ Повторные нажатия игнорируются (debounce)
- ✅ Нет конфликтов состояния микрофона

**Проверка в логах:**
```
PRESS debounced: too rapid
```

---

## 🔍 Как проверить состояние микрофона

### В логах

Ищите строки с префиксом `🔄 [MIC_STATE]`:
```bash
grep "\[MIC_STATE\]" logs/app.log
```

### Через ApplicationStateManager

Если у вас есть доступ к `state_manager` в коде:
```python
# Проверка состояния
state, session_id = state_manager.get_microphone_state()
print(f"State: {state}, Session: {session_id}")

# Проверка активности
is_active = state_manager.is_microphone_active()
print(f"Is active: {is_active}")
```

---

## ⚠️ Известные проблемы (если обнаружены)

### Проблема: Микрофон не закрывается при SHORT_PRESS

**Симптомы:**
- После SHORT_PRESS микрофон остается активным
- В логах нет `🔄 [MIC_STATE] active → idle`

**Проверка:**
1. Проверьте, что `VoiceRecognitionIntegration._on_recording_stop` обрабатывает `session_id=None`
2. Проверьте, что `state_manager.force_close_microphone()` вызывается
3. Проверьте логи на наличие ошибок

---

### Проблема: Состояние не синхронизируется

**Симптомы:**
- `state_manager.is_microphone_active()` возвращает `False`, но микрофон работает
- Или наоборот: возвращает `True`, но микрофон закрыт

**Проверка:**
1. Проверьте, что `VoiceRecognitionIntegration` обновляет состояние при всех переходах
2. Проверьте логи на наличие ошибок при обновлении состояния
3. Проверьте, что нет race conditions (используйте `threading.Lock`)

---

## 📊 Метрики успеха

После тестирования всех сценариев проверьте:

1. ✅ **Нет залипания микрофона** - все сценарии завершаются корректно
2. ✅ **Синхронизация состояния** - `state_manager` всегда отражает реальное состояние
3. ✅ **Принудительное закрытие работает** - SHORT_PRESS корректно закрывает микрофон
4. ✅ **Нет race conditions** - повторные активации работают стабильно
5. ✅ **Обработка ошибок** - при ошибках состояние корректно обновляется

---

## 🚀 Следующие шаги после тестирования

Если все сценарии работают корректно:
1. ✅ Этап 1 завершен успешно
2. 📝 Можно переходить к Этапу 2 (полная централизация с `MicrophoneStateManager`) - опционально
3. 📝 Или оставить текущее решение, если оно решает все проблемы

Если обнаружены проблемы:
1. 🔍 Соберите логи с проблемными сценариями
2. 📝 Опишите проблему и шаги для воспроизведения
3. 🔧 Исправьте проблему и повторите тестирование

