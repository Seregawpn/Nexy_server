# 📋 Требования к Идеальной Архитектуре

**Дата:** 2025-12-13  
**Статус:** Финальные требования с учетом архитектуры, правил проекта и логирования

---

## 📊 Часть 1: Общие Требования

### 1.1 Соответствие архитектуре проекта

**Требование REQ-ARCH-001: Разделение ответственности**
- ✅ Модули (`modules/`) — низкоуровневая логика, изолированная ответственность
- ✅ Интеграции (`integration/integrations/`) — координация через EventBus, тонкие обёртки
- ✅ Core (`integration/core/`) — инфраструктура (EventBus, StateManager, ErrorHandler)
- ✅ Workflows (`integration/workflows/`) — координаторы режимов

**Проверка:**
- [ ] Модуль отвечает за одну задачу
- [ ] Интеграция только координирует через EventBus
- [ ] Core компоненты не содержат бизнес-логику

---

**Требование REQ-ARCH-002: Порядок инициализации**
- ✅ Соблюдение порядка в `SimpleModuleCoordinator._create_integrations()`
- ✅ Зависимости между интеграциями учтены
- ✅ Блокирующие интеграции (FirstRunPermissions, PermissionRestart) инициализируются первыми

**Проверка:**
- [ ] Порядок инициализации соответствует `SimpleModuleCoordinator`
- [ ] Зависимости между интеграциями учтены
- [ ] Блокирующие интеграции инициализируются первыми

---

**Требование REQ-ARCH-003: Защита модулей от изменений**
- ✅ Модули изолированы и имеют четко определенные границы
- ✅ Изменения в модули только через ADR (Architecture Decision Record)
- ✅ Интеграция с модулями ТОЛЬКО через отдельные интеграции
- ✅ Прямые изменения в модули запрещены без обоснования

**Проверка:**
- [ ] Модуль изолирован и имеет четкие границы
- [ ] Изменения в модуль только через ADR
- [ ] Интеграция с модулем только через интеграцию

---

### 1.2 Соответствие правилам проекта

**Требование REQ-RULES-001: Конфигурация**
- ✅ Единственный источник правды: `config/unified_config.yaml`
- ✅ Загрузка через `UnifiedConfigLoader`
- ✅ Валидация через схемы `config/schemas/`
- ✅ Новые параметры добавляются в `unified_config.yaml`

**Проверка:**
- [ ] Конфигурация в `unified_config.yaml`
- [ ] Загрузка через `UnifiedConfigLoader`
- [ ] Валидация через схемы

---

**Требование REQ-RULES-002: EventBus контракты**
- ✅ Каждая интеграция имеет четкий контракт EventBus
- ✅ События документированы с полными payload схемами
- ✅ Валидация payload на границе интеграции
- ✅ Обработка ошибок через ErrorHandler с кодами

**Проверка:**
- [ ] Контракт EventBus создан
- [ ] События документированы
- [ ] Payload валидируется
- [ ] Ошибки обрабатываются через ErrorHandler

---

**Требование REQ-RULES-003: Feature Flags и Kill-Switches**
- ✅ Любое поведение, затрагивающее >2 оси состояния, раскатывается через feature flag
- ✅ Мгновенный откат через kill-switch
- ✅ Все флаги зарегистрированы в `Docs/FEATURE_FLAGS.md`

**Проверка:**
- [ ] Feature flags используются для >2 осей состояния
- [ ] Kill-switch реализован
- [ ] Флаги зарегистрированы в `Docs/FEATURE_FLAGS.md`

---

## 📊 Часть 2: Требования к Модулям

### 2.1 Структура модуля

**Требование REQ-MOD-001: Размер модуля**
- ✅ Размер модуля ≤ 500 строк
- ✅ Количество методов ≤ 20
- ✅ Количество блоков try/except ≤ 50

**Проверка:**
- [ ] Размер модуля ≤ 500 строк
- [ ] Количество методов ≤ 20
- [ ] Количество блоков try/except ≤ 50

---

**Требование REQ-MOD-002: Ответственность модуля**
- ✅ Модуль отвечает за одну конкретную задачу
- ✅ Модуль не знает о других модулях (кроме явных зависимостей)
- ✅ Модуль не знает о EventBus
- ✅ Модуль возвращает результаты, не публикует события

**Проверка:**
- [ ] Модуль отвечает за одну задачу
- [ ] Модуль не знает о других модулях
- [ ] Модуль не знает о EventBus
- [ ] Модуль возвращает результаты

---

**Требование REQ-MOD-003: Зависимости модуля**
- ✅ Зависимости через конструктор
- ✅ Конфигурация через конструктор
- ✅ Нет глобальных зависимостей
- ✅ Легко тестировать изолированно

**Проверка:**
- [ ] Зависимости через конструктор
- [ ] Конфигурация через конструктор
- [ ] Нет глобальных зависимостей
- [ ] Легко тестировать изолированно

---

**Требование REQ-MOD-004: Документация модуля**
- ✅ README.md с описанием модуля
- ✅ Публичные интерфейсы в types.py
- ✅ Примеры использования
- ✅ Диагностические эндпоинты (если нужно)

**Проверка:**
- [ ] README.md создан
- [ ] types.py с публичными интерфейсами
- [ ] Примеры использования
- [ ] Диагностические эндпоинты (если нужно)

---

### 2.2 Логирование в модулях

**Требование REQ-MOD-LOG-001: Формат логов**
- ✅ Формат: `YYYY-MM-DD HH:MM:SS - Nexy - LEVEL - [session_id] - Message`
- ✅ Структурированные события: `module.start|ok|fail` с session_id, duration_ms, контекстом
- ✅ Ошибки: code, reason, module, retries, timeout_ms через ErrorHandler

**Пример:**
```python
logger.info("2024-01-15 10:30:45 - Nexy - INFO - [abc123] - avf_manager.initialize.start - duration_ms=150")
logger.info("2024-01-15 10:30:46 - Nexy - INFO - [abc123] - avf_manager.initialize.ok - duration_ms=150")
logger.error("2024-01-15 10:30:47 - Nexy - ERROR - [abc123] - avf_manager.activate.fail - error_code=E_MIC_ACTIVATION_FAILED")
```

**Проверка:**
- [ ] Формат логов соответствует стандарту
- [ ] Структурированные события с session_id
- [ ] Ошибки через ErrorHandler

---

**Требование REQ-MOD-LOG-002: Уровни логирования**
- ✅ `warn` — ожидаемые аномалии
- ✅ `error` — потеря функции
- ✅ `info` — бизнес-события
- ✅ `debug` — детальная диагностика

**Проверка:**
- [ ] Уровни логирования используются правильно
- [ ] Нет избыточного логирования
- [ ] Нет недостаточного логирования

---

**Требование REQ-MOD-LOG-003: Диагностическое логирование**
- ✅ Логирование на каждом этапе инициализации
- ✅ Логирование критических операций (активация, деактивация)
- ✅ Логирование ошибок с полным контекстом
- ✅ Логирование метрик (duration_ms, retry_count)

**Пример:**
```python
async def initialize(self) -> bool:
    logger.info("🔍 [AVF] Начало инициализации AVF...")
    try:
        start_time = time.monotonic()
        self._engine = AVFAudioEngine(self._config)
        duration_ms = int((time.monotonic() - start_time) * 1000)
        logger.info(f"✅ [AVF] AVFAudioEngine инициализирован - duration_ms={duration_ms}")
        return True
    except Exception as e:
        logger.error(f"❌ [AVF] Ошибка инициализации: {e}")
        logger.exception("❌ [AVF] Детали исключения:")
        return False
```

**Проверка:**
- [ ] Логирование на каждом этапе инициализации
- [ ] Логирование критических операций
- [ ] Логирование ошибок с контекстом
- [ ] Логирование метрик

---

**Требование REQ-MOD-LOG-004: Избегание PII/секретов**
- ✅ Нет PII/секретов в логах (микрофонные данные, токены, URL)
- ✅ Маскировка чувствительных данных
- ✅ Лейблы в событиях без чувствительных данных

**Проверка:**
- [ ] Нет PII/секретов в логах
- [ ] Чувствительные данные замаскированы
- [ ] События без чувствительных данных

---

## 📊 Часть 3: Требования к Интеграциям

### 3.1 Структура интеграции

**Требование REQ-INT-001: Размер интеграции**
- ✅ Размер интеграции ≤ 500 строк
- ✅ Количество методов ≤ 15
- ✅ Количество блоков try/except ≤ 30

**Проверка:**
- [ ] Размер интеграции ≤ 500 строк
- [ ] Количество методов ≤ 15
- [ ] Количество блоков try/except ≤ 30

---

**Требование REQ-INT-002: Ответственность интеграции**
- ✅ Интеграция только координирует через EventBus
- ✅ Логика в модулях, не в интеграции
- ✅ Подписки и публикации событий документированы
- ✅ Контракты событий валидируются

**Проверка:**
- [ ] Интеграция только координирует
- [ ] Логика в модулях
- [ ] События документированы
- [ ] Контракты валидируются

---

**Требование REQ-INT-003: Состояние в интеграции**
- ✅ Нет локальных флагов состояния
- ✅ Использование только `ApplicationStateManager`
- ✅ Все изменения состояния через `state_manager.set_*()`
- ✅ Все проверки состояния через `state_manager.is_*()`

**Проверка:**
- [ ] Нет локальных флагов состояния
- [ ] Использование только state_manager
- [ ] Изменения состояния через state_manager
- [ ] Проверки состояния через state_manager

---

**Требование REQ-INT-004: Инициализация интеграции**
- ✅ Понятная последовательность инициализации
- ✅ Диагностическое логирование на каждом этапе
- ✅ Обработка ошибок инициализации
- ✅ Возврат bool результата

**Проверка:**
- [ ] Понятная последовательность
- [ ] Диагностическое логирование
- [ ] Обработка ошибок
- [ ] Возврат bool результата

---

### 3.2 Логирование в интеграциях

**Требование REQ-INT-LOG-001: Логирование событий**
- ✅ Логирование входа в обработчик события
- ✅ Логирование результата обработки события
- ✅ Логирование ошибок обработки события
- ✅ Логирование метрик (duration_ms)

**Пример:**
```python
async def _on_recording_start(self, event: Dict[str, Any]):
    session_id = event.get("data", {}).get("session_id")
    logger.info(f"🔍 [VOICE] Начало записи: session_id={session_id}")
    start_time = time.monotonic()
    
    try:
        # Координация
        device_info = await self._avf_manager.activate(duration_sec=1.0)
        duration_ms = int((time.monotonic() - start_time) * 1000)
        logger.info(f"✅ [VOICE] Запись активирована - duration_ms={duration_ms}")
    except Exception as e:
        duration_ms = int((time.monotonic() - start_time) * 1000)
        logger.error(f"❌ [VOICE] Ошибка активации записи - duration_ms={duration_ms}: {e}")
        await self.error_handler.handle_error(...)
```

**Проверка:**
- [ ] Логирование входа в обработчик
- [ ] Логирование результата
- [ ] Логирование ошибок
- [ ] Логирование метрик

---

**Требование REQ-INT-LOG-002: Логирование координации**
- ✅ Логирование вызовов менеджеров
- ✅ Логирование публикации событий
- ✅ Логирование обновления состояния
- ✅ Логирование последовательности операций

**Пример:**
```python
async def _on_recording_start(self, event: Dict[str, Any]):
    logger.info("🔍 [VOICE] Начало координации записи")
    
    # 1. AVF диагностика
    logger.info("🔍 [VOICE] Шаг 1: AVF диагностика...")
    device_info = await self._avf_manager.activate(duration_sec=1.0)
    logger.info(f"✅ [VOICE] AVF диагностика получена: {device_info.device_name}")
    
    # 2. Деактивация AVF
    logger.info("🔍 [VOICE] Шаг 2: Деактивация AVF...")
    await self._avf_manager.deactivate()
    logger.info("✅ [VOICE] AVF деактивирован")
    
    # 3. Пауза
    logger.info("🔍 [VOICE] Шаг 3: Пауза 0.2 сек...")
    await asyncio.sleep(0.2)
    
    # 4. Google активация
    logger.info("🔍 [VOICE] Шаг 4: Google активация...")
    await self._google_manager.activate(callback=self._on_google_audio_chunk)
    logger.info("✅ [VOICE] Google запись активирована")
    
    # 5. Обновление состояния
    logger.info("🔍 [VOICE] Шаг 5: Обновление состояния...")
    self.state_manager.set_microphone_state("active", session_id)
    logger.info("✅ [VOICE] Состояние обновлено")
    
    # 6. Публикация события
    logger.info("🔍 [VOICE] Шаг 6: Публикация события...")
    await self.event_bus.publish("microphone.opened", {...})
    logger.info("✅ [VOICE] Событие опубликовано")
```

**Проверка:**
- [ ] Логирование вызовов менеджеров
- [ ] Логирование публикации событий
- [ ] Логирование обновления состояния
- [ ] Логирование последовательности

---

**Требование REQ-INT-LOG-003: Decision-лог (для gateways)**
- ✅ Канонический формат: `decision=<start|abort|retry|degrade> ctx={mic=...,screen=...,device=...,network=...,firstRun=...,appMode=...} source=<domain> duration_ms=<int>`
- ✅ Обязателен для всех gateways, принимающих решения
- ✅ Проверяется при ревью и релизе

**Пример:**
```python
def decide_start_listening(snapshot: Snapshot, source: str) -> Decision:
    start_time = time.monotonic()
    
    # Логика принятия решения
    decision = Decision.START if ... else Decision.ABORT
    
    duration_ms = int((time.monotonic() - start_time) * 1000)
    logger.info(
        f"decision={decision.value} "
        f"ctx={{mic={snapshot.perm_mic.value},screen={snapshot.perm_screen.value},"
        f"device={snapshot.device_input.value},network={snapshot.network.value},"
        f"firstRun={snapshot.first_run},appMode={snapshot.app_mode.value}}} "
        f"source=audio_gateway duration_ms={duration_ms}"
    )
    
    return decision
```

**Проверка:**
- [ ] Decision-лог в каноническом формате
- [ ] Decision-лог для всех gateways
- [ ] Decision-лог проверяется при ревью

---

## 📊 Часть 4: Требования к Централизации

### 4.1 Централизация состояния

**Требование REQ-CENT-STATE-001: Единый источник истины**
- ✅ Только `ApplicationStateManager` управляет состоянием
- ✅ Нет локальных флагов состояния в интеграциях/модулях
- ✅ Все изменения состояния через `state_manager.set_*()`
- ✅ Все проверки состояния через `state_manager.is_*()`

**Проверка:**
- [ ] Только ApplicationStateManager управляет состоянием
- [ ] Нет локальных флагов состояния
- [ ] Изменения состояния через state_manager
- [ ] Проверки состояния через state_manager

---

**Требование REQ-CENT-STATE-002: Атомарные операции**
- ✅ Все изменения состояния с lock
- ✅ Thread-safe операции
- ✅ Гарантированная консистентность

**Проверка:**
- [ ] Изменения состояния с lock
- [ ] Thread-safe операции
- [ ] Консистентность гарантирована

---

### 4.2 Централизация конфигурации

**Требование REQ-CENT-CONFIG-001: Единый источник конфигурации**
- ✅ Единственный источник: `config/unified_config.yaml`
- ✅ Загрузка через `UnifiedConfigLoader`
- ✅ Валидация через схемы `config/schemas/`
- ✅ Документация конфигурации

**Проверка:**
- [ ] Конфигурация в unified_config.yaml
- [ ] Загрузка через UnifiedConfigLoader
- [ ] Валидация через схемы
- [ ] Документация конфигурации

---

**Требование REQ-CENT-CONFIG-002: Feature Flags и Kill-Switches**
- ✅ Feature flags через `unified_config.yaml` или env переменные
- ✅ Kill-switches через env переменные или `unified_config.yaml`
- ✅ Все флаги зарегистрированы в `Docs/FEATURE_FLAGS.md`
- ✅ Постепенный rollout (1% → 25% → 100%)

**Проверка:**
- [ ] Feature flags через unified_config или env
- [ ] Kill-switches через env или unified_config
- [ ] Флаги зарегистрированы в Docs/FEATURE_FLAGS.md
- [ ] Постепенный rollout

---

### 4.3 Централизация обработки ошибок

**Требование REQ-CENT-ERROR-001: Единый обработчик ошибок**
- ✅ Все ошибки через `ErrorHandler`
- ✅ Структурированные коды ошибок (E_INPUT_INVALID, E_STATE_INVAR, E_DEP_TIMEOUT, E_DEP_UNAVAILABLE, E_RATE_LIMITED, E_UNKNOWN)
- ✅ Логирование ошибок с контекстом
- ✅ Восстановление состояния после ошибок

**Проверка:**
- [ ] Все ошибки через ErrorHandler
- [ ] Структурированные коды ошибок
- [ ] Логирование с контекстом
- [ ] Восстановление состояния

---

## 📊 Часть 5: Требования к Конфигурациям

### 5.1 AVF Конфигурация

**Требование REQ-CONFIG-AVF-001: Структура конфигурации**
- ✅ Конфигурация в `config/unified_config.yaml` под ключом `audio.avf`
- ✅ Загрузка через `UnifiedConfigLoader.get_audio_avf_config()`
- ✅ Валидация через схему `config/schemas/audio_avf_schema.yaml`
- ✅ Использование в `AVFManager` через `AVFConfig`

**Пример:**
```yaml
audio:
  avf:
    enabled: true
    rollout_percentage: 0
    input_format: "16kHz, mono, int16"
    output_format: "auto"
    buffer_size_ms: 100
    enable_hardware_optimization: true
  
  ks_avf:
    enabled: false  # Kill-switch для AVF
```

**Проверка:**
- [ ] Конфигурация в unified_config.yaml
- [ ] Загрузка через UnifiedConfigLoader
- [ ] Валидация через схему
- [ ] Использование в AVFManager

---

### 5.2 Google Конфигурация

**Требование REQ-CONFIG-GOOGLE-001: Структура конфигурации**
- ✅ Конфигурация в `config/unified_config.yaml` под ключом `voice_recognition`
- ✅ Загрузка через `UnifiedConfigLoader.get_voice_recognition_config()`
- ✅ Валидация через схему `config/schemas/voice_recognition_schema.yaml`
- ✅ Использование в `GoogleManager` через `GoogleConfig`

**Пример:**
```yaml
voice_recognition:
  enabled: true
  language: en-US
  max_alternatives: 3
  phrase_timeout: 0.3
  silence_timeout: 0.8

voice:
  start_retry_delay_ms: 300
```

**Проверка:**
- [ ] Конфигурация в unified_config.yaml
- [ ] Загрузка через UnifiedConfigLoader
- [ ] Валидация через схему
- [ ] Использование в GoogleManager

---

## 📊 Часть 6: Требования к Соединению Компонентов

### 6.1 Соединение модулей с интеграциями

**Требование REQ-CONN-MOD-INT-001: Изоляция модулей**
- ✅ Модули не знают о EventBus
- ✅ Модули не знают о других модулях (кроме явных зависимостей)
- ✅ Модули возвращают результаты, не публикуют события
- ✅ Интеграции координируют через EventBus

**Проверка:**
- [ ] Модули не знают о EventBus
- [ ] Модули не знают о других модулях
- [ ] Модули возвращают результаты
- [ ] Интеграции координируют через EventBus

---

**Требование REQ-CONN-MOD-INT-002: Зависимости через конструктор**
- ✅ Зависимости модулей через конструктор
- ✅ Конфигурация через конструктор
- ✅ Нет глобальных зависимостей
- ✅ Легко тестировать изолированно

**Проверка:**
- [ ] Зависимости через конструктор
- [ ] Конфигурация через конструктор
- [ ] Нет глобальных зависимостей
- [ ] Легко тестировать изолированно

---

### 6.2 Соединение с ApplicationStateManager

**Требование REQ-CONN-STATE-001: Использование state_manager**
- ✅ Все изменения состояния через `state_manager.set_*()`
- ✅ Все проверки состояния через `state_manager.is_*()`
- ✅ Атомарные операции с lock
- ✅ Нет локальных флагов состояния

**Проверка:**
- [ ] Изменения состояния через state_manager
- [ ] Проверки состояния через state_manager
- [ ] Атомарные операции с lock
- [ ] Нет локальных флагов состояния

---

## 📊 Часть 7: Требования к Интеграции в Проект

### 7.1 Интеграция в SimpleModuleCoordinator

**Требование REQ-INTEG-COORD-001: Порядок инициализации**
- ✅ Правильный порядок в `SimpleModuleCoordinator._create_integrations()`
- ✅ Зависимости между интеграциями учтены
- ✅ Блокирующие интеграции инициализируются первыми

**Порядок:**
1. InstanceManager
2. HardwareId
3. FirstRunPermissions (блокирующая)
4. PermissionRestart (блокирующая)
5. Tray
6. ModeManagement
7. InputProcessing
8. VoiceRecognition ← Здесь
9. NetworkManager
10. ...

**Проверка:**
- [ ] Порядок инициализации соответствует SimpleModuleCoordinator
- [ ] Зависимости между интеграциями учтены
- [ ] Блокирующие интеграции инициализируются первыми

---

**Требование REQ-INTEG-COORD-002: Обработка ошибок инициализации**
- ✅ Логирование ошибок инициализации
- ✅ Продолжение инициализации других интеграций при ошибке
- ✅ Возврат bool результата

**Проверка:**
- [ ] Логирование ошибок инициализации
- [ ] Продолжение инициализации при ошибке
- [ ] Возврат bool результата

---

### 7.2 Интеграция в EventBus

**Требование REQ-INTEG-EVENT-001: Контракты событий**
- ✅ Контракт EventBus создан и документирован
- ✅ События документированы с полными payload схемами
- ✅ Валидация payload на границе интеграции
- ✅ Версионирование для breaking changes

**Проверка:**
- [ ] Контракт EventBus создан
- [ ] События документированы
- [ ] Payload валидируется
- [ ] Версионирование для breaking changes

---

**Требование REQ-INTEG-EVENT-002: Приоритеты событий**
- ✅ Использование `EventPriority` для подписок
- ✅ Критические события с `EventPriority.HIGH`
- ✅ Обычные события с `EventPriority.MEDIUM`
- ✅ Низкоприоритетные события с `EventPriority.LOW`

**Проверка:**
- [ ] Использование EventPriority
- [ ] Критические события с HIGH приоритетом
- [ ] Обычные события с MEDIUM приоритетом
- [ ] Низкоприоритетные события с LOW приоритетом

---

### 7.3 Интеграция в конфигурацию

**Требование REQ-INTEG-CONFIG-001: Использование UnifiedConfigLoader**
- ✅ Загрузка конфигурации через `UnifiedConfigLoader`
- ✅ Валидация через схемы
- ✅ Документация конфигурации

**Проверка:**
- [ ] Загрузка через UnifiedConfigLoader
- [ ] Валидация через схемы
- [ ] Документация конфигурации

---

## 📊 Часть 8: Требования к Логированию (Детально)

### 8.1 Общие требования к логированию

**Требование REQ-LOG-001: Формат логов**
- ✅ Формат: `YYYY-MM-DD HH:MM:SS - Nexy - LEVEL - [session_id] - Message`
- ✅ Структурированные события: `module.start|ok|fail` с session_id, duration_ms, контекстом
- ✅ Ошибки: code, reason, module, retries, timeout_ms через ErrorHandler

**Пример:**
```
2024-01-15 10:30:45 - Nexy - INFO - [abc123] - avf_manager.initialize.start - duration_ms=150
2024-01-15 10:30:46 - Nexy - INFO - [abc123] - avf_manager.initialize.ok - duration_ms=150
2024-01-15 10:30:47 - Nexy - ERROR - [abc123] - avf_manager.activate.fail - error_code=E_MIC_ACTIVATION_FAILED
```

**Проверка:**
- [ ] Формат логов соответствует стандарту
- [ ] Структурированные события с session_id
- [ ] Ошибки через ErrorHandler

---

**Требование REQ-LOG-002: Уровни логирования**
- ✅ `warn` — ожидаемые аномалии
- ✅ `error` — потеря функции
- ✅ `info` — бизнес-события
- ✅ `debug` — детальная диагностика

**Проверка:**
- [ ] Уровни логирования используются правильно
- [ ] Нет избыточного логирования
- [ ] Нет недостаточного логирования

---

**Требование REQ-LOG-003: Избегание PII/секретов**
- ✅ Нет PII/секретов в логах (микрофонные данные, токены, URL)
- ✅ Маскировка чувствительных данных
- ✅ Лейблы в событиях без чувствительных данных

**Проверка:**
- [ ] Нет PII/секретов в логах
- [ ] Чувствительные данные замаскированы
- [ ] События без чувствительных данных

---

### 8.2 Логирование инициализации

**Требование REQ-LOG-INIT-001: Логирование этапов инициализации**
- ✅ Логирование начала инициализации
- ✅ Логирование каждого этапа инициализации
- ✅ Логирование завершения инициализации
- ✅ Логирование ошибок инициализации

**Пример:**
```python
async def initialize(self) -> bool:
    logger.info("🔍 [AVF] Начало инициализации AVF...")
    
    # Этап 1: Загрузка конфигурации
    logger.info("🔍 [AVF] Этап 1: Загрузка конфигурации...")
    audio_config = self._load_audio_config()
    logger.info("✅ [AVF] Конфигурация загружена")
    
    # Этап 2: Создание AVFAudioEngine
    logger.info("🔍 [AVF] Этап 2: Создание AVFAudioEngine...")
    try:
        self._engine = AVFAudioEngine(audio_config)
        logger.info("✅ [AVF] AVFAudioEngine создан")
    except Exception as e:
        logger.error(f"❌ [AVF] Ошибка создания AVFAudioEngine: {e}")
        logger.exception("❌ [AVF] Детали исключения:")
        return False
    
    logger.info("✅ [AVF] Инициализация завершена")
    return True
```

**Проверка:**
- [ ] Логирование начала инициализации
- [ ] Логирование каждого этапа
- [ ] Логирование завершения
- [ ] Логирование ошибок

---

### 8.3 Логирование критических операций

**Требование REQ-LOG-CRIT-001: Логирование активации/деактивации**
- ✅ Логирование начала активации
- ✅ Логирование завершения активации
- ✅ Логирование начала деактивации
- ✅ Логирование завершения деактивации
- ✅ Логирование метрик (duration_ms)

**Пример:**
```python
async def activate(self, duration_sec: float = 1.0) -> DeviceInfo:
    logger.info(f"🔍 [AVF] Начало активации микрофона на {duration_sec}с...")
    start_time = time.monotonic()
    
    try:
        result = await self._engine.start_input()
        self._active = True
        
        await asyncio.sleep(duration_sec)
        
        device_info = DeviceInfo(...)
        duration_ms = int((time.monotonic() - start_time) * 1000)
        logger.info(f"✅ [AVF] Микрофон активирован - duration_ms={duration_ms}")
        return device_info
    except Exception as e:
        duration_ms = int((time.monotonic() - start_time) * 1000)
        logger.error(f"❌ [AVF] Ошибка активации - duration_ms={duration_ms}: {e}")
        logger.exception("❌ [AVF] Детали исключения:")
        raise

async def deactivate(self) -> bool:
    logger.info("🔍 [AVF] Начало деактивации микрофона...")
    start_time = time.monotonic()
    
    try:
        if self._engine is not None:
            await self._engine.stop_input()
        self._active = False
        
        duration_ms = int((time.monotonic() - start_time) * 1000)
        logger.info(f"✅ [AVF] Микрофон деактивирован - duration_ms={duration_ms}")
        return True
    except Exception as e:
        duration_ms = int((time.monotonic() - start_time) * 1000)
        logger.error(f"❌ [AVF] Ошибка деактивации - duration_ms={duration_ms}: {e}")
        logger.exception("❌ [AVF] Детали исключения:")
        return False
```

**Проверка:**
- [ ] Логирование начала активации
- [ ] Логирование завершения активации
- [ ] Логирование начала деактивации
- [ ] Логирование завершения деактивации
- [ ] Логирование метрик

---

### 8.4 Логирование координации

**Требование REQ-LOG-COORD-001: Логирование последовательности операций**
- ✅ Логирование каждого шага координации
- ✅ Логирование вызовов менеджеров
- ✅ Логирование публикации событий
- ✅ Логирование обновления состояния

**Пример:**
```python
async def _on_recording_start(self, event: Dict[str, Any]):
    session_id = event.get("data", {}).get("session_id")
    logger.info(f"🔍 [VOICE] Начало координации записи: session_id={session_id}")
    
    # Шаг 1: AVF диагностика
    logger.info("🔍 [VOICE] Шаг 1: AVF диагностика...")
    device_info = await self._avf_manager.activate(duration_sec=1.0)
    logger.info(f"✅ [VOICE] AVF диагностика получена: {device_info.device_name}")
    
    # Шаг 2: Деактивация AVF
    logger.info("🔍 [VOICE] Шаг 2: Деактивация AVF...")
    await self._avf_manager.deactivate()
    logger.info("✅ [VOICE] AVF деактивирован")
    
    # Шаг 3: Пауза
    logger.info("🔍 [VOICE] Шаг 3: Пауза 0.2 сек...")
    await asyncio.sleep(0.2)
    
    # Шаг 4: Google активация
    logger.info("🔍 [VOICE] Шаг 4: Google активация...")
    await self._google_manager.activate(callback=self._on_google_audio_chunk)
    logger.info("✅ [VOICE] Google запись активирована")
    
    # Шаг 5: Обновление состояния
    logger.info("🔍 [VOICE] Шаг 5: Обновление состояния...")
    self.state_manager.set_microphone_state("active", session_id)
    logger.info("✅ [VOICE] Состояние обновлено")
    
    # Шаг 6: Публикация события
    logger.info("🔍 [VOICE] Шаг 6: Публикация события...")
    await self.event_bus.publish("microphone.opened", {...})
    logger.info("✅ [VOICE] Событие опубликовано")
    
    logger.info("✅ [VOICE] Координация записи завершена")
```

**Проверка:**
- [ ] Логирование каждого шага
- [ ] Логирование вызовов менеджеров
- [ ] Логирование публикации событий
- [ ] Логирование обновления состояния

---

### 8.5 Decision-лог для gateways

**Требование REQ-LOG-DECISION-001: Канонический формат decision-лога**
- ✅ Формат: `decision=<start|abort|retry|degrade> ctx={mic=...,screen=...,device=...,network=...,firstRun=...,appMode=...} source=<domain> duration_ms=<int>`
- ✅ Обязателен для всех gateways, принимающих решения
- ✅ Проверяется при ревью и релизе

**Пример:**
```python
def decide_start_listening(snapshot: Snapshot, source: str) -> Decision:
    start_time = time.monotonic()
    
    # Логика принятия решения
    if snapshot.perm_mic != PermissionStatus.GRANTED:
        decision = Decision.ABORT
    elif snapshot.network != NetworkStatus.ONLINE:
        decision = Decision.DEGRADE
    else:
        decision = Decision.START
    
    duration_ms = int((time.monotonic() - start_time) * 1000)
    logger.info(
        f"decision={decision.value} "
        f"ctx={{mic={snapshot.perm_mic.value},screen={snapshot.perm_screen.value},"
        f"device={snapshot.device_input.value},network={snapshot.network.value},"
        f"firstRun={snapshot.first_run},appMode={snapshot.app_mode.value}}} "
        f"source=audio_gateway duration_ms={duration_ms}"
    )
    
    return decision
```

**Проверка:**
- [ ] Decision-лог в каноническом формате
- [ ] Decision-лог для всех gateways
- [ ] Decision-лог проверяется при ревью

---

## 📊 Часть 9: Чек-лист Проверки Требований

### 9.1 Чек-лист модулей

- [ ] Размер модуля ≤ 500 строк
- [ ] Модуль отвечает за одну задачу
- [ ] Модуль не знает о EventBus
- [ ] Модуль не знает о других модулях
- [ ] Зависимости через конструктор
- [ ] Легко тестировать изолированно
- [ ] README.md документирован
- [ ] Логирование соответствует стандарту
- [ ] Нет PII/секретов в логах

---

### 9.2 Чек-лист интеграций

- [ ] Размер интеграции ≤ 500 строк
- [ ] Интеграция только координирует через EventBus
- [ ] Логика в модулях, не в интеграции
- [ ] Нет локальных флагов состояния
- [ ] Использование только state_manager
- [ ] Контракты EventBus документированы
- [ ] Подписки и публикации с приоритетами
- [ ] Логирование координации
- [ ] Логирование событий

---

### 9.3 Чек-лист централизации

- [ ] Только ApplicationStateManager управляет состоянием
- [ ] Нет локальных флагов состояния
- [ ] Все изменения состояния через state_manager.set_*()
- [ ] Все проверки состояния через state_manager.is_*()
- [ ] Конфигурация через UnifiedConfigLoader
- [ ] Ошибки через ErrorHandler
- [ ] Атомарные операции с lock

---

### 9.4 Чек-лист логирования

- [ ] Формат логов соответствует стандарту
- [ ] Структурированные события с session_id
- [ ] Ошибки через ErrorHandler
- [ ] Уровни логирования используются правильно
- [ ] Нет PII/секретов в логах
- [ ] Логирование инициализации
- [ ] Логирование критических операций
- [ ] Логирование координации
- [ ] Decision-лог для gateways (если применимо)

---

## ✅ Часть 10: Итоговые Требования

### 10.1 Обязательные требования

1. **Архитектура:**
   - ✅ Разделение ответственности (модули ≤ 500 строк, интеграции ≤ 500 строк)
   - ✅ Единый источник истины (только ApplicationStateManager)
   - ✅ Изоляция и тестируемость (легко тестировать изолированно)

2. **Централизация:**
   - ✅ Состояние: только ApplicationStateManager
   - ✅ Конфигурация: только UnifiedConfigLoader
   - ✅ Ошибки: только ErrorHandler

3. **Логирование:**
   - ✅ Формат: `YYYY-MM-DD HH:MM:SS - Nexy - LEVEL - [session_id] - Message`
   - ✅ Структурированные события с session_id, duration_ms
   - ✅ Ошибки через ErrorHandler
   - ✅ Нет PII/секретов в логах

---

### 10.2 Метрики качества

| Требование | Целевое значение | Проверка |
|------------|------------------|----------|
| Размер модуля | ≤ 500 строк | ✅ |
| Размер интеграции | ≤ 500 строк | ✅ |
| Источники истины | 1 | ✅ |
| Локальные флаги | 0 | ✅ |
| Время теста | < 1 сек | ✅ |
| Формат логов | Стандартный | ✅ |
| Decision-лог | Канонический формат | ✅ |

---

## 📊 Часть 11: Коммуникации, Объединения и Точки Соединения

### 11.1 Все точки коммуникации

**Требование REQ-COMM-ALL-001: Полная карта коммуникаций**
- ✅ EventBus (события) — подписки и публикации
- ✅ ApplicationStateManager (состояние) — изменения и проверки
- ✅ ErrorHandler (ошибки) — обработка и восстановление
- ✅ UnifiedConfigLoader (конфигурация) — загрузка и валидация
- ✅ Прямые вызовы (интеграции → модули) — только разрешенные

**Детальная документация:** См. `Docs/ARCHITECTURE_COMMUNICATION_AND_INTEGRATION.md`

---

### 11.4 Использование флагов и конфигураций

**Требование REQ-FLAGS-CONFIG-001: Загрузка и использование**
- ✅ Загрузка feature flags через UnifiedConfigLoader
- ✅ Проверка env переменных (приоритет)
- ✅ Проверка unified_config (fallback)
- ✅ Валидация через схемы
- ✅ Создание конфигурации через from_unified_config()
- ✅ Использование конфигурации через конструктор

**Детальная документация:** См. `Docs/ARCHITECTURE_FLAGS_AND_CONFIG_USAGE.md`

**Проверка:**
- [ ] Загрузка через UnifiedConfigLoader
- [ ] Проверка env переменных
- [ ] Валидация через схемы
- [ ] Создание через from_unified_config()
- [ ] Использование через конструктор

**Проверка:**
- [ ] Все коммуникации документированы
- [ ] Все точки соединения учтены
- [ ] Все объединения централизованы
- [ ] Запрещенные коммуникации не используются

---

### 11.2 Точки соединения между компонентами

**Требование REQ-COMM-CONN-001: Соединение модулей с интеграциями**
- ✅ Интеграции вызывают модули напрямую (разрешено)
- ✅ Модули не вызывают другие модули (запрещено)
- ✅ Интеграции не вызывают другие интеграции (запрещено)
- ✅ Все коммуникации через EventBus или прямые вызовы менеджеров

**Проверка:**
- [ ] Интеграции → Модули (разрешено)
- [ ] Модули → Модули (запрещено)
- [ ] Интеграции → Интеграции (запрещено)

---

### 11.3 Объединения и централизация

**Требование REQ-COMM-UNIFY-001: Объединение состояния**
- ✅ Только `ApplicationStateManager` управляет состоянием
- ✅ Все изменения через `state_manager.set_*()`
- ✅ Все проверки через `state_manager.is_*()`
- ✅ Нет локальных флагов состояния

**Требование REQ-COMM-UNIFY-002: Объединение конфигурации**
- ✅ Только `UnifiedConfigLoader` загружает конфигурацию
- ✅ Все конфигурации в `config/unified_config.yaml`
- ✅ Валидация через схемы `config/schemas/`

**Требование REQ-COMM-UNIFY-003: Объединение обработки ошибок**
- ✅ Только `ErrorHandler` обрабатывает ошибки
- ✅ Структурированные коды ошибок (E_*)
- ✅ Восстановление состояния после ошибок

**Проверка:**
- [ ] Состояние централизовано в ApplicationStateManager
- [ ] Конфигурация централизована в UnifiedConfigLoader
- [ ] Ошибки централизованы в ErrorHandler

---

## 🎯 Заключение

**Требования к идеальной архитектуре:**
1. ✅ Соответствие архитектуре проекта (разделение ответственности, порядок инициализации)
2. ✅ Соответствие правилам проекта (конфигурация, EventBus контракты, feature flags)
3. ✅ Централизация (состояние, конфигурация, ошибки)
4. ✅ Логирование (формат, уровни, диагностика, decision-лог)
5. ✅ Коммуникации (EventBus, ApplicationStateManager, ErrorHandler, UnifiedConfigLoader)
6. ✅ Точки соединения (модули ↔ интеграции, интеграции ↔ интеграции)
7. ✅ Объединения (состояние, конфигурация, ошибки)

**Все требования проверяемы через чек-листы.**

**Связанные документы:**
- `Docs/AUDIO_SYSTEM_REFACTORING_MASTER_PLAN.md` — **ЕДИНЫЙ МАСТЕР-ПЛАН** для планирования и реализации
- `Docs/ARCHITECTURE_COMMUNICATION_AND_INTEGRATION.md` — детальная карта коммуникаций
- `Docs/ARCHITECTURE_FLAGS_AND_CONFIG_USAGE.md` — использование флагов и конфигураций
- `Docs/ARCHITECTURE_CRITERIA_AND_REQUIREMENTS.md` — критерии и требования
- `Docs/ARCHITECTURE_FULL_ANALYSIS_AND_PROPOSAL.md` — полный анализ и предложение

