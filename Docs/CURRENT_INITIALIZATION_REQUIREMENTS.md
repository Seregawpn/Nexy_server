# 📋 Требования к Текущей Реализации Инициализации

**Дата:** 2025-12-13  
**Статус:** Полное описание требований к текущей реализации инициализации

---

## 📊 Часть 1: Общая Структура Инициализации

### 1.1 Последовательность этапов

**Текущая последовательность в `SimpleModuleCoordinator.initialize()`:**

1. **Создание core компонентов** (строки 127-132)
   - `EventBus()` — шина событий
   - `ApplicationStateManager()` — менеджер состояния
   - `ErrorHandler(event_bus)` — обработчик ошибок

2. **Запуск фонового asyncio loop** (строка 135)
   - `_start_background_loop()` — для EventBus и интеграций

3. **Настройка критичных подписок** (строки 137-147)
   - `state_manager.attach_event_bus(event_bus)`
   - `event_bus.attach_loop(bg_loop)`
   - `_setup_critical_subscriptions()` — **КРИТИЧНО: ДО инициализации интеграций**

4. **Создание интеграций** (строки 149-152)
   - `_create_integrations()` — создание всех интеграций

5. **Создание workflows** (строки 440-453)
   - `ListeningWorkflow(event_bus)`
   - `ProcessingWorkflow(event_bus)`

6. **Инициализация интеграций** (строки 154-157)
   - `_initialize_integrations()` — инициализация всех интеграций

7. **Настройка координации** (строки 159-162)
   - `_setup_coordination()` — настройка связей между компонентами

8. **Настройка авто-всё связей** (строки 164-167)
   - `_setup_auto_audio_connections()` — настройка audio_default

---

## 📊 Часть 2: Создание Интеграций (_create_integrations)

### 2.1 Порядок создания интеграций

**Текущий порядок (строки 188-457):**

| № | Ключ | Интеграция | Зависимости при создании | Условия |
|---|------|------------|---------------------------|---------|
| 1 | `instance_manager` | `InstanceManagerIntegration` | Нет | **КРИТИЧНО: ПЕРВЫЙ, БЛОКИРУЮЩИЙ** |
| 2 | `hardware_id` | `HardwareIdIntegration` | Нет | Ранний старт для ID |
| 3 | `tray` | `TrayControllerIntegration` | Нет | Условно: `enabled` в конфиге |
| 4 | `input` | `InputProcessingIntegration` | Нет | Всегда создается |
| 5 | `updater` | `UpdaterIntegration` | Нет | Всегда создается |
| 6 | `permission_restart` | `PermissionRestartIntegration` | `updater` | Получает `updater_integration` |
| 7 | `update_notification` | `UpdateNotificationIntegration` | Нет | Всегда создается |
| 8 | `network` | `NetworkManagerIntegration` | Нет | Всегда создается |
| 9 | `interrupt` | `InterruptManagementIntegration` | Нет | Всегда создается |
| 10 | `screenshot_capture` | `ScreenshotCaptureIntegration` | Нет | Всегда создается |
| 11 | `voice_recognition` | `VoiceRecognitionIntegration` | Нет | Всегда создается |
| 12 | `mode_management` | `ModeManagementIntegration` | Нет | Всегда создается |
| 13 | `grpc` | `GrpcClientIntegration` | Нет | Всегда создается |
| 14 | `action_execution` | `ActionExecutionIntegration` | Нет | Условно: `env == 'development'` или `enabled` в конфиге |
| 15 | `speech_playback` | `SpeechPlaybackIntegration` | Нет | Всегда создается |
| 16 | `signals` | `SignalIntegration` | Нет | Всегда создается |
| 17 | `autostart_manager` | `AutostartManagerIntegration` | Нет | Всегда создается |
| 18 | `welcome_message` | `WelcomeMessageIntegration` | Нет | Всегда создается |
| 19 | `voiceover_ducking` | `VoiceOverDuckingIntegration` | Нет | Всегда создается |
| 20 | `first_run_permissions` | `FirstRunPermissionsIntegration` | Нет | Всегда создается |

**ИТОГО:** 20 интеграций

---

### 2.2 Зависимости при создании

**Явные зависимости:**
- `permission_restart` → `updater` (строка 251): получает `updater_integration` в конструкторе

**Условное создание:**
- `tray`: создается только если `integrations.tray_controller.enabled == true` (строки 211-224)
- `action_execution`: создается только если `env == 'development'` ИЛИ `actions.open_app.enabled == true` (строки 346-363)

---

### 2.3 Конфигурация при создании

**Источники конфигурации:**
- `instance_manager`: `config_data.get('instance_manager', {})`
- `tray`: `config_data.get('integrations', {}).get('tray_controller', {})`
- `input`: `self.config.get_input_processing_config()`
- `updater`: `config_data.get('updater', {})`
- `permission_restart`: `config_data.get('integrations', {}).get('permission_restart', {})`
- `update_notification`: `config_data.get('integrations', {}).get('update_notification', {})`
- `network`: `None` (загружается внутри интеграции)
- `interrupt`: `InterruptManagementIntegrationConfig` из `integrations.interrupt_management`
- `screenshot_capture`: `None` (без конфигурации)
- `voice_recognition`: `VoiceRecognitionConfig` из `integrations.voice_recognition` + `get_stt_language()`
- `mode_management`: `None` (без конфигурации)
- `grpc`: `None` (загружается внутри интеграции)
- `action_execution`: `None` (без конфигурации)
- `speech_playback`: `None` (без конфигурации)
- `signals`: `SignalsIntegrationConfig` из `integrations.signals`
- `autostart_manager`: `config_data.get('autostart', {})`
- `welcome_message`: `None` (без конфигурации)
- `voiceover_ducking`: `config_data.get("accessibility", {}).get("voiceover_control", {})`
- `first_run_permissions`: `config_data.get("permissions", {}).get("first_run", {})`

---

## 📊 Часть 3: Инициализация Интеграций (_initialize_integrations)

### 3.1 Порядок инициализации

**Текущий порядок (строки 473-535):**

1. **permissions_queue** (строки 476-478)
   - Условно: если `self.permissions_queue` существует
   - `await self.permissions_queue.initialize()`

2. **voice_recognition** (строки 480-505) ⚠️ **СПЕЦИАЛЬНАЯ ОБРАБОТКА**
   - **КРИТИЧНО: ПЕРВЫМ** для получения `AVFAudioEngine`
   - `await voice_integration.initialize()`
   - Получение `_avf_engine` из `voice_integration`
   - Сохранение в переменную `avf_engine` для передачи в `speech_playback`

3. **Остальные интеграции** (строки 507-524)
   - Цикл по всем интеграциям
   - Пропуск `voice_recognition` (уже инициализирован)
   - **Специальная обработка для `speech_playback`**: передача `avf_engine` (строки 515-517)

4. **Workflows** (строки 526-531)
   - `await workflow.initialize()` для каждого workflow

---

### 3.2 Специальные требования

**1. voice_recognition (строки 480-505):**
- Должен инициализироваться **ПЕРВЫМ** из всех интеграций
- После инициализации извлекается `_avf_engine`
- Если `_avf_engine` не создан — предупреждение, но не ошибка
- `avf_engine` сохраняется для передачи в `speech_playback`

**2. speech_playback (строки 515-517):**
- Получает `avf_engine` из `voice_recognition` (если доступен)
- `await integration.initialize(avf_engine=avf_engine)`

**3. Критичные подписки (строки 537-571):**
- Должны быть настроены **ДО** инициализации интеграций
- Подписки на события:
  - `permissions.first_run_started` (HIGH priority)
  - `permissions.first_run_completed` (HIGH priority)
  - `permissions.first_run_failed` (HIGH priority)
  - `permissions.first_run_restart_pending` (CRITICAL priority)

---

### 3.3 Обработка ошибок

**Текущая обработка:**
- Если `voice_recognition.initialize()` не удался → `raise Exception` (строка 493)
- Если любая другая интеграция не инициализировалась → `raise Exception` (строка 523)
- Если ошибка в цикле → `raise` (строка 534)

**Проблемы:**
- ❌ Нет таймаутов
- ❌ Нет retry
- ❌ Нет различения критичных/некритичных ошибок
- ❌ Нет детального логирования ошибок

---

## 📊 Часть 4: Workflows

### 4.1 Создание workflows

**Текущий порядок (строки 440-453):**

1. `listening` → `ListeningWorkflow(event_bus)`
2. `processing` → `ProcessingWorkflow(event_bus)`

**Зависимости:**
- Нет явных зависимостей от интеграций при создании
- Зависимости только через EventBus

---

### 4.2 Инициализация workflows

**Текущий порядок (строки 526-531):**
- Инициализируются **ПОСЛЕ** всех интеграций
- `await workflow.initialize()` для каждого workflow

---

## 📊 Часть 5: Специальные Требования

### 5.1 AVF Engine передача

**Требование:**
- `voice_recognition` должен инициализироваться первым
- `_avf_engine` извлекается из `voice_integration`
- `avf_engine` передается в `speech_playback.initialize(avf_engine=avf_engine)`

**Проблема:**
- ⚠️ Hardcoded специальная обработка
- ⚠️ Нет явной зависимости в конфигурации
- ⚠️ Сложно диагностировать, если `_avf_engine` не создан

---

### 5.2 Критичные подписки

**Требование:**
- Подписки должны быть настроены **ДО** инициализации интеграций
- Это предотвращает потерю событий `permissions.first_run_completed`

**Проблема:**
- ⚠️ Порядок жестко закодирован
- ⚠️ Нет явного описания зависимости

---

### 5.3 Условная инициализация

**1. tray:**
- Создается только если `integrations.tray_controller.enabled == true`
- Если не создан → пропускается при инициализации

**2. action_execution:**
- Создается только если `env == 'development'` ИЛИ `actions.open_app.enabled == true`
- Если не создан → пропускается при инициализации

---

## 📊 Часть 6: Требования к Логированию

### 6.1 Текущее логирование

**Этапы логирования:**
1. Начало `initialize()` (строки 117-119)
2. Создание core компонентов (строка 132)
3. Настройка критичных подписок (строка 145)
4. Создание интеграций (строка 152)
5. Инициализация интеграций (строка 157)
6. Настройка координации (строка 162)
7. Настройка авто-всё связей (строка 167)
8. Диагностические логи для `voice_recognition` (строки 485, 489)

**Проблемы:**
- ❌ Нет детального логирования для каждой интеграции
- ❌ Нет логирования времени выполнения
- ❌ Нет логирования зависимостей
- ❌ Нет централизованного отчета

---

## 📊 Часть 7: Требования к Обработке Ошибок

### 7.1 Текущая обработка

**Уровни обработки:**
1. **Критичные ошибки:** `raise Exception` → остановка инициализации
2. **Предупреждения:** `logger.warning()` → продолжение инициализации

**Проблемы:**
- ❌ Нет таймаутов для инициализации
- ❌ Нет retry для временных ошибок
- ❌ Нет различения критичных/некритичных интеграций
- ❌ Нет graceful degradation (продолжение при некритичных ошибках)

---

## 📊 Часть 8: Итоговая Таблица Требований

### 8.1 Компоненты инициализации

| Компонент | Тип | Порядок создания | Порядок инициализации | Критичность | Зависимости |
|-----------|-----|------------------|----------------------|-------------|-------------|
| EventBus | Core | 1 | - | ✅ Критично | Нет |
| StateManager | Core | 2 | - | ✅ Критично | EventBus |
| ErrorHandler | Core | 3 | - | ✅ Критично | EventBus |
| Background Loop | Core | 4 | - | ✅ Критично | Нет |
| Critical Subscriptions | Core | 5 | - | ✅ Критично | EventBus, StateManager |
| instance_manager | Integration | 1 | 1 | ✅ Критично | Core |
| hardware_id | Integration | 2 | 2 | ✅ Критично | instance_manager |
| first_run_permissions | Integration | 20 | 3 | ✅ Критично | hardware_id |
| permission_restart | Integration | 6 | 4 | ⚠️ Некритично | first_run_permissions, updater |
| tray | Integration | 3 | 5 | ✅ Критично | permission_restart |
| mode_management | Integration | 12 | 6 | ✅ Критично | tray |
| input | Integration | 4 | 7 | ✅ Критично | mode_management |
| voice_recognition | Integration | 11 | **8 (ПЕРВЫЙ)** | ✅ Критично | input |
| network | Integration | 8 | 9 | ⚠️ Некритично | voice_recognition |
| interrupt | Integration | 9 | 10 | ✅ Критично | network |
| screenshot_capture | Integration | 10 | 11 | ✅ Критично | interrupt |
| grpc | Integration | 13 | 12 | ✅ Критично | screenshot_capture |
| speech_playback | Integration | 15 | 13 | ✅ Критично | grpc, **voice_recognition (AVF)** |
| signals | Integration | 16 | 14 | ⚠️ Некритично | speech_playback |
| updater | Integration | 5 | 15 | ⚠️ Некритично | signals |
| autostart_manager | Integration | 17 | 16 | ⚠️ Некритично | updater |
| welcome_message | Integration | 18 | 17 | ⚠️ Некритично | autostart_manager |
| voiceover_ducking | Integration | 19 | 18 | ⚠️ Некритично | welcome_message |
| action_execution | Integration | 14 | 19 | ⚠️ Некритично | voiceover_ducking |
| update_notification | Integration | 7 | 20 | ⚠️ Некритично | action_execution |
| listening | Workflow | - | 21 | ✅ Критично | Все интеграции |
| processing | Workflow | - | 22 | ✅ Критично | Все интеграции |

---

### 8.2 Специальные требования

| Требование | Описание | Где реализовано |
|------------|----------|-----------------|
| **AVF Engine передача** | `voice_recognition` → `speech_playback` | Строки 480-505, 515-517 |
| **Критичные подписки** | ДО инициализации интеграций | Строки 137-147, 537-571 |
| **Условное создание tray** | Только если `enabled == true` | Строки 211-224 |
| **Условное создание action_execution** | Только если `env == 'development'` или `enabled == true` | Строки 346-363 |
| **permission_restart зависимость** | Получает `updater_integration` | Строка 251 |

---

## 📊 Часть 9: Проблемы Текущей Реализации

### 9.1 Архитектурные проблемы

1. ❌ **Порядок жестко закодирован** в коде
2. ❌ **Зависимости неявные** (в комментариях)
3. ❌ **Специальная обработка** для `voice_recognition` и `speech_playback`
4. ❌ **Нет централизованного контроля**
5. ❌ **Сложно изменить порядок** без изменения кода

---

### 9.2 Проблемы диагностики

1. ❌ **Нет детального логирования** для каждой интеграции
2. ❌ **Нет метрик времени выполнения**
3. ❌ **Нет централизованного отчета**
4. ❌ **Сложно найти проблемную инициализацию**

---

### 9.3 Проблемы обработки ошибок

1. ❌ **Нет таймаутов**
2. ❌ **Нет retry**
3. ❌ **Нет различения критичных/некритичных**
4. ❌ **Нет graceful degradation**

---

## ✅ Заключение

**Текущие требования к инициализации:**
1. ✅ 3 core компонента (EventBus, StateManager, ErrorHandler)
2. ✅ 20 интеграций (с условным созданием для `tray` и `action_execution`)
3. ✅ 2 workflows (listening, processing)
4. ✅ Специальная обработка для AVF engine
5. ✅ Критичные подписки ДО инициализации
6. ✅ Зависимости при создании (`permission_restart` → `updater`)
7. ✅ Зависимости при инициализации (порядок выполнения)

**Проблемы:**
- ❌ Порядок жестко закодирован
- ❌ Зависимости неявные
- ❌ Нет централизованного контроля
- ❌ Сложно диагностировать проблемы

**Готов к централизации через конфигурацию!**
