# Чек-лист готовности close_app к production

## ⚠️ Критические проверки перед деплоем

### ✅ 1. Фича-флаги в окружении (ОБЯЗАТЕЛЬНО)

**Проблема:** Без этих флагов `command_payload` не форвардится на клиент.

**Действие:**
```bash
# На сервере (prod/stage) установить:
export FORWARD_ASSISTANT_ACTIONS=true
export NEXY_KS_DISABLE_FORWARD_ASSISTANT_ACTIONS=false

# Или добавить в config.env:
# ⚠️  ВАЖНО: В config.env.example по умолчанию false для безопасности
# Включите осознанно только в production/staging
FORWARD_ASSISTANT_ACTIONS=true
NEXY_KS_DISABLE_FORWARD_ASSISTANT_ACTIONS=false
```

**Примечание:** В `config.env.example` флаг установлен в `false` по умолчанию для безопасности. Включите `true` осознанно только после проверки готовности.

**Проверка:**
```bash
cd server/server
python scripts/verify_close_app_production_readiness.py
# Проверка 1 должна пройти: ✅ Фича-флаги настроены правильно

# Если структура репозитория отличается, используйте --project-root:
python scripts/verify_close_app_production_readiness.py --project-root /path/to/Nexy
```

---

### ✅ 2. Источник системного промпта (ОБЯЗАТЕЛЬНО)

**Проблема:** Если используется `GEMINI_SYSTEM_PROMPT` из env без `close_app`, модель не будет генерировать команду.

**Проверка:**
```bash
# Проверить, установлен ли GEMINI_SYSTEM_PROMPT
echo $GEMINI_SYSTEM_PROMPT

# Если установлен - проверить наличие close_app
echo $GEMINI_SYSTEM_PROMPT | grep -i "close_app"
```

**Решение:**
- **Вариант A (рекомендуется):** Удалить `GEMINI_SYSTEM_PROMPT` из env
  ```bash
  unset GEMINI_SYSTEM_PROMPT
  # Будет использоваться промпт из unified_config.py (содержит close_app)
  ```

- **Вариант B:** Обновить `GEMINI_SYSTEM_PROMPT` новым промптом из `unified_config.py`

**Проверка:**
```bash
cd server/server
python scripts/verify_close_app_production_readiness.py
# Проверка 2 должна показать:
# ✅ Промпт содержит все необходимые инструкции для close_app!
```

---

### ✅ 3. MCP сервер close_app (ОБЯЗАТЕЛЬНО)

**Проблема:** В конфиге указан путь `mcp_close_app_test/server/close_app_server.py` — нужно убедиться, что файл существует.

**Проверка:**
```bash
cd client
python scripts/verify_close_app_client_readiness.py
# Проверка 1 должна пройти: ✅ Файл MCP сервера существует

# Если структура репозитория отличается, используйте --project-root:
python scripts/verify_close_app_client_readiness.py --project-root /path/to/Nexy
```

**Текущая конфигурация:**
```yaml
# client/config/unified_config.yaml
mcp:
  close_app:
    server_path: "mcp_close_app_test/server/close_app_server.py"  # Относительно Nexy root
    enabled: true
    timeout_sec: 10.0
```

**Если файл не существует:**
1. Проверить путь относительно корня проекта Nexy
2. Обновить `mcp.close_app.server_path` в `unified_config.yaml`
3. Убедиться, что файл исполняемый: `chmod +x path/to/close_app_server.py`

---

### ✅ 4. E2E проверка (ОБЯЗАТЕЛЬНО)

**Проблема:** Нет факта, что события реально приходят на клиенте.

**Проверка:**
```bash
# Серверные тесты
cd server/server
python scripts/test_mcp_chain.py
# Должен пройти все тесты, включая close_app

# Клиентские E2E тесты
cd client
python scripts/test_close_app_e2e.py
# Должен показать:
# ✅ Событие actions.close_app.started получено
# ✅ Событие actions.close_app.completed получено
```

**Ожидаемые события:**
- `actions.close_app.started` — начало выполнения
- `actions.close_app.completed` — успешное завершение
- `actions.close_app.failed` — ошибка (если произошла)

---

## Быстрый чек-лист

### Перед деплоем в production:

- [ ] **Фича-флаги установлены:**
  - [ ] `FORWARD_ASSISTANT_ACTIONS=true`
  - [ ] `NEXY_KS_DISABLE_FORWARD_ASSISTANT_ACTIONS=false`

- [ ] **Промпт содержит close_app:**
  - [ ] `GEMINI_SYSTEM_PROMPT` не установлен ИЛИ содержит `close_app`
  - [ ] Промпт из `unified_config.py` используется (содержит `close_app`)

- [ ] **MCP сервер настроен:**
  - [ ] `mcp.close_app.enabled: true` в `unified_config.yaml`
  - [ ] `mcp.close_app.server_path` указывает на существующий файл
  - [ ] Файл `close_app_server.py` существует и исполняемый

- [ ] **Тесты пройдены:**
  - [ ] `verify_close_app_production_readiness.py` — все проверки ✅
  - [ ] `verify_close_app_client_readiness.py` — все проверки ✅
  - [ ] `test_mcp_chain.py` — все тесты ✅
  - [ ] `test_close_app_e2e.py` — события получены ✅

---

## Автоматическая проверка

### Сервер:
```bash
cd server/server
python scripts/verify_close_app_production_readiness.py

# Или с кастомным путем:
python scripts/verify_close_app_production_readiness.py --project-root /path/to/Nexy
```

**Ожидаемый результат:**
```
✅ PASS - Фича-флаги
✅ PASS - Системный промпт
✅ PASS - MCP сервер

📊 Результат: 3/3 проверок пройдено
🎉 Все проверки пройдены! close_app готов к production.
```

### Клиент:
```bash
cd client
python scripts/verify_close_app_client_readiness.py

# Или с кастомным путем:
python scripts/verify_close_app_client_readiness.py --project-root /path/to/Nexy
```

**Ожидаемый результат:**
```
✅ PASS - MCP конфигурация
✅ PASS - ActionExecutionIntegration
✅ PASS - Actions конфигурация

📊 Результат: 3/3 проверок пройдено
🎉 Все проверки пройдены! Клиент готов к close_app.
```

---

## Что делать, если проверки не прошли

### Проблема: Фича-флаги не настроены
**Решение:**
```bash
export FORWARD_ASSISTANT_ACTIONS=true
export NEXY_KS_DISABLE_FORWARD_ASSISTANT_ACTIONS=false
# Перезапустить сервер
```

### Проблема: Промпт не содержит close_app
**Решение:**
```bash
# Удалить GEMINI_SYSTEM_PROMPT из env
unset GEMINI_SYSTEM_PROMPT
# Перезапустить сервер (будет использоваться unified_config.py)
```

### Проблема: MCP сервер не найден
**Решение:**
1. Проверить путь в `unified_config.yaml`
2. Убедиться, что файл существует относительно корня проекта Nexy
3. Обновить путь, если нужно

### Проблема: E2E тест не проходит
**Решение:**
1. Проверить логи на наличие ошибок
2. Убедиться, что все зависимости установлены
3. Проверить, что сервер запущен (для полного E2E)

---

## Связанные документы

- `Docs/CLOSE_APP_E2E_IMPLEMENTATION_GUIDE.md` — полная техническая инструкция
- `server/server/scripts/verify_close_app_production_readiness.py` — скрипт проверки сервера
- `client/scripts/verify_close_app_client_readiness.py` — скрипт проверки клиента
