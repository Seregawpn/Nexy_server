# Fix: Runtime GrpcClient Cache Issue - Диагностика и очистка

**Дата:** 2026-01-12  
**Тип:** fix  
**Ассистент:** cursor  
**Статус:** ✅ Готово к применению

---

## Executive Summary

Добавлена диагностика для выявления проблем с кэшем Python и устаревшими версиями `GrpcClient` в runtime. Создан скрипт для очистки кэша. Добавлено логирование информации о загруженном классе для мгновенного выявления неправильной версии.

---

## Проблема

**Симптомы:**
- В runtime всё ещё возникает `TypeError: missing 1 required positional argument: 'session_id'`
- Логи показывают, что используется старая сигнатура `stream_audio`

**Root Cause:**
- Python кэш (`__pycache__`) содержит устаревшие версии модулей
- Процесс запущен со старым кодом в памяти
- Возможен конфликт путей импорта (старая версия из `modules/grpc_client` загружается раньше)

---

## Решение

### 1. Добавлена диагностика при инициализации

**Файл:** `integration/integrations/grpc_client_integration.py` (строки 162-190)

**Изменения:**
- Логирование модуля и файла загруженного класса
- Логирование сигнатуры `stream_audio` с параметрами
- Явная проверка наличия `session_id` в сигнатуре

**Код:**
```python
self._client = GrpcClient(config=client_cfg)

# КРИТИЧНО: Диагностика - логируем информацию о загруженном классе для выявления проблем с кэшем/импортами
client_module = self._client.__class__.__module__
client_file = getattr(self._client.__class__, '__file__', 'unknown')
logger.info(f"🔍 [DIAG] GrpcClient загружен из модуля: {client_module}")
logger.info(f"🔍 [DIAG] GrpcClient файл: {client_file}")

# КРИТИЧНО: Проверяем, что используется правильная версия GrpcClient с session_id в сигнатуре
try:
    sig = inspect.signature(self._client.stream_audio)
    params = list(sig.parameters.keys())
    logger.info(f"🔍 [DIAG] stream_audio сигнатура: {params}")
    logger.info(f"🔍 [DIAG] stream_audio имеет session_id: {'session_id' in params}")
    
    if 'session_id' not in params:
        error_msg = (
            f"❌ КРИТИЧЕСКАЯ ОШИБКА: GrpcClient.stream_audio не имеет параметра session_id "
            f"(параметры: {params}). Используется неполная версия класса. "
            f"Модуль: {client_module}, Файл: {client_file}. "
            f"Ожидается полная версия из client/modules/grpc_client/core/grpc_client"
        )
        logger.error(error_msg)
        raise RuntimeError(error_msg)
    logger.info(f"✅ GrpcClient инициализирован корректно: stream_audio имеет параметр session_id (сигнатура: {params})")
except Exception as e:
    logger.error(f"❌ Ошибка проверки сигнатуры GrpcClient при инициализации: {e}")
    logger.error(f"🔍 [DIAG] Модуль класса: {client_module}, Файл: {client_file}")
    raise
```

### 2. Добавлена диагностика перед вызовом

**Файл:** `integration/integrations/grpc_client_integration.py` (строки 500-507)

**Изменения:**
- Логирование параметров перед вызовом `stream_audio`
- Логирование класса и модуля перед вызовом
- Подтверждение, что `session_id` передается

**Код:**
```python
# КРИТИЧНО: Диагностика перед вызовом - логируем параметры для выявления проблем
logger.debug(f"🔍 [DIAG] Вызов stream_audio: session_id={final_session_id}, hardware_id={hwid[:8]}..., prompt_len={len(text)}")
logger.debug(f"🔍 [DIAG] GrpcClient класс: {self._client.__class__.__name__}, модуль: {self._client.__class__.__module__}")

async for resp in self._client.stream_audio(
    prompt=text,
    screenshot_base64=screenshot_b64 or "",
    screen_info={"width": width, "height": height},
    hardware_id=hwid,
    session_id=final_session_id,  # КРИТИЧНО: передаем session_id из state_manager
):
```

### 3. Создан скрипт для очистки кэша

**Файл:** `scripts/clear_python_cache.sh`

**Назначение:**
- Удаление всех `__pycache__` директорий
- Удаление `.pyc` и `.pyo` файлов
- Специфичная очистка кэша `grpc_client` модулей

**Использование:**
```bash
./scripts/clear_python_cache.sh
```

---

## Инструкция по применению

### Шаг 1: Остановить текущий процесс

**ВАЖНО:** Полностью остановить приложение, чтобы освободить память от старого кода.

```bash
# Найти процесс
ps aux | grep "python.*main.py"

# Остановить процесс (если запущен)
pkill -f "python.*main.py"
```

### Шаг 2: Очистить Python кэш

```bash
cd /Users/sergiyzasorin/Fix_new
./scripts/clear_python_cache.sh
```

**Или вручную:**
```bash
# Удалить все __pycache__
find . -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null || true

# Удалить .pyc файлы
find . -type f -name "*.pyc" -delete 2>/dev/null || true

# Специфичная очистка grpc_client
rm -rf client/modules/grpc_client/core/__pycache__ 2>/dev/null || true
rm -rf modules/grpc_client/core/__pycache__ 2>/dev/null || true
rm -rf integration/integrations/__pycache__ 2>/dev/null || true
```

### Шаг 3: Перезапустить приложение

```bash
cd /Users/sergiyzasorin/Fix_new
python main.py
```

### Шаг 4: Проверить диагностические логи

**При инициализации ожидаются логи:**
```
✅ Используется GrpcClient из client/modules/grpc_client (полная версия с зависимостями)
🔍 [DIAG] GrpcClient загружен из модуля: client.modules.grpc_client.core.grpc_client
🔍 [DIAG] GrpcClient файл: /Users/sergiyzasorin/Fix_new/client/modules/grpc_client/core/grpc_client.py
🔍 [DIAG] stream_audio сигнатура: ['prompt', 'screenshot_base64', 'screen_info', 'hardware_id', 'session_id']
🔍 [DIAG] stream_audio имеет session_id: True
✅ GrpcClient инициализирован корректно: stream_audio имеет параметр session_id (сигнатура: ['prompt', 'screenshot_base64', 'screen_info', 'hardware_id', 'session_id'])
```

**При вызове stream_audio ожидаются логи:**
```
🔍 [DIAG] Вызов stream_audio: session_id=<uuid>, hardware_id=<id>..., prompt_len=<int>
🔍 [DIAG] GrpcClient класс: GrpcClient, модуль: client.modules.grpc_client.core.grpc_client
TRACE phase=grpc.start ts=<timestamp> session=<uuid> ...
```

### Шаг 5: Выполнить голосовой запрос

- Нажать PTT (push-to-talk)
- Произнести команду
- Проверить отсутствие `TypeError`

---

## Диагностика проблем

### Если логи показывают неправильный модуль

**Проблема:** `🔍 [DIAG] GrpcClient загружен из модуля: modules.grpc_client.core.grpc_client`

**Решение:**
1. Проверить порядок путей в `sys.path` в `main.py`
2. Убедиться, что `client/modules` добавлен в `sys.path` перед `modules`
3. Перезапустить после очистки кэша

### Если сигнатура не содержит session_id

**Проблема:** `🔍 [DIAG] stream_audio имеет session_id: False`

**Решение:**
1. Проверить, что `client/modules/grpc_client/core/grpc_client.py` содержит `session_id: str` в сигнатуре
2. Очистить кэш и перезапустить
3. Проверить, что нет конфликта с другими версиями класса

### Если TypeError всё ещё возникает

**Проблема:** `TypeError: missing 1 required positional argument: 'session_id'`

**Диагностика:**
1. Проверить логи `🔍 [DIAG] Вызов stream_audio: session_id=...` - должен быть валидный UUID
2. Проверить логи `🔍 [DIAG] GrpcClient класс: ...` - должен быть `GrpcClient` из `client.modules.grpc_client.core.grpc_client`
3. Если `session_id=None` или пустой - проблема в `final_session_id`
4. Если класс из другого модуля - проблема с импортом

---

## Критерии успеха

✅ **Используется актуальная реализация GrpcClient**
- Логи показывают модуль: `client.modules.grpc_client.core.grpc_client`
- Файл: `/Users/sergiyzasorin/Fix_new/client/modules/grpc_client/core/grpc_client.py`
- Сигнатура содержит `session_id`

✅ **Ошибка сигнатуры не воспроизводится**
- Нет `TypeError: missing 1 required positional argument: 'session_id'`
- Нет `invalid_client_signature` в событиях
- gRPC запрос успешно отправляется

---

## Заключение

**Статус:** ✅ **Готово к применению**

Добавлена диагностика для выявления проблем с кэшем и устаревшими версиями. Создан скрипт для очистки кэша. После очистки кэша и перезапуска приложения диагностические логи покажут, какой класс используется и почему возникает ошибка.

**Следующий шаг:** Выполнить инструкцию по применению (остановить процесс → очистить кэш → перезапустить → проверить логи).
