# 🔄 Как теперь работает система планирования и валидации кода

**Дата создания**: 2025-01-XX  
**Версия**: 1.0

---

## ⚠️ ВАЖНО: Система универсальна для ЛЮБОГО функционала

**КРИТИЧЕСКИ ВАЖНО**: Эта система применяется ко **ВСЕМ изменениям** в проекте, независимо от модуля или функционала:

- ✅ **Аудиосистема** (audio_avf, audio_system, voice_recognition, speech_playback)
- ✅ **Система разрешений** (permissions, first_run_permissions, permission_restart)
- ✅ **Сетевые модули** (grpc_client, network_manager)
- ✅ **Управление режимами** (mode_management, workflows)
- ✅ **Обработка ввода** (input_processing, keyboard, interrupt_management)
- ✅ **Система обновлений** (updater)
- ✅ **Скриншоты** (screenshot_capture)
- ✅ **Система сигналов** (signals)
- ✅ **Любые другие модули и функционалы**

**Правила одинаковы для всех**: планирование (5 чек-листов) + автоматическая валидация при каждом изменении кода.

---

## 🎯 Общая логика работы

Система работает в **два этапа**:

1. **Этап планирования** (5 обязательных чек-листов) — выполняется один раз перед началом работы **для ЛЮБОГО изменения**
2. **Автоматическая валидация** (при каждом изменении кода) — выполняется автоматически при генерации **любого кода** в **любом модуле**

---

## 📋 Этап 1: Планирование (5 обязательных чек-листов)

### Порядок выполнения (обязателен!)

```
1. CONTEXT_AWARE_PLANNING_GUIDE.md (ПЕРВЫЙ)
   ↓
2. ARCHITECTURE_ANALYSIS_CHECKLIST.md (ВТОРОЙ)
   ↓
3. CONFIGURATION_ANALYSIS_CHECKLIST.md (ТРЕТИЙ)
   ↓
4. PRE_CHANGE_PLANNING_CHECKLIST.md (ЧЕТВЕРТЫЙ)
   ↓
5. DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md (ПЯТЫЙ)
   ↓
   ГОТОВНОСТЬ К ГЕНЕРАЦИИ КОДА
```

### Что происходит на каждом этапе

#### Этап 1: CONTEXT_AWARE_PLANNING_GUIDE.md
**Цель**: Контекстный анализ (ЧТО учесть)

**Что делает**:
- Определяет область изменения (какие модули/интеграции/файлы затрагиваются)
- Анализирует конфигурации и флаги (высокоуровневый обзор)
- Анализирует архитектурные ограничения
- Анализирует существующий функционал
- Создает детальную Impact Map

**Результат**: Детальная Impact Map, полное понимание контекста изменения.

**Время**: 30-60 минут

---

#### Этап 2: ARCHITECTURE_ANALYSIS_CHECKLIST.md
**Цель**: Анализ архитектуры (КАК устроено)

**Что делает**:
- Изучает структуру проекта (модули, интеграции, workflows, core)
- Анализирует взаимодействия (EventBus события, workflows, selectors/gateways)
- Анализирует зависимости (прямые, обратные, внешние)
- Поняет инварианты (архитектурные правила, правила взаимодействия)

**Результат**: Полное понимание архитектуры системы.

**Время**: 45-90 минут

---

#### Этап 3: CONFIGURATION_ANALYSIS_CHECKLIST.md
**Цель**: Анализ конфигураций (КАКИЕ параметры)

**Что делает**:
- Идентифицирует все конфигурации, связанные с изменениями
- Анализирует параметры (значения, зависимости, логика использования)
- Анализирует флаги (feature flags, kill-switches)
- Анализирует зависимости между конфигурациями
- Создает таблицу анализа конфигураций

**Результат**: Полное понимание всех конфигураций и их зависимостей.

**Время**: 30-60 минут

---

#### Этап 4: PRE_CHANGE_PLANNING_CHECKLIST.md
**Цель**: Планирование изменений (КАК изменить)

**Что делает**:
- Понимает задачу и контекст
- Анализирует зависимости и влияние (модули, EventBus, конфигурации, оси состояния)
- Анализирует риски и инварианты
- Планирует реализацию (Impact Map, change_impact.yaml, план изменений)
- Проверяет перед началом кода (документация, конфигурация, тесты, мониторинг)

**Результат**: Полный план изменений, Impact Map, change_impact.yaml.

**Время**: 60-120 минут

---

#### Этап 5: DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md
**Цель**: Проверка дублирования/конфликтов/race conditions (ЧТО предотвратить)

**Что делает**:
- Проверяет дублирование кода (поиск существующей функциональности)
- Проверяет конфликты (конфликты с существующим кодом, архитектурой, конфигурациями)
- Проверяет race conditions (thread-safety, async/await паттерны)
- Проверяет состояние и синхронизацию (единый источник истины, атомарность)

**Результат**: Таблица проверки дублирования/конфликтов/race conditions, готовность к генерации кода.

**Время**: 20-40 минут

---

## ⚡ Этап 2: Автоматическая валидация при каждом изменении кода

### Как работает

**ВАЖНО**: Эти проверки выполняются **АВТОМАТИЧЕСКИ** при КАЖДОМ изменении кода, независимо от этапа планирования.

### Правило 1: Проверка дублирования перед созданием нового кода

**Когда срабатывает**: Перед созданием любой функции/класса/метода

**Что делает**:
1. Извлекает имя функции/метода из генерируемого кода
2. Выполняет поиск по кодовой базе (grep, codebase_search)
3. Если найдено → использует существующую функцию
4. Если не найдено → создает новую функцию

**Пример**:
```python
# Пользователь просит: "Создай функцию для определения Bluetooth устройств"
# 1. Ассистент автоматически ищет: grep -r "is_bluetooth_device\|bluetooth.*device" .
# 2. Находит: device_utils.is_bluetooth_device()
# 3. Использует существующую функцию вместо создания новой

# ✅ Результат:
from modules.audio_system.utils.device_utils import is_bluetooth_device
is_bluetooth = is_bluetooth_device(device_name)
```

---

### Правило 2: Проверка конфликтов перед изменением имен/состояний

**Когда срабатывает**: Перед созданием/изменением любой переменной/функции/класса

**Что делает**:
1. Извлекает имя переменной из генерируемого кода
2. Выполняет поиск по кодовой базе (grep)
3. Если найдено в том же контексте → использует существующую переменную
4. Если найдено в другом контексте → использует уникальное имя

**Пример**:
```python
# Пользователь просит: "Создай переменную _mic_state"
# 1. Ассистент автоматически ищет: grep -r "_mic_state" .
# 2. Находит: MicrophoneStateManager._state (похожее имя)
# 3. Проверяет конфликт: не конфликтует ли с существующим
# 4. Если конфликт → использует уникальное имя или существующую переменную

# ✅ Результат:
if self._mic_state_manager:
    state = await self._mic_state_manager.get_state()
```

---

### Правило 3: Проверка race conditions перед доступом к общим данным

**Когда срабатывает**: Перед доступом к любым данным из нескольких потоков

**Что делает**:
1. Определяет, доступны ли данные из нескольких потоков
2. Если да → проверяет наличие блокировок
3. Если блокировок нет → добавляет блокировки
4. Если блокировки есть → использует существующие

**Пример**:
```python
# Пользователь просит: "Добавь данные в _google_audio_chunks из callback"
# 1. Ассистент автоматически проверяет: Доступен ли из нескольких потоков?
# 2. Находит: Да, из callback потока и основного async потока
# 3. Проверяет: Есть ли блокировка?
# 4. Если нет → добавляет threading.Lock
# 5. Если есть → использует существующую блокировку

# ✅ Результат:
def callback(recognizer, audio):
    with self._google_audio_chunks_lock:
        self._google_audio_chunks.append(audio)
```

---

### Правило 4: Проверка async/await паттернов

**Когда срабатывает**: Перед созданием async задачи

**Что делает**:
1. Проверяет, отслеживаются ли задачи
2. Если есть предыдущая задача → отменяет перед созданием новой
3. Проверяет наличие таймаутов

**Пример**:
```python
# Пользователь просит: "Создай async задачу для прослушивания"
# 1. Ассистент автоматически проверяет: Есть ли уже запущенная задача?
# 2. Если да → отменяет предыдущую перед созданием новой
# 3. Создает новую задачу с отслеживанием

# ✅ Результат:
self._listen_task: Optional[asyncio.Task] = None

async def start_listening():
    if self._listen_task and not self._listen_task.done():
        self._listen_task.cancel()
        try:
            await self._listen_task
        except asyncio.CancelledError:
            pass
    self._listen_task = asyncio.create_task(self._listen_loop())
```

---

## 🔄 Полный цикл работы системы

### Сценарий: Пользователь просит изменить код

```
1. Пользователь: "Добавь функцию для определения Bluetooth устройств"
   ↓
2. Ассистент: "Перед началом изменений мне нужно пройти процесс планирования"
   ↓
3. Этап планирования (5 чек-листов):
   - CONTEXT_AWARE_PLANNING_GUIDE.md
   - ARCHITECTURE_ANALYSIS_CHECKLIST.md
   - CONFIGURATION_ANALYSIS_CHECKLIST.md
   - PRE_CHANGE_PLANNING_CHECKLIST.md
   - DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md
   ↓
4. Готовность к генерации кода подтверждена
   ↓
5. Ассистент начинает генерировать код
   ↓
6. Автоматическая валидация (при каждом изменении):
   - Перед созданием функции → поиск существующей функциональности
   - Находит: device_utils.is_bluetooth_device()
   - Использует существующую функцию вместо создания новой
   ↓
7. Код сгенерирован с использованием существующей функции
   ↓
8. Проверка: Нет дублирования, конфликтов, race conditions
   ↓
9. Готово!
```

---

## 📊 Примеры работы системы для разных модулей

### Пример 1: Аудиосистема (audio_avf)

**Запрос**: "Измени функцию определения Bluetooth устройств"

**Процесс**:
1. **Планирование** (80-110 минут):
   - Быстрый анализ контекста (15-20 минут)
   - Фокус на затронутых модулях (20-30 минут)
   - Фокус на затронутых конфигурациях (15-20 минут)
   - Планирование простого изменения (30-40 минут)
   - Быстрая проверка дублирования/конфликтов/race conditions (10-15 минут)

2. **Генерация кода**:
   - Автоматическая проверка: функция уже существует
   - Использование существующей функции
   - Нет дублирования, конфликтов, race conditions

**Результат**: Изменение выполнено без проблем.

---

### Пример 2: Система разрешений (permissions)

**Запрос**: "Добавь проверку разрешения на доступность"

**Процесс**:
1. **Планирование** (185-250 минут):
   - Полный анализ контекста (30-40 минут)
   - Полный анализ архитектуры (45-60 минут)
   - Полный анализ конфигураций (30-40 минут)
   - Планирование среднего изменения (60-80 минут)
   - Полная проверка дублирования/конфликтов/race conditions (20-30 минут)

2. **Генерация кода**:
   - Автоматическая проверка: поиск существующей функциональности проверки разрешений
   - Автоматическая проверка: проверка конфликтов имен (permissions.accessibility)
   - Автоматическая проверка: проверка thread-safety (разрешения могут проверяться из разных потоков)
   - Нет дублирования, конфликтов, race conditions

**Результат**: Изменение выполнено без проблем.

---

### Пример 3: Сетевой модуль (grpc_client)

**Запрос**: "Добавь обработку ошибок подключения"

**Процесс**:
1. **Планирование** (185-250 минут):
   - Полный анализ контекста (30-40 минут)
   - Полный анализ архитектуры (45-60 минут)
   - Полный анализ конфигураций (30-40 минут)
   - Планирование среднего изменения (60-80 минут)
   - Полная проверка дублирования/конфликтов/race conditions (20-30 минут)

2. **Генерация кода**:
   - Автоматическая проверка: поиск существующей обработки ошибок
   - Автоматическая проверка: проверка конфликтов имен (error_handler, retry_logic)
   - Автоматическая проверка: проверка thread-safety (async операции)
   - Автоматическая проверка: проверка async/await паттернов (отслеживание задач)
   - Нет дублирования, конфликтов, race conditions

**Результат**: Изменение выполнено без проблем.

---

### Пример 4: Управление режимами (mode_management)

**Запрос**: "Добавь новый режим работы приложения"

**Процесс**:
1. **Планирование** (270-370 минут):
   - Глубокий анализ контекста (45-60 минут)
   - Глубокий анализ архитектуры (60-90 минут)
   - Глубокий анализ конфигураций (45-60 минут)
   - Планирование большого изменения (90-120 минут)
   - Глубокая проверка дублирования/конфликтов/race conditions (30-40 минут)

2. **Генерация кода**:
   - Автоматическая проверка при каждом изменении
   - Использование существующих функций где возможно
   - Защита общих данных блокировками
   - Отслеживание async задач
   - Обновление STATE_CATALOG.md и interaction_matrix.yaml
   - Нет дублирования, конфликтов, race conditions

**Результат**: Изменение выполнено без проблем.

---

### Пример 5: Система обновлений (updater)

**Запрос**: "Добавь проверку версии перед обновлением"

**Процесс**:
1. **Планирование** (185-250 минут):
   - Полный анализ контекста (30-40 минут)
   - Полный анализ архитектуры (45-60 минут)
   - Полный анализ конфигураций (30-40 минут)
   - Планирование среднего изменения (60-80 минут)
   - Полная проверка дублирования/конфликтов/race conditions (20-30 минут)

2. **Генерация кода**:
   - Автоматическая проверка: поиск существующей проверки версий
   - Автоматическая проверка: проверка конфликтов имен (version_check, update_version)
   - Автоматическая проверка: проверка thread-safety (обновления могут выполняться в фоне)
   - Автоматическая проверка: проверка async/await паттернов (отслеживание задач обновления)
   - Нет дублирования, конфликтов, race conditions

**Результат**: Изменение выполнено без проблем.

---

### Пример 6: Обработка ввода (input_processing)

**Запрос**: "Добавь обработку комбинации клавиш"

**Процесс**:
1. **Планирование** (185-250 минут):
   - Полный анализ контекста (30-40 минут)
   - Полный анализ архитектуры (45-60 минут)
   - Полный анализ конфигураций (30-40 минут)
   - Планирование среднего изменения (60-80 минут)
   - Полная проверка дублирования/конфликтов/race conditions (20-30 минут)

2. **Генерация кода**:
   - Автоматическая проверка: поиск существующей обработки комбинаций клавиш
   - Автоматическая проверка: проверка конфликтов имен (key_combination, hotkey)
   - Автоматическая проверка: проверка thread-safety (обработка клавиш из разных потоков)
   - Автоматическая проверка: проверка async/await паттернов (отслеживание задач обработки)
   - Нет дублирования, конфликтов, race conditions

**Результат**: Изменение выполнено без проблем.

**Процесс**:
1. **Планирование** (80-110 минут):
   - Быстрый анализ контекста (15-20 минут)
   - Фокус на затронутых модулях (20-30 минут)
   - Фокус на затронутых конфигурациях (15-20 минут)
   - Планирование простого изменения (30-40 минут)
   - Быстрая проверка дублирования/конфликтов/race conditions (10-15 минут)

2. **Генерация кода**:
   - Автоматическая проверка: функция уже существует
   - Использование существующей функции
   - Нет дублирования, конфликтов, race conditions

**Результат**: Изменение выполнено без проблем.

---

### Пример 2: Среднее изменение (несколько модулей)

**Запрос**: "Добавь поддержку нового типа устройств"

**Процесс**:
1. **Планирование** (185-250 минут):
   - Полный анализ контекста (30-40 минут)
   - Полный анализ архитектуры (45-60 минут)
   - Полный анализ конфигураций (30-40 минут)
   - Планирование среднего изменения (60-80 минут)
   - Полная проверка дублирования/конфликтов/race conditions (20-30 минут)

2. **Генерация кода**:
   - Автоматическая проверка: поиск существующей функциональности
   - Автоматическая проверка: проверка конфликтов имен
   - Автоматическая проверка: проверка thread-safety
   - Нет дублирования, конфликтов, race conditions

**Результат**: Изменение выполнено без проблем.

---

### Пример 3: Большое изменение (функциональность)

**Запрос**: "Добавь новую систему обработки аудио"

**Процесс**:
1. **Планирование** (270-370 минут):
   - Глубокий анализ контекста (45-60 минут)
   - Глубокий анализ архитектуры (60-90 минут)
   - Глубокий анализ конфигураций (45-60 минут)
   - Планирование большого изменения (90-120 минут)
   - Глубокая проверка дублирования/конфликтов/race conditions (30-40 минут)

2. **Генерация кода**:
   - Автоматическая проверка при каждом изменении
   - Использование существующих функций где возможно
   - Защита общих данных блокировками
   - Отслеживание async задач
   - Нет дублирования, конфликтов, race conditions

**Результат**: Изменение выполнено без проблем.

---

## ✅ Преимущества новой системы

### 1. Предотвращение проблем ДО их возникновения

**Было**:
- Проблемы обнаруживались только при тестировании
- Много времени тратилось на исправление ошибок

**Стало**:
- Проблемы предотвращаются на этапе планирования
- Проблемы предотвращаются при генерации кода
- Меньше времени тратится на исправление ошибок

### 2. Автоматическая валидация при каждом изменении

**Было**:
- Проверки выполнялись только на этапе планирования
- При последующих изменениях проверки не выполнялись

**Стало**:
- Проверки выполняются автоматически при каждом изменении кода
- Дублирование, конфликты, race conditions предотвращаются автоматически

### 3. Единый процесс для всех изменений

**Было**:
- Разные процессы для разных типов изменений
- Неясно, какие проверки нужно выполнять

**Стало**:
- Единый процесс для всех изменений
- Четкие правила и проверки для каждого типа изменения

---

## 📚 Связанные документы

**Планирование**:
- `PLANNING_PROCESS_ORDER.md` — единый источник истины для порядка выполнения всех чек-листов
- `CONTEXT_AWARE_PLANNING_GUIDE.md` — контекстный анализ (ПЕРВЫЙ этап)
- `ARCHITECTURE_ANALYSIS_CHECKLIST.md` — анализ архитектуры (ВТОРОЙ этап)
- `CONFIGURATION_ANALYSIS_CHECKLIST.md` — анализ конфигураций (ТРЕТИЙ этап)
- `PRE_CHANGE_PLANNING_CHECKLIST.md` — планирование изменений (ЧЕТВЕРТЫЙ этап)
- `DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md` — проверка дублирования/конфликтов/race conditions (ПЯТЫЙ этап)

**Автоматическая валидация**:
- `.cursorrules` (раздел 11.2) — обязательные проверки при каждом изменении кода
- `CODE_CHANGE_VALIDATION_RULES.md` — детальные правила валидации изменений кода
- `REAL_TIME_CODE_VALIDATION.md` — автоматическая валидация при генерации кода
- `AUTOMATIC_CODE_VALIDATION_INTEGRATION.md` — документ интеграции

---

## 🎯 Итоговая логика работы

```
ПОЛЬЗОВАТЕЛЬ ПРОСИТ ИЗМЕНИТЬ КОД
         ↓
ЭТАП ПЛАНИРОВАНИЯ (5 чек-листов)
  - Контекстный анализ
  - Анализ архитектуры
  - Анализ конфигураций
  - Планирование изменений
  - Проверка дублирования/конфликтов/race conditions
         ↓
ГОТОВНОСТЬ К ГЕНЕРАЦИИ КОДА
         ↓
ГЕНЕРАЦИЯ КОДА
  - Автоматическая проверка дублирования
  - Автоматическая проверка конфликтов
  - Автоматическая проверка race conditions
  - Автоматическая проверка async/await паттернов
         ↓
КОД СГЕНЕРИРОВАН БЕЗ ПРОБЛЕМ
```

---

**Версия**: 1.0  
**Дата создания**: 2025-01-XX



