# 📋 Резюме: Инструкция по выявлению дублирования, конфликтов и race conditions

**Дата создания**: 2025-01-XX  
**Версия**: 1.0

---

## 🎯 Цель

Предоставить ассистенту детальную инструкцию с конкретными методами и техниками для правильного выявления дублирования кода, конфликтов и race conditions.

---

## 📚 Что было создано

### Детальная инструкция

**Документ**: `DETECTION_GUIDE_DUPLICATES_CONFLICTS_RACES.md`

**Содержание**:

1. **Выявление дублирования кода**:
   - Поиск существующей функциональности (grep, codebase_search, поиск по паттернам)
   - Проверка централизованных утилит
   - Анализ дублирования логики

2. **Выявление конфликтов**:
   - Проверка конфликтов имен (поиск существующих переменных/функций/классов)
   - Проверка конфликтов состояний (поиск всех мест управления состоянием)
   - Проверка конфликтов архитектуры (порядок инициализации, EventBus события)

3. **Выявление race conditions**:
   - Проверка thread-safety (поиск доступа к данным из нескольких потоков, поиск блокировок)
   - Проверка async/await паттернов (поиск async задач, поиск отмены задач)
   - Проверка смешивания async и threading (поиск run_coroutine_threadsafe)

4. **Инструменты и команды**:
   - Команды grep для поиска
   - Семантический поиск (codebase_search)
   - Поиск в документации

5. **Чек-листы проверки**:
   - Чек-лист проверки дублирования
   - Чек-лист проверки конфликтов
   - Чек-лист проверки race conditions

---

## 🔍 Конкретные методы выявления

### 1. Выявление дублирования

#### Метод 1: Поиск по имени функции/метода
```bash
# Поиск по точному имени
grep -r "function_name" .

# Поиск по части имени (для вариаций)
grep -r "is_bluetooth\|bluetooth.*device\|device.*bluetooth" .
```

#### Метод 2: Семантический поиск
```python
codebase_search("How is bluetooth device detected?")
codebase_search("Where is device selection logic implemented?")
```

#### Метод 3: Поиск в утилитных модулях
```bash
grep -r "function_name" modules/*/utils/*.py
grep -r "function_name" integration/core/*.py
```

---

### 2. Выявление конфликтов

#### Метод 1: Поиск существующих имен
```bash
# Поиск по имени переменной
grep -r "_variable_name\|self\.variable_name" .

# Поиск по имени функции
grep -r "def function_name\|function_name(" .
```

#### Метод 2: Поиск всех мест управления состоянием
```bash
# Поиск всех мест, где изменяется состояние
grep -r "state.*=\|\.state\s*=\|set_state\|_state\s*=" .

# Поиск единого источника истины
grep -r "axis_name\|state_name" Docs/STATE_CATALOG.md
```

---

### 3. Выявление race conditions

#### Метод 1: Поиск доступа к данным из нескольких потоков
```bash
# Поиск использования threading
grep -r "threading\.\|Thread(\|threading.Thread" .

# Поиск callback функций
grep -r "callback\|def.*callback\|callback.*=" .

# Поиск async функций
grep -r "async def\|asyncio\." .
```

#### Метод 2: Поиск блокировок
```bash
# Поиск блокировок
grep -r "Lock(\|RLock(\|threading\.Lock\|threading\.RLock" .

# Поиск использования блокировок
grep -r "with.*lock\|\.acquire(\|\.release(" .
```

#### Метод 3: Поиск async задач
```bash
# Поиск создания async задач
grep -r "asyncio\.create_task\|create_task(" .

# Поиск отслеживания задач
grep -r "_task\|\.task\|task.*=" .
```

---

## 📋 Чек-листы проверки

### Чек-лист проверки дублирования

**Перед созданием функции/метода**:
- [ ] Выполнен поиск по имени функции (grep -r "function_name" .)
- [ ] Выполнен семантический поиск (codebase_search("описание функциональности"))
- [ ] Проверены утилитные модули (modules/*/utils/*.py)
- [ ] Проверены core модули (integration/core/*.py)
- [ ] Найдена существующая функциональность → используется существующая
- [ ] Не найдена существующая функциональность → создается новая в правильном месте

### Чек-лист проверки конфликтов

**Перед созданием переменной/функции/класса**:
- [ ] Выполнен поиск существующих имен (grep -r "name" .)
- [ ] Проверены конфликты в том же контексте
- [ ] Проверены конфликты в других контекстах
- [ ] Если конфликт → используется уникальное имя

**Перед управлением состоянием**:
- [ ] Выполнен поиск всех мест управления состоянием (grep -r "state" .)
- [ ] Проверен единый источник истины (STATE_CATALOG.md)
- [ ] Проверены правила взаимодействия (interaction_matrix.yaml)
- [ ] Если несколько мест → используется единый источник истины

### Чек-лист проверки race conditions

**Перед доступом к данным**:
- [ ] Определено, доступны ли данные из нескольких потоков
- [ ] Выполнен поиск использования threading (grep -r "threading\|Thread" .)
- [ ] Выполнен поиск callback функций (grep -r "callback" .)
- [ ] Выполнен поиск async функций (grep -r "async def" .)
- [ ] Если данные доступны из нескольких потоков → проверены блокировки
- [ ] Если блокировок нет → добавлены блокировки
- [ ] Если блокировки есть → используются существующие

---

## 📚 Примеры применения

### Пример 1: Выявление дублирования функции

**Запрос**: "Создай функцию для определения Bluetooth устройств"

**Процесс**:
1. Поиск по имени: `grep -r "is_bluetooth_device\|bluetooth.*device" .`
2. Результат: Находит `device_utils.is_bluetooth_device()`
3. Действие: Использовать существующую функцию

---

### Пример 2: Выявление конфликта состояний

**Запрос**: "Создай переменную для состояния микрофона"

**Процесс**:
1. Поиск по имени: `grep -r "_mic_state\|mic.*state" .`
2. Результат: Находит `MicrophoneStateManager._state` и `ApplicationStateManager._mic_state`
3. Проверка источника истины: `grep -r "mic\|microphone" Docs/STATE_CATALOG.md`
4. Результат: Находит `permissions.mic` с владельцем `VoiceRecognitionIntegration`
5. Действие: Использовать единый источник истины через `MicrophoneStateManager`

---

### Пример 3: Выявление race condition

**Запрос**: "Добавь данные в _google_audio_chunks из callback"

**Процесс**:
1. Поиск использования: `grep -r "_google_audio_chunks" .`
2. Результат: Находит использование в callback (поток 1) и в async функции (поток 2)
3. Поиск блокировок: `grep -r "_google_audio_chunks.*lock\|lock.*google_audio_chunks" .`
4. Результат: Не находит блокировку
5. Действие: Добавить блокировку для синхронизации

---

## ✅ Интеграция в процесс

### Обновленные документы

- `DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md` — добавлены ссылки на инструкцию
- `.cursorrules` (раздел 11.2) — добавлены ссылки на инструкцию
- `CODE_CHANGE_VALIDATION_RULES.md` — добавлены ссылки на инструкцию
- `REAL_TIME_CODE_VALIDATION.md` — добавлены ссылки на инструкцию

---

## 🎯 Итог

Создана детальная инструкция с конкретными методами и командами для выявления:
- Дублирования кода (grep, codebase_search, поиск в утилитах)
- Конфликтов (поиск имен, состояний, архитектурных конфликтов)
- Race conditions (поиск thread-safety, async/await паттернов, блокировок)

Инструкция интегрирована в процесс валидации и применяется при каждом изменении кода.

---

**Версия**: 1.0  
**Дата создания**: 2025-01-XX

