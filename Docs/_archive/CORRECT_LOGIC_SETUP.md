# Правильная настройка логики распознавания речи

## 🎯 Принципы правильной логики

### 1. Единоличное владение микрофоном
- **AVFAudioEngine** — единственный владелец микрофона
- Никакие другие компоненты не открывают микрофон напрямую
- SpeechRecognition использует данные через эстафету, не открывая микрофон

### 2. Явная эстафета данных
- AVF собирает данные → публикует событие → SpeechRecognition обрабатывает
- Чёткое разделение фаз: запись → передача → распознавание
- Микрофон освобождён до начала распознавания

### 3. Защита от дубликатов
- Каждая сессия обрабатывается только один раз
- Механизм `_processed_mic_data_sessions` предотвращает повторную обработку

### 4. Использование библиотеки speech_recognition
- Используем библиотеку `speech_recognition` (не прямой Google API)
- Библиотека внутри вызывает Google Speech API через `recognize_google()`

---

## 📋 Правильная последовательность работы

### Этап 1: Начало записи (`voice.recording_start`)

```python
# ✅ ПРАВИЛЬНО:
async def _on_recording_start(self, event: Dict[str, Any]):
    # 1. Проверка блокировок
    if self._first_run_in_progress:
        return  # Блокируем во время запроса разрешений
    
    # 2. Установка сессии
    session_id = data.get("session_id")
    self._set_session_id(session_id, reason="recording_start")
    
    # 3. Очистка флага обработанных сессий (для новой записи)
    self._processed_mic_data_sessions.discard(session_id)
    
    # 4. Очистка буфера (fallback)
    self._clear_audio_buffer()
    
    # 5. Запуск AVF записи
    if self._use_avf and self._avf_engine:
        await self._avf_engine.start_input(callback=audio_callback)
        # AVF становится единственным владельцем микрофона
```

**Что происходит:**
- ✅ AVF открывает микрофон (единоличное владение)
- ✅ Собираются метаданные (device_info, format, diagnostics)
- ✅ Буфер очищается для новой записи
- ✅ Флаг обработанных сессий очищается

---

### Этап 2: Запись аудио (в процессе)

```python
# ✅ ПРАВИЛЬНО:
async def audio_callback(data: bytes, sample_rate: int, channels: int):
    # Сохранение параметров формата (первый чанк)
    if self._audio_buffer_sample_rate is None:
        self._audio_buffer_sample_rate = sample_rate
        self._audio_buffer_channels = channels
    
    # Накопление данных (fallback для legacy)
    self._audio_buffer.append(data)
    
    # AVF также накапливает данные внутри себя
    # + собирает диагностику (first_chunk, RMS, min/max)
```

**Что происходит:**
- ✅ AVF записывает чанки через `recording_callback()`
- ✅ Данные накапливаются в AVF (основной путь)
- ✅ Данные также накапливаются в буфере (fallback для legacy)
- ✅ Собирается диагностика (RMS, min/max, mean/std)

---

### Этап 3: Остановка записи (`voice.recording_stop`)

```python
# ✅ ПРАВИЛЬНО:
async def _on_recording_stop(self, event: Dict[str, Any]):
    # 1. Проверка сессии
    active_session_id = self._get_active_session_id()
    if active_session_id != session_id:
        logger.warning("⚠️ Session mismatch, игнорируем")
        return
    
    # 2. Остановка AVF записи
    if self._use_avf and self._avf_engine:
        result = await self._avf_engine.stop_input()
        # AVF освобождает микрофон здесь
        
        if result:
            # 3. Публикация события эстафеты
            await self._publish_mic_data_ready(result, session_id)
            # НЕ обрабатываем буфер здесь (избегаем двойного распознавания)
            
            # 4. Legacy fallback (только если result.data пустой)
            if (not result.data or len(result.data) == 0) and self._audio_buffer:
                # Используем буфер только если AVF не вернул данные
                total_audio = b''.join(self._audio_buffer)
                await self._recognize_avf_audio(total_audio, ...)
    
    # 5. Очистка буфера (в finally)
    finally:
        self._clear_audio_buffer()
```

**Что происходит:**
- ✅ AVF останавливает запись и освобождает микрофон
- ✅ Формируется `AudioInputResult` с полными данными
- ✅ Публикуется событие `voice.mic_data_ready` с payload
- ✅ Буфер НЕ обрабатывается здесь (избегаем двойного распознавания)

---

### Этап 4: Обработка эстафеты (`voice.mic_data_ready`)

```python
# ✅ ПРАВИЛЬНО:
async def _on_mic_data_ready(self, event: Dict[str, Any]):
    # 1. ЗАЩИТА ОТ ДВОЙНОЙ ОБРАБОТКИ
    session_id = data.get("session_id")
    if session_id in self._processed_mic_data_sessions:
        logger.warning("⚠️ Уже обрабатывается, игнорируем дубликат")
        return  # КРИТИЧНО: предотвращаем двойное распознавание
    
    # 2. Помечаем сессию как обрабатываемую
    self._processed_mic_data_sessions.add(session_id)
    
    # 3. Извлечение данных из payload
    pcm_bytes = data.get("pcm_bytes")
    sample_rate = data.get("sample_rate")
    channels = data.get("channels")
    
    # 4. Распознавание через библиотеку speech_recognition
    try:
        await self._recognize_avf_audio(pcm_bytes, sample_rate, channels, session_id)
    finally:
        # 5. Очистка флага после завершения (успех или ошибка)
        self._processed_mic_data_sessions.discard(session_id)
```

**Что происходит:**
- ✅ Проверка защиты от дубликатов
- ✅ Помечаем сессию как обрабатываемую
- ✅ Вызываем распознавание через библиотеку
- ✅ Очищаем флаг после завершения

---

### Этап 5: Распознавание через библиотеку (`_recognize_avf_audio`)

```python
# ✅ ПРАВИЛЬНО:
async def _recognize_avf_audio(self, audio_data: bytes, sample_rate: int, channels: int, session_id: str):
    import speech_recognition as sr
    
    # 1. Ресемплинг (если нужно)
    if sample_rate != 16000:
        audio_bytes_int16 = resample_to_16khz(audio_data, sample_rate)
        sample_rate = 16000
    else:
        audio_bytes_int16 = audio_data
    
    # 2. Нормализация (если нужно)
    # float32 → int16 (если данные в float32)
    
    # 3. Создание AudioData объекта из библиотеки
    audio_data_obj = sr.AudioData(audio_bytes_int16, sample_rate, 2)  # sample_width=2 (int16)
    
    # 4. Создание Recognizer из библиотеки
    recognizer = sr.Recognizer()  # НЕ используем self._recognizer (избегаем двойной активации)
    
    # 5. Вызов recognize_google() из библиотеки
    try:
        text = recognizer.recognize_google(audio_data_obj, language=language, show_all=False)
        # Библиотека внутри вызывает Google Speech API
        
        # 6. Публикация результата
        await self.event_bus.publish("voice.recognition_completed", {
            "session_id": session_id,
            "text": text,
            "source": "avf_recognition"
        })
    except sr.UnknownValueError:
        # Ошибка: речь не распознана
        await self.event_bus.publish("voice.recognition_failed", {
            "session_id": session_id,
            "error": "Speech not recognized",
            "source": "avf_recognition"
        })
    except sr.RequestError as e:
        # Ошибка: проблема с сервисом
        await self.event_bus.publish("voice.recognition_failed", {
            "session_id": session_id,
            "error": str(e),
            "source": "avf_recognition"
        })
```

**Что происходит:**
- ✅ Ресемплинг до 16kHz (если нужно)
- ✅ Нормализация формата данных
- ✅ Создание `AudioData` объекта из библиотеки
- ✅ Создание нового `Recognizer` (не используем `self._recognizer`)
- ✅ Вызов `recognize_google()` из библиотеки
- ✅ Библиотека внутри вызывает Google Speech API
- ✅ Публикация результата или ошибки

---

## 🔒 Критичные моменты

### 1. НЕ создаём SpeechRecognizer при использовании AVF

```python
# ❌ НЕПРАВИЛЬНО:
if self._use_avf:
    self._recognizer = SpeechRecognizer(...)  # Это откроет микрофон через PyAudio!

# ✅ ПРАВИЛЬНО:
if self._use_avf:
    self._recognizer = None  # Не создаём, избегаем двойной активации
    # В _recognize_avf_audio создаём sr.Recognizer() напрямую
```

### 2. НЕ обрабатываем буфер в _on_recording_stop

```python
# ❌ НЕПРАВИЛЬНО:
result = await self._avf_engine.stop_input()
if result:
    await self._recognize_avf_audio(result.data, ...)  # Двойное распознавание!
    await self._publish_mic_data_ready(result, ...)     # Ещё раз!

# ✅ ПРАВИЛЬНО:
result = await self._avf_engine.stop_input()
if result:
    await self._publish_mic_data_ready(result, ...)  # Только публикация события
    # Распознавание будет в _on_mic_data_ready
```

### 3. ЗАЩИТА от двойной обработки обязательна

```python
# ❌ НЕПРАВИЛЬНО:
async def _on_mic_data_ready(self, event):
    await self._recognize_avf_audio(...)  # Может вызваться дважды!

# ✅ ПРАВИЛЬНО:
async def _on_mic_data_ready(self, event):
    if session_id in self._processed_mic_data_sessions:
        return  # Игнорируем дубликат
    self._processed_mic_data_sessions.add(session_id)
    try:
        await self._recognize_avf_audio(...)
    finally:
        self._processed_mic_data_sessions.discard(session_id)
```

### 4. Используем библиотеку speech_recognition, не прямой API

```python
# ❌ НЕПРАВИЛЬНО:
import requests
response = requests.post("https://speech.googleapis.com/...")  # Прямой вызов API

# ✅ ПРАВИЛЬНО:
import speech_recognition as sr
recognizer = sr.Recognizer()
text = recognizer.recognize_google(audio_data_obj, language=language)
# Библиотека внутри вызывает Google Speech API
```

---

## 📊 Диаграмма правильного потока

```
┌─────────────────────────────────────────────────────────────────┐
│              ПРАВИЛЬНАЯ ЛОГИКА РАСПОЗНАВАНИЯ РЕЧИ               │
└─────────────────────────────────────────────────────────────────┘

1. voice.recording_start
   │
   ├─► _on_recording_start()
   │   ├─ Проверка блокировок (first_run)
   │   ├─ _set_session_id(session_id)
   │   ├─ Очистка: _processed_mic_data_sessions.discard(session_id)
   │   ├─ Очистка буфера: _clear_audio_buffer()
   │   └─ AVFAudioEngine.start_input()
   │       └─ AVF открывает микрофон (единоличное владение)
   │
   └─► AVFAudioEngine
       ├─ Сбор метаданных (device_info, format, diagnostics)
       └─ Запись начинается

2. Запись аудио (в процессе)
   │
   └─► AVFAudioEngine.recording_callback()
       ├─ Накопление данных в AVF
       ├─ Сбор диагностики (first_chunk, RMS, min/max)
       └─ Накопление в буфере (fallback)

3. voice.recording_stop
   │
   ├─► _on_recording_stop()
   │   ├─ Проверка сессии (active_session_id == request_session_id)
   │   ├─ AVFAudioEngine.stop_input()
   │   │   ├─ Остановка engine
   │   │   ├─ Удаление tap
   │   │   ├─ Объединение чанков → all_data
   │   │   └─ Формирование AudioInputResult (с метаданными)
   │   │
   │   └─ _publish_mic_data_ready(result, session_id)
   │       └─ event_bus.publish("voice.mic_data_ready", payload)
   │
   └─► AVF освобождает микрофон ✅

4. voice.mic_data_ready
   │
   └─► _on_mic_data_ready() [подписан с приоритетом HIGH]
       ├─ ✅ ЗАЩИТА: проверка _processed_mic_data_sessions
       │   └─ Если уже обрабатывается → return (игнорируем дубликат)
       │
       ├─ Помечаем: _processed_mic_data_sessions.add(session_id)
       │
       └─ _recognize_avf_audio(pcm_bytes, sample_rate, channels, session_id)
           ├─ Ресемплинг: 48kHz → 16kHz (если нужно)
           ├─ Нормализация: float32 → int16
           ├─ Создание: sr.AudioData(audio_bytes_int16, sample_rate, 2)
           ├─ Создание: sr.Recognizer() (новый, не self._recognizer)
           ├─ Вызов: recognizer.recognize_google(audio_data_obj, language)
           │   └─ Библиотека внутри вызывает Google Speech API
           │
           └─ finally: _processed_mic_data_sessions.discard(session_id)

5. Результат распознавания
   │
   └─► Публикация события:
       ├─ voice.recognition_completed {text, source: "avf_recognition"}
       └─ voice.recognition_failed {error, source: "avf_recognition"}
```

---

## ✅ Чек-лист правильной настройки

### Инициализация
- [ ] `self._recognizer = None` при `self._use_avf = True` (избегаем двойной активации)
- [ ] Подписка на `voice.mic_data_ready` с приоритетом HIGH
- [ ] Инициализация `self._processed_mic_data_sessions = set()`

### Начало записи
- [ ] Проверка блокировок (`_first_run_in_progress`)
- [ ] Установка сессии (`_set_session_id`)
- [ ] Очистка флага обработанных сессий (`discard(session_id)`)
- [ ] Очистка буфера (`_clear_audio_buffer()`)
- [ ] Запуск AVF записи (`AVFAudioEngine.start_input()`)

### Остановка записи
- [ ] Проверка сессии (соответствие active_session_id)
- [ ] Остановка AVF (`AVFAudioEngine.stop_input()`)
- [ ] Публикация события эстафеты (`_publish_mic_data_ready()`)
- [ ] НЕ обрабатываем буфер здесь (избегаем двойного распознавания)
- [ ] Очистка буфера в finally блоке

### Обработка эстафеты
- [ ] Проверка защиты от дубликатов (`if session_id in _processed_mic_data_sessions`)
- [ ] Помечаем сессию как обрабатываемую (`add(session_id)`)
- [ ] Вызов распознавания (`_recognize_avf_audio()`)
- [ ] Очистка флага в finally (`discard(session_id)`)

### Распознавание
- [ ] Ресемплинг до 16kHz (если нужно)
- [ ] Нормализация формата данных
- [ ] Создание `sr.AudioData` объекта
- [ ] Создание нового `sr.Recognizer()` (не `self._recognizer`)
- [ ] Вызов `recognize_google()` из библиотеки
- [ ] Обработка ошибок (`UnknownValueError`, `RequestError`)
- [ ] Публикация результата или ошибки

---

## 🎯 Итоговые принципы

1. **AVF — единственный владелец микрофона**
   - Никакие другие компоненты не открывают микрофон
   - SpeechRecognition использует данные через эстафету

2. **Явная эстафета через событие**
   - AVF публикует `voice.mic_data_ready` с полными данными
   - SpeechRecognition обрабатывает событие один раз

3. **Защита от дубликатов обязательна**
   - Механизм `_processed_mic_data_sessions` предотвращает повторную обработку
   - Флаг очищается после завершения и при начале новой записи

4. **Использование библиотеки speech_recognition**
   - Не прямой вызов Google Speech API
   - Библиотека внутри вызывает API через `recognize_google()`

5. **Правильная последовательность**
   - Запись → Остановка → Эстафета → Распознавание → Результат
   - Каждый этап чётко разделён и не дублируется

