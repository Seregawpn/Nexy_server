# Анализ кода стримингового распознавания

## 🔍 Обнаруженные проблемы

### 1. ⚠️ RACE CONDITION: Небезопасный доступ к `_use_streaming` в `audio_callback`

**Проблема:**
```python
# audio_callback вызывается из другого потока (AVFAudioEngine)
async def audio_callback(data: bytes, sample_rate: int, channels: int):
    # ❌ ПРОБЛЕМА: Проверка без блокировки
    if self._use_streaming and self._sf_recognizer is not None:
        # ...
    else:
        self._audio_buffer.append(data)
```

**Сценарий гонки:**
1. Запись началась, `_use_streaming = True`
2. `audio_callback` получает чанк 1 → отправляет в стриминг ✅
3. Стриминг падает, `_use_streaming = False` (в другом потоке)
4. `audio_callback` получает чанк 2 → проверяет `_use_streaming` (уже False) → добавляет в буфер
5. **Результат:** Чанк 1 в стриминге, чанк 2 в буфере → потеря данных или двойная обработка

**Решение:** Использовать локальную переменную или блокировку.

---

### 2. 🔄 ДУБЛИРОВАНИЕ: Повторяющиеся проверки `_use_streaming`

**Проблема:**
Проверка `if self._use_streaming and self._sf_recognizer is not None` повторяется **8 раз** в коде:
- Строка 474: `_on_recording_start`
- Строка 507: `audio_callback`
- Строка 531: обработка ошибки в `_on_recording_start`
- Строка 539: обработка ошибки в `_on_recording_start`
- Строка 669: `_on_recording_stop`
- И другие...

**Решение:** Вынести в метод-хелпер или использовать локальную переменную.

---

### 3. ⚠️ КОНФЛИКТ: Двойная обработка аудио при fallback

**Проблема:**
```python
# В audio_callback
if self._use_streaming and self._sf_recognizer is not None:
    success = self._sf_recognizer.append_audio(data, sample_rate, channels)
    if success:
        # Отправлено в стриминг
    else:
        # ❌ ПРОБЛЕМА: Добавляется в буфер, но стриминг может быть активен
        self._audio_buffer.append(data)
```

**Сценарий:**
1. Стриминг активен, но `append_audio` возвращает `False` (например, из-за ошибки)
2. Аудио добавляется в `_audio_buffer`
3. При остановке:
   - Стриминг пытается завершиться (может быть частичный результат)
   - **И** batch обрабатывает накопленный буфер
4. **Результат:** Двойная обработка одного и того же аудио

**Решение:** При ошибке стриминга сразу отключать его и переходить на batch.

---

### 4. ⚠️ RACE CONDITION: Изменение `_use_streaming` во время записи

**Проблема:**
```python
# В _on_recording_start
if not streaming_started:
    self._use_streaming = False  # ❌ Может измениться во время записи

# В audio_callback (вызывается из другого потока)
if self._use_streaming:  # ❌ Может прочитать устаревшее значение
```

**Сценарий:**
1. Запись началась, `_use_streaming = True`
2. `audio_callback` начинает получать чанки
3. Стриминг падает, `_use_streaming = False`
4. `audio_callback` продолжает проверять старое значение (из-за замыкания или кеширования)

**Решение:** Использовать локальную переменную в замыкании `audio_callback`.

---

### 5. 🔄 ДУБЛИРОВАНИЕ: Очистка буфера в нескольких местах

**Проблема:**
Очистка `_audio_buffer` происходит в **3 местах**:
- Строка 468: `_on_recording_start` (перед началом)
- Строка 731: `_on_recording_stop` (после batch-распознавания)
- И потенциально в других местах при ошибках

**Решение:** Вынести в метод `_clear_audio_buffer()`.

---

### 6. ⚠️ КОНФЛИКТ: Проверка `_audio_buffer` после стриминга

**Проблема:**
```python
# В _on_recording_stop
if self._use_streaming and self._sf_recognizer is not None:
    # Обработка стриминга
    final_text = await self._sf_recognizer.finish_recognition()
    
elif self._audio_buffer:  # ❌ ПРОБЛЕМА: elif вместо отдельной проверки
    # Batch обработка
```

**Сценарий:**
1. Стриминг активен, но часть аудио попала в буфер (из-за ошибок)
2. Стриминг завершается, но буфер не пустой
3. **Проблема:** `elif` не выполнится, буфер не обработается

**Решение:** Проверять буфер отдельно, даже после стриминга.

---

## ✅ Рекомендации по исправлению

### 1. Исправить race condition в `audio_callback`

```python
# ✅ ПРАВИЛЬНО: Использовать локальную переменную в замыкании
async def _on_recording_start(self, event: Dict[str, Any]):
    # ...
    # Захватываем текущее состояние в локальную переменную
    use_streaming = self._use_streaming
    sf_recognizer = self._sf_recognizer
    
    async def audio_callback(data: bytes, sample_rate: int, channels: int):
        # Используем локальные переменные (не изменяются во время записи)
        if use_streaming and sf_recognizer is not None:
            success = sf_recognizer.append_audio(data, sample_rate, channels)
            if success:
                logger.debug(f"🎤 [SFSpeech] Отправлен чанк: {len(data)} bytes")
            else:
                # При ошибке стриминга - отключаем и переходим на batch
                logger.warning(f"⚠️ [SFSpeech] Ошибка отправки чанка, переходим на batch")
                # НЕ меняем use_streaming здесь (это локальная переменная)
                # Просто добавляем в буфер для batch
                self._audio_buffer.append(data)
        else:
            self._audio_buffer.append(data)
```

### 2. Вынести проверку в хелпер

```python
def _is_streaming_active(self) -> bool:
    """Проверка активности стриминга (thread-safe)"""
    return self._use_streaming and self._sf_recognizer is not None
```

### 3. Исправить двойную обработку

```python
# В audio_callback
if use_streaming and sf_recognizer is not None:
    success = sf_recognizer.append_audio(data, sample_rate, channels)
    if not success:
        # При ошибке - отключаем стриминг глобально
        logger.warning("⚠️ [SFSpeech] Ошибка стриминга, отключаем")
        self._use_streaming = False  # Отключаем для следующих чанков
        self._audio_buffer.append(data)  # Добавляем в буфер
    # Если success - не добавляем в буфер (уже отправлено)
else:
    self._audio_buffer.append(data)
```

### 4. Исправить проверку буфера после стриминга

```python
# В _on_recording_stop
if self._use_streaming and self._sf_recognizer is not None:
    # Обработка стриминга
    final_text = await self._sf_recognizer.finish_recognition()
    # ...

# ✅ ПРАВИЛЬНО: Проверяем буфер отдельно (может быть не пустым даже после стриминга)
if self._audio_buffer:
    # Batch обработка
    # ...
```

### 5. Вынести очистку в метод

```python
def _clear_audio_buffer(self):
    """Очистка аудио буфера"""
    self._audio_buffer = []
    self._audio_buffer_sample_rate = None
    self._audio_buffer_channels = None
```

---

## 📊 Итоговая таблица проблем

| # | Проблема | Тип | Критичность | Строки |
|---|----------|-----|-------------|--------|
| 1 | Race condition в `audio_callback` | Race condition | 🔴 Высокая | 507-518 |
| 2 | Дублирование проверок `_use_streaming` | Дублирование | 🟡 Средняя | 474, 507, 531, 539, 669 |
| 3 | Двойная обработка аудио | Конфликт | 🔴 Высокая | 507-514 |
| 4 | Изменение `_use_streaming` во время записи | Race condition | 🔴 Высокая | 496, 507 |
| 5 | Дублирование очистки буфера | Дублирование | 🟡 Средняя | 468, 731 |
| 6 | Проверка буфера после стриминга | Конфликт | 🟡 Средняя | 669, 706 |

---

## 🎯 Приоритет исправлений

1. **Критично:** Исправить race conditions (#1, #4)
2. **Важно:** Исправить двойную обработку (#3)
3. **Желательно:** Убрать дублирование (#2, #5, #6)



