# Решение: Гарантия полного воспроизведения речи

## 🎯 Проблема

1. **Преждевременный callback**: Completion callback срабатывает через 0.89s вместо 5.28s
2. **Прерывание конфигурационными событиями**: Конфигурационные события прерывают воспроизведение
3. **Неполное воспроизведение**: Речь воспроизводится не полностью

---

## ✅ Реализованные исправления

### 1. Игнорирование преждевременного callback

**Местоположение**: `modules/audio_avf/core/avf_audio_engine.py:188-205`

**Логика**:
- Отслеживаем время начала воспроизведения (`_playback_start_time`)
- Отслеживаем ожидаемую длительность (`_expected_playback_duration`)
- В callback проверяем: если прошло менее 80% от ожидаемой длительности → игнорируем callback

**Код**:
```python
if engine_instance._playback_start_time is not None and engine_instance._expected_playback_duration is not None:
    elapsed_time = current_time - engine_instance._playback_start_time
    expected_duration = engine_instance._expected_playback_duration
    min_acceptable_duration = expected_duration * 0.8  # Минимум 80%
    
    if elapsed_time < min_acceptable_duration:
        logger.warning(f"⚠️ [AVF] Completion callback сработал ПРЕЖДЕВРЕМЕННО: "
                      f"прошло {elapsed_time:.2f}s из {expected_duration:.2f}s. "
                      f"Игнорируем callback, полагаемся на fallback таймер.")
        return  # Не обрабатываем преждевременный callback
```

**Результат**: Преждевременный callback (0.89s) игнорируется, система полагается на fallback таймер (5.78s).

---

### 2. Защита от конфигурационных событий во время воспроизведения

**Местоположение**: `modules/audio_avf/core/avf_audio_engine.py:706-718`

**Логика**:
- Если состояние `STARTING` → пропускаем переподключение
- Если состояние `RUNNING` → пропускаем переподключение
- Только обновляем кэш имени устройства

**Код**:
```python
# Защита для STARTING
if current_state_under_lock == AudioState.STARTING:
    logger.debug("🔍 [AVF] Воспроизведение в состоянии STARTING, пропускаем переподключение")
    self._cached_output_device_name = new_device_name
    return

# Защита для RUNNING
if current_state_under_lock == AudioState.RUNNING:
    logger.debug("🔍 [AVF] Воспроизведение активно (RUNNING), пропускаем переподключение "
                 "(гарантируем полное воспроизведение)")
    self._cached_output_device_name = new_device_name
    return
```

**Результат**: Конфигурационные события не прерывают активное воспроизведение, гарантируя полное воспроизведение.

---

### 3. Отслеживание времени начала воспроизведения

**Местоположение**: `modules/audio_avf/core/avf_audio_engine.py:358-360, 1891-1895`

**Логика**:
- При инициализации: `_playback_start_time = None`, `_expected_playback_duration = None`
- В `play_audio()`: сохраняем время начала и ожидаемую длительность
- В callback: проверяем время и игнорируем преждевременные вызовы
- При остановке: очищаем время начала

**Код**:
```python
# В __init__
self._playback_start_time: Optional[float] = None
self._expected_playback_duration: Optional[float] = None

# В play_audio()
with self._lock:
    import time
    self._playback_start_time = time.time()
    duration_sec = len(audio_data) / (sample_rate * channels * 2)
    self._expected_playback_duration = duration_sec
```

**Результат**: Система знает, когда началось воспроизведение и сколько оно должно длиться.

---

## 🎯 Гарантии

### Полное воспроизведение гарантируется:

1. ✅ **Преждевременный callback игнорируется**:
   - Callback, сработавший менее чем через 80% от ожидаемой длительности, игнорируется
   - Система полагается на fallback таймер, который срабатывает в правильное время

2. ✅ **Конфигурационные события не прерывают воспроизведение**:
   - В состоянии `STARTING` → переподключение пропускается
   - В состоянии `RUNNING` → переподключение пропускается
   - Только реальные прерывания (Ctrl+N, ошибки) могут остановить воспроизведение

3. ✅ **Fallback таймер всегда публикует событие**:
   - Даже если callback не сработал или сработал преждевременно
   - Событие публикуется через `duration + 0.5` секунды
   - Это гарантирует, что система всегда получает событие завершения

---

## 🔍 Исключения (когда воспроизведение может быть прервано)

### Реальные прерывания (разрешены):

1. **Пользователь прервал через Ctrl+N**:
   - `interrupt.request` → `stop_output()` → воспроизведение останавливается
   - Это правильное поведение

2. **Ручная остановка через API**:
   - `stop_output()` вызывается явно
   - Воспроизведение останавливается корректно

3. **Ошибка воспроизведения**:
   - Если произошла ошибка, воспроизведение останавливается
   - Это правильное поведение для обработки ошибок

---

## 📊 Примеры работы

### Пример 1: Нормальное воспроизведение (5.28s)

```
t=0.00s: play_audio() → STARTING → RUNNING
t=0.00s: _playback_start_time = 0.00, _expected_playback_duration = 5.28s
t=0.89s: Callback срабатывает → ИГНОРИРУЕТСЯ (0.89s < 4.22s = 80% от 5.28s)
t=5.78s: Fallback таймер срабатывает → событие публикуется
Результат: ✅ Полное воспроизведение (5.28s)
```

### Пример 2: Конфигурационное событие во время воспроизведения

```
t=0.00s: play_audio() → RUNNING
t=0.50s: Конфигурационное событие → состояние RUNNING → переподключение ПРОПУЩЕНО
t=5.78s: Fallback таймер срабатывает → событие публикуется
Результат: ✅ Полное воспроизведение (5.28s), не прервано
```

### Пример 3: Прерывание через Ctrl+N

```
t=0.00s: play_audio() → RUNNING
t=2.00s: Пользователь нажимает Ctrl+N → interrupt.request
t=2.00s: stop_output() → воспроизведение останавливается
t=2.00s: Событие публикуется с информацией о прерывании
Результат: ✅ Прерывание обработано корректно (2.00s из 5.28s)
```

---

## ✅ Итоговый результат

**Теперь система гарантирует**:
- ✅ Полное воспроизведение речи без сокращений (если нет реальных прерываний)
- ✅ Преждевременный callback игнорируется
- ✅ Конфигурационные события не прерывают воспроизведение
- ✅ Fallback таймер всегда публикует событие в правильное время
- ✅ Реальные прерывания (Ctrl+N, ошибки) обрабатываются корректно

**Проблема решена**: Речь теперь воспроизводится полностью, без сокращений, за исключением случаев реального прерывания.
