# 🎤 Последовательность активации микрофона через Google Speech Recognition

## 📋 Обзор

Этот документ описывает правильную последовательность операций для активации микрофона через `speech_recognition.listen_in_background()` с гарантией вызова callback и отсутствием race conditions.

## ✅ Правильная последовательность операций

### ЭТАП 1: Инициализация состояния (ПЕРЕД настройкой recognizer)

**Порядок операций:**
1. Сохранение `recognizer` и `microphone` в `self._google_recognizer` и `self._google_microphone`
2. Инициализация `self._google_audio_chunks = []` (с блокировкой)
3. Очистка `self._google_chunk_event`
4. **КРИТИЧНО:** Сброс `self._google_stop_listening = None` перед запуском
5. Сохранение времени начала записи `self._recording_start_time`
6. **КРИТИЧНО:** Установка `self._google_recording_active = True` **ПЕРЕД** настройкой recognizer

**Почему это важно:**
- Флаг `_google_recording_active` должен быть установлен **ДО** запуска `listen_in_background`, чтобы callback мог обрабатывать аудио с самого начала
- Сброс `_google_stop_listening` гарантирует, что не будет конфликтов с предыдущими запусками

### ЭТАП 2: Настройка recognizer (ПОСЛЕ инициализации состояния, ПЕРЕД запуском listen_in_background)

**Порядок операций:**
1. **Шаг 2.1:** Настройка фонового шума через `adjust_for_ambient_noise(duration=0.3)`
2. **Шаг 2.1:** Установка `energy_threshold` (снижение на 50%, минимум 50, максимум 100)
3. **Шаг 2.2:** Установка `dynamic_energy_threshold = False` для стабильности
4. **Шаг 2.3:** Установка `pause_threshold = 0.3` для частых callback'ов
5. **Шаг 2.3:** Установка `non_speaking_duration = 0.1` для частых callback'ов
6. **КРИТИЧНО:** Проверка, что все настройки применены корректно

**Почему это важно:**
- Все настройки recognizer должны быть выполнены **ДО** запуска `listen_in_background`
- Это гарантирует, что callback будет вызываться с правильными параметрами
- Низкий `energy_threshold` гарантирует вызов callback даже при тихой речи
- `dynamic_energy_threshold = False` предотвращает изменение порога во время записи

### ЭТАП 3: Проверка готовности (ПЕРЕД запуском listen_in_background)

**Порядок операций:**
1. Логирование параметров микрофона (sample_rate, sample_width)
2. Проверка доступности микрофонов через `sr.Microphone.list_microphone_names()`
3. **КРИТИЧНО:** Проверка, что `_google_recording_active == True`
4. **КРИТИЧНО:** Проверка, что `_google_stop_listening is None` (сброс при необходимости)
5. Логирование финальной готовности

**Почему это важно:**
- Проверка доступности микрофонов предотвращает запуск при отсутствии устройств
- Проверка флагов гарантирует правильное состояние перед запуском

### ЭТАП 4: Запуск listen_in_background (ПОСЛЕ всех настроек и проверок)

**Порядок операций:**
1. **КРИТИЧНО:** Атомарная операция - запуск и сохранение `stop_listening`:
   ```python
   self._google_stop_listening = recognizer.listen_in_background(microphone, callback)
   ```
2. **КРИТИЧНО:** Проверка, что `_google_stop_listening is not None`
3. **КРИТИЧНО:** Проверка, что `_google_stop_listening` сохранен корректно
4. Логирование успешного запуска
5. **Обработка ошибок:** При ошибке сброс `_google_recording_active = False` и `_google_stop_listening = None`

**Почему это важно:**
- Атомарная операция предотвращает race conditions между запуском и сохранением `stop_listening`
- Проверка возвращаемого значения предотвращает продолжение при ошибке запуска
- Сброс флагов при ошибке гарантирует правильное состояние для следующей попытки

### ЭТАП 5: Публикация событий (ПОСЛЕ успешного запуска listen_in_background)

**Порядок операций:**
1. Установка состояния микрофона через `state_manager.set_microphone_state("active", ...)`
2. Публикация события `microphone.opened` через EventBus
3. **Обработка ошибок:** Не прерываем запись из-за ошибки публикации событий

**Почему это важно:**
- События публикуются только после успешного запуска, гарантируя правильное состояние для других компонентов
- Ошибка публикации не должна прерывать запись, так как она уже запущена

### ЭТАП 6: Финальная проверка состояния (ПОСЛЕ всех операций)

**Порядок операций:**
1. Проверка всех компонентов:
   - `_google_recording_active == True`
   - `_google_stop_listening is not None`
   - `_google_recognizer is not None`
   - `_google_microphone is not None`
2. Логирование финального состояния
3. **КРИТИЧНО:** Выброс исключения при неудачной проверке

**Почему это важно:**
- Финальная проверка гарантирует, что все компоненты в правильном состоянии
- Выброс исключения предотвращает продолжение при неправильном состоянии

## 🔒 Защита от race conditions

### Проблемы, которые решены:

1. **Race condition между установкой флагов и вызовом callback:**
   - Решение: `_google_recording_active` устанавливается **ДО** запуска `listen_in_background`
   - Callback проверяет флаг перед обработкой аудио

2. **Race condition между запуском и сохранением stop_listening:**
   - Решение: Атомарная операция - запуск и сохранение в одной строке
   - Проверка сохранения сразу после запуска

3. **Race condition между настройкой recognizer и запуском:**
   - Решение: Все настройки выполняются **ДО** запуска `listen_in_background`
   - Финальная проверка настроек перед запуском

## 📊 Критические параметры для гарантированного вызова callback

### energy_threshold
- **Значение:** Максимум 100 (50% от исходного, минимум 50)
- **Почему:** Низкий порог гарантирует вызов callback даже при тихой речи

### dynamic_energy_threshold
- **Значение:** `False`
- **Почему:** Предотвращает изменение порога во время записи, обеспечивая стабильность

### pause_threshold
- **Значение:** `0.3`
- **Почему:** Минимальное значение для частых callback'ов

### non_speaking_duration
- **Значение:** `0.1`
- **Почему:** Минимальное значение для частых callback'ов
- **Важно:** Должен быть <= `pause_threshold` (иначе `AssertionError` в speech_recognition)

## 🐛 Диагностика проблем

### Callback не вызывается

**Проверьте:**
1. Логи: есть ли "🔍 [Google] CALLBACK ВЫЗВАН!"?
2. `energy_threshold`: должен быть <= 100
3. `_google_recording_active`: должен быть `True`
4. `_google_stop_listening`: должен быть не `None`
5. Разрешения микрофона: проверьте System Settings → Privacy & Security → Microphone
6. Доступность микрофонов: проверьте логи "Доступные микрофоны"

### Callback вызывается, но аудио не накапливается

**Проверьте:**
1. Логи: есть ли "🔍 [Google] Получен чанк аудио"?
2. `_google_audio_chunks`: должен содержать элементы
3. `_google_chunk_event`: должен быть установлен
4. Блокировка `_google_audio_chunks_lock`: не должно быть deadlock

### Запись не запускается

**Проверьте:**
1. Логи: есть ли "❌ [Google] КРИТИЧЕСКАЯ ОШИБКА"?
2. Финальная проверка: все компоненты должны быть инициализированы
3. Разрешения микрофона: должны быть предоставлены
4. Доступность микрофонов: должно быть хотя бы одно устройство

## 📝 Чек-лист для проверки последовательности

- [ ] ЭТАП 1: Инициализация состояния выполнена **ДО** настройки recognizer
- [ ] ЭТАП 1: `_google_recording_active = True` установлен **ПЕРЕД** настройкой recognizer
- [ ] ЭТАП 2: Все настройки recognizer выполнены **ДО** запуска `listen_in_background`
- [ ] ЭТАП 2: `energy_threshold` установлен корректно (<= 100)
- [ ] ЭТАП 2: `dynamic_energy_threshold = False` установлен
- [ ] ЭТАП 2: `pause_threshold` и `non_speaking_duration` установлены корректно
- [ ] ЭТАП 3: Проверка готовности выполнена **ПЕРЕД** запуском
- [ ] ЭТАП 4: `listen_in_background` запущен **ПОСЛЕ** всех настроек
- [ ] ЭТАП 4: `_google_stop_listening` сохранен атомарно
- [ ] ЭТАП 5: События опубликованы **ПОСЛЕ** успешного запуска
- [ ] ЭТАП 6: Финальная проверка пройдена

## 🔗 Связанные документы

- `integration/integrations/voice_recognition_integration.py` - реализация последовательности
- `Docs/CHUNK_MERGING_LOGIC.md` - логика объединения чанков аудио
- `tests/test_google_callback_activation.py` - тесты последовательности


