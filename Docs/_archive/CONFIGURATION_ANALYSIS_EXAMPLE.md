# 📚 Пример анализа конфигураций: Проблема с определением устройства по умолчанию в аудиосистеме

**ЦЕЛЬ**: Показать, как использование `CONFIGURATION_ANALYSIS_CHECKLIST.md` предотвращает проблемы, подобные проблеме с определением устройства по умолчанию в аудиосистеме.

**Дата создания**: 2025-01-XX  
**Версия**: 1.0

---

## 🐛 Описание проблемы

**Симптом**: При внесении изменений в аудиосистему устройство по умолчанию не определялось и не переключалось корректно.

**Причина**: При планировании изменений не были учтены все конфигурации, влияющие на логику выбора устройств:
- `audio_system.device_mode` (определяет режим работы: "default" | "explicit" | "auto")
- `audio_system.default_device.*` (настройки default режима)
- `audio_system.fallback.*` (настройки fallback логики)
- Зависимости между этими параметрами

**Последствия**: Изменения сломали логику выбора устройств, что привело к проблемам с определением и переключением устройств.

---

## ✅ Как CONFIGURATION_ANALYSIS_CHECKLIST.md предотвращает эту проблему

### Этап 1: Идентификация затронутых конфигураций

**Что нужно было сделать**:

```markdown
### 1.1 Определение области конфигураций
- [x] **Определен домен**: audio
- [x] **Определены разделы unified_config.yaml**:
  - audio_system.device_mode
  - audio_system.default_device.*
  - audio_system.fallback.*
  - audio_system.sample_rates.*
  - audio_system.channels.*
  - audio_system.bluetooth.*
  - default_audio.normalization.*
- [x] **Определены схемы**: audio_system_hybrid_schema.yaml
- [x] **Определены модули**:
  - modules/audio_system/core/device_manager.py
  - modules/audio_system/core/hybrid_stream_manager.py
  - modules/speech_playback/core/player.py
  - modules/voice_recognition/core/speech_recognizer.py
```

**Результат**: Все конфигурации, влияющие на логику выбора устройств, были бы идентифицированы до начала изменений.

---

### Этап 2: Детальный анализ конфигураций

**Что нужно было сделать**:

```markdown
### 2.1 Анализ структуры конфигурации
- [x] **Иерархия параметров**: Понятна иерархия device_mode → default_device → fallback
- [x] **Зависимости между параметрами**:
  - device_mode="auto" → требует default_device.enabled=true и fallback.enabled=true
  - default_device.enabled=true → требует max_failures_before_fallback
  - fallback.enabled=true → требует auto_return_to_default и success_count_for_return
- [x] **Условная активация**: Параметры активируются только при определенных значениях device_mode

### 2.2 Анализ значений и диапазонов
- [x] **Допустимые значения**:
  - device_mode: "default" | "explicit" | "auto" (enum)
  - default_device.max_failures_before_fallback: 1-10 (integer, default: 3)
  - fallback.success_count_for_return: integer (default: 5)
- [x] **Fallback значения**: При некорректных параметрах используются значения по умолчанию

### 2.3 Анализ логики использования
- [x] **Где читается**:
  - device_mode: device_manager.py:__init__(), hybrid_stream_manager.py:_should_use_default()
  - default_device.*: hybrid_stream_manager.py:_should_use_default()
  - fallback.*: hybrid_stream_manager.py:_handle_fallback()
- [x] **Как используется**:
  - device_mode определяет режим работы с устройствами
  - default_device определяет поведение в default режиме
  - fallback определяет логику перехода на explicit режим при ошибках
- [x] **Когда применяется**: При создании аудио потока, при переключении устройства, при ошибках
- [x] **Порядок применения**:
  1. Проверка device_mode
  2. Если "auto" → проверка default_failures
  3. Если default_failures >= max_failures → переход на explicit
```

**Результат**: Логика использования всех параметров была бы понятна до начала изменений, что предотвратило бы поломку логики выбора устройств.

---

### Этап 3: Анализ Feature Flags и Kill-Switches

**Что нужно было сделать**:

```markdown
### 3.1 Идентификация флагов
- [x] **Связанные флаги**:
  - audio.avf.enabled (AVFoundation аудиосистема)
  - audio.avf.rollout_percentage (постепенный rollout)
- [x] **Kill-switches**:
  - audio.ks_avf.enabled (мгновенный откат AVFoundation)
  - NEXY_DISABLE_AVF_AUDIO (env переменная)

### 3.2 Анализ логики флагов
- [x] **Условия активации**: AVFoundation активируется только если все проверки пройдены
- [x] **Приоритеты**: kill-switch > env > feature flag > rollout
- [x] **Rollout стратегия**: 1% → 25% → 100%

### 3.3 Анализ влияния на изменения
- [x] **Блокировка изменений**: Kill-switch может заблокировать изменения в AVFoundation
- [x] **Активация изменений**: Нужны ли новые флаги для безопасного rollout?
```

**Результат**: Все флаги, которые могут повлиять на изменения, были бы учтены.

---

### Этап 4: Анализ зависимостей между конфигурациями

**Что нужно было сделать**:

```markdown
### 4.1 Зависимости внутри домена
- [x] **Прямые зависимости**:
  - device_mode="auto" → требует default_device.enabled=true
  - device_mode="auto" → требует fallback.enabled=true
  - default_device.enabled=true → требует max_failures_before_fallback
  - fallback.enabled=true → требует auto_return_to_default
- [x] **Группы параметров**:
  - Группа 1: device_mode + default_device.* (режим работы с устройствами)
  - Группа 2: fallback.* (логика fallback)
  - Группа 3: sample_rates.* + channels.* (форматы устройств)

### 4.2 Зависимости между доменами
- [x] **Междоменные зависимости**:
  - audio_system.device_mode зависит от permissions.mic (если denied → не может работать)
  - audio.avf.enabled зависит от permissions.mic (AVFoundation требует разрешения)

### 4.3 Анализ рисков зависимостей
- [x] **Риск поломки**: Изменение device_mode может сломать логику выбора устройств
- [x] **Риск рассинхронизации**: Изменение одного параметра может привести к рассинхронизации других
```

**Результат**: Все зависимости между параметрами были бы понятны, что предотвратило бы поломку логики выбора устройств.

---

### Этап 5: Анализ влияния изменений на конфигурации

**Что нужно было сделать**:

```markdown
### 5.1 Новые параметры
- [ ] **Необходимость**: Нужны ли новые параметры для реализации изменений?
- [ ] **Место в иерархии**: Где в иерархии unified_config.yaml должны быть новые параметры?
- [ ] **Значения по умолчанию**: Какие значения по умолчанию должны быть у новых параметров?

### 5.2 Изменение существующих параметров
- [x] **Обратная совместимость**: Сохранится ли обратная совместимость при изменении параметров?
- [x] **Миграция**: Нужна ли миграция существующих значений параметров?
- [x] **Deprecation**: Нужно ли пометить старые параметры как deprecated?

### 5.3 Удаление параметров
- [x] **Необходимость**: Можно ли безопасно удалить параметр?
- [x] **Зависимости**: Есть ли код, который зависит от удаляемого параметра?
```

**Результат**: Влияние изменений на существующие конфигурации было бы оценено до начала изменений.

---

## 📊 Таблица анализа конфигураций (пример заполнения)

| Параметр | Раздел unified_config.yaml | Схема | Модули (чтение) | Модули (запись) | Значения по умолчанию | Зависимости | Риски изменений |
|----------|---------------------------|-------|-----------------|-----------------|----------------------|-------------|-----------------|
| `device_mode` | `audio_system.device_mode` | `audio_system_hybrid_schema.yaml` | `device_manager.py`, `hybrid_stream_manager.py` | Нет (только чтение) | `"auto"` | `default_device.*`, `fallback.*` | Изменение может сломать логику выбора устройств |
| `default_device.enabled` | `audio_system.default_device.enabled` | `audio_system_hybrid_schema.yaml` | `hybrid_stream_manager.py` | Нет (только чтение) | `true` | `device_mode="auto"` или `"default"` | Отключение может сломать fallback логику |
| `default_device.max_failures_before_fallback` | `audio_system.default_device.max_failures_before_fallback` | `audio_system_hybrid_schema.yaml` | `hybrid_stream_manager.py` | Нет (только чтение) | `3` | `default_device.enabled=true` | Изменение может привести к частым переключениям |
| `fallback.enabled` | `audio_system.fallback.enabled` | `audio_system_hybrid_schema.yaml` | `hybrid_stream_manager.py` | Нет (только чтение) | `true` | `device_mode="auto"` | Отключение может сломать автоматическое восстановление |
| `fallback.auto_return_to_default` | `audio_system.fallback.auto_return_to_default` | `audio_system_hybrid_schema.yaml` | `hybrid_stream_manager.py` | Нет (только чтение) | `true` | `fallback.enabled=true` | Отключение может привести к постоянному использованию explicit режима |
| `fallback.success_count_for_return` | `audio_system.fallback.success_count_for_return` | `audio_system_hybrid_schema.yaml` | `hybrid_stream_manager.py` | Нет (только чтение) | `5` | `fallback.auto_return_to_default=true` | Изменение может привести к слишком частым или редким переключениям |

---

## ✅ Выводы

**Что предотвратил бы CONFIGURATION_ANALYSIS_CHECKLIST.md**:

1. **Идентификация всех конфигураций**: Все параметры, влияющие на логику выбора устройств, были бы идентифицированы до начала изменений.

2. **Понимание зависимостей**: Зависимости между параметрами (device_mode → default_device → fallback) были бы понятны, что предотвратило бы поломку логики.

3. **Оценка рисков**: Риски изменений (например, изменение device_mode может сломать логику выбора устройств) были бы оценены до начала изменений.

4. **План миграции**: План миграции существующих значений параметров был бы определен до начала изменений.

**Результат**: Использование `CONFIGURATION_ANALYSIS_CHECKLIST.md` предотвратило бы проблему с определением устройства по умолчанию, так как все конфигурации, влияющие на логику выбора устройств, были бы учтены до начала изменений.

---

## 🔗 Связанные документы

- `CONFIGURATION_ANALYSIS_CHECKLIST.md` — обязательный чек-лист анализа конфигураций
- `PRE_CHANGE_PLANNING_CHECKLIST.md` — чек-лист планирования изменений
- `config/unified_config.yaml` — единый источник истины для конфигураций
- `config/schemas/audio_system_hybrid_schema.yaml` — схема валидации конфигураций аудиосистемы
- `Docs/FEATURE_FLAGS.md` — реестр feature flags и kill-switches

---

**Версия**: 1.0  
**Дата создания**: 2025-01-XX



