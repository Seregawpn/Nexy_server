# Реализация новой логики: AVF диагностика → Google запись

## 🎯 Цель

Реализовать логику, где:
1. **AVF активирует микрофон на ~1 секунду** → получает информацию об устройстве
2. **AVF дезактивирует микрофон** ✅
3. **Google активирует микрофон** через `sr.Microphone()` → записывает речь
4. **Google распознаёт** через `recognize_google()`

---

## ✅ Реализовано

### 1. Изменён `initialize()`

**Было:**
```python
if self._use_avf:
    self._recognizer = None  # Не создаём SpeechRecognizer
```

**Стало:**
```python
if self._use_avf:
    # Создаём SpeechRecognizer для Google записи
    self._recognizer = SpeechRecognizer(DEFAULT_RECOGNITION_CONFIG)
```

**Результат:**
- ✅ `SpeechRecognizer` создаётся даже при `_use_avf = True`
- ✅ AVF будет использоваться только для диагностики
- ✅ Google будет использоваться для записи

---

### 2. Создан `_get_device_info_via_avf()`

**Новый метод:**
```python
async def _get_device_info_via_avf(self) -> Optional[Dict[str, Any]]:
    """
    Получает информацию об устройстве через AVF (активация на ~1 секунду).
    
    Returns:
        Dict с информацией: device_info, input_format, diagnostics
    """
    # Активируем AVF на ~1 секунду
    success = await self._avf_engine.start_input()
    await asyncio.sleep(1.0)
    result = await self._avf_engine.stop_input()
    
    # Формируем информацию об устройстве
    device_info = {...}
    return device_info
```

**Результат:**
- ✅ AVF активирует на ~1 секунду
- ✅ Получает информацию об устройстве
- ✅ Дезактивирует AVF
- ✅ Возвращает информацию для использования

---

### 3. Изменён `_on_recording_start()`

**Новая логика:**
```python
# ✅ ШАГ 1: Получаем диагностику через AVF (активация на ~1 секунду)
device_info = await self._get_device_info_via_avf()
if device_info:
    self._avf_device_info = device_info

# ✅ ШАГ 2: Пауза 0.2 сек для гарантии деактивации AVF
await asyncio.sleep(0.2)

# ✅ ШАГ 3: Активируем Google через speech_recognition
import speech_recognition as sr
recognizer = sr.Recognizer()
microphone = sr.Microphone()

# Запускаем запись в отдельном потоке
def start_listening():
    with microphone:
        audio_data = recognizer.listen(microphone, phrase_time_limit=30.0)
        self._google_audio_data = audio_data

listening_thread = threading.Thread(target=start_listening, daemon=True)
listening_thread.start()
```

**Результат:**
- ✅ AVF диагностика получается первой
- ✅ AVF дезактивируется
- ✅ Google активируется для записи
- ✅ Запись происходит в отдельном потоке

---

### 4. Изменён `_on_recording_stop()`

**Новая логика:**
```python
# ✅ Останавливаем Google запись
if self._google_listening_thread and self._google_listening_thread.is_alive():
    # Ждём завершения записи
    self._google_listening_event.wait(timeout=2.0)
    self._google_listening_thread.join(timeout=3.0)

# Получаем записанные данные
if self._google_audio_data:
    await self._recognize_google_audio(self._google_audio_data, session_id)
```

**Результат:**
- ✅ Ожидаем завершения записи Google
- ✅ Получаем `AudioData` объект
- ✅ Распознаём через Google

---

### 5. Создан `_recognize_google_audio()`

**Новый метод:**
```python
async def _recognize_google_audio(self, audio_data: sr.AudioData, session_id: str):
    """Распознавание аудио через Google Speech API"""
    recognizer = self._google_recognizer or sr.Recognizer()
    text = recognizer.recognize_google(audio_data, language=language)
    
    # Публикуем результат с диагностикой от AVF
    await self.event_bus.publish("voice.recognition_completed", {
        "session_id": session_id,
        "text": text,
        "device_info": self._avf_device_info  # Диагностика от AVF
    })
```

**Результат:**
- ✅ Распознавание через Google Speech API
- ✅ Результат публикуется с диагностикой от AVF

---

## ⚠️ Известные проблемы

### 1. Проблема с остановкой `recognizer.listen()`

**Проблема:**
- `recognizer.listen()` блокирующий и его нельзя безопасно прервать
- Если пользователь отпустил клавишу раньше, мы не можем остановить запись

**Текущее решение:**
- Используем `phrase_time_limit=30.0` для ограничения длительности
- Ждём завершения записи (до 5 секунд)
- Если запись не завершилась, используем то, что есть

**Возможные улучшения:**
- Использовать `listen_in_background()` если доступен
- Использовать периодические вызовы `listen()` с коротким `phrase_time_limit`

---

### 2. Двойная активация микрофона

**Проблема:**
- AVF активирует на ~1 секунду
- Google активирует для записи
- Может вызвать два диалога разрешений (если разрешение ещё не дано)

**Текущее решение:**
- AVF активирует только на ~1 секунду (минимальное время)
- Google активирует сразу после (разрешение уже запрошено)

**Ожидаемое поведение:**
- Первый диалог при активации AVF
- Второй диалог не должен появляться (разрешение уже дано)

---

### 3. Задержка перед записью

**Проблема:**
- ~1.2 секунды задержка (1 сек AVF + 0.2 сек пауза)
- Пользователь может начать говорить до активации Google

**Текущее решение:**
- Логирование для пользователя: "Получение информации об устройстве..."
- Минимальная задержка для гарантии деактивации AVF

**Возможные улучшения:**
- Уменьшить время AVF диагностики (например, до 0.5 секунды)
- Уменьшить паузу между AVF и Google (например, до 0.1 секунды)

---

## 📊 Итоговая схема

```
1. voice.recording_start
   │
   ├─► _get_device_info_via_avf()
   │   ├─ AVF активирует (~1 сек)
   │   ├─ Получает: device_info, format, diagnostics
   │   └─ AVF дезактивирует ✅
   │
   ├─► Пауза 0.2 сек
   │
   └─► Google активирует через sr.Microphone()
       └─ recognizer.listen() в отдельном потоке

2. Пользователь говорит
   │
   └─► Google записывает через recognizer.listen()

3. voice.recording_stop
   │
   └─► Ждём завершения записи Google
       ├─ Получаем AudioData
       └─ _recognize_google_audio()
           ├─ recognizer.recognize_google()
           └─ Публикуем результат
```

---

## 🧪 Тестирование

### Что нужно протестировать:

1. **AVF диагностика:**
   - ✅ Получается ли информация об устройстве?
   - ✅ Корректны ли формат и диагностика?

2. **Google активация:**
   - ✅ Активируется ли микрофон через Google?
   - ✅ Нет ли конфликтов с AVF?

3. **Google запись:**
   - ✅ Записывается ли речь корректно?
   - ✅ Получаются ли данные после остановки?

4. **Google распознавание:**
   - ✅ Распознаётся ли речь корректно?
   - ✅ Публикуется ли результат с диагностикой от AVF?

---

## 📝 Следующие шаги

1. ⏳ Протестировать новую логику с реальным микрофоном
2. ⏳ Проверить работу с Bluetooth устройствами
3. ⏳ Оптимизировать задержку перед записью
4. ⏳ Удалить старый код (эстафета, защита от дубликатов) после тестирования

---

## 🎯 Ожидаемый результат

1. ✅ AVF получает информацию об устройстве (формат, имя, диагностика)
2. ✅ Google записывает речь напрямую (корректное распознавание)
3. ✅ Информация об устройстве доступна для использования
4. ✅ Нет проблем с некорректным распознаванием данных от AVF

