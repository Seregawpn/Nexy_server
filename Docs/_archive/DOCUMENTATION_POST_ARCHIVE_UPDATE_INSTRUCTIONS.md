# Детальная тех-инструкция: Обновление документации после архивации

**Дата создания**: 2025-01-15  
**Цель**: Убрать все ссылки на архивные документы и обеспечить консистентность документации без "битых" ссылок.

---

## 📋 Общий чек-лист

- [ ] Обновлены ссылки в `client/.cursorrules`
- [ ] Обновлены ссылки в `server/server/.cursorrules`
- [ ] Обновлен `client/Docs/DOCUMENTATION_MAP.md`
- [ ] Обновлен `server/server/Docs/ARCHITECTURE_OVERVIEW.md`
- [ ] Проверены все ссылки на существующие файлы
- [ ] Удалены ссылки на архивные документы (кроме явно помеченных секций "Archived")

---

## 1. Обновление ссылок в `.cursorrules` (клиент)

### 1.1 Файл: `client/.cursorrules`

**Цель**: Удалить/заменить ссылки на несуществующие файлы.

#### 1.1.1 Удалить ссылки на `Docs/NEXY_FIRST_RUN_LOG_EXPECTED.md`

**Проблема**: Файл не существует, но упоминается в `.cursorrules`.

**Местоположение ссылок**:
- Строка 136: раздел 8.1 "Shadow-mode и Expected Logs"
- Строка 197: раздел 10.1 "Shadow-mode и Expected Logs"

**Действие**: Удалить упоминания файла или заменить на актуальный источник.

**Конкретные изменения**:

**Строка 136** (раздел 8.1):
```markdown
# БЫЛО:
**Спецификация логов**: См. `Docs/NEXY_FIRST_RUN_LOG_EXPECTED.md` — точная последовательность логов для правильной работы процесса первого запуска.

# ДОЛЖНО БЫТЬ:
**Спецификация логов**: Логи первого запуска проверяются через `tests/test_golden_first_run_logs.py` и соответствуют спецификации в `Docs/first_run_flow_spec.md`.
```

**Строка 197** (раздел 10.1):
```markdown
# БЫЛО:
**Спецификация логов**: `Docs/NEXY_FIRST_RUN_LOG_EXPECTED.md` — канонический чек-лист для проверки правильности работы процесса первого запуска.

**Проверка логов**: Используй проверочный чек-лист из `NEXY_FIRST_RUN_LOG_EXPECTED.md` для валидации последовательности логов перед мерджем.

# ДОЛЖНО БЫТЬ:
**Спецификация логов**: Логи первого запуска проверяются через `tests/test_golden_first_run_logs.py` и соответствуют спецификации в `Docs/first_run_flow_spec.md`.

**Проверка логов**: Используй `tests/test_golden_first_run_logs.py` для валидации последовательности логов перед мерджем.
```

**Строка 139** (чек-лист в разделе 8.1):
```markdown
# БЫЛО:
- [ ] Логи сверены со спецификацией из `NEXY_FIRST_RUN_LOG_EXPECTED.md`

# ДОЛЖНО БЫТЬ:
- [ ] Логи сверены со спецификацией через `tests/test_golden_first_run_logs.py`
```

#### 1.1.2 Удалить ссылки на `Docs/PERMISSION_RESTART_BLOCKERS.md`

**Проблема**: Файл не существует, но упоминается в `.cursorrules`.

**Местоположение ссылок**:
- Строка 552: раздел 14.2 "Нормирование FirstRunPermissions/PermissionRestart"

**Действие**: Удалить ссылку на несуществующий файл.

**Конкретные изменения**:

**Строка 552**:
```markdown
# БЫЛО:
- Все блокировки и отсрочки описаны в `Docs/PERMISSION_RESTART_BLOCKERS.md` и отражены в `interaction_matrix.yaml`

# ДОЛЖНО БЫТЬ:
- Все блокировки и отсрочки отражены в `config/interaction_matrix.yaml` с приоритетами (hard_stop/graceful)
```

#### 1.1.3 Удалить ссылки на `RELEASE_INTEGRITY_PLAN.md`

**Проблема**: Файл не существует, но упоминается в разделах 11.3-11.9.

**Местоположение ссылок**:
- Строка 276: раздел 11.3 "Процесс обновления требований (RELEASE_INTEGRITY_PLAN этап 1)"
- Строка 307: раздел 11.4 "Pre-build Gate (RELEASE_INTEGRITY_PLAN этап 2)"
- Строка 339: раздел 11.5 "Release Suite (RELEASE_INTEGRITY_PLAN этап 3)"
- Строка 374: раздел 11.6 "Prepare Release (RELEASE_INTEGRITY_PLAN этап 4)"
- Строка 408: раздел 11.7 "Validate Release Bundle"
- Строка 438: раздел 11.8 "Continuous Verification (RELEASE_INTEGRITY_PLAN этап 5)"
- Строка 467: раздел 11.9 "Continuous Monitoring (RELEASE_INTEGRITY_PLAN этап 8)"

**Действие**: Убрать упоминания `RELEASE_INTEGRITY_PLAN.md` из ссылок, оставить только название процесса в заголовках.

**Конкретные изменения**:

**Строка 372** (раздел 11.5):
```markdown
# БЫЛО:
- `RELEASE_INTEGRITY_PLAN.md` — полный план целостности релиза

# ДОЛЖНО БЫТЬ:
- Процесс Release Suite описан в разделе 11.5 данного документа
```

**Строка 406** (раздел 11.6):
```markdown
# БЫЛО:
- `RELEASE_INTEGRITY_PLAN.md` — полный план целостности релиза

# ДОЛЖНО БЫТЬ:
- Процесс Prepare Release описан в разделе 11.6 данного документа
```

**Строка 436** (раздел 11.7):
```markdown
# БЫЛО:
- `RELEASE_INTEGRITY_PLAN.md` — полный план целостности релиза

# ДОЛЖНО БЫТЬ:
- Процесс Validate Release Bundle описан в разделе 11.7 данного документа
```

**Строка 465** (раздел 11.8):
```markdown
# БЫЛО:
- `RELEASE_INTEGRITY_PLAN.md` — полный план целостности релиза

# ДОЛЖНО БЫТЬ:
- Процесс Continuous Verification описан в разделе 11.8 данного документа
```

**Строка 499** (раздел 11.9):
```markdown
# БЫЛО:
- `RELEASE_INTEGRITY_PLAN.md` — полный план целостности релиза

# ДОЛЖНО БЫТЬ:
- Процесс Continuous Monitoring описан в разделе 11.9 данного документа
```

**Примечание**: Название процесса "RELEASE_INTEGRITY_PLAN" в заголовках разделов можно оставить, так как это просто название процесса, а не ссылка на файл.

---

## 2. Обновление ссылок в `.cursorrules` (сервер)

### 2.1 Файл: `server/server/.cursorrules`

**Цель**: Убедиться, что ссылки на архивные документы удалены/заменены.

**Проверка**: В текущей версии файла нет прямых ссылок на `STREAMING_WORKFLOW_FIX_IMPLEMENTATION_*` документы.

**Действие**: Проверить, что все ссылки ведут на существующие файлы.

**Команда для проверки**:
```bash
cd server/server
rg -n "STREAMING_WORKFLOW_FIX_IMPLEMENTATION|_archive" .cursorrules
```

**Ожидаемый результат**: Нет упоминаний архивных документов.

---

## 3. Обновление навигационных документов (клиент)

### 3.1 Файл: `client/Docs/DOCUMENTATION_MAP.md`

**Цель**: Убедиться, что все ссылки на архивные документы удалены или перенесены в секцию "Archived".

**Текущее состояние**: Файл уже содержит комментарии о переносе в архив:
- Строка 17: `<!-- CODE_REVIEW_ASSISTANT_PROMPT.md перенесён в _archive -->`
- Строка 41: `<!-- PERMISSION_RESTART_KEY_POINTS.md и PERMISSION_RESTART_BLOCKERS.md перенесены в _archive -->`

**Действие**: Проверить, что все упоминания архивных документов либо удалены, либо перенесены в секцию "Archived" (строки 165-179).

**Проверка**:
```bash
cd client/Docs
rg -n "PERMISSION_RESTART_BLOCKERS|NEXY_FIRST_RUN_LOG_EXPECTED|RELEASE_INTEGRITY_PLAN" DOCUMENTATION_MAP.md
```

**Ожидаемый результат**: Нет упоминаний этих файлов (кроме секции "Archived", если они там указаны).

**Дополнительная проверка**: Убедиться, что все файлы, упомянутые в таблицах, действительно существуют:
```bash
cd client/Docs
# Извлечь все упоминания файлов из DOCUMENTATION_MAP.md
rg -o '`[^`]+\.md`' DOCUMENTATION_MAP.md | sed 's/`//g' | sort -u | while read file; do
  if [[ ! -f "$file" ]] && [[ ! -f "Docs/$file" ]]; then
    echo "⚠️  Файл не найден: $file"
  fi
done
```

---

## 4. Обновление навигационных документов (сервер)

### 4.1 Файл: `server/server/Docs/ARCHITECTURE_OVERVIEW.md`

**Цель**: Убедиться, что сообщение про архив присутствует и все ссылки актуальны.

**Текущее состояние**: Файл уже содержит сообщение про архив (строка 5):
```markdown
Исторические планы и отчеты перенесены в `server/Docs/_archive` и не используются как источник истины.
```

**Действие**: Проверить, что все ссылки в документе ведут на существующие файлы.

**Проверка**:
```bash
cd server/server/Docs
rg -o '`[^`]+\.md`' ARCHITECTURE_OVERVIEW.md | sed 's/`//g' | sort -u | while read file; do
  if [[ ! -f "$file" ]] && [[ ! -f "Docs/$file" ]]; then
    echo "⚠️  Файл не найден: $file"
  fi
done
```

**Опционально**: Создать индекс канонов в `server/server/README.md` (если файл существует):
```markdown
## Каноны документации

- `Docs/ARCHITECTURE_OVERVIEW.md` — архитектурный обзор
- `Docs/SERVER_DEVELOPMENT_RULES.md` — правила разработки
- `Docs/GRPC_PROTOCOL_AUDIT.md` — аудит gRPC протокола
- `Docs/BACKPRESSURE_README.md` — документация по backpressure
- `Docs/LLM_RESPONSE_FORMAT.md` — формат ответов LLM
- `Docs/MCP_COMMAND_INTEGRATION_PLAN.md` — план интеграции MCP команд
```

---

## 5. Проверка "битых" ссылок

### 5.1 Автоматическая проверка всех ссылок

**Скрипт для проверки**:
```bash
#!/bin/bash
# scripts/check_documentation_links.sh

echo "🔍 Проверка ссылок в client/.cursorrules..."
cd client
rg -o '`[^`]+\.md`' .cursorrules | sed 's/`//g' | sort -u | while read file; do
  if [[ "$file" == Docs/* ]]; then
    if [[ ! -f "$file" ]]; then
      echo "❌ Файл не найден: $file"
    fi
  fi
done

echo "🔍 Проверка ссылок в server/server/.cursorrules..."
cd ../server/server
rg -o '`[^`]+\.md`' .cursorrules | sed 's/`//g' | sort -u | while read file; do
  if [[ "$file" == Docs/* ]]; then
    if [[ ! -f "$file" ]]; then
      echo "❌ Файл не найден: $file"
    fi
  fi
done

echo "✅ Проверка завершена"
```

**Запуск**:
```bash
chmod +x scripts/check_documentation_links.sh
./scripts/check_documentation_links.sh
```

### 5.2 Ручная проверка ключевых файлов

**Файлы для проверки**:
1. `client/.cursorrules` — все ссылки на `Docs/*.md`
2. `client/Docs/DOCUMENTATION_MAP.md` — все упоминания файлов
3. `server/server/.cursorrules` — все ссылки на `Docs/*.md`
4. `server/server/Docs/ARCHITECTURE_OVERVIEW.md` — все ссылки на документы

**Команда для каждой проверки**:
```bash
# Для client/.cursorrules
cd client
rg -n "Docs/[^`]+\.md" .cursorrules

# Для client/Docs/DOCUMENTATION_MAP.md
cd client/Docs
rg -n "`[^`]+\.md`" DOCUMENTATION_MAP.md

# Для server/server/.cursorrules
cd server/server
rg -n "Docs/[^`]+\.md" .cursorrules

# Для server/server/Docs/ARCHITECTURE_OVERVIEW.md
cd server/server/Docs
rg -n "`[^`]+\.md`" ARCHITECTURE_OVERVIEW.md
```

---

## 6. Финализация архивов

### 6.1 Правила работы с архивами

**Важно**: 
- В `client/Docs/_archive` и `server/server/Docs/_archive` ничего не править — это история.
- В активных документах не должно быть ссылок на `_archive`, кроме явно помеченной секции "Archived".

### 6.2 Проверка ссылок на архивы

**Команда**:
```bash
# Проверка ссылок на _archive в активных документах
cd /Users/sergiyzasorin/Fix_new

# Исключаем сами архивы из поиска
rg -n "_archive" \
  --glob '!**/_archive/**' \
  --glob '!**/DOCUMENTATION_POST_ARCHIVE_UPDATE_INSTRUCTIONS.md' \
  client/.cursorrules \
  client/Docs/DOCUMENTATION_MAP.md \
  server/server/.cursorrules \
  server/server/Docs/ARCHITECTURE_OVERVIEW.md
```

**Ожидаемый результат**: 
- Ссылки на `_archive` должны быть только в секции "Archived" в `DOCUMENTATION_MAP.md` (строки 165-179).
- В остальных местах ссылок на `_archive` быть не должно.

---

## 7. Чек-лист выполнения

### 7.1 Перед началом

- [ ] Создана резервная копия всех файлов, которые будут изменяться
- [ ] Прочитана данная инструкция полностью
- [ ] Подготовлены команды для проверки

### 7.2 Выполнение изменений

- [ ] Обновлен `client/.cursorrules`:
  - [ ] Удалены ссылки на `Docs/NEXY_FIRST_RUN_LOG_EXPECTED.md` (строки 136, 197, 139)
  - [ ] Удалены ссылки на `Docs/PERMISSION_RESTART_BLOCKERS.md` (строка 552)
  - [ ] Удалены ссылки на `RELEASE_INTEGRITY_PLAN.md` (строки 372, 406, 436, 465, 499)
- [ ] Проверен `server/server/.cursorrules` на отсутствие ссылок на архивные документы
- [ ] Проверен `client/Docs/DOCUMENTATION_MAP.md` на отсутствие ссылок на архивные документы (кроме секции "Archived")
- [ ] Проверен `server/server/Docs/ARCHITECTURE_OVERVIEW.md` на актуальность ссылок

### 7.3 Проверка после изменений

- [ ] Запущен скрипт `scripts/check_documentation_links.sh` (если создан)
- [ ] Выполнена ручная проверка всех ссылок (раздел 5.2)
- [ ] Проверено отсутствие ссылок на `_archive` в активных документах (раздел 6.2)
- [ ] Все файлы, упомянутые в документации, существуют

### 7.4 Финальная проверка

- [ ] Все изменения закоммичены
- [ ] Создан PR с описанием изменений
- [ ] PR проверен на отсутствие "битых" ссылок

---

## 8. Примеры конкретных изменений

### 8.1 Пример 1: Удаление ссылки на `NEXY_FIRST_RUN_LOG_EXPECTED.md`

**Файл**: `client/.cursorrules`  
**Строка**: 136

**Было**:
```markdown
**Спецификация логов**: См. `Docs/NEXY_FIRST_RUN_LOG_EXPECTED.md` — точная последовательность логов для правильной работы процесса первого запуска.
```

**Стало**:
```markdown
**Спецификация логов**: Логи первого запуска проверяются через `tests/test_golden_first_run_logs.py` и соответствуют спецификации в `Docs/first_run_flow_spec.md`.
```

### 8.2 Пример 2: Удаление ссылки на `PERMISSION_RESTART_BLOCKERS.md`

**Файл**: `client/.cursorrules`  
**Строка**: 552

**Было**:
```markdown
- Все блокировки и отсрочки описаны в `Docs/PERMISSION_RESTART_BLOCKERS.md` и отражены в `interaction_matrix.yaml`
```

**Стало**:
```markdown
- Все блокировки и отсрочки отражены в `config/interaction_matrix.yaml` с приоритетами (hard_stop/graceful)
```

### 8.3 Пример 3: Удаление ссылки на `RELEASE_INTEGRITY_PLAN.md`

**Файл**: `client/.cursorrules`  
**Строка**: 372

**Было**:
```markdown
**Связанные документы**:
- `scripts/run_release_suite.py` — скрипт Release Suite
- `Docs/PROJECT_REQUIREMENTS.md` — требования для сверки
- `RELEASE_INTEGRITY_PLAN.md` — полный план целостности релиза
```

**Стало**:
```markdown
**Связанные документы**:
- `scripts/run_release_suite.py` — скрипт Release Suite
- `Docs/PROJECT_REQUIREMENTS.md` — требования для сверки
- Процесс Release Suite описан в разделе 11.5 данного документа
```

---

## 9. Дополнительные проверки

### 9.1 Проверка тестов на ссылки на архивные документы

**Команда**:
```bash
cd client
rg -n "NEXY_FIRST_RUN_LOG_EXPECTED|PERMISSION_RESTART_BLOCKERS|RELEASE_INTEGRITY_PLAN" tests/
```

**Ожидаемый результат**: 
- Если есть упоминания, проверить, что они ссылаются на актуальные источники или удалены.

**Пример из `client/tests/test_golden_first_run_logs.py`**:
```python
# БЫЛО (если есть):
# See Docs/NEXY_FIRST_RUN_LOG_EXPECTED.md for full specification

# ДОЛЖНО БЫТЬ:
# See Docs/first_run_flow_spec.md for full specification
# Logs are validated through this test file
```

### 9.2 Проверка скриптов на ссылки на архивные документы

**Команда**:
```bash
cd client
rg -n "NEXY_FIRST_RUN_LOG_EXPECTED|PERMISSION_RESTART_BLOCKERS|RELEASE_INTEGRITY_PLAN" scripts/
```

**Ожидаемый результат**: Нет ссылок на архивные документы.

---

## 10. Резюме изменений

### 10.1 Файлы для изменения

1. **`client/.cursorrules`**:
   - Удалить ссылки на `Docs/NEXY_FIRST_RUN_LOG_EXPECTED.md` (3 места)
   - Удалить ссылки на `Docs/PERMISSION_RESTART_BLOCKERS.md` (1 место)
   - Удалить ссылки на `RELEASE_INTEGRITY_PLAN.md` (5 мест)

2. **`client/Docs/DOCUMENTATION_MAP.md`**:
   - Проверить отсутствие ссылок на архивные документы (кроме секции "Archived")

3. **`server/server/.cursorrules`**:
   - Проверить отсутствие ссылок на архивные документы

4. **`server/server/Docs/ARCHITECTURE_OVERVIEW.md`**:
   - Проверить актуальность всех ссылок

### 10.2 Файлы для проверки (без изменений)

1. `client/tests/test_golden_first_run_logs.py` — проверить ссылки
2. `client/Docs/CURRENT_STATUS_REPORT.md` — проверить упоминания процессов (не файлов)

---

## 11. Контрольный список перед мерджем

- [ ] Все изменения внесены согласно инструкции
- [ ] Все ссылки проверены и ведут на существующие файлы
- [ ] Нет ссылок на `_archive` в активных документах (кроме секции "Archived")
- [ ] Запущен скрипт проверки ссылок (если создан)
- [ ] Выполнена ручная проверка ключевых файлов
- [ ] Создан PR с описанием всех изменений
- [ ] PR проверен на отсутствие "битых" ссылок

---

**Владелец**: Tech Lead  
**Последнее обновление**: 2025-01-15
