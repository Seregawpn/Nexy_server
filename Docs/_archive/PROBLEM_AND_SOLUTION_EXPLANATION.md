# Объяснение проблемы и решения

## 🔴 Проблема (старая логика)

### Что было неправильно:

**Старая схема "эстафеты":**
```
1. AVFAudioEngine активирует микрофон
2. AVF записывает всё время (пока пользователь держит кнопку)
3. AVF накапливает аудио в буфере
4. При voice.recording_stop → AVF останавливает запись
5. AVF передаёт данные через событие voice.mic_data_ready
6. SpeechRecognition получает данные от AVF
7. SpeechRecognition пытается распознать через Google Speech API
8. ❌ Google возвращает UnknownValueError ("Speech not recognized")
```

### Почему не работало:

1. **Проблема с форматом данных:**
   - AVF записывает в формате AVFoundation (может быть 24kHz, 48kHz, float32)
   - Google Speech API требует строго 16kHz, int16, моно
   - Конвертация формата была неточной → Google не мог распознать

2. **Проблема с обработкой аудио:**
   - AVF передавал сырые PCM данные
   - Не было правильной нормализации и ресемплинга
   - Google получал "испорченные" данные

3. **Проблема с длительностью:**
   - AVF записывал всё время (до 30+ секунд)
   - В буфере накапливалось много тишины
   - Google получал длинные куски тишины → UnknownValueError

### Ошибки в логах:

```
⚠️ [Google] Речь не распознана (UnknownValueError)
❌ [Google] Speech not recognized
```

---

## ✅ Решение (новая логика)

### Правильная схема:

```
1. AVFAudioEngine активирует микрофон на ~1 секунду
   └─► Получает диагностику: формат, устройство, sample_rate
   └─► Дезактивирует микрофон ✅

2. Пауза 0.2 секунды (гарантия деактивации AVF)

3. Google (speech_recognition) активирует микрофон
   └─► Создаём sr.Microphone() напрямую
   └─► recognizer.listen() → запись речи
   └─► Google сам записывает через PyAudio (внутри библиотеки)

4. При voice.recording_stop:
   └─► recognizer.listen() завершается (phrase_time_limit=5.0)
   └─► Получаем sr.AudioData от Google
   └─► recognizer.recognize_google(audio_data) → распознавание
   └─► ✅ Google успешно распознаёт!
```

### Почему теперь работает:

1. **Google сам записывает:**
   - `speech_recognition` использует PyAudio напрямую
   - Формат данных правильный из коробки (16kHz, int16)
   - Google получает "родные" данные → распознавание работает

2. **AVF только для диагностики:**
   - AVF активирует на 1 секунду → получает информацию об устройстве
   - AVF дезактивирует → не мешает Google
   - Информация об устройстве сохраняется для использования

3. **Правильная длительность:**
   - `phrase_time_limit=5.0` → запись завершается быстро
   - Google получает только речь (без длинных тишины)
   - Распознавание работает корректно

---

## 🔧 Что изменилось в коде

### 1. `initialize()` - создание SpeechRecognizer

**Было:**
```python
if self._use_avf:
    self._recognizer = None  # Не создаём SpeechRecognizer
```

**Стало:**
```python
if self._use_avf:
    # Создаём SpeechRecognizer для Google записи
    self._recognizer = SpeechRecognizer(DEFAULT_RECOGNITION_CONFIG)
```

**Почему:** Теперь `SpeechRecognizer` создаётся даже при AVF, потому что Google будет записывать сам.

---

### 2. `_on_recording_start()` - новая логика активации

**Было:**
```python
# AVF активирует и записывает всё время
await self._avf_engine.start_input()
# ... накопление в буфере ...
```

**Стало:**
```python
# ✅ ШАГ 1: AVF диагностика (1 секунда)
device_info = await self._get_device_info_via_avf()
# AVF активирует → записывает 1 сек → дезактивирует

# ✅ ШАГ 2: Пауза 0.2 сек
await asyncio.sleep(0.2)

# ✅ ШАГ 3: Google активирует микрофон
import speech_recognition as sr
recognizer = sr.Recognizer()
microphone = sr.Microphone()

# Запускаем запись в отдельном потоке
def start_listening():
    with microphone:
        audio_data = recognizer.listen(microphone, phrase_time_limit=5.0)
        self._google_audio_data = audio_data

threading.Thread(target=start_listening, daemon=True).start()
```

**Почему:** 
- AVF используется только для диагностики (1 секунда)
- Google активирует микрофон сам и записывает правильно

---

### 3. `_on_recording_stop()` - новая логика остановки

**Было:**
```python
# AVF останавливает запись
result = await self._avf_engine.stop_input()
# Конвертируем данные от AVF → Google
await self._recognize_avf_audio(result.data, ...)
```

**Стало:**
```python
# ✅ Ждём завершения Google записи
if self._google_listening_event:
    completed = self._google_listening_event.wait(timeout=2.0)

# ✅ Получаем данные от Google
if self._google_audio_data:
    await self._recognize_google_audio(self._google_audio_data, session_id)

# ✅ Возвращаемся (не выполняем старую логику)
return
```

**Почему:**
- Получаем данные напрямую от Google (правильный формат)
- Не выполняем старую логику AVF → Google конвертации
- Распознавание работает корректно

---

### 4. `_recognize_google_audio()` - новый метод

**Новый метод:**
```python
async def _recognize_google_audio(self, audio_data: sr.AudioData, session_id: str):
    """
    Распознаёт аудио через Google Speech API.
    
    Args:
        audio_data: sr.AudioData от speech_recognition (правильный формат)
        session_id: ID сессии
    """
    recognizer = sr.Recognizer()
    text = recognizer.recognize_google(audio_data, language=self.config.language)
    
    # Публикуем результат с информацией об устройстве от AVF
    await self.event_bus.publish("voice.recognition_completed", {
        "session_id": session_id,
        "text": text,
        "device_info": self._avf_device_info  # Информация от AVF
    })
```

**Почему:**
- Получаем `sr.AudioData` напрямую от Google (правильный формат)
- Не нужно конвертировать формат → Google распознаёт корректно
- Информация об устройстве от AVF передаётся в результат

---

## 📊 Сравнение: Было vs Стало

| Аспект | Было (не работало) | Стало (работает) |
|--------|-------------------|------------------|
| **Активация AVF** | Всё время записи | Только 1 секунда (диагностика) |
| **Запись аудио** | AVF записывает → передаёт Google | Google записывает сам |
| **Формат данных** | AVF формат → конвертация → Google | Google формат напрямую |
| **Распознавание** | ❌ UnknownValueError | ✅ Успешно распознаёт |
| **Длительность** | До 30+ секунд (много тишины) | До 5 секунд (только речь) |
| **Информация об устройстве** | ❌ Не получали | ✅ Получаем от AVF |

---

## 🎯 Итог

### Проблема была в том, что:
1. AVF записывал аудио и передавал Google
2. Формат данных был неправильным после конвертации
3. Google не мог распознать "испорченные" данные

### Решение:
1. AVF используется только для диагностики (1 секунда)
2. Google сам записывает через `speech_recognition`
3. Google получает правильный формат данных → распознавание работает

### Результат:
✅ Распознавание работает корректно  
✅ Текст успешно распознаётся ("hey what's up")  
✅ Информация об устройстве доступна  
✅ Нет проблем с форматом данных

---

## 🔍 Ключевые моменты

1. **AVF для диагностики, Google для записи:**
   - AVF активирует на 1 секунду → получает информацию об устройстве
   - Google активирует → записывает речь → распознаёт

2. **Правильный формат данных:**
   - Google получает данные в правильном формате (16kHz, int16)
   - Не нужно конвертировать → распознавание работает

3. **Быстрое завершение записи:**
   - `phrase_time_limit=5.0` → запись завершается быстро
   - Google получает только речь (без длинных тишины)

4. **Информация об устройстве:**
   - AVF получает информацию об устройстве (формат, sample_rate)
   - Информация передаётся в результат распознавания
