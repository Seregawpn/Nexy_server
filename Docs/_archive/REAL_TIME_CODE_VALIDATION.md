# ⚡ Автоматическая валидация кода при генерации

**ЦЕЛЬ**: Обеспечить автоматическую проверку дублирования, конфликтов и race conditions при КАЖДОМ изменении кода в реальном времени.

**ВАЖНО**: Эти проверки выполняются **АВТОМАТИЧЕСКИ** при генерации любого кода, независимо от этапа планирования.

**Дата создания**: 2025-01-XX  
**Версия**: 1.0

---

## 🚨 Обязательные проверки при КАЖДОМ изменении кода

### Правило 1: Проверка дублирования перед созданием нового кода

**ПЕРЕД созданием любой функции/класса/метода**:

1. **Поиск существующей функциональности** (ОБЯЗАТЕЛЬНО):
   ```python
   # ✅ ХОРОШО: Перед созданием функции is_bluetooth_device()
   # 1. Выполнить поиск: grep -r "is_bluetooth_device" .
   # 2. Выполнить поиск: codebase_search("How is bluetooth device detected?")
   # 3. Если найдено → использовать существующую функцию
   # 4. Если не найдено → создать новую функцию
   ```

2. **Проверка централизованных утилит** (ОБЯЗАТЕЛЬНО):
   ```python
   # ✅ ХОРОШО: Проверить наличие в утилитах
   # - modules/audio_system/utils/device_utils.py
   # - modules/*/utils/*.py
   # - integration/core/*.py
   ```

3. **Правило**: Если функциональность уже существует → **ИСПОЛЬЗОВАТЬ**, не создавать новую.

**Пример нарушения**:
```python
# ❌ ПЛОХО: Создание новой функции без проверки
def _is_bluetooth_device(self, name: str) -> bool:
    return "bluetooth" in name.lower()
# Проблема: device_utils.is_bluetooth_device() уже существует!

# ✅ ХОРОШО: Использование существующей функции
from modules.audio_system.utils.device_utils import is_bluetooth_device
is_bluetooth = is_bluetooth_device(device_name)
```

---

### Правило 2: Проверка конфликтов перед изменением имен/состояний

**ПЕРЕД созданием/изменением любой переменной/функции/класса**:

1. **Проверка конфликтов имен** (ОБЯЗАТЕЛЬНО):
   ```python
   # ✅ ХОРОШО: Перед созданием переменной _mic_state
   # 1. Выполнить поиск: grep -r "_mic_state" .
   # 2. Проверить, не конфликтует ли с существующими переменными
   # 3. Если конфликт → использовать уникальное имя
   ```

2. **Проверка конфликтов состояний** (ОБЯЗАТЕЛЬНО):
   ```python
   # ✅ ХОРОШО: Перед управлением состоянием микрофона
   # 1. Проверить: Кто еще управляет этим состоянием?
   # 2. Если несколько мест → использовать единый источник истины
   # 3. Если единый источник → использовать его, не создавать новый
   ```

3. **Правило**: Все имена должны быть уникальными, состояние должно управляться из одного места.

**Пример нарушения**:
```python
# ❌ ПЛОХО: Два места управляют одним состоянием
# В MicrophoneStateManager:
self._state = "opening"

# В ApplicationStateManager:
self._mic_state = "opening"  # Конфликт!

# ✅ ХОРОШО: Одно место управляет состоянием
# Только через MicrophoneStateManager, синхронизация через события
```

---

### Правило 3: Проверка race conditions перед доступом к общим данным

**ПЕРЕД доступом к любым данным из нескольких потоков**:

1. **Проверка thread-safety** (ОБЯЗАТЕЛЬНО):
   ```python
   # ✅ ХОРОШО: Перед доступом к _google_audio_chunks
   # 1. Проверить: Доступен ли из нескольких потоков?
   # 2. Если да → добавить блокировку
   # 3. Если нет → можно без блокировки
   ```

2. **Проверка async/await паттернов** (ОБЯЗАТЕЛЬНО):
   ```python
   # ✅ ХОРОШО: Перед созданием asyncio.create_task()
   # 1. Проверить: Есть ли уже запущенная задача?
   # 2. Если да → отменить предыдущую перед созданием новой
   # 3. Если нет → создать новую с отслеживанием
   ```

3. **Правило**: Все общие данные должны быть защищены блокировками, все async задачи должны отслеживаться.

**Пример нарушения**:
```python
# ❌ ПЛОХО: Доступ без синхронизации
def callback(recognizer, audio):
    self._google_audio_chunks.append(audio)  # Поток 1

async def process_audio():
    self._google_audio_chunks = []  # Поток 2 (race condition!)

# ✅ ХОРОШО: Доступ с синхронизацией
self._google_audio_chunks_lock = threading.Lock()

def callback(recognizer, audio):
    with self._google_audio_chunks_lock:
        self._google_audio_chunks.append(audio)

async def process_audio():
    with self._google_audio_chunks_lock:
        if self._google_audio_chunks:
            self._google_audio_data = self._google_audio_chunks[-1]
        self._google_audio_chunks = []
```

---

## 🔍 Алгоритм автоматической проверки

### Шаг 1: Перед созданием функции/метода

1. Извлечь имя функции/метода из генерируемого кода
2. Выполнить поиск по кодовой базе (grep, codebase_search)
3. Если найдено → использовать существующую функцию
4. Если не найдено → создать новую функцию

**Пример**:
```python
# Пользователь просит: "Создай функцию для определения Bluetooth устройств"
# 1. Ассистент ищет: grep -r "is_bluetooth_device\|bluetooth.*device" .
# 2. Находит: device_utils.is_bluetooth_device()
# 3. Использует существующую функцию вместо создания новой
```

---

### Шаг 2: Перед созданием переменной

1. Извлечь имя переменной из генерируемого кода
2. Выполнить поиск по кодовой базе (grep)
3. Если найдено в том же контексте → использовать существующую переменную
4. Если найдено в другом контексте → использовать уникальное имя

**Пример**:
```python
# Пользователь просит: "Создай переменную _mic_state"
# 1. Ассистент ищет: grep -r "_mic_state" .
# 2. Находит: MicrophoneStateManager._state (похожее имя)
# 3. Проверяет конфликт: не конфликтует ли с существующим
# 4. Если конфликт → использует уникальное имя или существующую переменную
```

---

### Шаг 3: Перед доступом к данным

1. Определить, доступны ли данные из нескольких потоков
2. Если да → проверить наличие блокировок
3. Если блокировок нет → добавить блокировки
4. Если блокировки есть → использовать существующие

**Пример**:
```python
# Пользователь просит: "Добавь данные в _google_audio_chunks"
# 1. Ассистент проверяет: Доступен ли из нескольких потоков?
# 2. Находит: Да, из callback потока и основного async потока
# 3. Проверяет: Есть ли блокировка?
# 4. Если нет → добавляет threading.Lock
# 5. Если есть → использует существующую блокировку
```

---

## 📋 Краткий чек-лист при генерации кода

### Перед созданием новой функции/метода

- [ ] **Поиск существующей функциональности**: Выполнен поиск похожей функциональности (grep, codebase_search)
- [ ] **Проверка централизованных утилит**: Проверены утилиты (device_utils, audio_utils, и т.д.)
- [ ] **Использование существующего**: Если найдено → используется существующая функция
- [ ] **Создание нового**: Если не найдено → создается новая функция в правильном месте

### Перед созданием/изменением переменной

- [ ] **Проверка конфликтов имен**: Выполнен поиск существующих переменных с таким же именем
- [ ] **Уникальное имя**: Если конфликт → используется уникальное имя
- [ ] **Проверка состояний**: Если переменная управляет состоянием → проверен единый источник истины

### Перед доступом к общим данным

- [ ] **Проверка thread-safety**: Определено, доступны ли данные из нескольких потоков
- [ ] **Блокировки**: Если да → добавлены блокировки или используются существующие
- [ ] **Атомарность**: Критические операции защищены блокировками

### Перед созданием async задачи

- [ ] **Отслеживание задач**: Проверено, отслеживаются ли задачи
- [ ] **Отмена дубликатов**: Если есть предыдущая задача → отменяется перед созданием новой
- [ ] **Таймауты**: Все async операции имеют таймауты

---

## 🎯 Правила применения

### Правило 1: Всегда проверять перед созданием

**НЕ создавать код БЕЗ проверки**:
- ❌ Создать функцию без поиска существующей
- ❌ Создать переменную без проверки конфликтов
- ❌ Доступ к данным без проверки thread-safety

**ВСЕГДА проверять**:
- ✅ Поиск существующей функциональности перед созданием
- ✅ Проверка конфликтов перед созданием переменной
- ✅ Проверка thread-safety перед доступом к данным

### Правило 2: Использовать существующее вместо создания нового

**Если функциональность уже существует**:
- ✅ Использовать существующую функцию/класс/переменную
- ✅ Расширить существующую функциональность (если нужно)
- ❌ НЕ создавать новую реализацию

### Правило 3: Защищать общие данные

**Если данные доступны из нескольких потоков**:
- ✅ Добавить блокировки для синхронизации
- ✅ Использовать атомарные операции
- ❌ НЕ оставлять данные без защиты

---

## 📚 Примеры применения правил

### Пример 1: Создание функции определения Bluetooth устройств

**Запрос**: "Создай функцию для определения Bluetooth устройств"

**Проверка**:
1. Поиск: `grep -r "is_bluetooth_device\|bluetooth.*device" .`
2. Результат: Найдено `device_utils.is_bluetooth_device()`
3. Действие: Использовать существующую функцию

**Код**:
```python
# ✅ ХОРОШО: Использование существующей функции
from modules.audio_system.utils.device_utils import is_bluetooth_device

is_bluetooth = is_bluetooth_device(device_name)
```

---

### Пример 2: Создание переменной состояния микрофона

**Запрос**: "Создай переменную для состояния микрофона"

**Проверка**:
1. Поиск: `grep -r "_mic_state\|mic.*state" .`
2. Результат: Найдено `MicrophoneStateManager._state`
3. Действие: Использовать существующую переменную через MicrophoneStateManager

**Код**:
```python
# ✅ ХОРОШО: Использование существующего состояния
if self._mic_state_manager:
    state = await self._mic_state_manager.get_state()
```

---

### Пример 3: Доступ к общим данным из callback

**Запрос**: "Добавь данные в _google_audio_chunks из callback"

**Проверка**:
1. Проверка: Доступен ли из нескольких потоков?
2. Результат: Да, из callback потока и основного async потока
3. Действие: Добавить блокировку

**Код**:
```python
# ✅ ХОРОШО: Доступ с синхронизацией
def callback(recognizer, audio):
    with self._google_audio_chunks_lock:
        self._google_audio_chunks.append(audio)
```

---

## 🔧 Интеграция в процесс генерации кода

### Шаг 1: Перед генерацией кода

1. Выполнить проверки из чек-листа
2. Если найдено дублирование → использовать существующее
3. Если найдены конфликты → разрешить конфликты
4. Если найдены race conditions → добавить защиту

### Шаг 2: При генерации кода

1. Использовать существующие функции/классы/переменные
2. Использовать уникальные имена (если создается новое)
3. Добавлять блокировки для общих данных
4. Отслеживать async задачи

### Шаг 3: После генерации кода

1. Проверить, что все проверки выполнены
2. Проверить, что нет дублирования
3. Проверить, что нет конфликтов
4. Проверить, что нет race conditions

---

## 📚 Связанные документы

- `CODE_CHANGE_VALIDATION_RULES.md` — детальные правила валидации изменений кода
- `DETECTION_GUIDE_DUPLICATES_CONFLICTS_RACES.md` — **ИНСТРУКЦИЯ** по выявлению дублирования, конфликтов и race conditions
- `DUPLICATE_CONFLICT_RACE_PREVENTION_CHECKLIST.md` — детальная проверка на этапе планирования
- `.cursorrules` (раздел 11.2) — правила валидации изменений

---

**Версия**: 1.0  
**Дата создания**: 2025-01-XX



