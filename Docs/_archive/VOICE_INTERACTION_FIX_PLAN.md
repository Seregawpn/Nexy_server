# План исправления взаимодействия клавиатуры и голосового управления

## Дата создания
2025-11-30

## Основа анализа
- `Docs/KEYBOARD_VOICE_INTERACTION_FLOW.md` - полный поток взаимодействия
- `Docs/KEYBOARD_VOICE_INTERACTION_ANALYSIS.md` - детальный анализ проблем
- `Docs/KEYBOARD_VOICE_INTERACTION_DIAGRAM.md` - визуальные диаграммы
- `Docs/MICROPHONE_STICKING_FIX_V2.md` - исправление залипания микрофона
- `Docs/REQUEST_CLOSE_DEADLOCK_FIX.md` - исправление deadlock

---

## Этап 1: Синхронизация состояний микрофона (ПРИОРИТЕТ 1)

### Задача 1.1: Создать единую функцию проверки состояния микрофона

**Проблема:** Множественные источники истины для состояния микрофона (Проблема 2, Проблема 5)

**Файлы для изменения:**
- `integration/integrations/voice_recognition_integration.py`
- `integration/integrations/input_processing_integration.py`

**Действия:**

1. **Создать `is_microphone_actually_active()` в `VoiceRecognitionIntegration`:**

```python
def is_microphone_actually_active(self) -> bool:
    """
    Единый источник истины для проверки состояния микрофона.
    Проверяет централизованное состояние и физическое состояние потока.
    """
    # 1. Проверяем централизованное состояние (основной источник)
    if self.state_manager.is_microphone_active():
        return True
    
    # 2. Проверяем физическое состояние потока (fallback для обнаружения рассинхронизации)
    if self._recognizer and hasattr(self._recognizer, '_current_stream'):
        with getattr(self._recognizer, '_stream_lock', threading.RLock()):
            if self._recognizer._current_stream and self._recognizer._current_stream.active:
                logger.warning("⚠️ [VOICE] Обнаружена рассинхронизация: поток активен, но state_manager не знает")
                return True
    
    return False
```

2. **Использовать эту функцию в `InputProcessingIntegration`:**

```python
# Вместо множественных проверок:
# if self.state_manager.is_microphone_active() or self._recording_started:
#     ...

# Использовать:
if self.voice_recognition_integration.is_microphone_actually_active():
    ...
```

**Проверка:**
- [ ] Функция создана в `VoiceRecognitionIntegration`
- [ ] Функция используется в `InputProcessingIntegration._handle_long_press`
- [ ] Функция используется в `InputProcessingIntegration._handle_key_release`
- [ ] Все проверки состояния микрофона заменены на единую функцию

---

### Задача 1.2: Сбрасывать `_recording_started` только после закрытия микрофона

**Проблема:** `_recording_started` сбрасывается слишком рано (Проблема 3, Проблема 4)

**Файлы для изменения:**
- `integration/integrations/input_processing_integration.py`

**Действия:**

1. **Удалить немедленный сброс `_recording_started` в `RELEASE`:**

```python
# УДАЛИТЬ строку 1419:
# self._recording_started = False  # УДАЛИТЬ ЭТУ СТРОКУ
```

2. **Добавить подписку на `voice.mic_closed`:**

```python
async def initialize(self):
    # ... существующие подписки ...
    await self.event_bus.subscribe("voice.mic_closed", self._on_mic_closed, EventPriority.HIGH)
```

3. **Создать обработчик `_on_mic_closed`:**

```python
async def _on_mic_closed(self, event: Dict[str, Any]):
    """
    Обработчик события закрытия микрофона.
    Сбрасывает _recording_started только после подтверждения закрытия.
    """
    try:
        data = event.get("data", {}) or event
        session_id = data.get("session_id")
        
        logger.debug(f"🛑 [INPUT] voice.mic_closed получено, session={session_id}")
        
        # ✅ КРИТИЧНО: Сбрасываем _recording_started только после подтверждения закрытия
        if self._recording_started:
            self._recording_started = False
            logger.info("✅ [INPUT] _recording_started сброшен после закрытия микрофона")
        else:
            logger.debug("ℹ️ [INPUT] _recording_started уже был False")
            
    except Exception as e:
        logger.error(f"❌ [INPUT] Ошибка в _on_mic_closed: {e}")
```

**Проверка:**
- [ ] Строка 1419 удалена (немедленный сброс `_recording_started`)
- [ ] Подписка на `voice.mic_closed` добавлена
- [ ] Обработчик `_on_mic_closed` создан
- [ ] `_recording_started` сбрасывается только после `voice.mic_closed`

---

### Задача 1.3: Упростить проверки в LONG_PRESS

**Проблема:** Сложная логика проверок в LONG_PRESS (Проблема 4)

**Файлы для изменения:**
- `integration/integrations/input_processing_integration.py`

**Действия:**

1. **Создать единую функцию проверки готовности к записи:**

```python
async def _can_start_recording(self) -> tuple[bool, str]:
    """
    Проверяет готовность системы к записи.
    Единая функция для всех проверок состояния системы перед началом записи.
    
    ПРИМЕЧАНИЕ: Проверка _long_press_in_progress выполняется в _handle_long_press
    ДО вызова этой функции (защита от повторных LONG_PRESS), поэтому здесь её не проверяем.
    
    Returns:
        (can_start, reason) - можно ли начать запись и причина отказа (если нельзя)
    """
    # Проверка 1: _input_state
    if self._input_state != InputState.PENDING:
        return False, f"wrong_input_state_{self._input_state.name}"
    
    # Проверка 2: pending_session_id
    if self._pending_session_id is None:
        return False, "no_pending_session"
    
    # Проверка 3: keyboard_monitor.key_pressed
    if self.keyboard_monitor and hasattr(self.keyboard_monitor, 'key_pressed'):
        if not self.keyboard_monitor.key_pressed:
            return False, "key_not_pressed"
    
    # Проверка 4: микрофон уже активен (используем state_manager как единый источник истины)
    if self.state_manager.is_microphone_active():
        return False, "microphone_already_active"
    
    # Проверка 5: _recording_started
    if self._recording_started:
        return False, "recording_already_started"
    
    return True, "ok"
```

2. **Использовать эту функцию в `_handle_long_press`:**

```python
# Порядок проверок:
# 1. Защита от повторных LONG_PRESS (в _handle_long_press, ДО установки флага)
async with self._state_lock:
    if self._long_press_in_progress:
        return
    self._long_press_in_progress = True

# 2. Проверка готовности системы (через _can_start_recording)
can_start, reason = await self._can_start_recording()
if not can_start:
    logger.warning(f"⚠️ LONG_PRESS: нельзя начать запись - {reason}")
    async with self._state_lock:
        self._long_press_in_progress = False
    return
```

**КРИТИЧНО:** Не проверяем `_long_press_in_progress` в `_can_start_recording()`, так как эта проверка уже выполнена в `_handle_long_press` ДО установки флага.

**Проверка:**
- [x] Функция `_can_start_recording` создана
- [x] Проверка `_long_press_in_progress` убрана из функции (исправлена логическая ошибка)
- [x] Все проверки состояния системы в `_handle_long_press` заменены на единую функцию
- [x] Логирование улучшено (показывает причину отказа)

---

## Этап 2: Улучшение переходов режимов и ModeManagement (ПРИОРИТЕТ 1/2)

### Задача 2.1: Убрать условность для mode.request(LISTENING) в LONG_PRESS

**Проблема:** Условная публикация `mode.request(LISTENING)` (Проблема 1)

**Файлы для изменения:**
- `integration/integrations/input_processing_integration.py`

**Действия:**

1. **Упростить логику публикации `mode.request(LISTENING)`:**

```python
# Вместо условной логики (строки 1298-1331):
# Упростить до:
await self.event_bus.publish("mode.request", {
    "target": AppMode.LISTENING,
    "source": "input_processing",
    "session_id": active_session_id
})
logger.info(f"✅ LONG_PRESS: запрос на LISTENING отправлен (session={active_session_id})")

# ModeManagementIntegration сам обработает идемпотентность и приоритеты
```

**Проверка:**
- [ ] Условная логика удалена
- [ ] `mode.request(LISTENING)` публикуется всегда после `voice.recording_start`
- [ ] `ModeManagementIntegration` корректно обрабатывает идемпотентность

---

### Задача 2.2: Добавить блокировку для предотвращения повторных PROCESSING

**Проблема:** Повторные `voice.recording_stop` могут перезапускать PROCESSING

**Файлы для изменения:**
- `integration/integrations/mode_management_integration.py`
- `integration/workflows/processing_workflow.py`

**Действия:**

1. **Добавить флаг `processing_in_progress` в `ModeManagementIntegration`:**

```python
def __init__(self, ...):
    # ... существующий код ...
    self._processing_in_progress = False
    self._processing_lock = asyncio.Lock()
```

2. **Проверять флаг при переходе в PROCESSING:**

```python
async def _on_mode_request(self, event: Dict[str, Any]):
    # ... существующий код ...
    
    if target == AppMode.PROCESSING:
        async with self._processing_lock:
            if self._processing_in_progress:
                logger.debug("⚠️ [MODE] PROCESSING уже в процессе, игнорируем повторный запрос")
                return
            self._processing_in_progress = True
    
    # ... применяем переход ...
```

3. **Сбрасывать флаг при выходе из PROCESSING:**

```python
async def _bridge_playback_done(self, event: Dict[str, Any]):
    # ... существующий код ...
    async with self._processing_lock:
        self._processing_in_progress = False
```

**Проверка:**
- [ ] Флаг `processing_in_progress` добавлен
- [ ] Проверка флага при переходе в PROCESSING
- [ ] Флаг сбрасывается при выходе из PROCESSING

---

### Задача 2.3: Синхронизировать иконку меню с состоянием микрофона

**Проблема:** Иконка меню может не отражать реальное состояние микрофона

**Файлы для изменения:**
- `integration/integrations/tray_controller_integration.py`

**Действия:**

1. **Добавить подписку на `voice.mic_opened` и `voice.mic_closed`:**

```python
async def initialize(self):
    # ... существующие подписки ...
    await self.event_bus.subscribe("voice.mic_opened", self._on_mic_opened, EventPriority.MEDIUM)
    await self.event_bus.subscribe("voice.mic_closed", self._on_mic_closed, EventPriority.MEDIUM)
```

2. **Создать обработчики:**

```python
async def _on_mic_opened(self, event: Dict[str, Any]):
    """Обновляет иконку при открытии микрофона"""
    logger.debug("🎤 [TRAY] Микрофон открыт, обновляем иконку")
    # Принудительно обновляем иконку для синхронизации
    current_mode = self.state_manager.get_current_mode()
    await self._update_tray_icon(current_mode)

async def _on_mic_closed(self, event: Dict[str, Any]):
    """Обновляет иконку при закрытии микрофона"""
    logger.debug("🎤 [TRAY] Микрофон закрыт, обновляем иконку")
    # Принудительно обновляем иконку для синхронизации
    current_mode = self.state_manager.get_current_mode()
    await self._update_tray_icon(current_mode)
```

**Проверка:**
- [ ] Подписки на `voice.mic_opened` и `voice.mic_closed` добавлены
- [ ] Обработчики созданы
- [ ] Иконка обновляется при изменении состояния микрофона

---

## Этап 3: Стандартизация session_id и приоритетов (ПРИОРИТЕТ 2/3)

### Задача 3.1: Отказаться от локальных переменных session_id

**Проблема:** Множественные источники session_id (Проблема 6)

**Файлы для изменения:**
- `integration/integrations/input_processing_integration.py`

**Действия:**

1. **Удалить локальные переменные:**
   - `_pending_session_id`
   - `_active_grpc_session_id`
   - `_cancel_session_id`

2. **Использовать только `state_manager.get_current_session_id()`:**

```python
# Вместо:
# active_session_id = self._pending_session_id or self._active_grpc_session_id or ...

# Использовать:
active_session_id = self.state_manager.get_current_session_id()
```

**Проверка:**
- [ ] Локальные переменные удалены
- [ ] Все обращения к session_id используют `state_manager`
- [ ] Нет рассинхронизации session_id

---

### Задача 3.2: Ввести явную очередь для источников команд ModeManagement

**Проблема:** Команды от клавиатуры могут потеряться при приходе других событий

**Файлы для изменения:**
- `integration/integrations/mode_management_integration.py`

**Действия:**

1. **Добавить очередь для mode.request:**

```python
def __init__(self, ...):
    # ... существующий код ...
    self._mode_request_queue = asyncio.Queue()
    self._mode_request_processor_task: Optional[asyncio.Task] = None
```

2. **Создать процессор очереди:**

```python
async def _process_mode_request_queue(self):
    """Обрабатывает очередь mode.request последовательно"""
    while self._running:
        try:
            event = await asyncio.wait_for(
                self._mode_request_queue.get(),
                timeout=1.0
            )
            await self._on_mode_request_internal(event)
            self._mode_request_queue.task_done()
        except asyncio.TimeoutError:
            continue
        except Exception as e:
            logger.error(f"❌ [MODE] Ошибка обработки mode.request: {e}")
```

3. **Использовать очередь в `_on_mode_request`:**

```python
async def _on_mode_request(self, event: Dict[str, Any]):
    """Добавляет запрос в очередь для последовательной обработки"""
    await self._mode_request_queue.put(event)
```

**Проверка:**
- [ ] Очередь создана
- [ ] Процессор очереди запущен
- [ ] Все `mode.request` обрабатываются последовательно

---

## Этап 4: Диагностика и тесты (ПРИОРИТЕТ 2)

### Задача 4.1: Улучшить логирование последовательности событий

**Файлы для изменения:**
- `integration/integrations/input_processing_integration.py`
- `integration/integrations/voice_recognition_integration.py`

**Действия:**

1. **Добавить логирование с эмодзи для ключевых событий:**

```python
# В LONG_PRESS:
logger.info("🛑 [INPUT] LONG_PRESS → voice.recording_start (session={})", session_id)

# В RELEASE:
logger.info("🛑 [INPUT] RELEASE → voice.recording_stop (session={})", session_id)

# В _on_recording_start:
logger.info("✅ [VOICE] voice.recording_start обработан (session={})", session_id)

# В _on_recording_stop:
logger.info("🛑 [VOICE] voice.recording_stop обработан (session={})", session_id)
logger.info("🔍 [VOICE] Проверка состояния микрофона...")
logger.info("🎤 [VOICE] stop_listening вызван (session={})", session_id)
```

**Проверка:**
- [ ] Логирование добавлено для всех ключевых событий
- [ ] Эмодзи используются для визуального отслеживания
- [ ] Логи показывают полную последовательность событий

---

### Задача 4.2: Добавить unit/интеграционные тесты

**Файлы для создания:**
- `tests/integration/test_keyboard_voice_interaction.py`

**Действия:**

1. **Создать тесты для последовательности событий:**

```python
async def test_long_press_to_recording_start():
    """Тест: LONG_PRESS → voice.recording_start → microphone.opened"""
    # ...

async def test_release_to_recording_stop():
    """Тест: RELEASE → voice.recording_stop → microphone.closed → stop_listening"""
    # ...

async def test_recording_started_reset_after_mic_closed():
    """Тест: _recording_started сбрасывается только после microphone.closed"""
    # ...
```

**Проверка:**
- [ ] Тесты созданы
- [ ] Тесты покрывают все ключевые последовательности
- [ ] Тесты проходят

---

### Задача 4.3: Запустить live-тест после каждой ключевой правки

**Действия:**

1. **Создать скрипт для live-теста:**

```bash
#!/bin/bash
# scripts/test_keyboard_voice_interaction.sh

echo "🧪 Запуск live-теста взаимодействия клавиатуры и голосового управления..."
echo "📋 Тест длится ~60 секунд"
echo ""

# Запуск приложения
python client/main.py &
APP_PID=$!

# Ожидание запуска
sleep 5

# Мониторинг логов
tail -f log.md | grep -E "(🛑|✅|🔍|🎤)" &
LOG_PID=$!

# Ожидание завершения теста
sleep 60

# Остановка
kill $APP_PID $LOG_PID 2>/dev/null

echo "✅ Live-тест завершен"
```

**Проверка:**
- [ ] Скрипт создан
- [ ] Live-тест запускается после каждой ключевой правки
- [ ] Результаты фиксируются

---

## Этап 5: Документация и коммуникация (ПРИОРИТЕТ 3)

### Задача 5.1: Обновить диаграмму после правок

**Файлы для изменения:**
- `Docs/KEYBOARD_VOICE_INTERACTION_DIAGRAM.md`

**Действия:**

1. **Обновить диаграмму последовательности:**
   - Добавить новые события (`voice.mic_opened`, `voice.mic_closed`)
   - Обновить тайминг
   - Добавить ссылки на исправления

2. **Добавить ссылки на связанные документы:**
   - `Docs/MICROPHONE_STICKING_FIX_V2.md`
   - `Docs/REQUEST_CLOSE_DEADLOCK_FIX.md`

**Проверка:**
- [ ] Диаграмма обновлена
- [ ] Ссылки добавлены
- [ ] Тайминг актуален

---

### Задача 5.2: Создать отчёт по интеграции

**Файлы для создания:**
- `Docs/VOICE_INTERACTION_FIX_REPORT.md`

**Действия:**

1. **Создать отчёт с описанием:**
   - Какие изменения внесены
   - Как изменения влияют на UI/иконку
   - Как изменения влияют на mode changes
   - Результаты тестирования

**Проверка:**
- [ ] Отчёт создан
- [ ] Отчёт описывает все изменения
- [ ] Отчёт включает результаты тестирования

---

## Чек-лист выполнения

### Этап 1: Синхронизация состояний микрофона
- [ ] Задача 1.1: Единая функция проверки состояния микрофона
- [ ] Задача 1.2: Сброс `_recording_started` только после закрытия микрофона
- [ ] Задача 1.3: Упрощение проверок в LONG_PRESS

### Этап 2: Улучшение переходов режимов
- [ ] Задача 2.1: Убрать условность для mode.request(LISTENING)
- [ ] Задача 2.2: Добавить блокировку для PROCESSING
- [ ] Задача 2.3: Синхронизировать иконку меню

### Этап 3: Стандартизация session_id
- [ ] Задача 3.1: Отказаться от локальных переменных session_id
- [ ] Задача 3.2: Ввести очередь для mode.request

### Этап 4: Диагностика и тесты
- [ ] Задача 4.1: Улучшить логирование
- [ ] Задача 4.2: Добавить тесты
- [ ] Задача 4.3: Запустить live-тест

### Этап 5: Документация
- [ ] Задача 5.1: Обновить диаграмму
- [ ] Задача 5.2: Создать отчёт

---

## Порядок выполнения

1. **Начать с Этапа 1** (критические исправления)
2. **Проверить после каждого этапа** (live-тест)
3. **Документировать изменения** (обновить диаграмму)
4. **Создать финальный отчёт** (после всех исправлений)

---

## Связанные документы

- `Docs/KEYBOARD_VOICE_INTERACTION_FLOW.md` - полный поток взаимодействия
- `Docs/KEYBOARD_VOICE_INTERACTION_ANALYSIS.md` - детальный анализ проблем
- `Docs/KEYBOARD_VOICE_INTERACTION_DIAGRAM.md` - визуальные диаграммы
- `Docs/MICROPHONE_STICKING_FIX_V2.md` - исправление залипания микрофона
- `Docs/REQUEST_CLOSE_DEADLOCK_FIX.md` - исправление deadlock

