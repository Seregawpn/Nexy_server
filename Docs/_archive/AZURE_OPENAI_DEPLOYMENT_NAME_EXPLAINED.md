# 📦 Что такое Deployment Name в Azure OpenAI?

**Deployment Name** (Имя развертывания) — это имя конкретной модели AI, которую вы развернули в вашем Azure OpenAI ресурсе.

---

## 🎯 Простое объяснение

Представьте Azure OpenAI как библиотеку моделей:
- **Azure OpenAI ресурс** (`nexy-ai-core`) — это сама библиотека
- **Deployment Name** (`OpenAICreate-2026011`) — это конкретная книга (модель), которую вы взяли из библиотеки

Deployment Name нужен, чтобы Azure знал, какую именно модель использовать для ваших запросов.

---

## 🔍 Где найти Deployment Name в Azure Portal?

### Пошаговая инструкция:

1. **Откройте [Azure Portal](https://portal.azure.com)**

2. **Найдите ваш ресурс Azure OpenAI:**
   - В поиске введите: `nexy-ai-core`
   - Или перейдите: **All resources** → найдите ресурс типа **"Cognitive Services"**

3. **Откройте ресурс `nexy-ai-core`**

4. **В меню слева найдите раздел:**
   - **"Model deployments"** (Развертывания моделей)
   - Или **"Deployments"** (Развертывания)
   - Или **"Azure OpenAI Studio"** → **"Deployments"**

5. **Вы увидите список всех развертываний:**
   - Каждое развертывание имеет **имя** (Deployment Name)
   - Например: `OpenAICreate-2026011`, `gpt-4`, `gpt-35-turbo` и т.д.

6. **Скопируйте имя развертывания:**
   - Это и есть ваш **Deployment Name**
   - Используйте его в настройках Cursor

---

## 📋 Примеры Deployment Name

Deployment Name может быть любым именем, которое вы указали при создании развертывания:

**Примеры:**
- `OpenAICreate-2026011` (ваш текущий)
- `gpt-4`
- `gpt-35-turbo`
- `my-chat-model`
- `production-gpt4`

⚠️ **ВАЖНО:** Deployment Name должен точно совпадать с именем в Azure Portal (регистр важен!)

---

## 🎨 Визуальное представление

```
Azure OpenAI Resource (nexy-ai-core)
│
├── Deployment 1: "OpenAICreate-2026011"  ← Это ваш Deployment Name
│   └── Модель: GPT-4
│
├── Deployment 2: "gpt-35-turbo"
│   └── Модель: GPT-3.5 Turbo
│
└── Deployment 3: "my-custom-model"
    └── Модель: GPT-4
```

---

## 🔧 Как Deployment Name используется?

Когда вы делаете запрос к Azure OpenAI, используется полный путь:

```
https://nexy-ai-core.openai.azure.com/openai/deployments/OpenAICreate-2026011/chat/completions
                                    ↑
                                    Deployment Name здесь
```

**В Cursor:**
- **Base URL:** `https://nexy-ai-core.openai.azure.com/`
- **Deployment Name:** `OpenAICreate-2026011`
- Cursor автоматически объединяет их в полный путь

---

## ✅ Проверка правильности Deployment Name

### Способ 1: Через Azure Portal

1. Откройте Azure Portal → `nexy-ai-core` → **"Model deployments"**
2. Убедитесь, что deployment с именем `OpenAICreate-2026011` существует
3. Проверьте статус: должен быть **"Succeeded"** (Успешно)

### Способ 2: Через скрипт проверки

```bash
# Запустите скрипт проверки
export AZURE_OPENAI_API_KEY="ваш_ключ"
./scripts/test_azure_openai_key.sh
```

Если Deployment Name неправильный, вы получите ошибку **404 Not Found**.

---

## 🛠️ Если Deployment Name не найден

### Проблема: "Deployment not found" (404)

**Решение:**

1. **Проверьте список развертываний в Azure Portal:**
   - Откройте `nexy-ai-core` → **"Model deployments"**
   - Найдите правильное имя развертывания

2. **Если развертывания нет:**
   - Создайте новое развертывание в Azure Portal
   - Или используйте существующее развертывание

3. **Проверьте регистр:**
   - `OpenAICreate-2026011` ≠ `openAICreate-2026011` (регистр важен!)

---

## 📝 Часто задаваемые вопросы

### Вопрос 1: Может ли быть несколько Deployment Name?

**Ответ:** Да! В одном Azure OpenAI ресурсе может быть несколько развертываний разных моделей:
- `gpt-4` — для GPT-4
- `gpt-35-turbo` — для GPT-3.5 Turbo
- `OpenAICreate-2026011` — ваше кастомное развертывание

### Вопрос 2: Нужно ли создавать новое развертывание?

**Ответ:** Нет, если у вас уже есть развертывание `OpenAICreate-2026011`, просто используйте его имя.

### Вопрос 3: Можно ли изменить Deployment Name?

**Ответ:** Нет, имя развертывания нельзя изменить после создания. Можно только создать новое развертывание с другим именем.

### Вопрос 4: Что если я не знаю Deployment Name?

**Ответ:** 
1. Откройте Azure Portal → `nexy-ai-core` → **"Model deployments"**
2. Посмотрите список всех развертываний
3. Выберите нужное развертывание и скопируйте его имя

---

## 🔗 Полезные ссылки

- [Azure Portal - Cognitive Services](https://portal.azure.com/#view/Microsoft_Azure_ProjectOxford/CognitiveServicesHub)
- [Azure OpenAI Studio](https://oai.azure.com/) — управление развертываниями
- [Azure OpenAI Documentation](https://learn.microsoft.com/en-us/azure/ai-services/openai/)

---

## 📌 Резюме

**Deployment Name** — это:
- ✅ Имя конкретной модели, развернутой в Azure OpenAI
- ✅ Уникальное имя для каждого развертывания
- ✅ Обязательный параметр для подключения к Azure OpenAI
- ✅ Находится в Azure Portal → Model deployments

**Ваш Deployment Name:** `OpenAICreate-2026011`

---

**Связанные документы:**
- `Docs/AZURE_OPENAI_CURSOR_SETUP.md` — полная инструкция по настройке
- `Docs/AZURE_OPENAI_FIX_BAD_API_KEY.md` — исправление ошибок
