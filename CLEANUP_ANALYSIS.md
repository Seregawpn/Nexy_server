# Анализ и очистка проекта от устаревших файлов

**Дата:** 2025-01-XX  
**Статус:** ✅ Частично завершено (безопасные файлы удалены)

## 📋 Категории файлов для удаления

## ✅ УДАЛЕНО (безопасные файлы)

### 1. ✅ Корневые тестовые файлы (устаревшие) - УДАЛЕНО

**Статус:** ✅ УДАЛЕНО - старые тесты, не используются в проекте

- `test_handoff.py` - тест эстафеты AVF → SpeechRecognition (старый)
- `test_new_logic.py` - тест новой логики AVF диагностика (старый)
- `test_speech_recognition_direct.py` - тест прямой работы SpeechRecognition (старый)
- `test_step2_init.py` - тест шага 2 инициализации (старый)
- `test_step3_recording.py` - тест шага 3 записи (старый)
- `test_step4_streaming.py` - тест шага 4 стриминга (старый)
- `test_step6_batch.py` - тест шага 6 batch (старый)
- `test_step6_short.py` - тест шага 6 short (старый)

**Причина:** Эти файлы были созданы для пошагового тестирования старой логики и больше не актуальны. Актуальные тесты находятся в `client/tests/` и `client/integration/tests/`.

---

### 2. ✅ Временные документы (завершенные задачи) - УДАЛЕНО

**Статус:** ✅ УДАЛЕНО - временные документы для исправления проблем

- `QUICK_FIX_STREAMING.md` - быстрый фикс для включения стриминга (завершено)
- `STREAMING_ISSUES_FIXED.md` - документ об исправлениях стриминга (завершено)

**Причина:** Эти документы описывали временные исправления, которые уже интегрированы в проект. Актуальная документация находится в `Docs/`.

---

### 3. ✅ Дубликаты конфигурации - УДАЛЕНО

**Статус:** ✅ УДАЛЕНО - дубликат конфигурации в корне

- `config/` (вся директория в корне проекта)
  - `config/audio_config.py`
  - `config/unified_config_loader.py`
  - `config/unified_config.yaml`

**Причина:** Актуальная конфигурация находится в `client/config/`. Корневая директория `config/` является дубликатом и не используется (проверено: импорты идут из `client/config/`).

---

### 4. ✅ Временные отчеты миграции аудио - УДАЛЕНО

**Статус:** ✅ УДАЛЕНО - временные отчеты, миграция завершена

- `client/audio_migration_compliance_report.json`
- `client/audio_migration_readiness_detailed_report.json`
- `client/audio_migration_readiness_report.json`

**Причина:** Эти отчеты были созданы для анализа готовности миграции аудио системы. Миграция завершена, отчеты больше не нужны.

---

### 5. ✅ Временные логи - УДАЛЕНО

**Статус:** ✅ УДАЛЕНО - временные логи, не должны быть в репозитории

- `log.md` (в корне проекта)
- `client/log.md` (если это временный лог)
- `client/logs.md` (если это временный лог)

**Причина:** Логи не должны храниться в репозитории. Они генерируются во время выполнения и должны быть в `.gitignore`.

---

### 6. Тестовые директории (требуют проверки)

**Статус:** ⚠️ ТРЕБУЕТ ПРОВЕРКИ - возможно устаревшие, но могут использоваться для прототипирования

#### 6.1 `test_audio_output/`

**Анализ:**
- Содержит множество тестовых файлов и прототипов
- Используется для тестирования аудио вывода
- Содержит `prototype/current_system/` - копия текущей системы для валидации

**Рекомендация:** 
- Если прототипирование завершено и код интегрирован → ❌ УДАЛИТЬ
- Если используется для текущих экспериментов → ✅ ОСТАВИТЬ

#### 6.2 `test_audio_device_switching/`

**Анализ:**
- Содержит решения для переключения аудио устройств
- Используется для тестирования различных подходов (Core Audio, Hybrid, FSM)
- Содержит `solution_1_core_audio_notifications/` и `solution_3_hybrid_monitoring/`

**Рекомендация:**
- Если решения интегрированы в основной код → ❌ УДАЛИТЬ
- Если используется для текущих экспериментов → ✅ ОСТАВИТЬ

#### 6.3 `mcp_close_app_test/`

**Анализ:**
- Содержит тестовый сервер для MCP close_app функциональности
- Содержит `server/app_launcher_server.py` и `server/close_app_server.py`

**Рекомендация:**
- Если функциональность интегрирована и тесты перенесены в `client/integration/tests/` → ❌ УДАЛИТЬ
- Если используется для текущего тестирования → ✅ ОСТАВИТЬ

---

### 7. Тестовые файлы в client/ (требуют проверки)

**Статус:** ⚠️ ТРЕБУЕТ ПРОВЕРКИ - могут быть актуальными тестами

- `client/test_mcp_open_app_integration.py` - тест MCP открытия приложения
- `client/test_open_close_safari.py` - тест открытия/закрытия Safari

**Анализ:**
- Эти файлы выглядят как актуальные тесты для MCP функциональности
- Содержат Feature ID: F-2025-016-mcp-app-opening-integration, F-2025-014-close-app

**Рекомендация:**
- Если это актуальные тесты → ✅ ОСТАВИТЬ (но переместить в `client/tests/` или `client/integration/tests/`)
- Если это старые тесты → ❌ УДАЛИТЬ

---

## 📊 Итоговая таблица

| Категория | Файлы/Директории | Статус | Действие |
|-----------|------------------|--------|----------|
| Корневые тесты | `test_*.py` (8 файлов) | ✅ УДАЛЕНО | Удалено все |
| Временные документы | `QUICK_FIX_STREAMING.md`, `STREAMING_ISSUES_FIXED.md` | ✅ УДАЛЕНО | Удалено оба |
| Дубликаты конфигов | `config/` (вся директория) | ✅ УДАЛЕНО | Удалена директория |
| Временные отчеты | `client/audio_migration_*.json` (3 файла) | ✅ УДАЛЕНО | Удалено все |
| Временные логи | `log.md`, `client/log.md`, `client/logs.md` | ✅ УДАЛЕНО | Удалено все |
| Тестовые директории | `test_audio_output/`, `test_audio_device_switching/`, `mcp_close_app_test/` | ⚠️ Требует проверки | Решение после проверки |
| Тесты в client/ | `client/test_*.py` (2 файла) | ⚠️ Требует проверки | Решение после проверки |

---

## 🎯 План действий

### Этап 1: ✅ Безопасное удаление (точно устаревшие) - ЗАВЕРШЕНО

1. ✅ Удалить корневые тестовые файлы (`test_*.py`) - **ВЫПОЛНЕНО**
2. ✅ Удалить временные документы (`QUICK_FIX_STREAMING.md`, `STREAMING_ISSUES_FIXED.md`) - **ВЫПОЛНЕНО**
3. ✅ Удалить дубликаты конфигов (`config/` в корне) - **ВЫПОЛНЕНО**
4. ✅ Удалить временные отчеты (`client/audio_migration_*.json`) - **ВЫПОЛНЕНО**
5. ✅ Удалить временные логи (`log.md`, `client/log.md`, `client/logs.md`) - **ВЫПОЛНЕНО**

### Этап 2: Проверка и решение (требуют анализа)

6. ⚠️ Проверить `test_audio_output/` - используется ли для текущих экспериментов?
7. ⚠️ Проверить `test_audio_device_switching/` - интегрированы ли решения?
8. ⚠️ Проверить `mcp_close_app_test/` - перенесены ли тесты в основную структуру?
9. ⚠️ Проверить `client/test_*.py` - актуальны ли тесты, нужно ли переместить?

---

## ⚠️ Предупреждения

1. **Перед удалением тестовых директорий** убедитесь, что:
   - Код из прототипов интегрирован в основной проект
   - Тесты перенесены в `client/tests/` или `client/integration/tests/`
   - Нет активных экспериментов в этих директориях

2. **Перед удалением конфигов** убедитесь, что:
   - Никакой код не импортирует из `config/` (только из `client/config/`)
   - В `main.py` используются пути из `client/config/`

3. **Перед удалением тестов** убедитесь, что:
   - Тесты не используются в CI/CD
   - Функциональность покрыта другими тестами

---

## ✅ Рекомендации

1. **Создать `.gitignore` правила** для предотвращения попадания логов в репозиторий:
   ```
   *.log
   log.md
   logs.md
   *_report.json
   ```

2. **Переместить актуальные тесты** из корня в правильные директории:
   - `client/test_*.py` → `client/tests/` или `client/integration/tests/`

3. **Документировать прототипные директории** если они остаются:
   - Добавить README с описанием назначения
   - Указать срок жизни директории
