# Автоматическая активация виртуального окружения при входе в директорию
# Требует установки direnv: brew install direnv
# После установки добавьте в ~/.zshrc: eval "$(direnv hook zsh)"

source .venv/bin/activate
